# gem5 to rem6 Migration
This document is the current migration ledger from gem5 concepts and tests to
rem6. The gem5 tree under `temp/reference_designs/gem5` is read-only audit
input; do not copy test binaries, generated outputs, or build products into
rem6.

## Document Boundary

`docs/architecture/rem6-architecture.md` owns the stable architecture story:
runtime shape, ownership model, invariants, and design motivation.

This migration ledger owns changing state: component scores and calculations,
checklists, migrated and missing behavior, evidence notes, gem5 test-anchor
crosswalks, and external-adapter rows. Do not duplicate the architecture
document's invariant list here or put percentages/proof logs there.

## Scoring Rubric

A percentage is a behavior-boundary score, not a count of related files.
Checklist items use markdown checkboxes:

- `[x]` means current rem6 has executable evidence for that item.
- `[ ]` means the item is not migrated or lacks executable rem6 evidence.

Checklist source: the component-local markdown checkbox list is the auditable
source for the component percentage. The gem5 test-anchor table uses row scores
only as compact crosswalk markers; those row scores do not override component
checklist calculations.

For each component, the score calculation must count completed checklist items,
round that ratio to the nearest whole percent, apply the evidence-breadth
bucket as an upper bound, and state both the checklist fraction and any bucket
cap. The component checklists are the auditable source for migration progress.

| Score | Bucket | Meaning |
| --- | --- | --- |
| 0% | open | No rem6 owner, or the row is scoped to the wrong gem5 target. |
| 1-19% | scoped | Owner or data types exist, but executable behavior evidence is absent or tiny. |
| 20-39% | unit-slice | Narrow unit behavior exists, with major integration axes absent. |
| 40-59% | single-axis | At least one executable ISA, device, config, or workload path works. |
| 60-74% | representative | Representative integration exists with typed artifacts, stats, or checkpoints. |
| 75-89% | matrix-gapped | Major gem5 matrix axes are covered, with explicit remaining gaps. |
| 90-99% | near-covered | All subtargets are covered except accepted non-goals or small gaps. |
| 100% | covered | Equivalent or stronger rem6 evidence exists for the named boundary. |

Detection-gated static newlib or qemu smoke tests count only for the narrow
path they execute. Unknown-syscall records count as diagnostics and
observability, not as implemented syscall coverage.

## Score Audit Format

Each component section below is the single source for that component's score.
Audit it by counting `[x]` items and total checklist items, confirming the
section-local `Score calculation` states the same fraction, rounded raw
percentage, and bucket cap label, confirming the section header score is the
raw percentage after the cap is applied, and using `Not migrated` plus `Next
evidence` as the blocking-boundary summary.

Do not add a second component-score table here; duplicated mutable scores drift
from the checklist source.

## Component Progress

### RISC-V ISA and Privileged Substrate - 59% single-axis

**Score calculation:** 21 of 25 items have executable evidence, or 84% raw,
capped to 59% by the single-axis bucket.
The bucket cap is single-axis because non-RISC-V ISAs and full RV64GC/vector
parity are not present.

- [x] RV64 integer, atomic, CSR, trap, counter, WFI, fence, PMP/PMA slices have tests.
- [x] RV64C integer/load-store/control-flow slices have tests.
- [x] RV64F/RV64D scalar load/store, arithmetic, comparisons, conversions, legal FP arithmetic and integer-to-float rounding-mode decode, exact static non-RNE integer-to-float conversion execution, inexact integer-to-float accrued flag updates, rounding-insensitive static arithmetic execution, `fadd.s`/`fsub.s` exact-wide-sum directed rounding, `fadd.d`/`fsub.d` finite directed rounding with NX and directed overflow flags, `fmul.s` exact-product and `fmul.d` normal finite exact-product plus directed-overflow and directed-underflow subnormal rounding, `fdiv.s` and `fdiv.d` finite-quotient directed rounding with NX, `fsqrt.s` and `fsqrt.d` directed rounding with NX, `fcvt.s.d` directed rounding with NX, `fcvt.d.s` signaling-NaN invalid accrual, `fmadd.s` exact-window directed rounding, narrow `fmadd.d` exact-window, product-overflow, and max-boundary directed rounding slices, `fadd.s`/`fsub.s`/`fmul.s`/`fmadd.s` invalid, overflow, underflow, and inexact accrued flags, and NaN-boxing have tests.
- [x] RV64F/RV64D integer-to-float conversions execute inexact static directed rounding and valid dynamic `frm` modes with accrued inexact flags.
- [x] RV64C double-precision FP load/store decode and compressed FP load/store CPU data-access slices have tests.
- [x] RVV vector-configuration instruction family (`vsetvli`, `vsetivli`, `vsetvl`) decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer `vadd.vv` LMUL=1 and m2 register-group slices, invalid-`vtype` and unaligned-group traps, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer `vadd.vx` and signed-immediate `vadd.vi`, plus masked integer `vadd.vv`/`vadd.vx`/`vadd.vi` slices, decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer `vsub.vv` LMUL=1 plus m2 register-group slices and `vsub.vx` decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer bitwise `vand`, `vor`, and `vxor` vv/vx/vi forms decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer shift `vsll`, `vsrl`, and `vsra` vv/vx/vi forms decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer min/max `vminu`, `vmin`, `vmaxu`, and `vmax` vv/vx forms decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer multiply `vmul`, `vmulhu`, `vmulhsu`, and `vmulh` vv/vx forms decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer divide and remainder `vdivu`, `vdiv`, `vremu`, and `vrem` vv/vx forms decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer mask compare `vmseq` and `vmsne` vv/vx/vi forms decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV unmasked integer ordered mask compare `vmsltu`, `vmslt`, `vmsleu`, `vmsle`, `vmsgtu`, and `vmsgt` supported vv/vx/vi forms decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV integer unmasked `vslideup`/`vslidedown` vx/vi forms plus `vslide1up`/`vslide1down` vx and `vrgather` vv/vx/vi forms, mask reductions `vcpop.m`/`vfirst.m`, mask prefixes `vmsbf.m`/`vmsof.m`/`vmsif.m`, mask indexes `viota.m`/`vid.v`, `vmerge` masked vv/vx/vi forms, `vmv.v` unmasked v/x/i, `vmv.x.s`/`vmv.s.x` scalar/lane-zero forms, and `vmv<nr>r.v` whole-register moves, `vcompress.vm`, `vzext`/`vsext` masked/unmasked extension-family decode, `vf2` extension hart execution, unmasked fixed-point saturating `vsaddu`/`vsadd`/`vssubu`/`vssub` vv/vx plus `vsaddu`/`vsadd` vi forms, unmasked fixed-point averaging `vaaddu`/`vaadd`/`vasubu`/`vasub` vv/vx forms, unmasked fixed-point signed fractional multiply `vsmul` vv/vx forms, integer carry/borrow `vadc`/`vmadc`/`vsbc`/`vmsbc` vv/vx plus add-immediate forms with reserved-encoding coverage, representative ISA execution, and representative CPU fetch-stream/fetch-ahead execution, single-width integer multiply-add `vmadd`/`vnmsub`/`vmacc`/`vnmsac` vv/vx decode with representative masked/unmasked ISA execution and representative unmasked CPU fetch-stream execution, base widening integer add/subtract `vwaddu`/`vwadd`/`vwsubu`/`vwsub` vv/vx/wv/wx decode with representative masked/unmasked ISA execution and representative unmasked CPU fetch-stream execution, widening integer multiply `vwmulu`/`vwmulsu`/`vwmul` vv/vx and widening integer multiply-add `vwmaccu`/`vwmacc`/`vwmaccsu` plus vx-only `vwmaccus` decode with representative masked/unmasked ISA execution and representative unmasked CPU fetch-stream execution, integer reductions `vredsum`, `vredand`, `vredor`, `vredxor`, `vredminu`, `vredmin`, `vredmaxu`, and `vredmax` plus widening reductions `vwredsumu` and `vwredsum` masked/unmasked vs forms at ISA level with unmasked CPU fetch-stream execution, and unmasked slide/gather, mask reductions, mask prefixes, mask indexes, plus `vzext.vf2` CPU fetch-stream execution have tests.
- [x] RVV mask logical `vmand`, `vmnand`, `vmandn`, `vmxor`, `vmor`, `vmnor`, `vmorn`, and `vmxnor` `.mm` forms decode, hart execution, and CPU fetch-stream execution have tests.
- [x] RVV floating-point `vfadd`, `vfsub`, `vfmin`, `vfmax`, `vfmul`, and `vfdiv` vv/vf forms plus `vfrsub.vf`, `vfrdiv.vf`, `vfmacc`, `vfnmacc`, `vfmsac`, and `vfnmsac` vv/vf forms and `vfsqrt.v` exact finite SEW=32 lane execution, E64 `vfadd.vv/vf`, `vfsub.vv/vf`, `vfrsub.vf`, `vfmul.vv/vf`, `vfdiv.vv/vf`, and `vfrdiv.vf` exact finite CPU fetch-stream execution, E64 `vfmin.vv/vf` and `vfmax.vv/vf` signed-zero and NaN CPU fetch-stream execution, `vfclass.v` SEW=32 classification lane execution, `vfmv.v.f` SEW=32 scalar splat lane execution, `vfmerge.vfm` SEW=32 masked scalar merge execution, `vfmv.s.f` SEW=32 scalar-to-lane-zero execution, `vfmv.f.s` SEW=32 lane-zero-to-scalar execution, integer-to-float `vfcvt.f.xu.v` and `vfcvt.f.x.v` plus float-to-integer `vfcvt.xu.f.v`, `vfcvt.x.f.v`, `vfcvt.rtz.xu.f.v`, and `vfcvt.rtz.x.f.v` dynamic/directed SEW=32 lane execution with NX accrual, E64 `vfcvt.f.xu.v`, `vfcvt.f.x.v`, `vfcvt.xu.f.v`, `vfcvt.x.f.v`, `vfcvt.rtz.xu.f.v`, and `vfcvt.rtz.x.f.v` CPU fetch-stream execution, FP mask compare `vmfeq`, `vmfne`, `vmflt`, and `vmfle` vv/vf SEW=32 mask results, `vfmin`, `vfsqrt.v`, and FP compare invalid-flag accrual, `vfsqrt.v` non-exact finite rejection, and `vfsgnj`, `vfsgnjn`, and `vfsgnjx` vv/vf sign-bit lane execution have tests, with `vfadd.vv` fetch-ahead coverage and the remaining listed forms CPU fetch-stream coverage.
- [x] Sv39 helpers and CPU memory-walker paths have tests.
- [x] RISC-V SE ecalls reach the system syscall table.
- [ ] Full RV64GC including vector execution and directed rounding coverage is complete.
- [ ] Linux-grade privileged CSR, interrupt, and exception breadth is complete.
- [ ] ARM, x86, Power, SPARC, MIPS, and GPU ISA execution have gem5-class owners.
- [ ] Hardware fetch translation and full boot-time privileged behavior are complete.

**Migrated:** RISC-V architectural state, large RV64 scalar slices, FP slices
including fused multiply-add special-case exception flags and legal static FP
arithmetic plus integer-to-float conversion rounding-mode decoding, exact static
non-RNE integer-to-float conversion execution, integer-to-float inexact
`fflags`/`fcsr` updates, inexact static directed and valid dynamic `frm`
integer-to-float execution, rounding-insensitive static FP arithmetic
execution, `fadd.s`/`fsub.s` exact-wide-sum static/dynamic directed rounding and `fadd.d`/`fsub.d` finite static directed rounding plus directed overflow flags
with NX accrual, `fmul.s` exact-product static directed rounding with NX,
UF, and directed-boundary overflow accrual, `fmul.d` normal finite exact-product
static directed rounding with NX, directed overflow boundaries with OF/NX, and
directed subnormal underflow boundaries with UF/NX, `fdiv.s` and `fdiv.d`
finite-quotient static directed rounding with NX accrual, `fsqrt.s` and `fsqrt.d` static directed rounding with NX accrual, `fcvt.s.d` static/dynamic directed rounding with NX accrual, `fcvt.d.s` signaling-NaN invalid accrual, `fmadd.s` exact-window
static/dynamic directed rounding with RMM tie-away, NX, and directed-boundary
overflow accrual, narrow `fmadd.d`
static RoundUp and dynamic RoundDown exact-window plus product-overflow and max-boundary directed rounding with
directed zero-sign and overflow-flag boundary slices, plus
`fadd.s`/`fsub.s` signaling-NaN, infinity invalid, overflow, and wider finite
NX flag slices, compressed double FP load/store decoding, compressed FP load/store
CPU data access, privileged interrupt fixed-priority machine and delegated
supervisor trap entry, RVV vector-configuration decode and unmasked integer
`vadd.vv` LMUL=1 plus m2 register-group execution, unmasked integer
`vadd.vx`, signed-immediate `vadd.vi`, masked `vadd.vv`/`vadd.vx`/`vadd.vi`,
unmasked integer `vsub.vv` LMUL=1 plus
m2 register-group execution, `vsub.vx`, `vrsub.vx`/`vrsub.vi`, and unmasked integer bitwise
`vand`/`vor`/`vxor`, shift `vsll`/`vsrl`/`vsra`, and min/max
`vminu`/`vmin`/`vmaxu`/`vmax` plus multiply
`vmul`/`vmulhu`/`vmulhsu`/`vmulh` and divide/remainder
`vdivu`/`vdiv`/`vremu`/`vrem`, integer carry/borrow `vadc`/`vmadc`/`vsbc`/`vmsbc` vv/vx plus add-immediate forms with reserved-encoding coverage and representative execution, single-width integer multiply-add `vmadd`/`vnmsub`/`vmacc`/`vnmsac` vv/vx decode with representative execution, integer reductions `vredsum`/`vredand`/`vredor`/`vredxor`/`vredminu`/`vredmin`/`vredmaxu`/`vredmax` plus widening reductions `vwredsumu`/`vwredsum`, base widening add/subtract `vwaddu`/`vwadd`/`vwsubu`/`vwsub` vv/vx/wv/wx decode with representative execution, widening multiply `vwmulu`/`vwmulsu`/`vwmul` vv/vx and widening multiply-add `vwmaccu`/`vwmacc`/`vwmaccsu` plus vx-only `vwmaccus` decode with representative execution, and mask compare
`vmseq`/`vmsne` plus ordered mask compare
`vmsltu`/`vmslt`/`vmsleu`/`vmsle`/`vmsgtu`/`vmsgt`, merge `vmerge`, move
`vmv.v`, `vmv.x.s`/`vmv.s.x`, and `vmv<nr>r.v`, unmasked slide `vslideup`/`vslidedown`/`vslide1up`/`vslide1down`, gather `vrgather`, mask reductions `vcpop.m`/`vfirst.m`, mask prefixes `vmsbf.m`/`vmsof.m`/`vmsif.m`, mask indexes `viota.m`/`vid.v`, compress `vcompress.vm`, zero/sign extension `vzext`/`vsext` masked/unmasked decode with `vf2` hart execution and unmasked slide/gather, mask reductions, mask prefixes, mask indexes, plus `vzext.vf2` CPU fetch-stream execution, unmasked fixed-point saturating `vsaddu`/`vsadd`/`vssubu`/`vssub` vv/vx plus `vsaddu`/`vsadd` vi forms, unmasked fixed-point averaging `vaaddu`/`vaadd`/`vasubu`/`vasub` vv/vx forms, unmasked fixed-point signed fractional multiply `vsmul` vv/vx forms, unmasked fixed-point scaling shifts `vssrl`/`vssra` vv/vx/vi, fixed-point narrowing shifts `vnsrl.wv`/`vnsra.wv`/`vnsrl.wx`/`vnsra.wx`/`vnsrl.wi`/`vnsra.wi` and narrow clip `vnclipu.wv`/`vnclip.wv`/`vnclipu.wx`/`vnclip.wx`/`vnclipu.wi`/`vnclip.wi`, and unmasked floating-point `vfadd`, `vfsub`, `vfmin`, `vfmax`, `vfmul`, and
`vfdiv` vv/vf forms plus `vfrsub.vf`, `vfrdiv.vf`, `vfmacc`,
`vfnmacc`, `vfmsac`, and `vfnmsac` vv/vf exact finite SEW=32 lanes,
E64 `vfadd.vv/vf`, `vfsub.vv/vf`, `vfrsub.vf`, `vfmul.vv/vf`, `vfdiv.vv/vf`, and `vfrdiv.vf` exact finite CPU fetch-stream lanes, E64 `vfmin.vv/vf` and `vfmax.vv/vf` signed-zero and NaN CPU fetch-stream lanes, `vfclass.v` SEW=32 classification lanes, `vfmv.v.f`
SEW=32 scalar splat lanes, `vfmerge.vfm` SEW=32 masked scalar merge,
`vfmv.s.f` SEW=32 scalar-to-lane-zero, `vfmv.f.s` SEW=32
lane-zero-to-scalar, integer-to-float `vfcvt.f.xu.v` and `vfcvt.f.x.v`, and
float-to-integer `vfcvt.xu.f.v`, `vfcvt.x.f.v`, `vfcvt.rtz.xu.f.v`,
and `vfcvt.rtz.x.f.v` dynamic/directed SEW=32 lanes with NX accrual plus
E64 `vfcvt.f.xu.v`, `vfcvt.f.x.v`, `vfcvt.xu.f.v`, `vfcvt.x.f.v`,
`vfcvt.rtz.xu.f.v`, and `vfcvt.rtz.x.f.v` CPU fetch-stream execution, FP
mask compare `vmfeq`, `vmfne`, `vmflt`, and `vmfle` vv/vf SEW=32 mask results,
`vfmin` signaling-NaN invalid-flag accrual, `vfsqrt.v` negative and
signaling-NaN invalid-flag accrual, `vmfeq`/`vmfne` quiet-compare
signaling-NaN invalid-flag accrual, `vmflt`/`vmfle` signaling-compare invalid-flag accrual,
`vfsqrt.v` non-exact finite rejection, `vfsgnj`, `vfsgnjn`, and `vfsgnjx`
vv/vf sign-bit lanes, reserved-`frm` and NaN-boxing trap coverage,
`vfadd.vv` fetch-ahead coverage, and CPU fetch-stream coverage for the remaining listed forms, plus
`vxrm`/`vxsat`/`vcsr` CSR visibility through the CPU fetch stream
or hart execution; mask logical `.mm` forms through decode, hart execution, and
CPU fetch-stream tests; unmasked RVV unit-stride `vle8`/`vle16`/`vle32`/`vle64`
and `vse8`/`vse16`/`vse32`/`vse64` m1 CPU data-path execution, masked m1
unit-stride `vle`/`vse` e8/e16/e32/e64 CPU data-path execution, plus aligned `e32,m2`
full 32-byte register-group load/store execution, unmasked and masked e32/m1 `vlseg2e32`/`vsseg2e32` plus unmasked e16/m2 `vlseg2e16`/`vsseg2e16` segment CPU data-path execution, and misaligned rejection; and traps, translation
helpers, and SE ecall plumbing.

**Not migrated:** Full RV64GC/vector data-operation breadth, other major ISAs, directed
rounding breadth beyond the covered integer-to-float, single-precision
add/sub/multiply/divide/fused multiply-add, double-precision multiply/divide, and narrow
double-precision `fmadd.d` slices, and complete Linux privileged behavior.

**Evidence:** `RiscvInstruction::decode_with_length`,
`decode_float_op`, `decode_compressed`, `walk_sv39_page_table_with_context`,
tests `rv64i`, `rv64m`, `rv64f`, `rv64f_add`, `rv64f_sub`, `rv64f_fma`,
`rv64d`, `rv64d_fma`, `riscv_scalar_float_frontend`,
`vector_config_prediction`, `vector_compress`,
`vector_extend`, `vector_fixed_point`, `vector_integer`, `vector_merge_move`,
`vector_mask_compare`, `vector_mask_logical`, `riscv_frontend`,
`riscv_vector_extend_frontend`, `riscv_vector_float_frontend`, `sv39`, and privileged RISC-V tests.

**Next evidence:** Generated or imported RV64GC/vector instruction tests plus
privileged Linux trap and interrupt smoke tests.
### CPU Execution Models - 74% representative
**Score calculation:** 8 of 10 items have executable evidence, or 80% raw,
capped at the 74% representative bucket cap. The bucket cap is representative because
RISC-V core timing now uses focused `riscv_in_order_drive` authority to schedule one kernel tick per Fetch1/Fetch2/Decode/Execute/Commit transition in normal serial, parallel-cluster, and translated drivers, withholding architectural visibility until Commit across direct one-/two-core and cache/fabric/DRAM one-core CLI rows and through per-tick scheduler-owned scalar integer, scalar FP, and vector FU ExecuteWait stalls, including direct single-core MUL and FCLASS, direct two-core DIV, cache/fabric/DRAM single-core MUL, and focused FDIV.S/VDIVU.VV rows; one typed `riscv_fu_latency` authority also keeps vector-memory waits data-completion-owned and supplies O3 FU classes, and the CPU has a live execution-mode-transfer matrix across single-core CPU0 and two-worker multicore CPU1, direct untranslated and TOML-configured translated memory, cache/fabric/DRAM untranslated memory, single-/multicore untranslated readfile-MMIO targets, and single-core translated readfile-MMIO, live FU and outstanding scalar-load authority, mixed CPU0-memory/CPU1-device ownership, and peer-load suppression, plus direct completed-fetch overlap and bounded normal-driver
fetch-ahead, v1/v2 decode-compatible and current v3 instruction-keyed progress checkpoint replay for a partially consumed normal DIV ExecuteWait with replacement-fetch rebinding, same-instruction remaining-cycle preservation, equal-latency changed-instruction reset, latency-class reset before Execute or Commit, and exactly-once draining across a normal-to-detailed mode handoff, narrow pending-fetch and data-access resource-stall slices, and
a targeted execution-owned bounded four-row scalar FU/ALU ROB/rename staging window plus
configurable bounded untranslated scalar-memory windows covering four independent loads, a store-led mixed window with one leading cacheable store plus up to three younger independent loads, and an ordered cacheable store prefix whose overlap-connected or disjoint partial-range mixed-width stores occupy O3 in order, remain CPU-buffered until their predecessor succeeds, and compose later-load bytes from only overlapping stores with youngest-store precedence, four-row direct and three-row direct/cache/fabric/DRAM CLI overlap evidence plus direct and cache/fabric/DRAM disjoint-prefix evidence through `rem6_run_o3_detailed_disjoint_store_prefix_composition_direct`, `rem6_run_o3_detailed_disjoint_store_prefix_composition_cache_fabric_dram`, `rem6_run_o3_disjoint_store_prefix_rows_are_resident_before_responses`, and `rem6_run_o3_timing_disjoint_store_prefix_uses_transport_without_o3_composition`, including `sim.memory.resources.cache.data.activity`, `sim.memory.resources.transport.data.activity`, `sim.memory.resources.fabric.activity`, and `sim.memory.resources.dram.activity`, exact ROB/LSQ residency, leading-store forwarding across intervening loads, youngest-store selection, predecessor-gated transport visibility, oldest-first retirement, suffix cancellation, and timing-mode suppression, plus a single-outstanding scalar MMIO lifecycle and configurable untranslated scalar-memory-prefix/scalar-ALU live windows at total depths four, six, and eight with up to four memory rows, one-LSQ/eight-ROB deep residency, issue widths 1/2/4 across direct and cache/fabric/DRAM routes, scheduler-owned typed data/control dependency scopes, transitive/fan-in younger wakeup, conservative unresolved-load terminal boundaries, completed normal, contained-forwarded, and partial-overlay load responses waking resident dependent ALUs before ordered commit, JSON/text/`m5_dump_stats` issue surfaces, live checkpoint rejection, timing-preserving mode transfer, focused retry/failure/redirect cleanup, and timing-mode suppression, Pipeline-debug-derived exact same-sequence fetch-/data-wait ordering backlogs to later branch-prediction flush totals, plus a completed execute-wait/later-trap non-correlation boundary with lookahead-one/timing controls, plus an interrupt matrix covering SBI timer-backed CPU0 fetch-, data-, and execute-wait backlog, SBI timer-backed two-worker CPU1 data- and execute-wait backlog, and a non-timer SBI IPI/SSIP CPU1 fetch-wait row; the matrix correlates ordering- and resource-blocked history for explicit younger flushes and interrupt-cancelled current instructions only when a nonretiring advance terminates with a null destination stage, while `interrupt_live_advance_does_not_terminate_backlog` keeps live stage-to-stage advances active, distinguishes lookahead-one younger-flush suppression from retained current-instruction backlog, applies timing-mode suppression where applicable, and excludes pending-interrupt events from architectural and O3 retirement, and scheduled detailed-to-timing switches that capture either a live four-row FU ROB/rename/gate payload, one outstanding direct untranslated memory or readfile-MMIO load, one cold translated cacheable load with a bounded three-row scalar-ALU suffix through direct or cache/fabric/DRAM memory, one CPU0 TLB-cached translated load with a bounded three-row scalar-ALU suffix through direct or cache/fabric/DRAM memory, one two-worker CPU1 TLB-cached translated cache/fabric/DRAM load with the same suffix while an overlapping CPU0 translated peer request remains outside target authority, or one TLB-cached translated readfile-MMIO load with no younger O3 rows while preserving inherited issue/writeback/commit timing until ordered drain; the FU row additionally suppresses the first post-window timing instruction from O3 and returns through an architectural `m5` switch. Full realistic multi-stage stalls/squashes, general IQ/wakeup/select beyond bounded scoped issue authority, link-sourced coroutine target shapes outside exact same-link authority and producer-forwarded linked-call returns beyond the exact direct/scalar continuations, producer-forwarded controls beyond the exact scalar-plus-return continuation, deeper non-scalar/control/result/device chains beyond the configured eight-row untranslated scalar-only lane, arbitrary broader mixed windows, restorable transport ownership, broader mixed-data result depth, translated or MMIO result pairs, dependent stores/atomics, FP/vector dependent addresses, multiple unresolved addresses, and younger side-effect chains beyond one independent disjoint unordered AMO in result row two, broad vector shapes, broader translated memory/device shapes, cross-line and deeper multi-store byte composition, and a general O3 engine remain incomplete.
Two-restore O3 debug CLI evidence now derives unqualified component/chunk/payload/checksum stats from the same latest restore envelope emitted in structured JSON, while execution-mode checkpoint summaries and system restore staging share the `rem6-system` manifest decoder so duplicate targets fail closed and empty execution-mode authority restores as cleared rather than present zero-target authority; the live target map is the sole authority and capture/restore prune stale registry state without a registration latch. Final JSON, text, and `m5_dump_stats` now project gem5-compatible O3 IEW totals, phase latencies, `wbRate`, `wbFanout`, and scoped issue arbitration from one descriptor authority, preserving source units, reset policy, and active-hart suppression. Scoped O3 issue evidence covers `--riscv-o3-issue-width`/`riscv_o3_issue_width` widths 1, 2, and 4, load-head width reservation, cross-resource ALU/MUL co-issue, same-MUL serialization, producer-ready dependency blocking, direct and cache/fabric/DRAM rows, real JSON `/cores/0/o3_runtime/issue` plus `sim.cpu0.o3.issue_cycles`, `sim.cpu0.o3.issued_rows`, `sim.cpu0.o3.resource_blocked_row_cycles`, `sim.cpu0.o3.dependency_blocked_row_cycles`, `sim.cpu0.o3.max_rows_per_cycle`, text stats, `m5_dump_stats` `sim.host_actions.stats_dump.cpu0.o3.issue_cycles`, checkpoint version 22 stat compatibility with historical v21 zero defaults, detailed-to-timing issue/writeback/commit continuity and transfer stats, live checkpoint rejection, drained cleanup/restore, and timing suppression through `rem6_run_o3_scoped_issue_width_one_serializes_direct_window`, `rem6_run_o3_scoped_issue_width_two_coissues_cross_resource_rows`, `rem6_run_o3_scoped_issue_serializes_same_multiply_resource`, `rem6_run_o3_scoped_issue_dependency_waits_for_multiply`, `rem6_run_host_switch_preserves_o3_scoped_issue_ticks`, `rem6_run_o3_scoped_issue_checkpoint_boundary`, `rem6_run_timing_suppresses_o3_scoped_issue_surface`, `rem6_run_o3_scoped_issue_text_stats_expose_arbitration_counters`, and `rem6_run_o3_scoped_issue_stats_dump_exposes_arbitration_counters`.
Bounded shared scalar FU/load writeback ownership now accepts `--riscv-o3-writeback-width` and `riscv_o3_writeback_width` widths 1, 2, and 4, applies deterministic older-sequence reservation precedence across fixed-FU and scalar-load completions, wakes dependencies only at the admitted tick, and removes future reservations during rollback/retry cleanup; direct collision evidence is locked by `rem6_run_o3_writeback_width_one_serializes_direct_fu_dependent_collision`, `rem6_run_o3_writeback_width_two_exact_fit_direct_fu_dependent_collision`, `rem6_run_o3_writeback_scalar_load_fu_collision_blocks_architecture_until_admission`, and `rem6_run_o3_writeback_wrong_path_reservation_never_publishes`.
The same bounded scalar FU/load authority runs through direct and cache/fabric/DRAM routes via `rem6_run_o3_writeback_scalar_load_fu_collision_cache_fabric_dram`.
It writes checkpoint v23 while decoding v1-v22 compatibility with empty stable deferred state, rejects live checkpoints and restores drained state in `rem6_run_o3_writeback_port_checkpoint_boundary`, and preserves inherited issue/writeback/commit ticks across detailed-to-timing transfer in `rem6_run_host_switch_preserves_o3_writeback_port_ticks`.
**Bounded O3 memory-result writeback evidence:**
- CLI positives cover direct FP (`FLD`), LR (`LR.D`), AMO (`AMOSWAP.D`), masked-vector (`VLE64.V`), readfile-MMIO (`LD`), and successful nonzero-destination SC routes, plus cache/fabric/DRAM FP, AMO, vector, and SC routes through `rem6_run_o3_store_conditional_result_cache_fabric_dram`. A separate bounded one-result/three-scalar suffix matrix is locked by `rem6_run_o3_memory_result_scalar_suffix_matrix_direct`, `rem6_run_o3_memory_result_scalar_suffix_matrix_cache_fabric_dram`, `rem6_run_o3_memory_result_scalar_suffix_width_two_exact_fit_direct`, `rem6_run_o3_memory_result_scalar_suffix_readfile_mmio`, and `rem6_run_timing_suppresses_o3_memory_result_scalar_suffix`: cacheable `FLD`, masked e64/m1 `VLE64.V`, and `AMOSWAP.D` heads plus readfile `LD` each authorize exact four-row ROB residency with one result LSQ row or the AMO two-row span, an independent DIV issues before the result response while its transitive scalar continuation follows DIV writeback, exact integer-result consumers remain blocked until admitted result writeback, width one gives the older result priority over the colliding DIV while width two admits the pair, direct memory uses transport only, hierarchy rows exercise cache/fabric/DRAM, MMIO avoids the ordinary hierarchy, and pre-admission register/memory witnesses plus timing suppression keep publication and mode boundaries explicit. The bounded paired-result extension is locked by `rem6_run_o3_memory_result_pair_matrix_direct`, `rem6_run_o3_memory_result_pair_matrix_cache_fabric_dram`, `rem6_run_o3_memory_result_pair_width_two_exact_fit_direct`, `rem6_run_o3_memory_result_pair_boundaries`, and `rem6_run_timing_suppresses_o3_memory_result_pairs`: disjoint unordered `AMOSWAP.D`/`FLD`, masked e64/m1 `VLE64.V`/`LD`, and `FLD`/masked e64/m1 `VLE64.V` rows issue both real requests before the first response, expose exact four-row ROB and class-specific LSQ residency, wake scalar consumers from the correct first or second integer result, preserve oldest-first publication and commit, distinguish direct transport from cache/fabric/DRAM activity, serialize two older colliding results ahead of DIV at width one while width two admits the exact result pair, serialize dependent second addresses and acquire/release atomic heads, hold a third result outside the pair, and preserve timing-mode architecture without O3 pair surfaces.
- Width one serializes the memory-result/DIV collision by older sequence and width two admits the exact pair, with SC coverage locked by `rem6_run_o3_store_conditional_result_width_one_serializes_direct` and `rem6_run_o3_store_conditional_result_width_two_exact_fit_direct` plus pre-/post-admission and final architectural witnesses. One bounded younger side-effect row is now locked by `rem6_run_o3_younger_atomic_result_matrix_direct`, `rem6_run_o3_younger_atomic_result_matrix_cache_fabric_dram`, `rem6_run_o3_younger_atomic_result_boundaries_and_live_actions`, and `rem6_run_timing_suppresses_o3_younger_atomic_result`: untranslated cacheable `LD`, `FLD`, and masked e64/m1 `VLE64.V` heads admit one independent disjoint unordered nonzero-destination AMO plus two scalar rows into an exact four-ROB/three-LSQ window, record AMO issue before the head response while keeping its target request CPU-buffered until predecessor success, preserve oldest-first publication, exact FP/vector/integer/memory witnesses, direct versus cache/fabric/DRAM activity, serial and parallel release, older-retry cancellation, younger-retry preservation of the completed head, ordered/overlap/dependent-address/zero-destination exclusion, live checkpoint/mode-switch rejection, and timing-mode architectural equivalence without O3 surfaces.
- Focused `rem6-cpu` tests cover typed completion physical address/size/offset/bytes, AMO two-sequence LSQ span ownership, and shared local/target-reported failed-SC status publication; `rem6_run_o3_store_conditional_failure_is_local_and_deferred` proves local failure consumes no transport request, publishes status one only at admitted writeback, and increments the failure counter once.
- CLI boundaries keep zero-destination integer/LR/AMO/SC results and unsupported LMUL2/fault-only-first vectors outside the bounded result lane, and prove all-inactive masked vector issue sends no request.
- CLI PMP/lifecycle/timing rows prove denied AMO traps before transport, `rem6_run_o3_store_conditional_result_live_actions_reject` rejects completed-but-unpublished SC checkpoint/mode-switch capture, and `rem6_run_timing_suppresses_o3_store_conditional_result_surface` preserves success/failure architecture while suppressing the O3 result surface.
Detailed traces expose explicit issue, raw-ready, admitted-writeback, writeback, and commit ticks while `rem6_run_timing_suppresses_o3_writeback_port_surface` keeps timing mode free of the O3 surface; `rem6_run_o3_writeback_port_json_exposes_counters`, `rem6_run_o3_writeback_port_text_stats_expose_counters`, and `rem6_run_o3_writeback_port_stats_dump_exposes_counters` cover `/cores/0/o3_runtime/writeback_port`, `sim.cpu0.o3.writeback_port.cycles`, `sim.cpu0.o3.writeback_port.admitted_rows`, `sim.cpu0.o3.writeback_port.deferred_rows`, `sim.cpu0.o3.writeback_port.deferred_row_cycles`, `sim.cpu0.o3.writeback_port.max_ready_rows_per_cycle`, `sim.cpu0.o3.writeback_port.max_deferred_rows`, and `sim.host_actions.stats_dump.cpu0.o3.writeback_port.cycles`. A bounded mixed no-link control-kind window is locked by `rem6_run_o3_mixed_control_kind_commits_direct`, `rem6_run_o3_mixed_control_kind_commits_cache_fabric_dram`, `rem6_run_o3_mixed_control_kind_requires_branch_lookahead_three`, `rem6_run_o3_mixed_control_kind_middle_repair_discards_indirect_jump_hierarchy`, `rem6_run_o3_mixed_control_kind_load_dependent_jalr_stays_terminal`, `rem6_run_host_switch_transfers_o3_mixed_control_kinds`, `rem6_run_o3_mixed_control_kind_checkpoint_boundary`, and `rem6_run_timing_suppresses_o3_mixed_control_kinds`: direct and cache/fabric/DRAM rows place `JAL x0`, a direct conditional branch, and `JALR x0` with an architectural target source behind one delayed scalar load, prove exact four-row ROB/one-row LSQ residency and branch-port serialization before response, preserve no-link writes and ordered commit, repair a middle misprediction without retiring the younger indirect jump, keep a load-produced JALR target terminal, transfer the non-restorable live rows across detailed-to-timing, reject a live checkpoint, restore a drained checkpoint with zero ROB/LSQ rows, and suppress O3 surfaces in timing mode. The adjacent bounded linked control-kind window is locked by `rem6_run_o3_link_kind_direct_call_commits_direct`, `rem6_run_o3_link_kind_indirect_call_commits_cache_fabric_dram`, `rem6_run_o3_link_kind_return_uses_committed_ras_entry`, `rem6_run_o3_link_kind_older_branch_discards_linked_call`, `rem6_run_o3_producer_forwarded_jalr_targets_cover_link_route_matrix`, `rem6_run_o3_unresolved_producer_forwarded_jalr_targets_stay_terminal`, `rem6_run_o3_producer_forwarded_jalr_target_requires_depth_three`, `rem6_run_timing_suppresses_o3_producer_forwarded_jalr_targets`, `rem6_run_host_switch_transfers_o3_link_kind_window`, `rem6_run_o3_link_kind_checkpoint_boundary`, and `rem6_run_timing_suppresses_o3_link_kind_window`: one delayed scalar load feeds either a `JAL x1` direct call, independent `JALR x5` through a committed non-link target, or `JALR x0` return using committed link/RAS state; direct, hierarchy indirect, and RAS-return rows prove exact four-ROB/one-LSQ residency, ordered issue/writeback/commit, exact register/memory witnesses, direct and cache/fabric/DRAM activity, branch kinds `call_direct`/`call_indirect`/`return`, link-write presence/absence, target provider, and RAS push/pop/used/correct/squash evidence. Linked calls own staged integer rename destination and one fixed-FU writeback slot, keep architectural link old before ordered retirement, and reach exact final fallthrough value, while returns own branch issue without an integer write/writeback slot; older repair removes a younger linked call and descendants, preserves the prior link register, and suppresses wrong-path Data/Memory; load-produced indirect targets stay terminal before response, while exact adjacent scalar producers forward no-link and split-link `JALR` targets across direct and cache/fabric/DRAM routes, revalidate both staged fetch identities, bind the exact branch speculation ID/kind and required RAS push, fetch each target exactly once before the delayed response, keep target issue behind that response, stage link rename and RAS push only for split-link calls, require scalar-memory depth three, and preserve architectural behavior while suppressing O3 surfaces in timing mode. The dependency-derived lineage extension is locked by `rem6_run_o3_nonadjacent_producer_forwarded_jalr_targets_cover_link_route_matrix`, `rem6_run_o3_warmed_producer_forwarded_targets_issue_descendants_before_load_response`, `rem6_run_o3_warmed_producer_forwarded_target_descendant_requires_depth_four`, `rem6_run_o3_warmed_target_does_not_bypass_unresolved_jalr_source`, `rem6_run_host_switch_transfers_nonadjacent_producer_forwarded_jalr_window`, `rem6_run_rejects_live_warmed_producer_forwarded_jalr_checkpoint`, and `rem6_run_timing_suppresses_producer_forwarded_lineage_windows`: exact no-link and split-link direct/hierarchy rows admit one independent spacer from the consumer dependency producer; a separate hierarchy-warmed depth-four row consumes a real completed target fetch and issues the target scalar strictly before the older DRAM-backed load response; cache fills preserve the DRAM controller ready tick across lower levels; trap completion discards stale in-order fetch rows without resetting pipeline history; focused compatibility rows retain reset-straddled LSQ latency, QoS response ordering, and scalar/vector writeback collisions under the longer hierarchy timing; depth three and unresolved load-produced target sources fail closed; schema-v7 transfer, live checkpoint rejection, and timing suppression preserve lifecycle boundaries without claiming a fifth row or arbitrary depth. For the existing adjacent matrix, detailed-to-timing transfer is timing-preserving and non-restorable, live checkpoint rejects, drained restore has zero ROB/LSQ and no live-data chunk, and timing mode suppresses O3 runtime, trace, and aliases. The bounded same-window link-return capability is locked by `rem6_run_o3_same_window_link_return_commits_direct`, `rem6_run_o3_same_window_indirect_link_return_commits_cache_fabric_dram`, `rem6_run_o3_same_window_link_return_requires_branch_lookahead_two`, `rem6_run_o3_same_window_overwritten_link_return_stays_terminal`, `rem6_run_o3_older_branch_discards_same_window_link_return_chain`, `rem6_run_host_switch_transfers_o3_same_window_link_return`, `rem6_run_o3_same_window_link_return_checkpoint_boundary`, and `rem6_run_timing_suppresses_o3_same_window_link_return`: direct `JAL x1` and independent `JALR x5` calls feed same-window returns with RAS-only return-target authority and no architectural fallback for the live return; the direct and cache/fabric/DRAM hierarchy positives expose exactly four ROB rows and one LSQ row, staged link rename and fixed-FU writeback order, return issue strictly after call writeback, and descendant issue strictly after return writeback, while the direct route shows transport activity with zero cache/fabric/DRAM activity and the hierarchy route exercises all four layers. One speculative RAS push and one speculative RAS pop remain pending behind the delayed load; final counters show exactly one RAS push, pop, use, correct result, and RAS target-provider selection. Lookahead one opens only the call target and suppresses the live return/descendant chain. An overwritten link remaps live `x1` to the overwrite row and keeps the return terminal at or after overwrite writeback; before older repair, the wrong-path call owns live `x1`, then repair squashes the call, return, rename state, and both speculative RAS operations while RAS used/correct/incorrect remain zero. Timing-preserving non-restorable mode transfer proves one resident load row, three younger rows, and the first load operation, memory target, address, and width; live-checkpoint rejection plus drained zero-ROB/zero-LSQ restore and timing suppression cover the remaining boundaries. This bounded link-return capability does not provide general live indirect-target forwarding. The adjacent bounded same-window coroutine capability is locked by `rem6_run_o3_same_window_coroutine_commits_direct`, `rem6_run_o3_same_window_indirect_coroutine_commits_cache_fabric_dram`, `rem6_run_o3_same_window_coroutine_requires_branch_lookahead_two`, `rem6_run_o3_same_window_indirect_coroutine_requires_branch_lookahead_two`, `rem6_run_o3_same_window_overwritten_coroutine_source_stays_terminal`, `rem6_run_o3_same_window_indirect_overwritten_coroutine_source_stays_terminal`, `rem6_run_o3_older_branch_discards_same_window_coroutine_chain`, `rem6_run_o3_older_branch_discards_same_window_indirect_coroutine_chain`, `rem6_run_host_switch_transfers_o3_same_window_coroutine`, `rem6_run_o3_same_window_coroutine_checkpoint_boundary`, `rem6_run_timing_suppresses_o3_same_window_coroutine`, `rem6_run_o3_same_window_coroutine_wrong_target_repairs_descendants`, and `rem6_run_o3_same_window_indirect_coroutine_wrong_target_repairs_descendants`: one delayed scalar load precedes a call, a distinct-link `JALR` coroutine PopThenPush, and one scalar descendant; direct `JAL x1` to `JALR x5,x1` and indirect `JALR x5` to `JALR x1,x5` rows prove both `x1->x5` and `x5->x1` positive execution, exact producer/consumer RAS lineage, staged link rename and fixed-FU writeback ownership, strict call-to-coroutine-to-descendant readiness, exact four-ROB/one-LSQ residency, direct transport-only activity, and cache/fabric/DRAM hierarchy activity. Both `x1->x5` and `x5->x1` boundary rows cover lookahead/overwrite suppression, older/wrong-target repair, and replacement-push preservation for a later ordinary return after ordered repair and drain. The shared lifecycle rows cover detailed-to-timing switch, live-checkpoint rejection, drained zero-ROB/zero-LSQ restore, and timing suppression in both directions. The exact adjacent call `Push` -> distinct-link coroutine `PopThenPush` -> ordinary return `Pop` round trip is separately locked by `rem6_run_o3_same_window_coroutine_round_trip_commits_direct`, `rem6_run_o3_same_window_indirect_coroutine_round_trip_commits_cache_fabric_dram`, `rem6_run_o3_same_window_coroutine_round_trip_requires_branch_lookahead_three`, `rem6_run_o3_same_window_coroutine_round_trip_middle_repair_discards_return`, `rem6_run_o3_same_window_coroutine_round_trip_terminal_return_repairs_direction`, `rem6_run_host_switch_transfers_o3_same_window_coroutine_round_trip`, `rem6_run_o3_same_window_coroutine_round_trip_checkpoint_boundary`, and `rem6_run_timing_suppresses_o3_same_window_coroutine_round_trip`: the forward `x1->x5` positive runs on direct memory, while the reverse `x5->x1` positive runs on `cache-fabric-dram` hierarchy memory; lookahead-two suppression, mode switch, live rejection plus drained checkpoint restore, and timing suppression iterate both cases and directions; middle wrong-target repair and terminal direction-only repair are focused direct-only fixtures. The route and repair rows remain case-specific, and no broader forwarding is claimed. The second-linked replacement chain is locked by `rem6_run_o3_second_linked_coroutine_consumes_replacement_direct`, `rem6_run_o3_second_linked_coroutine_consumes_replacement_cache_fabric_dram`, and `rem6_run_o3_second_linked_coroutine_requires_branch_lookahead_three`: direct `x1->x5->x1` and cache/fabric/DRAM `x5->x1->x5` rows place a delayed load, call, first `PopThenPush` coroutine, and second `PopThenPush` coroutine in one four-ROB/one-LSQ window; both linked consumers issue and open their RAS targets before response, rename points to the newest link writers, final register/memory witnesses and route activity are exact, three pushes/two pops/two RAS targets are observed, and lookahead two suppresses the second consumer to a three-row window. The committed- and exact live-source same-link call matrix is locked by `rem6_run_o3_committed_same_link_calls_cover_link_and_route_matrix`, `rem6_run_o3_live_same_link_targets_forward_across_link_and_route_matrix`, `rem6_run_o3_unresolved_live_same_link_targets_stay_terminal`, and `rem6_run_o3_older_branch_discards_same_link_call_and_descendant`: `JALR x1,0(x1)` and `JALR x5,0(x5)` cover both link registers across direct and cache/fabric/DRAM routes; committed-source rows retain exact four-ROB/one-LSQ residency, architectural old-target plus staged rename evidence, forwarded fallthrough descendants, indirect target-provider and push-only RAS counters, while live-source rows require one exact adjacent scalar producer, deliberately stale committed link state, a three-ROB/one-LSQ pre-response window, exact target-fetch issue before the delayed load response, target-descendant issue at the direct response boundary or the next hierarchy tick, exact predicted-taken target metadata, indirect target-provider, push-only RAS, and no repair or squash. Representative diagonal load-produced unresolved targets (`x1`/direct and `x5`/cache-fabric-DRAM) stay terminal, older-branch repair still squashes the call and descendant, `live_same_link_control_exposes_exact_producer_forwarded_target` and `detailed_live_same_link_control_uses_runtime_forwarded_target` lock typed authority and durable transient identity, and `hart_jalr_reads_same_register_source_before_writing_link` locks ISA read-before-write semantics. The exact producer-forwarded linked-call-to-return runtime descendant, with pairwise same-link and split-link CLI coverage, is locked by `rem6_run_o3_producer_forwarded_return_descendants_cover_link_shape_route_matrix`, `rem6_run_o3_producer_forwarded_return_requires_branch_lookahead_two`, `rem6_run_timing_suppresses_o3_producer_forwarded_returns`, `producer_forwarded_linked_calls_append_target_returns`, `producer_forwarded_linked_call_rejects_nonordinary_target_controls`, `producer_forwarded_split_link_call_appends_return_after_data_head_retires`, `pending_data_gate_admits_producer_forwarded_call_target_return`, `pending_data_gate_admits_same_and_split_link_direct_returns`, `producer_forwarded_return_apply_fails_closed_after_descendant_invalidation`, `producer_forwarded_return_apply_fails_closed_after_ras_lineage_changes`, and `branch_lookahead_one_does_not_stage_producer_forwarded_return`: pairwise matrix rows cover same-link `x1`/direct and `x5`/cache-fabric-DRAM plus split-link `x11->x5`/direct and `x11->x1`/cache-fabric-DRAM, separate target-source authority from link-destination/RAS ownership, and retain stale architectural target suppression, issue the return at the direct response boundary or ten ticks after the hierarchy response, use the call's speculative RAS push to fetch the exact fallthrough, preserve exact indirect/RAS provider, producer/call rename, direct-or-hierarchy route, and push/pop/use/correct metadata, require lookahead two, prove hierarchy response-plus-ten issue, preserve focused successful post-head runtime authority while the producer/call rows remain live, reject nonordinary target controls, fail closed when O3 or RAS lineage changes, prevent lookahead-one return staging, and preserve architecture while timing mode suppresses O3 runtime, trace, and stats surfaces. The bounded scalar-continuation return extension is locked by `rem6_run_o3_producer_forwarded_scalar_returns_cover_link_shape_route_matrix`, `rem6_run_o3_producer_forwarded_scalar_return_lookahead_one_keeps_return_unfetched`, `rem6_run_o3_producer_forwarded_scalar_return_rejects_non_link_scalar`, `rem6_run_timing_suppresses_o3_producer_forwarded_scalar_returns`, `producer_forwarded_scalar_lineage_survives_successful_data_head_retirement`, `producer_forwarded_scalar_return_waits_for_data_head_retirement`, `producer_forwarded_split_link_scalar_return_uses_link_destination`, `producer_forwarded_split_link_scalar_requires_link_dependency`, `producer_forwarded_scalar_return_rejects_nonordinary_shapes`, `producer_forwarded_scalar_lineage_fails_closed_after_identity_change`, `live_data_head_allows_scalar_sequential_fetch_but_not_return_row`, `pending_data_gate_allows_typed_scalar_sequential_fetch`, `scalar_continuation_preparation_holds_lineage_while_fetch_is_pending`, `committed_scalar_continuation_retains_exact_return_authority`, `committed_call_seed_reconstructs_unstaged_scalar_return_authority`, `committed_call_seed_reconstructs_already_executed_scalar_return_authority`, `full_lookahead_at_call_recording_retains_later_scalar_return_authority`, `prepared_scalar_continuation_survives_parent_commit_before_apply`, `scalar_stage_retains_authority_before_continuation_decision_is_prepared`, `retired_data_head_opens_scalar_sequential_fetch`, `retired_data_head_admits_same_and_split_link_scalar_return_predictions`, `branch_lookahead_one_does_not_stage_scalar_sequential_return`, `stale_ras_does_not_stage_scalar_sequential_return`, `incorrect_parent_resolution_discards_retained_scalar_authority`, `branch_checkpoint_restore_discards_retained_scalar_authority`, `o3_checkpoint_restore_discards_retained_scalar_authority`, `scalar_return_issue_waits_for_data_head_retirement_tick`, and `scalar_return_apply_fails_closed_after_scalar_lineage_changes`: a pairwise matrix covers same-link `x1`/direct and `x5`/cache-fabric-DRAM plus split-link `x5`/direct and `x1`/cache-fabric-DRAM rows, separating the target-source register from the link destination while extending each exact call target with one link-consuming scalar and a later ordinary return; pre-response evidence remains a three-row load/producer/call ROB with one scalar target fetch and no return or landing fetch, scalar issue occurs at the direct response boundary or ten ticks after the hierarchy response, and typed call-parent/fetch/RAS lineage preserves the first predicted return stream across correct hierarchy call retirement without admitting a five-row live window. Lookahead one, a scalar that does not consume the link, stale RAS, incorrect parent repair, predictor/RAS and O3-runtime checkpoint restore, scalar identity mutation, and nonordinary shapes fail closed, while timing mode preserves architecture without O3 surfaces; a retained historical return fetch is clamped to the completed data-head retirement tick. This does not claim strict pre-response scalar-row residency or arbitrary deeper chains. Open boundaries remain: link-sourced coroutine target shapes outside exact same-link authority and producer-forwarded linked-call returns beyond the exact direct/scalar continuations, producer-forwarded controls beyond the exact scalar-plus-return continuation, deeper control or mixed scalar/control chains beyond the configured untranslated scalar-only lane, arbitrary broader mixed windows, and general O3.
- [x] RISC-V atomic execution and parallel clusters execute real instructions.
- [x] Data access issue/response and `system.cpu.lsq0.storeConditionalFailures` diagnostics have tests.
- [x] Basic, GShare, BiMode, Tournament, TAGE-SC-L, and multiperspective perceptron branch predictors are trained from retired control flow.
- [x] Checker CPU support exists as a RISC-V retire-path reference hart with CLI-visible counts.
- [x] Minor-like fetch/decode/execute/commit timing, including scheduler-owned non-memory scalar/vector FU ExecuteWait latency with vector-memory waits owned by data completion, is wired into normal CPU execution.
- [x] Branch predictors steer fetch with speculation snapshots, squash, and rollback.
- [ ] A running O3 engine owns ROB, LSQ, rename map, commit, store-to-load forwarding, and FU latency.
- [x] CPU mode switching transfers live architectural and timing authority.
- [ ] KVM or equivalent fast-forward execution exists.
- [x] CPU instruction/data traffic uses the top-level L1/L2/L3 cache, fabric, and DRAM hierarchy by default.
**Live scalar-memory handoff update:** The direct and cache/fabric/DRAM untranslated rows transfer a versioned `o3-live-data-handoff` artifact. Every current capture writes one typed v7 schema with explicit operation, target, transport-or-buffered ownership, predecessor, row-count, forwarded-row, pending or completed aggregate partial-overlay, and ordered source-provenance fields across either one outstanding cacheable load plus up to three CPU-owned younger rows, one store plus one fully forwarded load with exact source-request and forwarded-byte identity, one transport-owned store plus one transport-owned partially overlaid load, an overlap-connected prefix of up to three older stores plus one pending partially overlaid load, or one completed partially overlaid load plus its still-live source stores. The load-only single-/multicore rows preserve exact request, route, address, issue, response, and ordered-retirement identity, and the two-worker rows keep peer memory progress outside the target CPU's non-restorable transfer. `rem6_run_host_switch_transfers_outstanding_o3_scalar_load_mmio` and `rem6_run_host_switch_transfers_multicore_cpu1_mmio_with_peer_memory` cover untranslated readfile-backed device loads with typed MMIO targets, exact source/target partitions and route latencies, no ordinary data transport, exactly-once architectural completion, and baseline timing preservation. `rem6_run_host_switch_transfers_cold_translated_scalar_load_younger_window_direct` and `rem6_run_host_switch_transfers_cold_translated_scalar_load_younger_window_cache_fabric_dram` cover a cold nonzero-latency page-map miss that opens a validated pre-issue scalar window, transfers four ROB/one LSQ rows with three younger scalar ALUs through direct or cache/fabric/DRAM memory, preserves Fetch/Memory/Data identity and baseline timing, rejects live checkpoint capture in `rem6_run_rejects_live_cold_translated_scalar_load_younger_window_checkpoint`, round-trips a drained zero-ROB/zero-LSQ runtime with matching load/store stats and a non-idempotent `x16` register replay witness in `rem6_run_restores_drained_cold_translated_scalar_load_younger_window_and_stats`, and suppresses O3 artifacts in `rem6_run_timing_suppresses_cold_translated_scalar_load_younger_window_o3_artifacts`; `riscv_core_translated_driver_fetches_cold_younger_window_before_data_issue` independently locks nonzero-latency serial fetch-before-issue ordering; `rem6_run_host_switch_transfers_cached_translated_scalar_load_younger_window_direct` warms the TLB, transfers the cached direct-memory load plus three younger scalar-ALU rows, preserves Fetch/Memory/Data identity and baseline timing, and leaves translated stores outside O3 until after the switch. `rem6_run_host_switch_transfers_cached_translated_scalar_load_younger_window_cache_fabric_dram` runs that same cached CPU0 window through L1/L2/L3, fabric, and DRAM with matching architectural and inherited O3 timing plus nonzero hierarchy resource activity. `rem6_run_host_switch_transfers_multicore_cpu1_cached_translated_scalar_load_cache_fabric_dram` removes the stale single-core boundary with two scheduler workers: both harts warm and issue translated hierarchy requests, CPU0 and CPU1 data windows overlap the host switch, the CPU1 transfer carries exact agent, partition, route, physical address, four-row ROB/one-row LSQ counts, and a three-younger-row count, the younger ALUs retain their final architectural values, CPU0 completes independently, and no CPU0 component enters CPU1's non-restorable authority. `rem6_run_host_switch_transfers_cached_translated_mmio_load_without_younger_window` removes the stale top-level readfile rejection: a cold translated MMIO load fills the configured TLB, the same-page second 64-bit load records the matching TLB hit and switches mid-flight with exactly one ROB row, one LSQ row, zero younger rows, a typed MMIO target, non-restorable and non-quiescent transfer metadata, matching Fetch and HostAction artifacts, two architectural Data completions, no Memory data-channel records, zero ordinary data-transport activity, and Exec-traced post-switch timing-mode execution of three real younger ALUs. `rem6_run_unused_readfile_preserves_cached_translated_memory_younger_window` and `riscv_cluster_mmio_translation_driver_preserves_unmapped_cached_memory_fetch_ahead` prove an unrelated readfile does not alter the translated-memory window, while `riscv_core_leaves_unmapped_translated_atomic_for_memory_after_mmio_probe` proves ordinary translated atomics bypass an unmapped MMIO bus. Cached permission faults, unresolved cold misses before translation completion, PMA-uncacheable physical mappings, configured translated window depth, and younger translated memory prefixes still suppress translated fetch-ahead; `riscv_cluster_parallel_turns_issue_translated_data_accesses` locks translation-ready wakes, and `riscv_system_tick_limited_parallel_driver_routes_translated_mmio_and_memory_data` records one translated MMIO TLB miss followed by one hit while routing a peer translated memory target without false idle. Overlapping mappings fail closed; `rem6_run_rejects_unsupported_riscv_data_translation_combinations` still rejects RISC-V SE and GDB combinations, while translated instruction budgets remain separately unsupported. `rem6_run_host_switch_transfers_multi_source_partial_forwarded_store_load_direct` and `rem6_run_host_switch_transfers_multi_source_partial_forwarded_store_load_cache_fabric_dram` transfer a four-row three-store/one-load window before its first response: the oldest store and load remain transport-owned, the middle and youngest stores remain CPU-buffered behind explicit predecessor requests, one aggregate 8-byte overlay owns mask `0x0f`, ordered source masks `0x03`, `0x08`, and `0x04` prove youngest-store precedence, response mask `0xf0` preserves uncovered memory bytes, all issue/writeback/commit ticks match the no-switch baseline, and the hierarchy row emits cache, transport, fabric, and DRAM activity. `rem6_run_host_switch_rejects_multi_source_partial_forwarded_store_load_with_younger_row` keeps the pending bounded authority closed when an independent younger ALU is resident. `rem6_run_host_switch_transfers_completed_multi_source_partial_forwarded_store_load_direct` and `rem6_run_host_switch_transfers_completed_multi_source_partial_forwarded_store_load_cache_fabric_dram` transfer the post-load/pre-source-response three-row state: one transport-owned middle halfword store, one buffered youngest byte store, and one completed 8-byte load with original/response/live/retired masks `0x0f`/`0xf0`/`0x0c`/`0x03`, final data `aa00dd0655667788`, exact two-source provenance, baseline-equivalent issue/writeback/commit ticks, and route-appropriate cache, transport, fabric, and DRAM activity; `rem6_run_host_switch_rejects_completed_partial_forwarded_store_load_with_younger_row` keeps an unrelated younger row outside that completed authority. Current load-only, MMIO, fully forwarded, pending single-/multi-source partial-overlay, and completed partial-overlay handoffs expose `schema_version = 7` in top-level transfer and HostAction artifacts; historical v1-v6 payloads are decode-only; `live_data_handoff_round_trips_completed_partial_overlay_v7` and `live_data_handoff_decodes_v6_multi_source_partial_overlay` lock current round-trip and frozen v6 compatibility; HostAction exposes `first_completed_partial_overlay_original_forwarded_mask`, `first_completed_partial_overlay_live_forwarded_mask`, and `first_completed_partial_overlay_retired_forwarded_mask`; malformed current ownership, predecessor, source-mask, target, forwarding, pending-overlay, and completed-overlay mask/sequence shapes fail closed, valid MMIO and forwarding handoffs remain explicitly non-restorable, and consumers use typed target, ownership, forwarded-row, aggregate-overlay, and source-provenance accessors. The transfer stats continue to expose live handoff, non-restorable, latest-handoff, latest-restorable, and target-scoped component evidence. `rem6_run_delays_architectural_visibility_until_scheduled_commit_stage` derives the first fetch-response tick and proves zero committed instructions and no `x5` visibility before the four scheduled pre-commit ticks, Commit residency at response-plus-four, and architectural retirement at response-plus-five across direct one-/two-core and cache/fabric/DRAM one-core runs; `rem6_run_keeps_scalar_float_result_invisible_through_execute_wait` proves a scalar FP destination remains unpublished through its two scheduled FCLASS wait cycles, focused FDIV.S/VDIVU.VV drive coverage locks the broader non-memory FU scheduler ownership, and source policy keeps `riscv_fu_latency` as the single latency/class/owner authority. Together with the live FU row, this representative matrix checks the mode-switch and normal in-order timing items and keeps CPU evidence at 8/10; broader translated multicore/memory shapes beyond the current CPU1 cacheable-load row, disjoint or deeper/cross-line store composition, broader MMIO device classes and multi-device concurrency, and restorable in-flight transport ownership remain open.
**Migrated:** One untranslated integer-result-to-scalar-`LD` address-generation lane now accepts scalar-load and disjoint unordered-atomic producer heads, keeps the younger load resident in the LSQ with `address: null` before the producer response, gives the older memory row priority at issue width one and co-issues the dependent memory row with an independent ALU row at width two, and materializes through the normal PMP/PMA/route/request-binding path. Direct and cache/fabric/DRAM evidence plus atomic-overlap, dynamic-route, retry/failure, live-checkpoint, mode-switch, and timing boundaries are locked by `rem6_run_o3_dependent_result_address_matrix_direct`, `rem6_run_o3_dependent_result_address_matrix_cache_fabric_dram`, `rem6_run_o3_dependent_result_address_boundaries_and_live_actions`, and `rem6_run_timing_suppresses_o3_dependent_result_address`. A bounded one-result/up-to-three-scalar suffix now carries first-fetch execution identity plus exact route and physical-span authorization across direct, cached-translated, hierarchy, and integer-MMIO lanes, with split-fetch, stale route/span/PMA, mapped noninteger-MMIO, abort, and timing suppression boundaries. A bounded two-result/four-row extension admits either one independent untranslated cacheable integer, FP, or restricted vector read after the first result or one independent disjoint unordered AMO after an untranslated pure-read head, with exact per-fetch `Head`/`YoungerRead`/`YoungerBufferedEffect` roles, direct and hierarchy overlap, predecessor-gated atomic transport, first- and second-result scalar wakeup, oldest-first publication, pair-wide abort and retry/failure cleanup, and acquire/release, overlap, `v0` mask, dependent-address, zero-destination, third-result, live-action, and timing suppression boundaries. `rem6_run_host_switch_transfers_live_o3_fu_authority_until_retirement` and `rem6_run_toml_schedules_host_execution_mode_switch` prove that CLI and TOML host scheduling can deliver the normal serialized execution-mode action while a real four-row scalar FU window is resident: the detailed-to-timing transfer captures the live gate plus exact ROB and live rename pressure, inherited rows preserve their pre-switch issue/writeback timing and ordered architectural commit, the first instruction beyond inherited authority executes in timing mode without an O3 event, a later architectural `m5` switch resumes detailed admission, and normal checkpoint quiescence remains strict. `manifest_decoder_returns_none_for_empty_execution_mode_authority` and `rem6_run_omits_execution_mode_authority_after_switch_transfer_rollback` prove that a real detailed-mode switch can register nonempty checkpoint authority, restore its earlier authority-free transfer, and take a later top-level checkpoint without reviving a zero-target `host.execution_modes` component or a second component/chunk stat emission. `rem6_run_m5_reset_between_o3_lsq_request_and_response_keeps_latency` proves that a direct LR and cache/fabric/DRAM AMO issued before delayed `m5_reset_stats` retain their transport-derived response latency in the new epoch across host-dump, gem5-style, runtime, and debug-summary aliases while pre-reset operation counts and raw O3 rows remain cleared; one pending-data authority survives reset and retry/restore controls discard it. Explicit `--riscv-o3-scalar-memory-depth 4` evidence now has `rem6_run_o3_detailed_four_scalar_loads_overlap_before_first_response_direct`, `rem6_run_o3_detailed_four_scalar_loads_overlap_through_cache_fabric_dram`, `rem6_run_o3_detailed_four_scalar_loads_remain_resident_at_tick_limit`, `rem6_run_o3_timing_four_scalar_loads_preserve_architecture_without_o3_window`, and `rem6_run_o3_scalar_memory_depth_config_and_cli_precedence_drive_four_load_window`, proving four independent cacheable untranslated scalar loads issue before the first response, reach exact ROB/LSQ peaks four, preserve nondecreasing program-order commit ticks, exact twelve-row Data access and four-before-first-response Memory request evidence, exercise direct and cache/fabric/DRAM routes, and reduce timing mode to one data request before its first response without widening branch speculation beyond two; focused CPU/runtime tests separately force reverse responses and prove oldest-first retirement, `translated_cached_memory_driver_fetches_third_younger_alu` fills a cached translated single-load scalar-ALU suffix while `translated_uncached_scalar_load_does_not_fetch_younger_alu`, `translated_cached_permission_fault_does_not_fetch_younger_alu`, `translated_cached_uncacheable_load_does_not_fetch_younger_alu`, `translated_cached_scalar_load_window_rejects_younger_memory_prefix`, and zero-destination coverage keep unsupported fetch-ahead suppressed, explicit depth one suppresses younger scalar-load fetch, omitted scalar-memory depth preserves the existing two-load default and explicit `--riscv-branch-lookahead 2` three-load behavior, and TOML depth plus CLI override precedence drive the same live window. Atomic RISC-V execution, frontend/data slices, branch predictor state and retired training, directly issued completed 4-byte fetches occupying the in-order timing state before retire, normal `drive_next_action`, cluster, and data-translation drivers issuing bounded fetch-ahead for completed straight-line 4-byte integer instructions, predictor-selected conditional branches including top-level `rem6 run --riscv-branch-predictor gshare`, `bimode`, `tage-sc-l`, and `multiperspective-perceptron` fetch-steering evidence, and direct RISC-V `JAL`/`JALR` targets before retire, with direct `JAL` front-end predictions preserved through retire in the normal driver; compressed straight-line instructions reaching the same normal parallel-cluster fetch-ahead path, branch and direct-jump fetch-ahead speculation history recorded and resolved at retire, and explicit `--riscv-branch-lookahead 2` two-deep conditional-branch fetch-ahead through the normal `rem6 run` path with an older misprediction repairing predictor history and removing younger branch speculation before wrong-path registers commit,
completed younger fetches consumed by in-order branch flushes, wrong-path branch fetch-ahead requests from fall-through and predicted-target paths squashed even when their memory response is still outstanding, bounded normal-driver fetch-ahead requests inserted into the in-order timing state when issued and before their memory response, per-retired instruction in-order stage advancement with runtime stats, non-retire fetch and intermediate retire-loop cycle records retained in per-core in-order pipeline history and consumed by top-level stats, data-response wait cycles emitted as explicit non-retire resource-stall in-order cycle records before data-instruction retire, per-stage occupied-cycle plus advanced/retired movement-cycle stats, per-stage resource-blocked and ordering-blocked instruction plus cycle summaries emitted from in-order cycle records, explicit data-stall cycle stats, head-of-line in-order resource-stall classification that attributes younger work as ordering-blocked by stall cause, retired Tournament predictor local/global selection counters plus GShare, BiMode, Tournament, TAGE-SC-L, and multiperspective-perceptron predictor snapshot lookup/update/repair counters emitted by top-level `rem6 run`, fixed scalar integer M-extension, vector integer multiply and multiply-accumulate including fixed-point signed fractional multiply and widening variants, divide, remainder, fixed-point scaling shifts, narrowing shifts, narrow clip, and integer/widening reductions, scalar FP arithmetic, compare, conversion, and misc, plus decoded vector FP add/sub/min/max, mask compare, conversion/move/merge, sign/class, multiply/FMA/divide/sqrt execute-stage latency emitted as additional in-order resource-stall cycles from top-level CLI runs,
per-core fetch-response and data-response wait cycle stats, retired branch
prediction, speculation repair, and redirect summaries in normal in-order timing records, RISC-V
normal parallel-cluster pending-fetch resource-stall accounting consumed by CLI run stats, core checkpoints preserving the basic fetch-steering branch predictor/BTB payload, including live fetch-ahead pending branch speculation state, plus GShare, BiMode, Tournament, TAGE-SC-L, and multiperspective perceptron predictor checkpoint payloads, a RISC-V checker CPU option that runs an
independent reference hart at retire, records structured execution/state
mismatches, and exposes aggregate plus single-mode functional/timing/detailed checked/mismatch mode-lane counts plus `/cores/0/checker/execution_mode` structured authority through `rem6 run --checker-cpu`
stats, per-stage in-order flush plus branch-prediction, trap-redirect, and
SBI timer interrupt-redirect attribution stats, plus aggregate and per-stage
branch/trap/interrupt redirect counters and flush-cycle stats from real redirect cycle records, top-level SBI timer and SBI IPI/SSIP interrupt redirects with Pipeline+Fetch debug evidence proving flushed younger work, non-branch attribution, JSON stage/cause conservation, gem5-style interruptRedirect aggregate, stage, flushCause, and redirectCause text aliases, and a same-sequence matrix correlating CPU0 six-cycle fetch wait, six-cycle delayed scalar-load data wait, and nineteen-cycle DIV execute wait, two-worker CPU1 six-cycle data wait and nineteen-cycle DIV execute wait, and a non-timer two-worker CPU1 nine-cycle fetch wait to later interrupt termination.
Correlation consumes both explicit `flushed` rows and interrupt-cancelled non-retiring `advanced` rows, so aggregate sequence totals preserve current-instruction resource backlog without double-counting a sequence that also accumulated ordering backlog. `rem6_run_pipeline_debug_correlates_data_wait_backlog_with_interrupt_flush` records a real CPU0 load from issue tick 108 through response tick 114, two explicit flushed rows plus one cancelled current row, and eighteen detailed-mode associations through `/debug/pipeline_summary/stall_backlog_flush/stall_cause/data_wait/flush_cause/interrupt_redirect/sequences`, `/debug/pipeline_summary/stall_backlog_flush/stall_cause/data_wait/flush_cause/interrupt_redirect/block_kind/ordering_blocked/blocked_stage/decode/flushed_stage/commit/stall_cycles`, `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.data_wait.flush_cause.interrupt_redirect.sequences`, and `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.data_wait.flush_cause.interrupt_redirect.block_kind.ordering_blocked.blocked_stage.decode.flushed_stage.commit.stall_cycles`; `rem6_run_pipeline_debug_correlates_execute_wait_backlog_with_interrupt_flush` records three younger ordering-blocked sequences with fifty-seven associations plus the cancelled DIV's nineteen-cycle resource backlog through `/debug/pipeline_summary/stall_backlog_flush/stall_cause/execute_wait/flush_cause/interrupt_redirect/sequences`, `/debug/pipeline_summary/stall_backlog_flush/stall_cause/execute_wait/flush_cause/interrupt_redirect/block_kind/ordering_blocked/blocked_stage/decode/flushed_stage/execute/stall_cycles`, and `/debug/pipeline_summary/stall_backlog_flush/stall_cause/execute_wait/flush_cause/interrupt_redirect/block_kind/resource_blocked/blocked_stage/execute/flushed_stage/commit/stall_cycles`, while proving the interrupted DIV and younger rows do not retire through O3 and draining interrupt-cancelled ROB/rename state; timing mode independently records the scheduler-owned cancelled DIV as a nineteen-cycle Execute-stage resource backlog terminating at Commit with no younger rows; `rem6_run_pipeline_debug_correlates_cpu1_execute_wait_backlog_with_interrupt_flush` starts hart 1 through SBI HSM and proves detailed resource and ordering cells plus the timing-mode cancelled-current lane through `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu1/stall_cause/execute_wait/flush_cause/interrupt_redirect/stall_cycles`, `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu1/stall_cause/execute_wait/flush_cause/interrupt_redirect/block_kind/resource_blocked/blocked_stage/execute/flushed_stage/commit/stall_cycles`, and `sim.debug.pipeline_trace.cpu.cpu1.stall_backlog_flush.stall_cause.execute_wait.flush_cause.interrupt_redirect.block_kind.ordering_blocked.blocked_stage.decode.flushed_stage.execute.stall_cycles`, with CPU0 zero; `rem6_run_pipeline_debug_correlates_cpu1_data_wait_backlog_with_interrupt_flush` records CPU1 load issue tick 157, response tick 163, three cancelled/flushed younger sequences, and eighteen detailed associations through `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu1/stall_cause/data_wait/flush_cause/interrupt_redirect/block_kind/ordering_blocked/blocked_stage/decode/flushed_stage/commit/stall_cycles` and `sim.debug.pipeline_trace.cpu.cpu1.stall_backlog_flush.stall_cause.data_wait.flush_cause.interrupt_redirect.block_kind.ordering_blocked.blocked_stage.decode.flushed_stage.commit.stall_cycles`, with CPU0 zero and timing suppression; `rem6_run_pipeline_debug_correlates_cpu1_ipi_fetch_wait_backlog_with_interrupt_flush` proves the SBI IPI event, CPU1 supervisor-software-interrupt cause, CPU0 isolation, a two-sequence twenty-six-cycle positive aggregate, the nine-cycle `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu1/stall_cause/fetch_wait/flush_cause/interrupt_redirect/block_kind/resource_blocked/blocked_stage/commit/flushed_stage/commit/stall_cycles`, eight-cycle `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu1/stall_cause/fetch_wait/flush_cause/interrupt_redirect/block_kind/ordering_blocked/blocked_stage/fetch2/flushed_stage/commit/stall_cycles`, and nine-cycle `sim.debug.pipeline_trace.cpu.cpu1.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.interrupt_redirect.block_kind.ordering_blocked.blocked_stage.execute.flushed_stage.execute.stall_cycles` leaves, while lookahead one removes younger/order-dependent leaves but preserves the cancelled current row's resource backlog; `interrupt_cancelled_advance_correlates_resource_blocked_backlog`, `riscv_core_interrupt_discards_live_gate_renames_without_committing_destination`, `riscv_core_driver_blocks_pending_fetch_retire_when_interrupt_can_redirect`, and `in_order_pipeline_cycle_summary_records_interrupt_redirect_flush_counts` lock the lower-level summary, architectural/O3, checker, and pipeline non-retirement boundaries,
top-level `rem6 run --riscv-in-order-width` configures the live five-stage in-order pipeline width, enforcing configured width-one backpressure and width-two multi-row occupancy through Fetch1, Fetch2, Decode, Execute, and Commit in direct and cache/fabric/DRAM CLI rows without false younger retire; `rem6_run_in_order_pipeline_width_changes_executed_stage_occupancy` locks the five stage maxima and hierarchy fetch activity.
`rem6_run_text_stats_emit_in_order_stage_movement_aliases` proves width-three movement counts can exceed movement-cycle presence when multiple instructions advance together, while preserving the gem5-compatible movement aliases.
Focused `riscv_direct_fetch_issue_defers_younger_row_without_advancing_time`, `riscv_completed_fetches_overlap_in_order_pipeline_before_retire`, `riscv_core_driver_in_order_width_allows_frontend_overlap_without_false_retire`, and `riscv_core_driver_width_one_advances_before_second_fetch` cover pure enqueue, direct overlap, normal serial admission, and width-one scheduled backpressure.
`riscv_core_translated_driver_admits_width_two_before_pipeline_cycle`, `riscv_cluster_parallel_fetch_admits_width_two_before_pipeline_cycle`, and `riscv_cluster_parallel_translation_admits_width_two_before_pipeline_cycle` extend that explicit admission boundary through translated serial, prepared parallel, and translated parallel drivers without a second timing authority.
Nested predicted-control evidence is locked by `rem6_run_o3_nested_controls_commit_direct`, `rem6_run_o3_outer_misprediction_discards_nested_control_cache_fabric_dram`, `rem6_run_o3_inner_misprediction_preserves_outer_control_direct`, `rem6_run_o3_load_dependent_inner_branch_suppresses_descendant`, `rem6_run_o3_nested_control_requires_branch_lookahead_two`, `rem6_run_host_switch_transfers_o3_nested_controls`, `rem6_run_host_switch_preserves_load_dependent_inner_control_timing`, `rem6_run_host_switch_preserves_inner_misprediction_rollback`, `rem6_run_o3_nested_control_checkpoint_boundary`, and `rem6_run_timing_suppresses_o3_nested_controls`: one delayed load, two direct conditional branches, and one scalar MUL occupy four ROB/one LSQ rows; both branches and the descendant issue before the load response when lookahead is two, lookahead one records only the outer predictor lookup and stages only the load plus outer branch before the response, outer rollback removes the inner branch and descendant while inner rollback preserves the outer branch, a load-dependent inner branch remains terminal, same-byte split-suffix replacement prunes the invalid inner chain, cache/fabric/DRAM wrong-path memory remains absent, schema-v7 detailed-to-timing transfer preserves inherited issue/writeback/commit timing by retaining fetch ordering for the live control-window ROB rows independently of resolved issue-gating edges, the three-row load-dependent-inner switch preserves the terminal branch's baseline timing, switched inner rollback preserves outer timing while removing descendant effects and drains ROB/LSQ state, live checkpoint capture rejects, drained restore has zero ROB/LSQ and no live-data chunk, and timing mode exposes no O3 surface. The three-deep matrix extends that bounded authority with one delayed load plus mixed `BNE`/`BLT`/`BGEU` controls in the exact four-row ROB: `rem6_run_o3_three_deep_mixed_controls_commit_direct` and `rem6_run_o3_three_deep_control_requires_branch_lookahead_three` lock three pre-response predictor lookups versus lookahead-two suppression; `rem6_run_o3_three_deep_outer_misprediction_discards_younger_controls_cache_fabric_dram`, `rem6_run_o3_three_deep_middle_misprediction_preserves_outer_control_direct`, and `rem6_run_o3_three_deep_inner_misprediction_preserves_older_controls_direct` preserve only older controls and keep wrong-path Data/Memory effects absent, including cache/fabric/DRAM activity for the outer case; `rem6_run_o3_three_deep_load_dependent_inner_control_is_terminal` keeps a load-dependent third control resident but unpredicted; `rem6_run_host_switch_transfers_o3_three_deep_controls` preserves all four rows' issue/writeback/commit ticks through a non-restorable schema-v7 timing handoff; and `rem6_run_o3_three_deep_control_checkpoint_boundary` plus `rem6_run_timing_suppresses_o3_three_deep_controls` preserve live rejection, drained zero-ROB/LSQ restore, and complete O3-surface suppression without increasing the representative score.
Top-level default RISC-V instruction/data traffic still runs through L1/L2/L3 MSI cache/fabric/DRAM with explicit `--memory-system direct` timing-test coverage, a top-level dependent-negative detailed-mode scalar DIV live-retire gate that schedules one 19-tick pre-architectural wait while timing mode remains ungated, leaves valid younger fetch-ahead intact, preserves `/memory/0/hex`, and delays `/simulation/final_tick`, with `/cores/0/o3_runtime/live_retire_gate/scheduled_waits`, `/cores/0/o3_runtime/live_retire_gate/wait_ticks`, `/cores/0/o3_runtime/live_retire_gate/max_wait_ticks`, `sim.cpu0.o3.live_retire_gate.scheduled_waits`, `sim.cpu0.o3.live_retire_gate.wait_ticks`, and `sim.cpu0.o3.live_retire_gate.max_wait_ticks` evidence plus a tick-limited dependent pre-retirement snapshot whose `/cores/0/o3_runtime/snapshot/rob/count` is two for ordered PCs `0x8000000c` and `0x80000010`, whose rows are `ready=false` and `live_staged=true`, and whose live rename map already contains integer `x3` and `x4` while architectural memory and stop witnesses remain untouched; core-only checkpoint-v21 request/absolute-ready-tick plus live-staged ROB/rename restore rebinds the refetch, schedules once, avoids restored physical-ID reuse, preserves public-snapshot rollback provenance, rejects inconsistent live metadata and legacy public-overlay physical aliases, survives stats reset and mode change, rolls interrupt/manual redirects back to the prior committed physical register, decodes v20 without live-staged metadata, and decodes v19 with no gate, target-scoped CPU1 O3 mode-switch state-transfer stats such as `sim.host_actions.execution_mode_switch_state_transfer.target.cpu1.component.cpu1.chunk.o3_runtime_state.payload_checksum_accumulator`, top-level CPU1 `/cores/1/o3_runtime/execution_mode` and `/cores/1/o3_runtime/stats_reset_tick` detailed-switch authority plus `sim.cpu1.o3.execution_mode.detailed`, `sim.cpu1.o3.stats_epoch`, and `sim.cpu1.o3.stats_reset_tick`, top-level debug O3 per-event timing lanes `issue_tick`, `writeback_tick`, and `commit_tick` plus ROB commit-boundary lanes `rob_commits_at_tick` and `rob_commit_blocked` and event-summary aggregates `commit_blocked_events` and `max_commits_at_tick` proving the resident DIV owns that dependent-negative two-row peak while the pre-retirement snapshot, rather than a misattributed retirement event, exposes the unready head; each row commits at its own architectural boundary and the dependent younger ADDI is not reported issued before older writeback, resettable host-action dump aggregates `sim.host_actions.stats_dump.cpu0.o3.event_summary.rob.commits`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.rob.max_occupancy`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.rob.commit_blocked_events`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.rob.max_commits_at_tick`, and `sim.host_actions.stats_dump.cpu0.o3.event_summary.rename.map_entries`, resettable host-action event-window row stats `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.issue_tick`, `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.writeback_tick`, `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.commit_tick`, `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.rob_commits_at_tick`, and `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.rob_commit_blocked`, top-level `/cores/0/o3_runtime/event_window/max_rob_occupancy/tick`, `/cores/0/o3_runtime/event_window/max_structural_pressure/lsq_occupancy`, `/cores/0/o3_runtime/event_window/max_fu_latency/fu_latency_cycles`, and `/cores/0/o3_runtime/event_window/max_lsq_data_latency/lsq_data_latency_ticks` trace-window evidence plus `/cores/0/o3_runtime/event_summary/rob/allocations`, `/cores/0/o3_runtime/branch_direction_mismatch/mismatches`, `/cores/0/o3_runtime/branch_target_mismatch/targetless_mismatches`, `sim.cpu0.o3.event_summary.event_window.max_rob_occupancy.tick`, `sim.cpu0.o3.event_summary.event_window.max_rob_occupancy.issue_tick`, `sim.cpu0.o3.event_summary.event_window.max_structural_pressure.rename_map_entries`, `sim.cpu0.o3.event_summary.event_window.max_structural_pressure.writeback_tick`, `sim.cpu0.o3.event_summary.event_window.max_fu_latency.fu_latency_cycles`, `sim.cpu0.o3.event_summary.event_window.max_lsq_data_latency.lsq_data_latency_ticks`, and `sim.cpu0.o3.event_summary.event_window.max_lsq_data_latency.commit_tick`, debug O3 event-window stat evidence `sim.debug.o3_trace.event_window.records`, `sim.debug.o3_trace.event_window.max_rob_occupancy.issue_tick`, `sim.debug.o3_trace.event_window.max_structural_pressure.lsq_occupancy`, `sim.debug.o3_trace.event_window.max_structural_pressure.rob_commits_at_tick`, `sim.debug.o3_trace.event_window.max_structural_pressure.rob_commit_blocked`, `sim.debug.o3_trace.event_window.max_fu_latency.fu_latency_cycles`, `sim.debug.o3_trace.event_window.max_lsq_data_latency.lsq_data_latency_ticks`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_rob_occupancy.rob_occupancy`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_structural_pressure.rob_occupancy`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_structural_pressure.rob_commits_at_tick`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_structural_pressure.rob_commit_blocked`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_fu_latency.commit_tick`, and `sim.debug.o3_trace.cpu.cpu0.event_window.max_lsq_data_latency.pc`, `sim.cpu0.o3.event_summary.branch_direction_mismatch.mismatches`, `sim.cpu0.o3.event_summary.branch_target_mismatch.targetless_mismatches`, and `sim.cpu0.o3.event_summary.records` trace-aggregate evidence, final snapshot stats `sim.cpu0.o3.snapshot.rob.entries`, `sim.cpu0.o3.snapshot.lsq.entries`, `sim.cpu0.o3.snapshot.rename_map.count`, and `sim.cpu0.o3.snapshot.rename_map.entries`, dump-boundary snapshot stats `sim.host_actions.stats_dump.cpu0.o3.snapshot.rob.count`, `sim.host_actions.stats_dump.cpu0.o3.snapshot.lsq.count`, `sim.host_actions.stats_dump.cpu0.o3.snapshot.rename_map.count`, `sim.host_actions.stats_dump.cpu0.o3.snapshot.rob.entries`, `sim.host_actions.stats_dump.cpu0.o3.snapshot.lsq.entries`, and `sim.host_actions.stats_dump.cpu0.o3.snapshot.rename_map.entries`, sole-authority O3 checkpoint capture where new captures prune stale historical `o3-pending-state` chunks from reused registries and emit only the validated `o3-runtime-state` payload, while legacy pending-only checkpoints still merge into a complete runtime payload and historical dual chunks are consistency-checked before restore, sparse top-level `/cores/0/o3_runtime/checkpoint_restore/components` restore metadata, host-action checkpoint-restore authority decode-error stat `sim.host_actions.checkpoint_restore.execution_mode_authority.decode_errors`, decoded O3 checkpoint chunk lanes `o3_runtime/decode_error`, `o3_runtime/snapshot_rename_map_entries`, `o3_runtime/stats_max_rob_occupancy`, and `o3_runtime/stats_max_lsq_occupancy`, plus attached-scheduler live-retire-gate authority lanes `o3_runtime/live_retire_gate_request_agent`, `o3_runtime/live_retire_gate_request_sequence`, and `o3_runtime/live_retire_gate_ready_tick` with capture/restore/HostAction-debug parity and null inactive/decode-error suppression, plus `sim.cpu1.o3.checkpoint_restore.payload_bytes`, `sim.cpu1.o3.checkpoint_restore.execution_mode_authority.target.cpu1.mode.detailed`, and `sim.cpu1.o3.checkpoint_restore.target.cpu1.component.cpu1.chunk.o3_runtime_state.payload_checksum_accumulator`, and O3 policy helpers.
Top-level scalar-load overlap evidence preserves the default two-row direct/cache-fabric-DRAM path with `sim.cpu0.o3.max_rob_occupancy=2`, an independent younger ADDI issue before the real load response, load-dependent issue at or after response, in-order commit, and timing suppression, while explicit `--riscv-o3-scalar-memory-depth 4` now adds a standalone untranslated cacheable load head plus three younger scalar ALUs through `rem6_run_o3_scalar_load_head_issues_three_younger_alus_before_response_direct`, `rem6_run_o3_scalar_load_head_issues_three_younger_alus_cache_fabric_dram`, `rem6_run_o3_scalar_load_head_exposes_four_resident_rows_before_response`, `rem6_run_o3_scalar_load_head_stops_at_load_dependent_boundary`, and `rem6_run_timing_scalar_load_head_has_no_detailed_o3_window`: the positive chain reaches exact ROB peak four with one LSQ row, all three ALUs issue transitively with fan-in before the load response and commit no earlier than the load, Data/Memory traces identify the real load request rather than witness stores, the cache-backed resident snapshot exposes four unready live rows, the unshadowed load-dependent row is admitted as a three-row terminal boundary while its downstream consumer is absent until ordered execution resumes, hierarchy activity reaches cache/data, transport/data, fabric, and DRAM, and timing mode exposes no detailed O3 surface. Bounded mixed control-window evidence is locked by `rem6_run_o3_mixed_load_alu_branch_exposes_terminal_resident_row_direct`, `rem6_run_o3_mixed_load_alu_branch_squash_direct`, `rem6_run_o3_mixed_load_alu_branch_squash_cache_fabric_dram`, `rem6_run_timing_suppresses_o3_mixed_load_alu_branch_window`, `rem6_run_host_switch_transfers_o3_mixed_load_alu_branch_until_squash`, `rem6_run_o3_mixed_branch_checkpoint_rejects_live_resident_window`, and `rem6_run_o3_mixed_branch_checkpoint_captures_drained_runtime_state`: a scalar load + two younger ALUs + terminal direct conditional branch is resident as an exact 4-row ROB / 1-row LSQ window; the branch row has no destination or rename destination and is terminal, not speculative execution authority; the completed run deterministically predicts not taken then resolves taken, and the direct conditional branch squashes the wrong-path fall-through store; the wrong-path store has no O3 event, Data trace row, or third transport request while the target store occurs exactly once; direct and cache/fabric/DRAM rows preserve the evidence, timing mode suppresses the O3 surface, schema-v7 detailed-to-timing handoff remains live and non-restorable with runtime 4 ROB/1 LSQ rows, handoff resident/younger/outstanding counts of 1/3/1, and preserved event timing, live checkpoint capture rejects the proven four-row resident window, and post-branch-drain checkpoint capture succeeds with no live-data chunk and a zero ROB/LSQ runtime snapshot. `rem6_run_o3_predicted_descendants_commit_direct`, `rem6_run_o3_correctly_predicted_taken_descendants_commit_direct`, `rem6_run_o3_predicted_descendants_squash_cache_fabric_dram`, `rem6_run_o3_load_dependent_branch_suppresses_predicted_descendants`, `rem6_run_host_switch_transfers_o3_predicted_descendants`, `rem6_run_o3_predicted_descendant_checkpoint_boundary`, and `rem6_run_timing_suppresses_o3_predicted_descendants` add a second bounded control shape: one delayed load, one independent direct conditional branch, one predicted-path scalar MUL, and one dependent ADD occupy four ROB/one LSQ rows; the branch and MUL issue before the load response, dependent issue honors MUL writeback, correct not-taken execution commits in order, a trained correctly predicted taken iteration preserves target MUL/ADD pre-response timing without split/refetch identity drift, focused runtime coverage proves issued wrong-path branch/MUL/ADD state before descendant discard, taken misprediction removes younger rename and speculative execution state before a wrong-path store can issue, the load-dependent branch remains a two-row terminal boundary, cache/fabric/DRAM activity is nonzero, detailed-to-timing transfer reuses schema v7 with baseline-equivalent timing, live checkpoint capture rejects, drained capture/restore has zero ROB/LSQ and no live-data chunk, and timing mode exposes no O3 surface. `rem6_run_o3_detailed_two_scalar_loads_overlap_before_first_response_direct`, `rem6_run_o3_detailed_two_scalar_loads_overlap_through_cache_fabric_dram`, `rem6_run_o3_detailed_two_scalar_loads_remain_resident_at_tick_limit`, and `rem6_run_o3_timing_two_scalar_loads_preserve_architecture_without_o3_window` add two simultaneous untranslated load rows, second-issue-before-first-response timing, exact LSQ peak two, a two-resident-row tick-limit snapshot, final register/memory witnesses, hierarchy activity, and timing-mode negative control under the routes' response ordering, while CPU/system tests own younger-response-first matching, oldest-first writeback/commit, exact pending counts, and one-at-a-time instruction-headroom draining; `older_detailed_scalar_load_failure_replays_younger_cancelled_request`, `older_detailed_scalar_load_retry_replays_younger_cancelled_request`, `younger_completed_load_replay_replaces_cancelled_timing_provenance`, `disabling_detailed_mode_preserves_ordered_pending_load_retirement`, `pending_scalar_memory_retires_after_detailed_mode_turns_off`, `older_mmio_load_blocks_younger_memory_issue`, and `younger_mmio_load_does_not_fall_through_to_memory_while_load_is_outstanding` cover terminal cancellation with younger reissue, stale callback suppression, fresh replay timing, ordered pending retirement and global accounting across detailed-mode disable, and serialized older/younger MMIO routing, while redirect cleanup, post-response provenance through stats reset, configured load-head ALU depth, and pending-fetch suppression remain covered separately; a mixed resident checkpoint remains rejected before any bank writes. Explicit `--riscv-branch-lookahead 2` now supplies a third independent load before the first response in direct and cache-fabric-DRAM runs, with exact LSQ peak three, three-resident tick-limit rows, O3/Data/Memory request evidence, oldest-first architectural retirement, timing-mode suppression, full-window dependency and PMA rejection, oldest/middle failure suffix cancellation, and HTM cleanup. Explicit `--riscv-o3-scalar-memory-depth 4` independently supplies a fourth load without widening branch speculation, with exact ROB/LSQ peaks four, exact four-row resident snapshots and twelve-row Data traces, direct and cache-fabric-DRAM activity evidence, one-request-before-first-response timing control, TOML and CLI-override runtime evidence, translated/depth-one fetch suppression, all-older dependency and fourth-load PMA rejection, oldest/third failure suffix cancellation, reverse-response oldest-first retirement in focused CPU/runtime tests, and four-request HTM cleanup. Top-level `rem6 run --execute --debug-flags O3,Data,Memory` store-load evidence is now locked by `rem6_run_o3_detailed_store_then_disjoint_load_overlap_direct`, `rem6_run_o3_detailed_store_then_disjoint_load_overlap_cache_fabric_dram`, `rem6_run_o3_detailed_store_and_disjoint_load_remain_resident_together`, `rem6_run_o3_detailed_aliasing_store_load_forwards_without_second_memory_request_direct`, `rem6_run_o3_detailed_aliasing_store_load_forwards_without_second_memory_request_cache_fabric_dram`, `rem6_run_o3_detailed_store_load_width_mismatch_merges_store_byte`, `rem6_run_o3_timing_store_load_preserves_architecture_without_o3_window`, `rem6_run_o3_detailed_contained_store_load_forwarding_direct_matrix`, `rem6_run_o3_detailed_contained_store_load_forwarding_cache_fabric_dram_matrix`, `rem6_run_o3_timing_contained_store_load_uses_transport_without_o3_window`, `rem6_run_o3_detailed_partial_store_load_merge_direct_matrix`, `rem6_run_o3_detailed_partial_store_load_merge_cache_fabric_dram_matrix`, and `rem6_run_o3_timing_partial_store_load_uses_transport_without_o3_merge`: a cacheable untranslated normal store and exact-range-disjoint younger scalar load can occupy two LSQ rows and issue before the store response through direct and cache/fabric/DRAM routes while commits remain oldest-first; an exact-range same-width or fully contained byte/halfword younger load instead receives the selected little-endian bytes through a local completion with no load transport request, completes before the store response, reaches LSQ peak two, applies architectural sign or zero extension, and writes back only after oldest-first retirement. A single older cacheable untranslated store may also contribute one, two, or four bytes to a wider younger scalar load while that load still issues a real transport request; merged response bytes feed deferred writeback plus O3/Data events. The contained `lb`/`lbu`/`lh`/`lhu` matrix preserves three unique data requests in detailed mode across direct and cache/fabric/DRAM routes while timing mode uses four real serialized requests; byte-to-word, half-to-word, and word-to-doubleword partial-overlap matrices keep a real younger load request, merge only store-owned response bytes, reach LSQ peak two, expose `store_load_forwarding_partial` and `store_load_forwarding_bytes`, preserve oldest-first retirement, and retain four requests in the full direct detailed matrix, two detailed cache/fabric/DRAM representatives, and the direct timing control. Data trace still records every architectural access. Focused CPU/runtime tests cover load-store serialization and partial/width-overlap response-merge pairs, PMA-uncacheable serialization and fetch-ahead suppression, ordered architectural load writeback, and older-store retry/failure cancellation plus replay after an already completed contained forwarded load, and partial-overlay retry/failure cancellation with stale-response suppression; forwarding counters now share the runtime range classifier and require live forwarding provenance instead of adjacent retired values. A bounded ordered overlap-connected cacheable store prefix now composes byte ownership across partial-range mixed-width older stores, gives younger overlaps precedence, keeps each younger store transport-gated by predecessor success, and merges uncovered load bytes from the real response; `rem6_run_o3_detailed_multi_store_byte_composition_direct`, `rem6_run_o3_detailed_multi_store_byte_composition_cache_fabric_dram`, `rem6_run_o3_multi_store_byte_composition_rows_are_resident_before_responses`, `rem6_run_o3_timing_multi_store_byte_composition_uses_transport_without_o3_merge`, and `failed_middle_partial_store_cancels_the_younger_composed_suffix` lock direct, cache/fabric/DRAM, four-row residency, timing suppression, and focused middle-store failure cancellation. `younger_mmio_load_does_not_issue_while_store_is_outstanding` locks request-level MMIO serialization while target-aware suppression before MMIO classification remains future evidence. Full-forwarded store/load switch handoff is now covered by `rem6_run_host_switch_transfers_full_forwarded_store_load_direct` and `rem6_run_host_switch_transfers_full_forwarded_store_load_cache_fabric_dram`: one leading untranslated cacheable store remains transport-owned, one same-address load completes locally without a second transport request, and the detailed-to-timing switch lands before the store response while preserving two ROB/two LSQ rows, oldest-first commit, final register/memory witnesses, hierarchy activity, `live_data_handoff=true`, `restorable=false`, `quiescence_gate.validated=false`, HostAction trace parity, and artifact lanes `outstanding_requests=1`, `resident_rows=2`, `transport_owned_rows=1`, `forwarded_rows=1`, `first_operation=store`, `first_forwarded_operation=load`, `first_forwarded_data_hex=2a000000`, plus exact forwarding-source request identity. Pending single-source partial-overlay switch handoff is covered by `rem6_run_host_switch_transfers_partial_forwarded_store_load_direct` and `rem6_run_host_switch_transfers_partial_forwarded_store_load_cache_fabric_dram`: the switch lands after both requests issue but before either response, preserves two ROB/two LSQ rows and baseline issue/writeback/commit ticks, keeps four architectural data requests, exposes hierarchy activity, and carries `transport_owned_rows=2`, `buffered_store_rows=0`, `partial_overlay_rows=1`, `partial_overlay_source_rows=1`, source/load request identity, `first_partial_overlay_source_data_hex=5a`, `first_partial_overlay_forwarded_mask=2`, `first_partial_overlay_response_owned_mask=13`, and `first_partial_overlay_forwarded_data_hex=005a0000`. Pending multi-source partial-overlay switch handoff is covered by `rem6_run_host_switch_transfers_multi_source_partial_forwarded_store_load_direct` and `rem6_run_host_switch_transfers_multi_source_partial_forwarded_store_load_cache_fabric_dram`: three ordered stores and one 64-bit load remain resident before the first response, the artifact records `resident_rows=4`, `transport_owned_rows=2`, `buffered_store_rows=2`, `partial_overlay_rows=1`, and `partial_overlay_source_rows=3`, predecessor-linked buffered ownership, aggregate mask `15`, response mask `240`, source masks `3`/`8`/`4`, exact source requests and bytes, baseline-equivalent timing, and hierarchy activity. `rem6_run_host_switch_rejects_partial_forwarded_store_load_with_younger_row` and `rem6_run_host_switch_rejects_multi_source_partial_forwarded_store_load_with_younger_row` keep an already-issued independent younger ALU outside both bounded authorities, and `completed_partial_overlay_becomes_live_handoff_authority` locks extraction of the completed load, while the completed direct/hierarchy transfer rows and post-load younger-row rejection lock its bounded three-row authority.
Attached-scheduler checkpoint capture now projects uniquely claimed scheduler-instance-bound full live-retire-gate wake snapshots, including a crate-internal non-restored runtime event token in addition to id/tick/order/kind, out of the scheduler payload without canceling the running wake, retains redirect-cleared no-op wakes as CPU-owned until they disappear, registers exact host checkpoint-control barriers as preserve-on-restore events, and on immediate restore discards only the revalidated CPU wake before the CPU rebinds and re-arms exactly once while future controls remain queued; mixed dispatch keeps parallel callbacks out of the serial dispatcher while preserving recorded progress transitions, serial-blocker metadata, and restorable partition clocks, binds runtime plan provenance to the scheduler instance, normalizes peer clocks after positive-tick callback panics, excludes serial-blocked epochs from planned parallel-capacity aggregates while retaining their actual dispatch evidence, full and discard-only restore revisions abort the old epoch horizon, projected-restore preflight now validates the exact discard/preserve event sets and preserved-event clocks before host registry or execution-mode commit, tracked borrowed scheduler chunks are stripped before every staged capture so inactive state cannot leak into later scheduler-aware or direct manifests, attached-bank pruning prevents stale control claims from exempting counter-reused foreign events, and a borrowed legacy restore with no scheduler chunk discards only exact pending CPU wakes while preserving control and foreign work, while non-checkpoint host actions avoid relocking attached schedulers from parallel callbacks, planned checkpoint, restore, and mode-switch workload actions execute as serial borrowed-scheduler deliveries and register future planned controls as preserve-on-restore claims while empty borrowed contexts do not synthesize zero-component transfers, periodic state-transfer deliveries use borrowed serial context and register each recurrence, raw or unbound state-transfer emits are rejected, whole unbound state-transfer turns fail before any event is scheduled, component bindings are checked against immutable and live scheduler instances plus stable storage provenance, scheduler aliases including rebound shared mutexes are rejected, blocking bank acquisition follows global scheduler-storage order while preserving component-ordered records, busy attached peers are rejected without blocking under the controller lock, attached scheduler-bank guards remain held through manifest capture and complete restore, and foreign, duplicate-claimed, wrong-scheduler, detached-scheduler, and unattached-scheduler callbacks remain non-quiescent for scheduler-payload capture/restore, with `rem6_run_rejects_live_retire_gate_checkpoint_then_restores_drained_state_with_attached_scheduler`, `attached_scheduler_checkpoint_captures_restores_and_rearms_live_div_gate_once`, `redirected_live_gate_wake_remains_owned_until_scheduler_restore_discards_it`, `restore_preserves_checkpoint_external_control_events`, `borrowed_scheduler_restore_aborts_the_outer_serial_epoch`, `borrowed_scheduler_capture_removes_stale_chunk_from_later_direct_manifest`, `borrowed_scheduler_restore_tracks_chunk_for_later_direct_manifest`, `attached_scheduler_ownership_supersedes_stale_borrowed_tracking`, `attached_scheduler_ownership_replaces_live_borrowed_component`, `non_checkpoint_action_does_not_lock_attached_scheduler`, `periodic_checkpoint_preserves_registered_recurrence_with_attached_scheduler`, `rem6_run_repeats_m5_checkpoint_host_actions_when_period_is_set_from_real_riscv_execution`, `workload_replay_executes_planned_host_actions`, `riscv_system_event_switch_cpu_targets_originating_core`, `planned_checkpoint_preserves_future_scheduler_control`, `projected_restore_rejects_past_preserved_event_before_mode_commit`, `checkpoint_banks_share_scheduler_storage_lock_order`, `raw_host_checkpoint_emit_requires_component_binding`, `checkpoint_schedule_requires_component_binding`, `borrowed_scheduler_rejects_attached_component_instance_mismatch`, `borrowed_scheduler_rejects_attached_alias_component_without_relocking`, `scheduler_checkpoint_bank_rejects_component_aliases`, `borrowed_scheduler_checkpoint_rejects_locked_attached_peer`, `attached_scheduler_replacement_rejects_colliding_owned_event`, `scheduler_checkpoint_bank_rejects_rebound_shared_mutex_aliases`, `unbound_state_transfer_turn_rejects_before_scheduling_ordinary_events`, `checkpoint_holds_attached_scheduler_through_later_bank_capture`, `restore_holds_attached_scheduler_through_later_bank_restore`, `borrowed_scheduler_rejects_detached_instance_after_attached_storage_rebind`, `borrowed_scheduler_restore_without_chunk_discards_only_pending_owned_wakes`, `borrowed_scheduler_restore_accepts_empty_legacy_component`, `mixed_event_dispatch_runs_parallel_and_serial_callbacks_without_cross_dispatch`, `mixed_event_dispatch_rejects_stale_parallel_epoch_plan`, `mixed_event_dispatch_rejects_same_tick_replacement_plan`, `mixed_event_dispatch_rejects_event_outside_plan_horizon`, `mixed_event_dispatch_rejects_publicly_constructed_plan`, `mixed_event_dispatch_rejects_replaced_non_head_serial_blocker`, `mixed_event_dispatch_rejects_plan_from_another_scheduler_instance`, `mixed_parallel_turn_preserves_progress_transitions_before_serial_barrier`, `mixed_serial_dispatch_leaves_restorable_quiescent_snapshot`, `mixed_parallel_dispatch_leaves_restorable_quiescent_snapshot`, `mixed_parallel_panic_leaves_restorable_quiescent_snapshot`, `system_run_excludes_serial_blocked_mixed_epochs_from_planned_capacity`, `serial_epoch_stops_at_callback_scheduler_restore`, `discard_exact_events_aborts_active_serial_epoch`, `discard_exact_events_to_idle_leaves_restorable_snapshot`, `checkpoint_projection_restore_discards_and_preserves_exact_owned_events`, `checkpoint_projection_validation_rejects_past_preserved_event_without_mutation`, `direct_apply_prunes_stale_control_claim_before_event_identity_reuse`, `runtime_scheduler_event_token_stays_internal`, `stale_parallel_epoch_plan_scheduler_error_round_trips`, `legacy_snapshot_pending_scheduler_error_decodes_tag_8`, `parallel_state_transfer_source_is_a_registered_serial_control_event`, `owned_event_capture_still_reports_unrelated_pending_work`, `owned_event_is_bound_to_its_scheduler_instance`, `unattached_scheduler_wake_cannot_exclude_colliding_attached_event`, and `duplicate_owned_event_claim_does_not_bypass_quiescence` top-level/system/kernel evidence.
The readfile-MMIO switch rows prove single- and two-worker same-run device completion, mixed peer-memory progress, and typed non-restorable target authority without claiming device restore, multiple simultaneous MMIO requests, or broader device classes; artifact/stats evidence includes `sim.host_actions.execution_mode_switch_state_transfer.target.cpu1.components`, `sim.host_actions.execution_mode_switch_state_transfer.live_data_handoffs`, `sim.host_actions.execution_mode_switch_state_transfer.non_restorable`, `sim.host_actions.execution_mode_switch_state_transfer.latest_live_data_handoff`, and `sim.host_actions.execution_mode_switch_state_transfer.latest_restorable`. `mmio_aware_cached_translated_compressed_load_suppresses_younger_fetch_ahead` uses the canonical completed-instruction assembler to keep a line-end compressed translated MMIO load conservative, while paired `riscv_system_parallel_driver_routes_translated_mmio_and_memory_data` and `riscv_system_tick_limited_parallel_driver_routes_translated_mmio_and_memory_data` rows retain coverage for `RiscvSystemRunDriver::drive_until_host_stop_parallel_with_mmio_and_data_translation` and `RiscvSystemRunDriver::drive_until_host_stop_or_tick_limit_parallel_with_mmio_and_data_translation`. Direct-call and return O3 host-action dumps now expose `sim.host_actions.stats_dump.cpu0.o3.branch_event.kind.call_direct`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.squash_kind.call_direct`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.squashed_target_link_write_kind.call_direct`, `system.cpu.ftq.squashes_0::CallDirect`, `system.cpu.ftq.squashedTargets_0::CallDirect`, `system.cpu.ftq.squashedTargetsWithLinkWrites_0::CallDirect`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.kind.return`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.taken_kind.return`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.without_link_write_kind.return`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.squash_kind.return`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.squashed_target_without_link_write_kind.return`, `sim.host_actions.stats_dump.cpu0.o3.branch_repair_direction_only_kind.return`, `system.cpu.ftq.squashes_0::Return`, `system.cpu.ftq.squashedTargets_0::Return`, and `system.cpu.ftq.squashedTargetsWithoutLinkWrites_0::Return` at the `m5_dump_stats` boundary, with multicore CPU1 active-hart coverage for `sim.host_actions.stats_dump.cpu1.o3.branch_event.kind.call_direct`, `system.cpu1.ftq.squashes_0::CallDirect`, `sim.host_actions.stats_dump.cpu1.o3.branch_event.kind.return`, `sim.host_actions.stats_dump.cpu1.o3.branch_event.taken_kind.return`, `sim.host_actions.stats_dump.cpu1.o3.branch_event.squashed_target_without_link_write_kind.return`, `system.cpu1.ftq.squashes_0::Return`, `system.cpu1.ftq.squashedTargets_0::Return`, `system.cpu1.ftq.squashedTargetsWithoutLinkWrites_0::Return`, `sim.host_actions.stats_dump.cpu1.o3.branch_event.kind.call_indirect`, `sim.host_actions.stats_dump.cpu1.o3.branch_event.kind.direct_unconditional`, `system.cpu1.ftq.squashes_0::CallIndirect`, `system.cpu1.ftq.squashes_0::DirectUncond`, and `system.cpu1.ftq.squashedTargetsWithLinkWrites_0::CallIndirect`, explicit CPU1 reset-scoped dump evidence where `m5_reset_stats` clears post-reset `sim.host_actions.stats_dump.cpu1.o3.branch_event.branches`, `sim.host_actions.stats_dump.cpu1.o3.branch_event.kind.call_indirect`, `system.cpu1.ftq.squashes_0::CallIndirect`, `system.cpu1.ftq.squashes_0::DirectUncond`, and `system.cpu1.ftq.squashedTargets_0::total` while final JSON keeps `sim.cpu1.o3.branch_event.branches` and `system.cpu1.ftq.squashes_0::CallIndirect` at zero, plus suppression of generic/CPU0 FTQ aliases.
**Not migrated:** Full Minor-like resource contention with realistic multi-stage stalls, squashes, width interactions, and interruption/replay beyond the scheduler-owned normal Fetch1/Fetch2/Decode/Execute/Commit path and representative scalar MUL/DIV ExecuteWait resource stalls, general CPU-mode replacement beyond the currently covered scheduled live four-row scalar FU handoff, and broader same-run scalar-memory switch matrices beyond the proven single-/multicore untranslated loads, translated cacheable loads, translated and untranslated readfile-MMIO loads, bounded direct/cache-fabric-DRAM full-forward pairs, bounded pending single-source partial-overlay pairs, the bounded pending three-store/one-load overlap-connected composition, and the bounded completed two-store/one-load post-response composition; open rows include additional translated multicore target and peer combinations, disjoint, cross-line, fifth-row, or deeper store composition, broader MMIO device classes and multi-device live transfer, restorable in-flight transport ownership, broad executable O3 timing beyond the current targeted bounded four-row scalar FU/ALU, configurable four/six/eight-row untranslated scalar-only load/ALU windows, one-result/up-to-three-scalar FP/vector/atomic/integer-MMIO suffix and bounded two-result/four-row mixed-data pairs including one pure-read/younger-AMO row, configurable four-entry independent untranslated load and one-leading-store/younger-load windows, existing bounded one-, two-, and three-deep direct-conditional predicted scalar-descendant windows, and bounded no-link and linked direct-call/indirect-call/return control-kind windows; link-sourced coroutine target shapes outside exact same-link authority and producer-forwarded linked-call returns beyond the exact direct/scalar continuations, producer-forwarded controls beyond the exact scalar-plus-return continuation, deeper non-scalar/control/result/device chains beyond the configured eight-row untranslated scalar-only lane, arbitrary broader mixed windows, broader mixed-data result depth, translated or MMIO result pairs, dependent stores/atomics, FP/vector dependent addresses, multiple unresolved addresses, and younger side-effect chains beyond one independent disjoint unordered AMO in result row two, and broader forwarding shapes beyond the current contained-forward plus pending and completed single-/multi-source partial-overlay cases,
general multiple outstanding memory operations beyond those bounded slices including fifth and deeper or disjoint store prefixes, translated multi-device overlap, cross-line scalar overlays, target-aware MMIO fetch-ahead suppression before data-target classification, broader predictor-family speculation snapshots and rollback beyond the current direct, conditional, and indirect selected-family
top-level slices, gem5-class checker coverage across future timing CPU modes, and KVM equivalents.
**Evidence:** `RiscvCore::execute_next_completed_fetch`, `RiscvCore::has_pending_o3_runtime_retirement`, `rem6_run_host_switch_transfers_live_o3_fu_authority_until_retirement`, `rem6_run_toml_schedules_host_execution_mode_switch`, `rem6_run_host_switch_transfers_outstanding_o3_scalar_load_direct`, `rem6_run_host_switch_transfers_outstanding_o3_scalar_load_cache_fabric_dram`, `rem6_run_host_switch_transfers_full_forwarded_store_load_direct`, `rem6_run_host_switch_transfers_full_forwarded_store_load_cache_fabric_dram`, `rem6_run_host_switch_transfers_partial_forwarded_store_load_direct`, `rem6_run_host_switch_transfers_partial_forwarded_store_load_cache_fabric_dram`, `rem6_run_host_switch_transfers_multi_source_partial_forwarded_store_load_direct`, `rem6_run_host_switch_transfers_multi_source_partial_forwarded_store_load_cache_fabric_dram`, `rem6_run_host_switch_rejects_partial_forwarded_store_load_with_younger_row`, `rem6_run_host_switch_rejects_multi_source_partial_forwarded_store_load_with_younger_row`, `rem6_run_host_switch_transfers_multicore_cpu1_scalar_load_direct`, `rem6_run_host_switch_transfers_multicore_cpu1_scalar_load_cache_fabric_dram`, `rem6_run_host_switch_transfers_outstanding_o3_scalar_load_mmio`, `rem6_run_host_switch_transfers_multicore_cpu1_mmio_with_peer_memory`, `rem6_run_host_switch_transfers_cold_translated_scalar_load_younger_window_direct`, `rem6_run_host_switch_transfers_cold_translated_scalar_load_younger_window_cache_fabric_dram`, `rem6_run_rejects_live_cold_translated_scalar_load_younger_window_checkpoint`, `rem6_run_restores_drained_cold_translated_scalar_load_younger_window_and_stats`, `rem6_run_timing_suppresses_cold_translated_scalar_load_younger_window_o3_artifacts`, `riscv_core_translated_driver_fetches_cold_younger_window_before_data_issue`, `rem6_run_host_switch_transfers_cached_translated_scalar_load_younger_window_direct`, `rem6_run_host_switch_transfers_cached_translated_scalar_load_younger_window_cache_fabric_dram`, `rem6_run_host_switch_transfers_multicore_cpu1_cached_translated_scalar_load_cache_fabric_dram`, `rem6_run_host_switch_transfers_cached_translated_mmio_load_without_younger_window`, `riscv_cluster_parallel_turns_issue_translated_data_accesses`, `riscv_system_parallel_driver_supplies_page_map_to_translated_data_path`, `riscv_system_tick_limited_parallel_driver_routes_translated_mmio_and_memory_data`, `RiscvSystemRunDriver::drive_until_host_stop_or_tick_limit_parallel_with_data_translation`, `RiscvSystemRunDriver::drive_until_host_stop_or_tick_limit_parallel_with_mmio_and_data_translation`, `manifest_decoder_returns_none_for_empty_execution_mode_authority`, `empty_execution_mode_restore_prunes_owned_component_with_extra_chunks`, `empty_execution_modes_prune_prepopulated_checkpoint_component`, `rem6_run_omits_execution_mode_authority_after_switch_transfer_rollback`, `rem6_run_m5_reset_between_o3_lsq_request_and_response_keeps_latency`,
`RiscvClusterRun`, `record_data_retire_cycle`, `RiscvCheckerCpu`,
`InOrderPipelineState`, `RiscvLiveRetireGateState`, `O3RuntimeState`, `O3DistributedIssueScheduler`, `O3SourceRenamePlan`, and the focused O3/runtime live-window modules, including `rem6_run_o3_detailed_scalar_load_overlaps_independent_issue_direct`, `rem6_run_o3_detailed_scalar_load_overlaps_independent_issue_cache_fabric_dram`, `rem6_run_o3_detailed_scalar_load_exposes_younger_row_before_response`, `rem6_run_o3_timing_scalar_load_has_no_younger_o3_issue_window`, `rem6_run_o3_detailed_two_scalar_loads_overlap_before_first_response_direct`, `rem6_run_o3_detailed_two_scalar_loads_overlap_through_cache_fabric_dram`, `rem6_run_o3_detailed_two_scalar_loads_remain_resident_at_tick_limit`, `rem6_run_o3_timing_two_scalar_loads_preserve_architecture_without_o3_window`, `rem6_run_o3_detailed_three_scalar_loads_overlap_before_first_response_direct`, `rem6_run_o3_detailed_three_scalar_loads_overlap_through_cache_fabric_dram`, `rem6_run_o3_detailed_three_scalar_loads_remain_resident_at_tick_limit`, `rem6_run_o3_timing_three_scalar_loads_preserve_architecture_without_o3_window`, `rem6_run_o3_detailed_store_then_disjoint_load_overlap_direct`, `rem6_run_o3_detailed_store_then_disjoint_load_overlap_cache_fabric_dram`, `rem6_run_o3_detailed_store_and_disjoint_load_remain_resident_together`, `rem6_run_o3_detailed_aliasing_store_load_forwards_without_second_memory_request_direct`, `rem6_run_o3_detailed_aliasing_store_load_forwards_without_second_memory_request_cache_fabric_dram`, `rem6_run_o3_detailed_store_load_width_mismatch_merges_store_byte`, `rem6_run_o3_timing_store_load_preserves_architecture_without_o3_window`, `rem6_run_o3_detailed_contained_store_load_forwarding_direct_matrix`, `rem6_run_o3_detailed_contained_store_load_forwarding_cache_fabric_dram_matrix`, `rem6_run_o3_timing_contained_store_load_uses_transport_without_o3_window`, `rem6_run_o3_detailed_partial_store_load_merge_direct_matrix`, `rem6_run_o3_detailed_partial_store_load_merge_cache_fabric_dram_matrix`, `rem6_run_o3_timing_partial_store_load_uses_transport_without_o3_merge`, `rem6_run_o3_detailed_multi_store_byte_composition_direct`, `rem6_run_o3_detailed_multi_store_byte_composition_cache_fabric_dram`, `rem6_run_o3_multi_store_byte_composition_rows_are_resident_before_responses`, `rem6_run_o3_timing_multi_store_byte_composition_uses_transport_without_o3_merge`, and `failed_middle_partial_store_cancels_the_younger_composed_suffix`,
CPU frontend, checker, and O3 tests, including `rem6_run_o3_detailed_div_live_retire_gate_delays_architectural_commit`, `rem6_run_o3_detailed_mode_exposes_live_rob_overlap`, `rem6_run_o3_detailed_add_only_does_not_schedule_live_retire_gate`, `riscv_core_only_checkpoint_restores_pending_live_retire_gate_and_rearms_refetch`, `riscv_core_checkpoint_emits_only_runtime_o3_authority_and_prunes_stale_pending`, `riscv_core_checkpoint_accepts_matching_legacy_pending_beside_runtime`, `riscv_core_checkpoint_restores_legacy_o3_pending_chunk_without_runtime_chunk`, `riscv_core_checkpoint_rejects_mismatched_o3_pending_chunk_without_partial_restore`, `riscv_core_checkpoint_restores_runtime_only_and_no_o3_chunk_variants`, `riscv_core_checkpoint_rejects_malformed_o3_chunks_without_partial_restore`, `live_rename_overlay_rolls_back_to_committed_mapping_on_discard`, and `live_staged_rob_checkpoint_reconstructs_rename_overlay`, plus O3 branch-event aggregate misprediction and squashed-target-kind evidence at `/cores/0/o3_runtime/branch_event/mispredictions`, `sim.cpu0.o3.branch_event.mispredictions`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.mispredictions`, `/cores/0/o3_runtime/branch_event/squashed_target_kind/call_indirect`, `/cores/0/o3_runtime/branch_event/squashed_target_kind/direct_unconditional`, `sim.cpu0.o3.branch_event.squashed_target_kind.call_indirect`, `sim.cpu0.o3.branch_event.squashed_target_kind.direct_unconditional`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.squashed_target_kind.call_indirect`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.squashed_target_kind.direct_conditional`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.squashed_target_kind.direct_unconditional`, and live branch-mismatch JSON/text aliases `system.cpu.iew.branchDirectionMismatches`, `system.cpu.iew.branchDirectionMismatch_0::DirectUncond`, `system.cpu.iew.branchTargetlessMismatches`, `system.cpu.iew.branchTargetlessMismatch_0::DirectCond`, `system.cpu.iew.branchWrongTargets`, and `system.cpu.iew.branchWrongTarget_0::CallIndirect`.
CLI run stats include per-core in-order pipeline cycle, retired, and
fetch-response wait counters from executed RISC-V instructions, and CLI data
stats show load/store response wait changing the in-order pipeline cycle
counter, data-stall cycle stat, and data-wait cycle stat; top-level CLI
latency tests also cover vector fixed-point scaling shifts, narrowing shifts,
narrow clip, and integer/widening reduction execute-stage stall cycles from
executed RISC-V runs; CLI run stats also expose per-core
in-order cycle-plan advance, general flush, resource-blocked,
ordering-blocked, branch-prediction, branch-misprediction,
branch-prediction flush, redirect, branch-prediction redirect, trap redirect, and interrupt redirect counters from the same executed RISC-V
timing records, including nonzero branch-prediction flush evidence for a real
taken RISC-V branch; they also expose branch-speculation prediction, repair, younger-removal, and maximum-pending counters for a real nested conditional branch run where the corrected older branch prevents younger wrong-path register writes from committing. CLI `rem6 run --riscv-branch-predictor gshare`, `bimode`, `tage-sc-l`, and `multiperspective-perceptron` drive the normal RISC-V fetch-ahead path with the selected predictor, clean stale wrong-path in-order state across loop backedges, and change deterministic fetch-steering timing or branch-steering counters while preserving the final architectural register state; CLI stats now expose per-family GShare, BiMode, Tournament, TAGE-SC-L, and multiperspective-perceptron predictor counters plus `system.cpu.branchPred.<family>.*` text aliases from live core snapshots, including selected GShare, BiMode, and Tournament rollback counters from the live top-level nested-branch repair path plus explicit selected TAGE-SC-L and multiperspective-perceptron rollback counters from top-level selected snapshot cleanup runs.
The selected snapshot cleanup runs include conditional multiperspective-perceptron wrong-path branches and indirect JALR wrong-path fetch-ahead for TAGE-SC-L plus
multiperspective-perceptron, with indirect-unconditional branch-kind lookup
stats proving the top-level path.
Branch redirect CLI runs now emit `Branch` debug records and `sim.debug.branch_trace.records`, `sim.debug.branch_trace.mispredictions`, `sim.debug.branch_trace.flushed`, and CPU/kind/outcome scoped stats from executed in-order branch-prediction cycle records, with flushed counts checked against emitted flushed-sequence lists.
These runs also emit per-stage in-order flush counts, aggregate and per-stage flush-cycle and branch-prediction flush-cycle counts, proving squash attribution and cycle-level squash presence from the same executed cycle records.
The same Pipeline rows now enforce plan-owned redirect/flush conservation: every nonempty `flushed` list has identical `flush_cause` and `redirect_cause`, empty lists have no flush cause, raw branch-flush rows equal `sim.debug.pipeline_trace.flush_cause.branch_prediction.records`, and focused branch, trap, interrupt, and redirect-without-younger-work tests derive from the same typed authority.
Normal in-order pipeline cause-record and stage-record matrices now expose `sim.cpu0.pipeline.in_order.stall_cause.data_wait.records`, `/cores/0/in_order_pipeline/stall_cause/data_wait/records`, `sim.cpu0.pipeline.in_order.stall_cause.data_wait.stage.commit.records`, `/cores/0/in_order_pipeline/stall_cause/data_wait/stage_records/commit`, `sim.cpu0.pipeline.in_order.stall_cause.execute_wait.records`, `sim.cpu0.pipeline.in_order.stall_cause.execute_wait.stage.decode.records`, `/cores/0/in_order_pipeline/stall_cause/execute_wait/records`, `/cores/0/in_order_pipeline/stall_cause/execute_wait/stage_records/decode`, `sim.cpu0.pipeline.in_order.flush_cause.branch_prediction.records`, `sim.cpu0.pipeline.in_order.flush_cause.branch_prediction.stage.fetch1.records`, `/cores/0/in_order_pipeline/flush_cause/branch_prediction/records`, `/cores/0/in_order_pipeline/flush_cause/branch_prediction/stage_records/fetch1`, `sim.cpu0.pipeline.in_order.redirect_cause.trap_redirect.records`, `sim.cpu0.pipeline.in_order.redirect_cause.trap_redirect.stage.commit.records`, `/cores/0/in_order_pipeline/redirect_cause/trap_redirect/records`, `/cores/0/in_order_pipeline/redirect_cause/trap_redirect/stage_records/commit`, `system.cpu.pipeline.inOrder.stallCause.dataWait.records`, `system.cpu.pipeline.inOrder.stallCause.dataWait.stage.commit.records`, `system.cpu.pipeline.inOrder.stallCause.executeWait.records`, `system.cpu.pipeline.inOrder.stallCause.executeWait.stage.decode.records`, `system.cpu.pipeline.inOrder.flushCause.branchPrediction.records`, `system.cpu.pipeline.inOrder.flushCause.branchPrediction.stage.fetch1.records`, `system.cpu.pipeline.inOrder.redirectCause.trapRedirect.records`, and `system.cpu.pipeline.inOrder.redirectCause.trapRedirect.stage.commit.records` from top-level `rem6 run --execute` JSON/text outputs without requiring Pipeline debug.
Pipeline debug runs emit per-cycle `Pipeline` debug records with `sim.debug.pipeline_trace.records`, `sim.debug.pipeline_trace.branch_prediction_flushed`, `sim.debug.pipeline_trace.flush_cause.branch_prediction.records`, `sim.debug.pipeline_trace.flush_cause.branch_prediction.stage.fetch1.flushed_cycles`, `sim.debug.pipeline_trace.flush_cause.trap_redirect.records`, `sim.debug.pipeline_trace.flush_cause.trap_redirect.flushed`, `sim.debug.pipeline_trace.redirect_cause.trap_redirect.records`, `sim.debug.pipeline_trace.redirect_cause.interrupt_redirect.records`, `sim.debug.pipeline_trace.redirect_cause.trap_redirect.flushed`, `sim.debug.pipeline_trace.redirect_cause.trap_redirect.stage.commit.flushed_cycles`, `sim.debug.pipeline_trace.redirect_cause.trap_redirect.branch_prediction_flushed`, `sim.debug.pipeline_trace.stall_cause.fetch_wait.records`, `sim.debug.pipeline_trace.stall_cause.data_wait.stall_cycles`, `sim.debug.pipeline_trace.stall_cause.data_wait.stage.commit.resource_blocked_cycles`, `sim.debug.pipeline_trace.stall_cause.execute_wait.records`, `sim.debug.pipeline_trace.stall_cause.execute_wait.stage.decode.ordering_blocked_cycles`, `sim.debug.pipeline_trace.stage.commit.before_in_flight_cycles`, `sim.debug.pipeline_trace.stage.commit.advanced`, `sim.debug.pipeline_trace.stage.commit.advanced_cycles`, `sim.debug.pipeline_trace.stage.commit.retired`, `sim.debug.pipeline_trace.stage.commit.retired_cycles`, `sim.debug.pipeline_trace.stage.fetch1.flushed`, `sim.debug.pipeline_trace.stage.fetch1.flushed_cycles`, `sim.debug.pipeline_trace.stage.fetch1.branch_prediction_flushed_cycles`, `sim.debug.pipeline_trace.stage.commit.trap_redirect_flushed_cycles`, `sim.debug.pipeline_trace.stage.commit.resource_blocked`, `sim.debug.pipeline_trace.stage.decode.ordering_blocked_cycles`, `sim.debug.pipeline_trace.stage.commit.resource_blocked_cycles`, stage-scoped in-flight and movement count/cycle, stall-cause blocked/cycle, aggregate flush-cycle, branch/trap/interrupt-specific stage flushed-cycle, flush-cause flushed/cycle, redirect-cause stage flushed/cycle stats, CPU-scoped debug matrices such as `sim.debug.pipeline_trace.cpu.cpu0.stall_cause.data_wait.records`, `sim.debug.pipeline_trace.cpu.cpu0.stall_cause.data_wait.stage.commit.resource_blocked_cycles`, `sim.debug.pipeline_trace.cpu.cpu0.flush_cause.branch_prediction.records`, `sim.debug.pipeline_trace.cpu.cpu0.redirect_cause.branch_prediction.stage.fetch1.flushed_cycles`, and `sim.debug.pipeline_trace.cpu.cpu1.stage.commit.resource_blocked_cycles`, and CPU/state scoped stats from the executed in-order cycle plans; `debug.pipeline_summary` mirrors the same Pipeline trace into fixed-axis JSON matrices including `/debug/pipeline_summary/stage/commit/resource_blocked_cycles`, `/debug/pipeline_summary/stall_cause/fetch_wait/records`, `/debug/pipeline_summary/stall_cause/fetch_wait/stage/commit/records`, `/debug/pipeline_summary/stall_cause/data_wait/stage/commit/resource_blocked_cycles`, `/debug/pipeline_summary/stall_cause/data_wait/stage/commit/records`, `/debug/pipeline_summary/stall_cause/fetch_wait/stage/execute/resource_blocked_cycles`, `/debug/pipeline_summary/stall_cause/execute_wait/stage/decode/records`, `/debug/pipeline_summary/flush_cause/branch_prediction/stage/fetch1/records`, `/debug/pipeline_summary/flush_cause/branch_prediction/stage/fetch1/flushed_cycles`, `/debug/pipeline_summary/flush_cause/trap_redirect/stage/commit/records`, `/debug/pipeline_summary/redirect_cause/trap_redirect/stage/commit/records`, and `/debug/pipeline_summary/redirect_cause/trap_redirect/stage/commit/flushed_cycles`, with explicit zero lanes for absent cause-stage combinations; normal run stats expose `sim.cpu0.pipeline.in_order.execute_wait_cycles`, `sim.cpu0.pipeline.in_order.stage.fetch1.advanced_cycles`, `sim.cpu0.pipeline.in_order.stage.commit.retired_cycles`, `/cores/0/in_order_pipeline/stage_advanced/fetch1`, `/cores/0/in_order_pipeline/stage_retired_cycles/commit`, `sim.cpu0.pipeline.in_order.flush_cause.branch_prediction.stage.fetch1.flushed_cycles`, `sim.cpu0.pipeline.in_order.flush_cause.trap_redirect.stage.commit.flushed_cycles`, `/cores/0/in_order_pipeline/flush_cause/branch_prediction/stage_flushed_cycles/fetch1`, `sim.cpu0.pipeline.in_order.redirect_cause.branch_prediction.stage.fetch1.flushed_cycles`, `sim.cpu0.pipeline.in_order.redirect_cause.trap_redirect.stage.commit.flushed_cycles`, `/cores/0/in_order_pipeline/redirect_cause/trap_redirect/stage_flushed_cycles/commit`, `sim.cpu0.pipeline.in_order.stall_cause.data_wait.stage.commit.resource_blocked`, `sim.cpu0.pipeline.in_order.stall_cause.data_wait.stage.commit.resource_blocked_cycles`, `/cores/0/in_order_pipeline/stall_cause/data_wait/stage_resource_blocked_cycles/commit`, `sim.cpu0.pipeline.in_order.stall_cause.execute_wait.stage.execute.resource_blocked_cycles`, `sim.cpu0.pipeline.in_order.stall_cause.execute_wait.stage.decode.ordering_blocked`, `sim.cpu0.pipeline.in_order.stall_cause.execute_wait.stage.decode.ordering_blocked_cycles`, `/cores/0/in_order_pipeline/stall_cause/execute_wait/stage_ordering_blocked_cycles/decode`, `sim.cpu0.pipeline.in_order.branch_prediction_redirects`, `sim.cpu0.pipeline.in_order.interrupt_redirects`, `sim.cpu0.pipeline.in_order.trap_redirects`, `sim.cpu0.pipeline.in_order.trap_redirect_flushes`, `sim.cpu0.pipeline.in_order.trap_redirect_flush_cycles`, `sim.cpu0.pipeline.in_order.stage.decode.trap_redirect_flushed`, `sim.cpu0.pipeline.in_order.stage.decode.trap_redirect_flushed_cycles`, plus JSON/text aggregate aliases `system.cpu.pipeline.inOrder.stallCycles`, `system.cpu.pipeline.inOrder.fetchWaitCycles`, `system.cpu.pipeline.inOrder.executeWaitCycles`, `system.cpu.pipeline.inOrder.resourceBlocked`, `system.cpu.pipeline.inOrder.orderingBlocked`, `system.cpu.pipeline.inOrder.flushCycles`, `system.cpu.pipeline.inOrder.branchPredictionFlushes`, `system.cpu.pipeline.inOrder.branchPredictionFlushCycles`, `system.cpu.pipeline.inOrder.branchSpeculationPredictions`, `system.cpu.pipeline.inOrder.branchSpeculationRepairs`, `system.cpu.pipeline.inOrder.branchSpeculationRemovedYoungers`, `system.cpu.pipeline.inOrder.branchSpeculationMaxPending`, `system.cpu.pipeline.inOrder.branchPredictionRedirects`, `system.cpu.pipeline.inOrder.interruptRedirects`, `system.cpu.pipeline.inOrder.interruptRedirectFlushes`, `system.cpu.pipeline.inOrder.interruptRedirectFlushCycles`, `system.cpu.pipeline.inOrder.trapRedirects`, `system.cpu.pipeline.inOrder.trapRedirectFlushes`, and `system.cpu.pipeline.inOrder.trapRedirectFlushCycles`, JSON/text stall-cause, flush-cause, and redirect-cause stage aliases such as `system.cpu.pipeline.inOrder.stallCause.fetchWait.stage.fetch1.resourceBlocked`, `system.cpu.pipeline.inOrder.flushCause.branchPrediction.stage.fetch1.flushed`, `system.cpu.pipeline.inOrder.redirectCause.branchPrediction.stage.fetch1.flushed`, `system.cpu.pipeline.inOrder.stallCause.dataWait.stage.commit.resourceBlocked`, `system.cpu.pipeline.inOrder.stallCause.dataWait.stage.commit.resourceBlockedCycles`, `system.cpu.pipeline.inOrder.stallCause.executeWait.stage.execute.resourceBlockedCycles`, `system.cpu.pipeline.inOrder.stallCause.executeWait.stage.decode.orderingBlocked`, and `system.cpu.pipeline.inOrder.stallCause.executeWait.stage.decode.orderingBlockedCycles`, plus text aliases `system.cpu.pipeline.inOrder.stage.fetch1.advancedCycles`, `system.cpu.pipeline.inOrder.stage.commit.retiredCycles`, `system.cpu.pipeline.inOrder.flushCause.branchPrediction.stage.fetch1.flushedCycles`, `system.cpu.pipeline.inOrder.redirectCause.trapRedirect.stage.commit.flushedCycles`, `system.cpu.pipeline.inOrder.stage.decode.trapRedirectFlushed`, and `system.cpu.pipeline.inOrder.stage.decode.trapRedirectFlushedCycles` from the same executed pipeline records. The detailed-mode interrupt redirect debug row also checks a flushed younger fetch sequence against the Fetch trace fallback-loop PC while text stats expose `sim.cpu0.pipeline.in_order.interrupt_redirect_flushes`, `sim.cpu0.pipeline.in_order.interrupt_redirect_flush_cycles`, `sim.cpu0.pipeline.in_order.stage.decode.interrupt_redirect_flushed`, `sim.cpu0.pipeline.in_order.stage.decode.interrupt_redirect_flushed_cycles`, cause-level `/cores/0/in_order_pipeline/flush_cause/interrupt_redirect/flushed`, `/cores/0/in_order_pipeline/redirect_cause/interrupt_redirect/flushed_cycles`, `sim.cpu0.pipeline.in_order.flush_cause.interrupt_redirect.flushed`, `sim.cpu0.pipeline.in_order.flush_cause.interrupt_redirect.flushed_cycles`, `sim.cpu0.pipeline.in_order.redirect_cause.interrupt_redirect.flushed`, `sim.cpu0.pipeline.in_order.redirect_cause.interrupt_redirect.flushed_cycles`, `system.cpu.pipeline.inOrder.interruptRedirects`, `system.cpu.pipeline.inOrder.interruptRedirectFlushes`, `system.cpu.pipeline.inOrder.interruptRedirectFlushCycles`, `system.cpu.pipeline.inOrder.stage.decode.interruptRedirectFlushed`, `system.cpu.pipeline.inOrder.stage.decode.interruptRedirectFlushedCycles`, `system.cpu.pipeline.inOrder.flushCause.interruptRedirect.flushed`, `system.cpu.pipeline.inOrder.flushCause.interruptRedirect.flushedCycles`, `system.cpu.pipeline.inOrder.redirectCause.interruptRedirect.flushed`, `system.cpu.pipeline.inOrder.redirectCause.interruptRedirect.flushedCycles`, `system.cpu.pipeline.inOrder.flushCause.interruptRedirect.stage.fetch1.flushedCycles`, and `system.cpu.pipeline.inOrder.redirectCause.interruptRedirect.stage.commit.flushedCycles`. The sequence-aware `rem6_run_pipeline_debug_correlates_fetch_wait_backlog_with_branch_flush`, `rem6_run_pipeline_debug_correlates_data_wait_backlog_with_branch_flush`, and `rem6_run_pipeline_debug_does_not_correlate_completed_execute_wait_with_later_trap_flush` rows independently intersect raw `(cpu, sequence)` observations and prove two branch live cells plus one trap negative: fetch-wait and detailed data-wait correlate to branch-prediction flush, while the scheduler-owned DIV wait completes before the later trap redirect and contributes zero correlation. The fetch row exposes aggregate `/debug/pipeline_summary/stall_backlog_flush/stall_cause/fetch_wait/flush_cause/branch_prediction/sequences`, stage `/debug/pipeline_summary/stall_backlog_flush/stall_cause/fetch_wait/flush_cause/branch_prediction/block_kind/ordering_blocked/blocked_stage/fetch1/flushed_stage/fetch1/stall_cycles`, CPU-scoped `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu0/stall_cause/fetch_wait/flush_cause/branch_prediction/stall_cycles`, CPU-scoped stage `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu0/stall_cause/fetch_wait/flush_cause/branch_prediction/block_kind/ordering_blocked/blocked_stage/fetch1/flushed_stage/fetch1/stall_cycles`, and matching `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.branch_prediction.sequences`, `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.branch_prediction.block_kind.ordering_blocked.blocked_stage.fetch1.flushed_stage.fetch1.stall_cycles`, `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.branch_prediction.stall_cycles`, and `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.branch_prediction.block_kind.ordering_blocked.blocked_stage.fetch1.flushed_stage.fetch1.stall_cycles` stats; lookahead two reports two correlated wrong-path sequences and six prior stall records/cycles, while lookahead one still executes a branch flush with zero correlated backlog. The data row exposes aggregate `/debug/pipeline_summary/stall_backlog_flush/stall_cause/data_wait/flush_cause/branch_prediction/sequences`, stage `/debug/pipeline_summary/stall_backlog_flush/stall_cause/data_wait/flush_cause/branch_prediction/block_kind/ordering_blocked/blocked_stage/fetch1/flushed_stage/execute/stall_cycles`, CPU-scoped `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu0/stall_cause/data_wait/flush_cause/branch_prediction/stall_cycles`, CPU-scoped stage `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu0/stall_cause/data_wait/flush_cause/branch_prediction/block_kind/ordering_blocked/blocked_stage/fetch1/flushed_stage/execute/stall_cycles`, and matching `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.data_wait.flush_cause.branch_prediction.sequences`, `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.data_wait.flush_cause.branch_prediction.block_kind.ordering_blocked.blocked_stage.fetch1.flushed_stage.execute.stall_cycles`, `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.data_wait.flush_cause.branch_prediction.stall_cycles`, and `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.data_wait.flush_cause.branch_prediction.block_kind.ordering_blocked.blocked_stage.fetch1.flushed_stage.execute.stall_cycles` stats; detailed mode reports one correlated sequence and two prior records/cycles while timing mode preserves a real data wait and branch flush with zero correlation. The execute row exposes aggregate `/debug/pipeline_summary/stall_backlog_flush/stall_cause/execute_wait/flush_cause/trap_redirect/sequences`, stage `/debug/pipeline_summary/stall_backlog_flush/stall_cause/execute_wait/flush_cause/trap_redirect/block_kind/ordering_blocked/blocked_stage/fetch1/flushed_stage/fetch2/stall_cycles`, CPU-scoped `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu0/stall_cause/execute_wait/flush_cause/trap_redirect/stall_cycles`, CPU-scoped stage `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu0/stall_cause/execute_wait/flush_cause/trap_redirect/block_kind/ordering_blocked/blocked_stage/fetch1/flushed_stage/fetch2/stall_cycles`, and matching `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.execute_wait.flush_cause.trap_redirect.sequences`, `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.execute_wait.flush_cause.trap_redirect.block_kind.ordering_blocked.blocked_stage.fetch1.flushed_stage.fetch2.stall_cycles`, `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.execute_wait.flush_cause.trap_redirect.stall_cycles`, and `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.execute_wait.flush_cause.trap_redirect.block_kind.ordering_blocked.blocked_stage.fetch1.flushed_stage.fetch2.stall_cycles` stats; both lookahead rows preserve nineteen DIV wait records/cycles and one trap redirect with zero correlated backlog; lookahead two independently retains one younger trap flush after the DIV wait lifetime has ended, while lookahead one emits no younger trap flush. The SBI-backed detailed-mode `rem6_run_pipeline_debug_correlates_detailed_fetch_wait_backlog_with_interrupt_flush` row adds a fourth live cell: one younger Fetch1 sequence accumulates six ordering-blocked fetch-wait records/cycles before an interrupt-redirect flush, while the cancelled current instruction contributes six Fetch1 and six Commit resource-blocked records/cycles; lookahead one removes the younger flush and retains only a six-cycle Fetch2-to-Commit resource backlog. It exposes aggregate `/debug/pipeline_summary/stall_backlog_flush/stall_cause/fetch_wait/flush_cause/interrupt_redirect/sequences`, stage `/debug/pipeline_summary/stall_backlog_flush/stall_cause/fetch_wait/flush_cause/interrupt_redirect/block_kind/ordering_blocked/blocked_stage/fetch1/flushed_stage/fetch1/stall_cycles`, CPU-scoped `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu0/stall_cause/fetch_wait/flush_cause/interrupt_redirect/stall_cycles`, CPU-scoped stage `/debug/pipeline_summary/stall_backlog_flush/cpu/cpu0/stall_cause/fetch_wait/flush_cause/interrupt_redirect/block_kind/ordering_blocked/blocked_stage/fetch1/flushed_stage/fetch1/stall_cycles`, and matching `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.interrupt_redirect.sequences`, `sim.debug.pipeline_trace.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.interrupt_redirect.block_kind.ordering_blocked.blocked_stage.fetch1.flushed_stage.fetch1.stall_cycles`, `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.interrupt_redirect.stall_cycles`, and `sim.debug.pipeline_trace.cpu.cpu0.stall_backlog_flush.stall_cause.fetch_wait.flush_cause.interrupt_redirect.block_kind.ordering_blocked.blocked_stage.fetch1.flushed_stage.fetch1.stall_cycles` stats, all cross-checked against the raw trace. The table-driven `all_stall_causes_correlate_with_all_flush_causes` records use unique per-cell cycle totals and prove all nine typed stall/flush cells across aggregate and CPU JSON plus aggregate and CPU pair/stage metrics, while `redirect_and_flushed_rows_without_flush_cause_do_not_count_backlog`, CPU identity, in-flight lifetime, and same-cycle suppression remain explicit negatives.
Final JSON/text stats now also expose per-stage in-order aliases such as `system.cpu.pipeline.inOrder.stage.fetch1.width`, `system.cpu.pipeline.inOrder.stage.fetch1.inFlight`, `system.cpu.pipeline.inOrder.stage.fetch1.maxInFlight`, `system.cpu.pipeline.inOrder.stage.fetch1.occupiedCycles`, `system.cpu.pipeline.inOrder.stage.fetch1.advancedCycles`, `system.cpu.pipeline.inOrder.stage.commit.retiredCycles`, `system.cpu.pipeline.inOrder.stage.fetch1.resourceBlocked`, `system.cpu.pipeline.inOrder.stage.fetch1.resourceBlockedCycles`, `system.cpu.pipeline.inOrder.stage.decode.branchPredictionFlushed`, `system.cpu0.pipeline.inOrder.stage.fetch1.maxInFlight`, and `system.cpu1.pipeline.inOrder.stage.fetch1.maxInFlight`, plus selected branchPred conditional/BTB count aliases for single-core `system.cpu` and multicore per-core `system.cpu0`/`system.cpu1` paths including `system.cpu.branchPred.condPredicted`, `system.cpu.branchPred.condPredictedTaken`, `system.cpu.branchPred.condIncorrect`, `system.cpu.branchPred.BTBLookups`, `system.cpu.branchPred.btb.lookups::DirectCond`, `system.cpu.branchPred.btb.lookups::DirectUncond`, `system.cpu.branchPred.btb.misses::total`, `system.cpu.branchPred.BTBUpdates`, `system.cpu.branchPred.btb.updates::total`, `system.cpu.branchPred.BTBMispredicted`, `system.cpu.branchPred.predTakenBTBMiss`, `system.cpu.branchPred.mispredictDueToBTBMiss_0::DirectCond`, `system.cpu0.branchPred.condPredicted`, and `system.cpu1.branchPred.BTBLookups`, plus broader selected `branchPred` JSON aliases for lookup, committed, mispredicted, corrected, target-wrong, predictor-cause, target-provider, indirect lookup/hit/miss, squash, RAS, and indirect-mispredicted families such as `system.cpu.branchPred.lookups_0::DirectCond`, `system.cpu.branchPred.committed_0::DirectCond`, `system.cpu.branchPred.mispredicted_0::DirectCond`, `system.cpu.branchPred.corrected_0::DirectCond`, `system.cpu.branchPred.targetWrong_0::DirectCond`, `system.cpu.branchPred.mispredictDueToPredictor_0::DirectCond`, `system.cpu.branchPred.targetProvider_0::BTB`, `system.cpu.branchPred.targetProvider_0::NoTarget`, `system.cpu.branchPred.indirectLookups`, `system.cpu.branchPred.indirectMisses`, `system.cpu.branchPred.squashes_0::DirectCond`, `system.cpu.branchPred.ras.pushes`, `system.cpu.branchPred.indirectMispredicted`, `system.cpu0.branchPred.lookups_0::DirectCond`, `system.cpu0.branchPred.ras.used`, `system.cpu1.branchPred.targetProvider_0::BTB`, `system.cpu1.branchPred.indirectMisses`, and `system.cpu1.branchPred.indirectMispredicted`, appended after existing multicore aggregate aliases with ambiguous multicore `system.cpu.branchPred.*` still suppressed and backed by live top-level selected-branch-predictor JSON stats without changing scores.
CLI `rem6 run --riscv-in-order-width 2` applies a uniform stage-width config to the live `RiscvCore` in-order pipeline snapshot, emits per-stage width plus max-in-flight occupancy stats, and exposes widened `ecall` fallthrough fetch-ahead squashed by trap redirects with nonzero trap-redirect flush and stage-cycle counters. Pending-fetch runs expose per-stage resource-blocked instruction counts whose total matches aggregate resource-blocked counts, plus per-stage resource-blocked and ordering-blocked cycle stat families derived from the same cycle records, and real `m5_switch_cpu` runs expose per-component/per-chunk execution-mode transfer stats for cpu0 in-order/O3 checkpoint chunks, repeated-switch architectural `xregs` and O3-runtime payload checksum accumulator stats from changed transfer manifests, detailed-mode O3 single/multicore aggregate and CPU-scoped ROB/LSQ/rename debug trace stats with `sim.debug.o3_trace.cpu.cpu0.records`/`sim.debug.o3_trace.cpu.cpu1.event.tick_span`, debug event-window row stats `sim.debug.o3_trace.event_window.records`, `sim.debug.o3_trace.event_window.max_structural_pressure.rob_occupancy`, `sim.debug.o3_trace.event_window.max_fu_latency.fu_latency_cycles`, `sim.debug.o3_trace.event_window.max_lsq_data_latency.lsq_data_latency_ticks`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_rob_occupancy.rob_occupancy`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_structural_pressure.rob_occupancy`, and `sim.debug.o3_trace.cpu.cpu0.event_window.max_lsq_data_latency.pc`, cycle-visible event ticks, per-event ROB/LSQ/rename occupancy, system-event provenance via `sim.debug.o3_trace.event.system_events`, first/last/span stats plus `/debug/o3_trace/0/event_summary/event_window/max_structural_pressure/rob_occupancy` and sequence-ordered retired-lifecycle ROB allocation/commit, and event-summary ROB/rename/LSQ/FU aggregates through `/debug/o3_trace/0/event_summary/rob_allocations`, `/debug/o3_trace/0/event_summary/rob/allocations`, `/debug/o3_trace/0/event_summary/rob/commits`, `/debug/o3_trace/0/event_summary/rob/max_occupancy`, `/debug/o3_trace/0/event_summary/rename_writes`, `/debug/o3_trace/0/event_summary/rename/writes`, `/debug/o3_trace/0/event_summary/rename/map_entries`, `/debug/o3_trace/0/event_summary/lsq_loads`, `/debug/o3_trace/0/event_summary/lsq_operation_load`, `/debug/o3_trace/0/event_summary/lsq_operation_store`, `/debug/o3_trace/0/event_summary/lsq_load_bytes`, `/debug/o3_trace/0/event_summary/lsq_store_bytes`, `/debug/o3_trace/0/event_summary/lsq_store_conditional_failures`, `/debug/o3_trace/0/event_summary/store_load_forwarding_candidates`, `/debug/o3_trace/0/event_summary/store_load_forwarding_matches`, `/debug/o3_trace/0/event_summary/store_load_forwarding_suppressed`, `/debug/o3_trace/0/event_summary/store_load_forwarding_address_mismatches`, `/debug/o3_trace/0/event_summary/store_load_forwarding_byte_mismatches`, `/debug/o3_trace/0/event_summary/lsq_data_latency/samples`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_candidates`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_matches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_suppressed`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_address_mismatches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_byte_mismatches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/load_bytes`, `/debug/o3_trace/0/event_summary/lsq_operation/atomic/store_bytes`, `/debug/o3_trace/0/event_summary/lsq_operation/load/latency/ticks`, `/debug/o3_trace/0/event_summary/lsq_operation/store/latency/ticks`, `/debug/o3_trace/0/event_summary/lsq_operation/load_reserved/latency/samples`, `/debug/o3_trace/0/event_summary/lsq_ordering/acquire`, `/debug/o3_trace/0/event_summary/lsq_ordering/release`, `/debug/o3_trace/0/event_summary/lsq_ordering/acquire_release`, `/debug/o3_trace/0/event_summary/fu_latency_instructions`, `/debug/o3_trace/0/event_summary/fu_latency_cycles`, and `/debug/o3_trace/0/event_summary/fu_latency_class/integer_mul/instructions`, FU-latency-class including scalar/vector integer mul/div plus multiply-add/widening/saturating-multiply and scalar/vector float add/compare/misc/mul/FMA/div/sqrt event count/cycle/min/max/avg stats, LSQ address-matched store-forwarding and address-mismatch suppression, vector LSQ load/store byte event trace stats, scalar/vector/float/atomic LSQ operation-shape event classification with aggregate float counters, failed store-conditional outcome JSON/stats, direct/cache-backed LSQ data-response tick/latency JSON plus aggregate total/min/max/avg plus sample-count and operation-class sample-count Count stats plus total/min/max/avg Tick stats, and `sim.debug.o3_trace.event.lsq_operation.*` counters, aq/rl LSQ ordering classification with `sim.debug.o3_trace.event.lsq_ordering.*` counters, direct-conditional taken/not-taken, warmed predicted-target-match, and predicted-taken/not-taken mismatch and indirect-unconditional predicted-wrong-target and linked indirect-call predicted-wrong-target branch event classification with per-event predicted/resolved/mispredicted/wrong-target/squash-target/branch-repair JSON, fall-through memory evidence, plus `sim.debug.o3_trace.event.branches`, `sim.debug.o3_trace.event.branch_taken`, `sim.debug.o3_trace.event.branch_not_taken`, `sim.debug.o3_trace.event.branch_predicted_taken`, `sim.debug.o3_trace.event.branch_predicted_not_taken`, `sim.debug.o3_trace.event.branch_predicted_targets`, `sim.debug.o3_trace.event.branch_predicted_target_matches`, `sim.debug.o3_trace.event.branch_predicted_target_mismatches`, `sim.debug.o3_trace.event.branch_wrong_targets`, `sim.debug.o3_trace.event.branch_wrong_target_squashed_targets`, `sim.debug.o3_trace.event.branch_wrong_target_squashed_target_without_link_writes`, `sim.debug.o3_trace.event.branch_wrong_target_link_writes`, `sim.debug.o3_trace.event.branch_wrong_target_without_link_writes`, `sim.debug.o3_trace.event.branch_resolved_targets`, `sim.debug.o3_trace.event.branch_mispredictions`, `sim.debug.o3_trace.event.branch_squashes`, `sim.debug.o3_trace.event.branch_squashed_targets`, `sim.debug.o3_trace.event.branch_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_not_taken_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_predicted_taken_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_predicted_target_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_predicted_target_match_kind.direct_conditional`, and `sim.debug.o3_trace.event.branch_predicted_target_mismatch_kind.direct_conditional` counters, plus direct-unconditional branch event/squash-kind, direct-call branch event/misprediction-kind, and return plus indirect-unconditional/call branch event/taken/resolved-target/link-kind and linked indirect-call wrong-target classification with skipped fallthrough/link-register or target-register memory evidence, per-event squashed-fallthrough target JSON, and `sim.debug.o3_trace.event.branch_kind.direct_unconditional`/`sim.debug.o3_trace.event.branch_squash_kind.direct_unconditional`/`sim.debug.o3_trace.event.branch_kind.call_direct`/`sim.debug.o3_trace.event.branch_misprediction_kind.call_direct`/`sim.debug.o3_trace.event.branch_kind.return`/`sim.debug.o3_trace.event.branch_taken_kind.return`/`sim.debug.o3_trace.event.branch_kind.indirect_unconditional`/`sim.debug.o3_trace.event.branch_resolved_target_kind.indirect_unconditional`/`sim.debug.o3_trace.event.branch_wrong_target_kind.indirect_unconditional`/`sim.debug.o3_trace.event.branch_wrong_target_without_link_write_kind.indirect_unconditional`/`sim.debug.o3_trace.event.branch_wrong_target_squashed_target_without_link_write_kind.indirect_unconditional`/`sim.debug.o3_trace.event.branch_wrong_target_kind.call_indirect`/`sim.debug.o3_trace.event.branch_wrong_target_squashed_target_kind.call_indirect`/`sim.debug.o3_trace.event.branch_wrong_target_link_write_kind.call_indirect`/`sim.debug.o3_trace.event.branch_kind.call_indirect`/`sim.debug.o3_trace.event.branch_predicted_not_taken_kind.call_indirect`/`sim.debug.o3_trace.event.branch_squashed_target_kind.call_indirect`/`sim.debug.o3_trace.event.branch_link_write_kind.call_indirect` counters, detailed-mode O3 structured per-core runtime JSON including ordered atomic aggregate and operation-class sample-counted LSQ latency plus direct/cache-backed float/vector LSQ operation/order runtime counters and data-response latency aliases, normal-runtime return branch-event matrix JSON/stats `/cores/0/o3_runtime/branch_event/kind/return`, `/cores/0/o3_runtime/branch_event/squashed_target_without_link_write_kind/return`, `sim.cpu0.o3.branch_event.kind.return`, and `sim.cpu0.o3.branch_event.squashed_target_without_link_write_kind.return`, branch-repair outcome/kind matrix JSON for call-indirect wrong-target plus direct-unconditional direction-only repairs, table-driven scalar/vector FU-latency class runtime counters including float-misc/vector-float-misc text stats, structured IQ and IEW dispatch/commit, path-safe `sim.cpu0.o3.iew.writeback_rate_ppm`/`sim.cpu0.o3.iew.producer_consumer_fanout_ppm` plus final JSON `system.cpu.iew.wbRate`/`system.cpu.iew.wbFanout` aliases derived from those PPM samples, predicted-taken/not-taken incorrect counters, ROB allocation/commit, LSQ byte, JSON/text `system.cpu.lsq0.forwLoads`, LSQ operation/order aliases `system.cpu.lsq0.operation.load`/`system.cpu.lsq0.operation.total`/`system.cpu.lsq0.ordering.acquireRelease`/`system.cpu.lsq0.ordering.total`, LSQ data-response aliases `system.cpu.lsq0.dataResponse.samples`, `system.cpu.lsq0.dataResponse.totalLatency`, `system.cpu.lsq0.dataResponse.maxLatency`, `system.cpu.lsq0.dataResponse.minLatency`, `system.cpu.lsq0.dataResponse.avgLatency`, `system.cpu.lsq0.dataResponse.load.totalLatency`, `system.cpu.lsq0.dataResponse.storeConditional.totalLatency`, `system.cpu.lsq0.dataResponse.atomic.avgLatency`, `system.cpu.lsq0.dataResponse.floatLoad.totalLatency`, `system.cpu.lsq0.dataResponse.floatStore.totalLatency`, `system.cpu.lsq0.dataResponse.vectorLoad.totalLatency`, and `system.cpu.lsq0.dataResponse.vectorStore.totalLatency`, and JSON/text IQ instsIssued/memInstsIssued/branchInstsIssued plus `sim.cpu0.o3.iq.branch_insts_issued` and MemRead/MemWrite/IntMult/IntDiv/FloatMisc/SimdFloatMisc issuedInstType plus final JSON/text `system.cpu.iq.issuedInstType_0::*` bucket aliases, plus raw commit `sim.cpu0.o3.commit.committed_inst_type.mem_read`, `sim.cpu0.o3.commit.committed_inst_type.mem_write`, `sim.cpu0.o3.commit.committed_inst_type.int_mul`, `sim.cpu0.o3.commit.committed_inst_type.int_div`, `sim.cpu0.o3.commit.committed_inst_type.float_misc`, `sim.cpu0.o3.commit.committed_inst_type.vector_float_misc` and JSON/text commit op-class bucket aliases `system.cpu.commit.committedInstType_0::MemRead`/`MemWrite`/`IntMult`/`IntDiv`/`FloatMisc`/`SimdFloatMisc`, and IEW dispatchedInsts/instsToCommit plus IEW predictedTakenIncorrect/predictedNotTakenIncorrect, branchRepair dotted JSON aliases, branchRepair_0 JSON/text bucket aliases, and IEW/commit branchMispredicts JSON/text aliases plus `m5_reset_stats`-scoped ROB/LSQ JSON counters, O3 debug trace `stats_epoch`/`stats_reset_tick` scope metadata with post-reset-only event stats, checkpoint-restore count plus label-list and latest label/tick/manifest/payload/component scope metadata, manifest-tick stats `sim.debug.o3_trace.checkpoint_restore_manifest_tick` and `sim.debug.o3_trace.cpu.cpu0.checkpoint_restore_manifest_tick`, and post-restore-only replay events and store/load memory evidence, including same-checkpoint and distinct-label two-restore rows whose O3 traces are scoped to the latest restore while `sim.debug.o3_trace.checkpoint_restores` counts both restore actions, plus a two-hart one-restore row whose `checkpoint_restore_records` counts both scoped trace records while restore actions remain single-counted, and namespaced `m5_dump_stats` boundary snapshot samples for O3 ROB/LSQ/rename, LSQ latency, IQ branch-issued, and IQ/IEW dispatch stats including `sim.host_actions.stats_dump.cpu0.o3.iq.branch_insts_issued`, scheduled run checkpoint/restore replay of detailed-mode O3 runtime state and restored counter-backed dump snapshots after ROB/LSQ/rename/FU work with `/cores/0/o3_runtime/checkpoint_restore_label`, `/cores/0/o3_runtime/checkpoint_restore_tick`, `/cores/0/o3_runtime/checkpoint_restore_manifest_tick`, `/cores/0/o3_runtime/checkpoint_restore_payload_bytes`, and `checkpoint_restores` JSON exposing restored `host.execution_modes`/`modes` detail plus restored component/chunk/payload JSON/stat totals, two-core hart-targeted `m5_switch_cpu` runs exposing final `execution_modes` authority and O3 debug trace target/mode labels for cpu1-only detailed O3 work with cpu0 O3 suppression, plus checker-enabled timing-mode quiescence counts through `sim.host_actions.execution_mode_switch_quiescence.checker.checked_instructions`, `sim.host_actions.execution_mode_switch_quiescence.checker.mismatches`, `sim.debug.host_action_trace.execution_mode_switch.quiescence.checker.checked_instructions`, and `sim.debug.host_action_trace.execution_mode_switch.quiescence.checker.mismatches`. The direct-conditional predicted-taken/not-taken O3 row also separates targetless mismatches from true wrong-target repairs with per-event `branch_targetless_mismatch` JSON, `sim.debug.o3_trace.event.branch_targetless_mismatches`, `sim.debug.o3_trace.event.branch_targetless_mismatch_kind.direct_conditional`, and `sim.debug.o3_trace.event.branch_targetless_mismatch_squashed_targets`, while keeping wrong-target counters at zero for the fall-through repair case.
RISC-V in-order timing tests cover retired taken/fall-through redirect, completed-fetch overlap, serial/translated/parallel fetch-ahead, direct `JAL` and register-target `JALR` fetch-ahead preservation, compressed fetch-ahead, wrong-path repair, pending occupancy before response completion, serial trap repair, widened trap fallthrough squash with wrong-path register suppression, stream-reset discard, and normal-driver fetch-response wait resource-stall records that preserve in-flight fetch state without breaking completed-fetch overlap, selected GShare, BiMode, and Tournament mutable speculative-history repair for younger fetch-ahead branch prediction plus TAGE-SC-L and multiperspective-perceptron speculative-history overlays, direct selected TAGE-SC-L and multiperspective-perceptron mutable history rollback on stale redirect cleanup, committed checkpoint snapshots, and restore-time selected-record invalidation, top-level retired Tournament predictor local/global selection stats, top-level predictor-family counter and text-alias stats, selected GShare/BiMode/Tournament rollback counter stats, top-level trap-redirect pipeline debug stats proving non-branch redirect classification with younger wrong-path register suppression, and selected TAGE-SC-L plus multiperspective-perceptron rollback counter stats, scalar integer M-extension multiply/divide/remainder, vector integer multiply and multiply-accumulate including fixed-point signed fractional multiply and widening variants, divide, remainder, fixed-point scaling shifts, narrowing shifts, narrow clip, and integer/widening reductions, scalar FP add/multiply/FMA/divide/sqrt, compare, conversion, and misc, decoded vector FP add/sub/min/max, mask compare, conversion/move/merge, sign/class, multiply/FMA/divide/sqrt execute-stage latency, and e8/e16/e32/e64 m1 unmasked/masked direct plus e32/m1 masked contiguous active-lane line/PMA-suppressed load/store, non-contiguous unit-stride load/store direct/cache-backed line/PMA suppression, all-inactive unit-stride load/store direct/cache-backed no-extra-access preservation, plus leading-inactive translated unit-stride load/store including non-contiguous active-span trims, all-inactive no-issued-request suppression, and strided/indexed load/store PMA suppression, aligned e16/m2 unmasked/masked, e16/m4 unmasked, and e32/m2/e32/m4/e32/m8 unmasked/masked direct/cache-backed full-register-group RVV unit-stride plus no-fault fault-only-first loads covering four unmasked e8/e16/e32/e64 m1 cases and three full e32 register-group cases (m2/m4/m8) plus an e32/m1 later-line fault-only-first top-level CLI suppression/reduced-`vl` row and masked e32/m1 later-line fault-only-first direct/cache-backed top-level CLI reduction/preservation rows, three e8/m1, two e16/m1, two e32/m1, and one e64/m1 single-line plus one e64/m1 non-contiguous multi-line unmasked/masked strided, four e8/m1 including e16-index, e32-index, and e64-index and four e16/m1 including e8-index, e32-index, and e64-index unmasked/masked non-contiguous unordered indexed, four e32/m1 including e8-index, e16-index, e64-index, and three e32-index unmasked/masked non-contiguous unordered indexed variants plus trailing-active lane preservation, four e64/m1 including e8-index, e16-index, e32-index, and e64-index unmasked/masked contiguous plus four e64/m1 including e8-index, e16-index, e32-index, and e64-index unmasked/masked non-contiguous unordered indexed load/store byte movement slices, unmasked e32/m1 two-field, three-field, and four-field, unmasked e16/m2 full-register-group two-field, plus masked e16/m1 two-field and unmasked/masked e64/m1 two-line segment load/store byte movement through direct/cache-backed top-level CLI paths, with vector LSU execute latency covered by execute-wait stats for existing slices and representative e32/m1 vector memory cycle and stall deltas, plus normal parallel-cluster pending-fetch resource stalls emitted through top-level CLI aggregate, per-stage instruction, and per-stage cycle stats. CLI tick-limit runs expose pending fetch occupancy, per-stage occupied-cycle stats, non-retire cycle advancement, and direct `JAL` target-fetch-in-flight behavior without committing the fall-through register update. These paths preserve branch speculation, pending-interrupt redirect, trap redirect, and data-access ordering.
Masked e16/m1 and e32/m1 segment load/store CLI coverage preserves inactive field lanes and skipped store elements through direct and cache-backed top-level run paths. O3 LSQ forwarding evidence now partitions load candidates/matches/suppression plus address-vs-byte suppression reasons through `sim.cpu0.o3.lsq_store_to_load_forwarding_suppressed`, `sim.cpu0.o3.lsq_store_to_load_forwarding_address_mismatches`, `sim.cpu0.o3.lsq_store_to_load_forwarding_byte_mismatches`, `sim.cpu0.o3.lsq_operation.load_forwarding_candidates`, `sim.cpu0.o3.lsq_operation.load_forwarding_matches`, `sim.cpu0.o3.lsq_operation.load_forwarding_suppressed`, `sim.cpu0.o3.lsq_operation.load_forwarding_address_mismatches`, `sim.cpu0.o3.lsq_operation.load_forwarding_byte_mismatches`, `/cores/0/o3_runtime/lsq_operation_load_forwarding_candidates`, `/cores/0/o3_runtime/lsq_operation_load_forwarding_matches`, `/cores/0/o3_runtime/lsq_operation_load_forwarding_suppressed`, `/cores/0/o3_runtime/lsq_operation_load_forwarding_address_mismatches`, `/cores/0/o3_runtime/lsq_operation_load_forwarding_byte_mismatches`, `system.cpu.lsq0.storeLoadForwardingSuppressed`, `system.cpu.lsq0.storeLoadForwardingAddressMismatches`, `system.cpu.lsq0.storeLoadForwardingByteMismatches`, `system.cpu.lsq0.operation.load.storeLoadForwardingCandidates`, `system.cpu.lsq0.operation.load.storeLoadForwardingMatches`, `system.cpu.lsq0.operation.load.storeLoadForwardingSuppressed`, `system.cpu.lsq0.operation.load.storeLoadForwardingAddressMismatches`, `system.cpu.lsq0.operation.load.storeLoadForwardingByteMismatches`, dump-snapshot `sim.host_actions.stats_dump.cpu0.o3.lsq_operation.load_forwarding_suppressed`/`sim.host_actions.stats_dump.cpu0.o3.lsq_operation.load_forwarding_address_mismatches`/`sim.host_actions.stats_dump.cpu0.o3.lsq_operation.load_forwarding_byte_mismatches`, and multicore CPU1 active-hart `m5_dump_stats` samples `sim.host_actions.stats_dump.cpu1.o3.lsq_store_to_load_forwarding_matches`, `sim.host_actions.stats_dump.cpu1.o3.lsq_operation.load_forwarding_matches`, and `system.cpu1.lsq0.operation.load.storeLoadForwardingMatches` while suppressing generic/CPU0 aliases.
CLI `rem6 run --checker-cpu` executes the same RISC-V retire path and emits per-core checked-instruction and mismatch counters from the checker snapshot before and after a timing-mode switch, while mode-switch authority matrix stats cover final detailed/timing/functional ownership through `sim.host_actions.execution_mode_authority.mode.timing`, final target-mode authority through `sim.host_actions.execution_mode_authority.target.cpu0.mode.functional`, `sim.host_actions.execution_mode_authority.target.cpu0.mode.timing`, and `sim.host_actions.execution_mode_authority.target.cpu0.mode.detailed`, default functional plus initial `--riscv-execution-mode detailed`/`timing` runs cover non-switch authority plus checker progress lanes through `sim.host_actions.execution_mode_authority.mode.detailed`, `sim.cpu0.checker.execution_mode.functional.checked_instructions`, `sim.cpu0.checker.execution_mode.timing.checked_instructions`, `sim.cpu0.checker.execution_mode.detailed.checked_instructions`, and `sim.cpu0.checker.execution_mode.detailed.mismatches`, O3 debug trace JSON/stats mirror initial detailed-mode authority through `/debug/o3_trace/0/execution_mode_authority/mode/detailed`, `/debug/o3_trace/0/execution_mode_authority/target/cpu0/mode/detailed`, `sim.debug.o3_trace.execution_mode.detailed`, `sim.debug.o3_trace.execution_mode_authority.targets`, `sim.debug.o3_trace.execution_mode_authority.mode.detailed`, `sim.debug.o3_trace.execution_mode_authority.target.cpu1.mode.detailed`, `sim.debug.o3_trace.execution_mode_authority.target.cpu1.mode.functional`, `sim.debug.o3_trace.execution_mode_authority.target.cpu1.mode.timing`, `sim.debug.o3_trace.cpu.cpu0.execution_mode.detailed`, `sim.debug.o3_trace.cpu.cpu1.execution_mode_authority.mode.detailed`, and `sim.debug.o3_trace.cpu.cpu1.execution_mode_authority.target.cpu1.mode.detailed`, with the hart-1 O3 debug row proving cpu1 detailed target-mode stats, explicit zero functional/timing lanes, plus cpu0 O3 suppression, aggregate switch lanes through `sim.host_actions.execution_mode_switch.mode.functional`, and target/repeated previous-mode lanes through `sim.host_actions.execution_mode_switch.target.cpu0.mode.functional`, `sim.host_actions.execution_mode_switch.target.cpu0.mode.timing`, `sim.host_actions.execution_mode_switch.target.cpu0.mode.detailed`, `sim.host_actions.execution_mode_switch.previous_mode.target.cpu0.none`, `sim.host_actions.execution_mode_switch.previous_mode.target.cpu0.functional`, `sim.host_actions.execution_mode_switch.previous_mode.target.cpu0.timing`, and `sim.host_actions.execution_mode_switch.previous_mode.target.cpu0.detailed`, quiescence validation/payload transfer through `sim.host_actions.execution_mode_switch.quiescence.target.cpu0.validated` plus `sim.host_actions.execution_mode_switch.quiescence.captured_payload_bytes`, HostAction debug trace switch handoff JSON/stats through `/debug/host_action_trace/0/state_transfer/quiescence_gate/checker/checked_instructions` plus `/debug/host_action_trace/0/state_transfer/quiescence_gate/captured_payload_bytes`, `sim.debug.host_action_trace.execution_mode_switch.quiescence.validated`, `sim.debug.host_action_trace.execution_mode_switch.quiescence.captured_components`, `sim.debug.host_action_trace.execution_mode_switch.quiescence.captured_chunks`, and `sim.debug.host_action_trace.execution_mode_switch.quiescence.captured_payload_bytes`, checkpoint-restore authority JSON/stats through `/debug/host_action_trace/1/execution_mode_authority/mode/detailed`, `/debug/host_action_trace/1/execution_mode_authority/target/cpu0/mode/detailed`, `sim.debug.host_action_trace.checkpoint_restore.execution_mode_authority.mode.detailed`, and `sim.debug.host_action_trace.checkpoint_restore.execution_mode_authority.target.cpu0.mode.detailed`, and generated-transfer restore rollback plus restored authority through `sim.host_actions.checkpoint_restore.execution_mode_authority.manifests`, `sim.host_actions.checkpoint_restore.execution_mode_authority.cleared_manifests`, `sim.host_actions.checkpoint_restore.execution_mode_authority.targets`, `sim.host_actions.checkpoint_restore.execution_mode_authority.mode.detailed`, `sim.host_actions.checkpoint_restore.execution_mode_authority.target.cpu0.mode.functional`, `sim.host_actions.checkpoint_restore.execution_mode_authority.target.cpu0.mode.timing`, and `sim.host_actions.checkpoint_restore.execution_mode_authority.target.cpu0.mode.detailed`;
checker tests cover retire comparison, data writeback sync, environment-call completion sync, public register writes, and HTM abort rollback. O3 event-summary branch-event/branch-repair outcome/resolved-target/misprediction/link/squashed-target anchors include `/debug/o3_trace/0/event_summary/branch_event/taken`, `/debug/o3_trace/0/event_summary/branch_event/not_taken`, `/debug/o3_trace/0/event_summary/branch_event/resolved_targets`, `/debug/o3_trace/0/event_summary/branch_event/taken_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/taken_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/not_taken_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_not_taken_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/resolved_target_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/resolved_target_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/misprediction_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/misprediction_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/link_writes`, `/debug/o3_trace/0/event_summary/branch_event/without_link_writes`, `/debug/o3_trace/0/event_summary/branch_event/squashed_targets`, `/debug/o3_trace/0/event_summary/branch_event/squashed_targets_with_link_writes`, `/debug/o3_trace/0/event_summary/branch_event/squashed_targets_without_link_writes`, `/debug/o3_trace/0/event_summary/branch_event/kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/taken_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/predicted_taken_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_match_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_mismatch_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/resolved_target_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/misprediction_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/link_write_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/link_write_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/squash_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/squashed_target_link_write_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/squashed_target_link_write_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/squashed_target_without_link_write_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/squashed_target_without_link_write_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_repair/targetless_mismatches`, `/debug/o3_trace/0/event_summary/branch_repair/wrong_targets`, `/debug/o3_trace/0/event_summary/branch_repair/direction_only_mismatches`, `/debug/o3_trace/0/event_summary/branch_repair/targetless_mismatch_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_repair/wrong_target_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_repair/direction_only_kind/direct_unconditional`, final runtime stats `sim.cpu0.o3.event_summary.branch_event.taken`, `sim.cpu0.o3.event_summary.branch_event.not_taken`, `sim.cpu0.o3.event_summary.branch_event.predicted_taken`, `sim.cpu0.o3.event_summary.branch_event.predicted_not_taken`, `sim.cpu0.o3.event_summary.branch_event.predicted_target_matches`, `sim.cpu0.o3.event_summary.branch_event.predicted_target_mismatches`, `sim.cpu0.o3.event_summary.branch_event.predicted_target_match_kind.direct_conditional`, `sim.cpu0.o3.event_summary.branch_event.predicted_target_mismatch_kind.direct_conditional`, `sim.cpu0.o3.event_summary.branch_event.resolved_target_kind.direct_unconditional`, `sim.cpu0.o3.event_summary.branch_event.misprediction_kind.direct_conditional`, `sim.cpu0.o3.event_summary.branch_event.link_write_kind.call_indirect`, `sim.cpu0.o3.event_summary.branch_event.without_link_write_kind.direct_unconditional`, `sim.cpu0.o3.event_summary.branch_event.squashes`, `sim.cpu0.o3.event_summary.branch_event.squashed_targets`, `sim.cpu0.o3.event_summary.branch_event.squashed_target_kind.direct_unconditional`, `sim.cpu0.o3.event_summary.branch_event.squashed_target_without_link_write_kind.direct_unconditional`, `sim.cpu0.o3.event_summary.branch_repair.targetless_mismatches`, `sim.cpu0.o3.event_summary.branch_repair.direction_only_mismatches`, `sim.cpu0.o3.event_summary.branch_repair.targetless_mismatch_kind.direct_conditional`, and `sim.cpu0.o3.event_summary.branch_repair.direction_only_kind.direct_unconditional` from top-level direct-conditional target-match/target-mismatch and call-indirect wrong-target O3 debug runs.
O3 IEW dependency and branch-mispredict debug-event evidence now exposes per-event producer/consumer counts through `/debug/o3_trace/0/events/1/iew_dependency_producers` and `/debug/o3_trace/0/events/4/iew_dependency_consumers`, event-summary dispatched/commit/writeback and dependency/fanout lanes `/debug/o3_trace/0/event_summary/iew/dispatched_insts`, `/debug/o3_trace/0/event_summary/iew/insts_to_commit`, `/debug/o3_trace/0/event_summary/iew/writeback_count`, `/debug/o3_trace/0/event_summary/iew/writeback_rate_ppm`, `/debug/o3_trace/0/event_summary/iew/producer_inst`, `/debug/o3_trace/0/event_summary/iew/consumer_inst`, `/debug/o3_trace/0/event_summary/iew/producer_consumer_fanout_ppm`, `/debug/o3_trace/0/event_summary/iew/dependency/producer`, `/debug/o3_trace/0/event_summary/iew/dependency/consumer`, final runtime stats `sim.cpu0.o3.event_summary.iew.writeback_rate_ppm`, `sim.cpu0.o3.event_summary.iew.producer_inst`, `sim.cpu0.o3.event_summary.iew.consumer_inst`, `sim.cpu0.o3.event_summary.iew.producer_consumer_fanout_ppm`, `sim.cpu0.o3.event_summary.iew.dependency.producer`, `sim.cpu0.o3.event_summary.iew.dependency.consumer`, `sim.cpu0.o3.event_summary.iew.predicted_taken_incorrect`, and `sim.cpu0.o3.event_summary.iew.predicted_not_taken_incorrect`, and split branch-mispredict lanes `/debug/o3_trace/0/event_summary/iew/predicted_taken_incorrect`, `/debug/o3_trace/0/event_summary/iew/predicted_not_taken_incorrect`, and `/debug/o3_trace/0/event_summary/iew/branch_mispredicts`, plus `sim.debug.o3_trace.event.iew_dispatched_insts`/`sim.debug.o3_trace.event.iew_insts_to_commit`/`sim.debug.o3_trace.event.iew_writeback_count`/`sim.debug.o3_trace.event.iew_writeback_rate_ppm`, `sim.debug.o3_trace.event.iew_dependency_producers`/`sim.debug.o3_trace.event.iew_dependency_consumers`, and `sim.debug.o3_trace.event.iew_predicted_taken_incorrect`/`sim.debug.o3_trace.event.iew_predicted_not_taken_incorrect`/`sim.debug.o3_trace.event.iew_branch_mispredicts` stats from the same top-level detailed O3 debug run.
O3 debug trace summary JSON now mirrors branch-event, branch-repair, branch-direction-mismatch, branch-target-mismatch, checkpoint-restore authority, event-window, event-summary branch-event/branch-repair/LSQ byte/forwarding/data-latency/ordering/failed-SC, event-summary FU-latency, and LSQ matrix aggregate shapes for top-level `rem6 run --execute --debug-flags O3` with `/debug/o3_trace/0/branch_event/branches`, `/debug/o3_trace/0/branch_event/squashed_target_link_write_kind/call_indirect`, `/debug/o3_trace/0/branch_repair/wrong_targets`, `/debug/o3_trace/0/branch_repair/direction_only_kind/direct_unconditional`, `/debug/o3_trace/0/branch_repair/targetless_mismatch_kind/direct_conditional`, `/debug/o3_trace/0/branch_direction_mismatch/mismatches`, `/debug/o3_trace/0/branch_direction_mismatch/link_write_kind/call_indirect`, `/debug/o3_trace/0/branch_direction_mismatch/without_link_write_kind/direct_conditional`, `/debug/o3_trace/0/branch_direction_mismatch/squashed_target_without_link_write_kind/direct_unconditional`, `/debug/o3_trace/0/branch_target_mismatch/wrong_targets`, `/debug/o3_trace/0/branch_target_mismatch/wrong_target_squashed_target_link_write_kind/call_indirect`, `/debug/o3_trace/0/branch_target_mismatch/wrong_target_squashed_target_without_link_write_kind/indirect_unconditional`, `/debug/o3_trace/0/branch_target_mismatch/targetless_mismatch_squashed_target_without_link_write_kind/direct_conditional`, `/debug/o3_trace/0/checkpoint_restore/execution_mode_authority/mode/detailed`, `/debug/o3_trace/0/checkpoint_restore/execution_mode_authority/target/cpu0/mode/detailed`, `sim.debug.o3_trace.checkpoint_restore.execution_mode_authority.mode.detailed`, `sim.debug.o3_trace.checkpoint_restore.execution_mode_authority.target.cpu0.mode.functional`, `sim.debug.o3_trace.checkpoint_restore.execution_mode_authority.target.cpu0.mode.timing`, `sim.debug.o3_trace.checkpoint_restore.execution_mode_authority.target.cpu0.mode.detailed`, CPU-scoped multicore restore authority through `sim.debug.o3_trace.cpu.cpu0.checkpoint_restore.execution_mode_authority.target.cpu1.mode.detailed` and `sim.debug.o3_trace.cpu.cpu1.checkpoint_restore.execution_mode_authority.target.cpu0.mode.detailed`, `/debug/o3_trace/0/event_summary/records`, `/debug/o3_trace/0/event_summary/span_ticks`, `/debug/o3_trace/0/event_summary/event_window/max_rob_occupancy/tick`, `/debug/o3_trace/0/event_summary/event_window/max_lsq_occupancy/pc`, `/debug/o3_trace/0/event_summary/event_window/max_rename_map_entries/sequence`, `/debug/o3_trace/0/event_summary/branch_event/branches`, `/debug/o3_trace/0/event_summary/branch_event/predicted_taken`, `/debug/o3_trace/0/event_summary/branch_event/predicted_not_taken`, `/debug/o3_trace/0/event_summary/branch_event/predicted_targets`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_matches`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_mismatches`, `/debug/o3_trace/0/event_summary/branch_event/mispredictions`, `/debug/o3_trace/0/event_summary/branch_event/squashes`, `/debug/o3_trace/0/event_summary/branch_event/kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_taken_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_not_taken_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_match_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_mismatch_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/squash_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/squash_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/lsq_data_latency/samples`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_candidates`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_matches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_suppressed`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_address_mismatches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_byte_mismatches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/load_bytes`, `/debug/o3_trace/0/event_summary/lsq_operation/atomic/store_bytes`, `/debug/o3_trace/0/event_summary/lsq_operation/load/latency/ticks`, `/debug/o3_trace/0/event_summary/lsq_ordering/acquire`, `/debug/o3_trace/0/event_summary/lsq_ordering/release`, `/debug/o3_trace/0/event_summary/lsq_ordering/acquire_release`, final runtime stats `sim.cpu0.o3.event_summary.rob_allocations`, `sim.cpu0.o3.event_summary.rob_commits`, `sim.cpu0.o3.event_summary.rename_writes`, `sim.cpu0.o3.event_summary.lsq_loads`, `sim.cpu0.o3.event_summary.lsq_stores`, `sim.cpu0.o3.event_summary.lsq_load_bytes`, `sim.cpu0.o3.event_summary.lsq_store_bytes`, `sim.cpu0.o3.event_summary.lsq_store_conditional_failures`, `sim.cpu0.o3.event_summary.lsq_operation_load`, `sim.cpu0.o3.event_summary.lsq_operation_store`, `sim.cpu0.o3.event_summary.store_load_forwarding_candidates`, `sim.cpu0.o3.event_summary.store_load_forwarding_matches`, `sim.cpu0.o3.event_summary.store_load_forwarding_suppressed`, `sim.cpu0.o3.event_summary.store_load_forwarding_address_mismatches`, `sim.cpu0.o3.event_summary.store_load_forwarding_byte_mismatches`, `sim.cpu0.o3.event_summary.lsq_operation.load.forwarding_candidates`, `sim.cpu0.o3.event_summary.lsq_operation.load.forwarding_matches`, `sim.cpu0.o3.event_summary.lsq_operation.load.forwarding_suppressed`, `sim.cpu0.o3.event_summary.lsq_operation.load.forwarding_address_mismatches`, `sim.cpu0.o3.event_summary.lsq_operation.load.forwarding_byte_mismatches`, `sim.cpu0.o3.event_summary.lsq_operation.load.load_bytes`, `sim.cpu0.o3.event_summary.lsq_operation.atomic.store_bytes`, `sim.cpu0.o3.event_summary.lsq_operation.load.latency.ticks`, `sim.cpu0.o3.event_summary.lsq_operation.store_conditional.latency.ticks`, `sim.cpu0.o3.event_summary.lsq_operation.atomic.latency.avg_ticks`, `sim.cpu0.o3.event_summary.lsq_ordering.acquire`, `sim.cpu0.o3.event_summary.lsq_ordering.acquire_release`, `sim.cpu0.o3.event_summary.fu_latency.instructions`, `sim.cpu0.o3.event_summary.fu_latency.cycles`, `sim.cpu0.o3.event_summary.fu_latency.max_cycles`, `sim.cpu0.o3.event_summary.fu_latency.min_cycles`, `sim.cpu0.o3.event_summary.fu_latency.avg_cycles`, `sim.cpu0.o3.event_summary.fu_latency_class.integer_mul.max_cycles`, `sim.cpu0.o3.event_summary.fu_latency_class.integer_mul.min_cycles`, `sim.cpu0.o3.event_summary.fu_latency_class.integer_mul.avg_cycles`, `sim.cpu0.o3.event_summary.fu_latency_class.integer_div.max_cycles`, `sim.cpu0.o3.event_summary.fu_latency_class.integer_div.min_cycles`, and `sim.cpu0.o3.event_summary.fu_latency_class.integer_div.avg_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_instructions`, `/debug/o3_trace/0/event_summary/fu_latency_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_max_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_min_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_avg_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_mul/instructions`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_mul/max_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_mul/min_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_mul/avg_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_div/cycles`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_div/max_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_div/min_cycles`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_div/avg_cycles`, `/debug/o3_trace/0/lsq/data_latency/samples`, `/debug/o3_trace/0/lsq/operation/store_conditional/count`, and `/debug/o3_trace/0/lsq/ordering/acquire_release`, covering indirect-call prediction/link/squash repair, direct-branch no-link direction repairs, targetless and wrong-target branch-target partitions, restored detailed-mode authority with zero functional/timing/cleared/error lanes, event-window span metadata, event-summary branch-event/branch-repair/LSQ forwarding/latency/ordering plus ROB/rename/LSQ/FU count/cycle/min/max/avg class aggregates, and ordered atomic load-reserved/store-conditional/acquire-release evidence in the debug artifact itself.
Detailed O3 commit evidence includes raw `sim.cpu0.o3.commit.committed_inst_type.mem_read`/`sim.cpu0.o3.commit.committed_inst_type.int_mul`/`sim.cpu0.o3.commit.committed_inst_type.float_misc`, raw combined `sim.cpu0.o3.iew.branch_mispredicts`/`sim.cpu0.o3.commit.branch_mispredicts`, initial detailed-mode normal-runtime `sim.cpu0.o3.instructions`/`sim.cpu0.o3.rob_allocations`/`sim.cpu0.o3.lsq_loads`/`sim.cpu0.o3.fu_integer_mul_instructions` plus `/cores/0/o3_runtime/instructions`/`/cores/0/o3_runtime/fu_integer_mul_instructions` evidence with timing-mode suppression, normal-runtime aggregate branch-outcome/prediction/squash/link JSON/raw stats `/cores/0/o3_runtime/branch_event/branches`, `/cores/0/o3_runtime/branch_event/taken`, `/cores/0/o3_runtime/branch_event/not_taken`, `/cores/0/o3_runtime/branch_event/predicted_taken`, `/cores/0/o3_runtime/branch_event/predicted_not_taken`, `/cores/0/o3_runtime/branch_event/predicted_targets`, `/cores/0/o3_runtime/branch_event/predicted_target_matches`, `/cores/0/o3_runtime/branch_event/predicted_target_mismatches`, `/cores/0/o3_runtime/branch_event/resolved_targets`, `/cores/0/o3_runtime/branch_event/not_taken_kind/direct_conditional`, `/cores/0/o3_runtime/branch_event/predicted_taken_kind/direct_conditional`, `/cores/0/o3_runtime/branch_event/predicted_not_taken_kind/direct_conditional`, `/cores/0/o3_runtime/branch_event/predicted_target_kind/direct_conditional`, `/cores/0/o3_runtime/branch_event/predicted_target_match_kind/direct_conditional`, `/cores/0/o3_runtime/branch_event/predicted_target_mismatch_kind/direct_conditional`, `/cores/0/o3_runtime/branch_event/misprediction_kind/call_indirect`, `/cores/0/o3_runtime/branch_event/misprediction_kind/direct_conditional`, `/cores/0/o3_runtime/branch_event/link_writes`, `/cores/0/o3_runtime/branch_event/without_link_writes`, `/cores/0/o3_runtime/branch_event/without_link_write_kind/call_indirect`, `/cores/0/o3_runtime/branch_event/without_link_write_kind/direct_conditional`, `/cores/0/o3_runtime/branch_event/squashes`, `/cores/0/o3_runtime/branch_event/squashed_targets`, `/cores/0/o3_runtime/branch_event/squashed_targets_with_link_writes`, `/cores/0/o3_runtime/branch_event/squashed_targets_without_link_writes`, `/cores/0/o3_runtime/branch_event/squashed_target_link_write_kind/call_indirect`, `sim.cpu0.o3.branch_event.branches`, `sim.cpu0.o3.branch_event.taken`, `sim.cpu0.o3.branch_event.not_taken`, `sim.cpu0.o3.branch_event.predicted_taken`, `sim.cpu0.o3.branch_event.predicted_not_taken`, `sim.cpu0.o3.branch_event.predicted_targets`, `sim.cpu0.o3.branch_event.predicted_target_matches`, `sim.cpu0.o3.branch_event.predicted_target_mismatches`, `sim.cpu0.o3.branch_event.resolved_targets`, `sim.cpu0.o3.branch_event.not_taken_kind.direct_conditional`, `sim.cpu0.o3.branch_event.predicted_taken_kind.direct_conditional`, `sim.cpu0.o3.branch_event.predicted_not_taken_kind.direct_conditional`, `sim.cpu0.o3.branch_event.predicted_target_kind.direct_conditional`, `sim.cpu0.o3.branch_event.predicted_target_match_kind.direct_conditional`, `sim.cpu0.o3.branch_event.predicted_target_mismatch_kind.direct_conditional`, `sim.cpu0.o3.branch_event.misprediction_kind.call_indirect`, `sim.cpu0.o3.branch_event.misprediction_kind.direct_conditional`, `sim.cpu0.o3.branch_event.link_writes`, `sim.cpu0.o3.branch_event.without_link_writes`, `sim.cpu0.o3.branch_event.without_link_write_kind.call_indirect`, `sim.cpu0.o3.branch_event.without_link_write_kind.direct_conditional`, `sim.cpu0.o3.branch_event.squashes`, `sim.cpu0.o3.branch_event.squashed_targets`, `sim.cpu0.o3.branch_event.squashed_targets_with_link_writes`, `sim.cpu0.o3.branch_event.squashed_targets_without_link_writes`, and `sim.cpu0.o3.branch_event.squashed_target_link_write_kind.call_indirect`, reset-scoped branch-event samples `sim.host_actions.stats_dump.cpu0.o3.branch_event.branches`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.not_taken_kind.direct_conditional`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.without_link_write_kind.call_indirect`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.without_link_write_kind.direct_unconditional`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.misprediction_kind.call_indirect`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.misprediction_kind.direct_conditional`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.predicted_targets`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.predicted_target_mismatches`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.predicted_target_mismatch_kind.direct_conditional`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.squash_kind.direct_unconditional`/`sim.host_actions.stats_dump.cpu0.o3.branch_event.squashed_target_without_link_write_kind.direct_unconditional`, dump/reset samples `sim.host_actions.stats_dump.cpu0.o3.commit.committed_inst_type.mem_read`/`sim.host_actions.stats_dump.cpu0.o3.commit.committed_inst_type.int_mul`/`sim.host_actions.stats_dump.cpu0.o3.commit.committed_inst_type.float_misc`, reset-scoped combined `sim.host_actions.stats_dump.cpu0.o3.iew.branch_mispredicts`/`sim.host_actions.stats_dump.cpu0.o3.commit.branch_mispredicts`, and JSON/text aliases `system.cpu.ftq.squashes`, `system.cpu.ftq.squashes_0::CallIndirect`, `system.cpu.ftq.squashes_0::DirectCond`, `system.cpu.ftq.squashes_0::DirectUncond`, `system.cpu.ftq.squashes_0::total`, `system.cpu.ftq.squashedTargets`, `system.cpu.ftq.squashedTargets_0::CallIndirect`, `system.cpu.ftq.squashedTargets_0::DirectUncond`, `system.cpu.ftq.squashedTargets_0::total`, `system.cpu.ftq.squashedTargetsWithLinkWrites_0::CallIndirect`, `system.cpu.ftq.squashedTargetsWithLinkWrites_0::total`, `system.cpu.ftq.squashedTargetsWithoutLinkWrites_0::DirectUncond`, `system.cpu.ftq.squashedTargetsWithoutLinkWrites_0::total`, `system.cpu.ftq.squashedTargetsWithLinkWrites`, `system.cpu.ftq.squashedTargetsWithoutLinkWrites`, `system.cpu.commit.committedInstType_0::MemRead`, `system.cpu.commit.committedInstType_0::MemWrite`, `system.cpu.commit.committedInstType_0::IntMult`, `system.cpu.commit.committedInstType_0::IntDiv`, `system.cpu.commit.committedInstType_0::FloatMisc`, and `system.cpu.commit.committedInstType_0::SimdFloatMisc` from the same detailed-mode runtime counters.
O3 event-window and nested ROB/rename final stats now expose aggregate metadata `sim.cpu0.o3.event_summary.event_window.records` and `sim.cpu0.o3.event_summary.event_window.span_ticks`, nested occupancy lanes `sim.cpu0.o3.event_summary.rob.max_occupancy` and `sim.cpu0.o3.event_summary.rename.map_entries`, PC-address lanes for `sim.cpu0.o3.event_summary.event_window.first.pc`, `sim.cpu0.o3.event_summary.event_window.last.pc`, `sim.cpu0.o3.event_summary.event_window.max_rob_occupancy.pc`, `sim.cpu0.o3.event_summary.event_window.max_lsq_occupancy.pc`, and `sim.cpu0.o3.event_summary.event_window.max_rename_map_entries.pc`, plus raw-event-selected latency rows `sim.cpu0.o3.event_summary.event_window.max_fu_latency.fu_latency_cycles` and `sim.cpu0.o3.event_summary.event_window.max_lsq_data_latency.lsq_data_latency_ticks` from the same top-level detailed O3 debug event-summary run; top-level `m5_dump_reset_stats` now snapshots reset-scoped event-window rows under `sim.host_actions.stats_dump.cpu0.o3.event_window.records`, `sim.host_actions.stats_dump.cpu0.o3.event_window.span_ticks`, `sim.host_actions.stats_dump.cpu0.o3.event_window.first.pc`, `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.lsq_occupancy`, `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.rename_map_entries`, `sim.host_actions.stats_dump.cpu0.o3.event_window.max_lsq_data_latency.lsq_data_latency_ticks`, and `sim.host_actions.stats_dump.cpu0.o3.event_window.max_fu_latency.fu_latency_cycles`, plus selected reset-scoped event-summary rows `sim.host_actions.stats_dump.cpu0.o3.event_summary.records`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_event.mispredictions`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_repair.direction_only_mismatches`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.branch_mispredicts`, IEW work/rate/dependency rows `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.dispatched_insts`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.insts_to_commit`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.writeback_count`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.writeback_rate_ppm`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.producer_inst`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.consumer_inst`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.producer_consumer_fanout_ppm`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.dependency.producer`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.dependency.consumer`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.predicted_taken_incorrect`, and `sim.host_actions.stats_dump.cpu0.o3.event_summary.iew.predicted_not_taken_incorrect`, IQ/commit rows `sim.host_actions.stats_dump.cpu0.o3.event_summary.iq.insts_issued`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iq.mem_insts_issued`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iq.branch_insts_issued`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iq.issued_inst_type.mem_read`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iq.issued_inst_type.mem_write`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iq.issued_inst_type.int_mul`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.iq.issued_inst_type.int_div`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.commit.branch_mispredicts`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.commit.committed_inst_type.mem_read`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.commit.committed_inst_type.mem_write`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.commit.committed_inst_type.int_mul`, and `sim.host_actions.stats_dump.cpu0.o3.event_summary.commit.committed_inst_type.int_div`, post-reset FU aggregate/class rows `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency.instructions`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency.cycles`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency.max_cycles`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency.min_cycles`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency.avg_cycles`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency_class.integer_mul.instructions`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency_class.integer_mul.max_cycles`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency_class.integer_div.cycles`, and zero-lane `sim.host_actions.stats_dump.cpu0.o3.event_summary.fu_latency_class.vector_float_misc.cycles`, branch-event/repair/mismatch matrix rows such as `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_event.branches`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_event.predicted_target_mismatch_kind.direct_conditional`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_event.squash_kind.direct_unconditional`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_repair.targetless_mismatch_kind.direct_conditional`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_repair.direction_only_kind.direct_unconditional`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_direction_mismatch.mismatches`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_direction_mismatch.kind.direct_unconditional`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_target_mismatch.targetless_mismatches`, and `sim.host_actions.stats_dump.cpu0.o3.event_summary.branch_target_mismatch.targetless_mismatch_kind.direct_conditional`, and LSQ matrix rows such as `sim.host_actions.stats_dump.cpu0.o3.event_summary.lsq_operation.store_conditional`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.lsq_ordering.acquire_release`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.lsq_operation.atomic.load_bytes`, and `sim.host_actions.stats_dump.cpu0.o3.event_summary.lsq_operation.store.latency.ticks`, with pre-reset branch/LSQ rows reset before post-reset FU/LSQ work, scheduled-restore runtime summary rows `/cores/0/o3_runtime/fu_latency_class/integer_mul/instructions`, `/cores/0/o3_runtime/fu_latency_class/vector_float_misc/cycles`, `/cores/0/o3_runtime/fu_latency_class/integer_mul/max_cycles`, and `/cores/0/o3_runtime/fu_latency_class/integer_div/avg_cycles`, final nested FU class rows such as `sim.cpu0.o3.fu_latency_class.integer_mul.instructions`, `sim.cpu0.o3.fu_latency_class.integer_mul.max_cycles`, `sim.cpu0.o3.fu_latency_class.integer_div.min_cycles`, and `sim.cpu0.o3.fu_latency_class.integer_div.avg_cycles`, and dump/reset nested FU class rows such as `sim.host_actions.stats_dump.cpu0.o3.fu_latency_class.float_misc.instructions`, `sim.host_actions.stats_dump.cpu0.o3.fu_latency_class.float_misc.max_cycles`, `sim.host_actions.stats_dump.cpu0.o3.fu_latency_class.float_misc.min_cycles`, `sim.host_actions.stats_dump.cpu0.o3.fu_latency_class.float_misc.avg_cycles`, and zero-lane `sim.host_actions.stats_dump.cpu0.o3.fu_latency_class.integer_mul.max_cycles`.
Multicore CPU1 detailed O3 final JSON now exposes scalar/vector float-misc op-class aliases `system.cpu1.iq.issuedInstType.FloatMisc`, `system.cpu1.iq.issuedInstType_0::FloatMisc`, `system.cpu1.iq.issuedInstType.SimdFloatMisc`, `system.cpu1.iq.issuedInstType_0::SimdFloatMisc`, `system.cpu1.commit.committedInstType.FloatMisc`, `system.cpu1.commit.committedInstType_0::FloatMisc`, `system.cpu1.commit.committedInstType.SimdFloatMisc`, and `system.cpu1.commit.committedInstType_0::SimdFloatMisc`, with generic and CPU0 aliases suppressed while CPU1 owns detailed mode; CPU1 reset/dump active-hart coverage also clears pre-reset scalar/vector float-misc work and exposes post-reset `sim.host_actions.stats_dump.cpu1.o3.fu_*` samples plus resettable `system.cpu1.iq.issuedInstType_0::*` and `system.cpu1.commit.committedInstType_0::*` bucket aliases while suppressing CPU0/generic aliases, CPU1 scheduled checkpoint restore replays restored-counter `m5_dump_stats` snapshots with preserved `cpu1` detailed-mode authority plus the same active-hart alias suppression, including restored snapshot rows `sim.host_actions.stats_dump.cpu1.o3.snapshot.rob.count`, `sim.host_actions.stats_dump.cpu1.o3.snapshot.lsq.count`, `sim.host_actions.stats_dump.cpu1.o3.snapshot.rename_map.count`, `sim.host_actions.stats_dump.cpu1.o3.snapshot.rob.entries`, `sim.host_actions.stats_dump.cpu1.o3.snapshot.lsq.entries`, and `sim.host_actions.stats_dump.cpu1.o3.snapshot.rename_map.entries`, and restored branch/FTQ samples `sim.host_actions.stats_dump.cpu1.o3.branch_event.kind.call_indirect`, `sim.host_actions.stats_dump.cpu1.o3.branch_event.misprediction_kind.direct_unconditional`, `system.cpu1.ftq.squashes_0::CallIndirect`, and `system.cpu1.ftq.squashedTargetsWithLinkWrites_0::CallIndirect` before and after a host restore while a post-checkpoint direct-call branch is replayed only after restore, CPU1 reset-scoped LSQ-forwarding `m5_dump_reset_stats` proves pre-reset matches and post-reset partial-match plus address-suppression lanes with byte-mismatch counters at zero under CPU1 aliases while CPU0/generic aliases stay absent, CPU1 LSQ-forwarding `m5_checkpoint` replay proves restored memory at `0x80000400`, resettable `sim.host_actions.stats_dump.cpu1.o3.lsq_*` and `system.cpu1.lsq0.operation.load.storeLoadForwardingMatches` samples, final monotonic CPU1 LSQ matches, O3 debug checkpoint-restore authority plus component payload metadata, and generic/CPU0 suppression, and sparse three-core host checkpoint restore now records only CPU0/CPU2 detailed O3 restore traces, preserves `cpu0`/`cpu2` detailed authority, suppresses CPU1 O3 records, and emits post-restore store/load replay events against distinct per-hart slots at `0x80000400` and `0x80000408`.
O3 host-action branch-mismatch dump note: top-level `rem6 run --execute --debug-flags O3` now covers reset-scoped no-link indirect-unconditional wrong-target dumps through `sim.host_actions.stats_dump.cpu0.o3.branch_target_mismatch.wrong_target_without_link_writes`, `sim.host_actions.stats_dump.cpu0.o3.branch_target_mismatch.wrong_target_without_link_write_kind.indirect_unconditional`, matching event-summary wrong-target rows, and `system.cpu.iew.branchWrongTargetWithoutLinkWrites_0::IndirectUncond` aliases, plus direct-conditional predicted-target-match snapshots through `sim.host_actions.stats_dump.cpu0.o3.branch_event.predicted_target_matches`, `sim.host_actions.stats_dump.cpu0.o3.branch_event.predicted_target_match_kind.direct_conditional`, and matching event-summary branch-event rows before `m5_dump_reset_stats` clears each row.
O3 live ROB/rename-map and scalar-memory lifecycle note: top-level `rem6 run --execute --debug-flags O3` now exposes a targeted execution-owned pre-retirement bounded four-row scalar DIV/ALU window with four ordered unready `live_staged` ROB rows and live physical mappings for the older `x3`, independent younger `x4`, `x4`-dependent younger `x5`, and `x4`/`x5` fan-in younger `x6` destinations while the architectural witness remains unchanged; the resident DIV owns the four-row event peak and commits when its 19-cycle FU wait completes without inheriting a blocked state from a younger row, the independent zero-latency `x4` ADDI issues before older DIV writeback, the dependent `x5` ADDI wakes one modeled tick after the speculative `x4` writeback and forwards that transient value, and the fan-in `x6` ADD wakes after both transient producers validate and also issues before DIV writeback while all three younger rows still commit in order with the final memory witness intact. A separate dependent `x3`-consuming ADDI CLI row still waits until DIV writeback. Scalar FU fetch and staging share one bounded row classifier: unsupported and `x0`-destination rows stop before staging, an unshadowed head-dependent ALU remains a blocked resident boundary row and stops later fetch and staging, and a prior younger overwrite of the head destination allows subsequent consumers to continue. Focused core/runtime tests establish that early execution evaluates a cloned hart, remains non-architectural until in-order retirement, reuses timing only for a structurally valid matching complete consumed-fetch identity including split prefix/suffix requests plus an exact execution record, rejects malformed empty/reordered/duplicate/cross-agent identities before they occupy a candidate, resolves each source against its nearest preceding live producer, blocks when that nearest producer lacks a validated transient execution, forwards the matching register write and producer-plus-one dependency-ready tick after speculative execution, and recursively revokes downstream speculative timing when a producer's final consumed-fetch identity or execution record fails validation. Bounded scalar-load fetch eligibility shares the retirement path's oldest-sequence same-agent split assembly, so duplicate completions and complete split successors preserve the logical three- and four-row windows while an incomplete split prefix is not refetched; the dedicated scalar-memory depth does not widen branch speculation. Transient state is discarded on manual redirect; the four-row interrupt fixture clears all staged rows and mappings, while the established checkpoint fixture preserves its three staged rows and intentionally drops transient executions. Focused serial and parallel load-then-DIV regressions prove that older unissued data work precedes younger fetch/execute work and that the dependent architectural result observes the completed load; translated and MMIO variants use the same data-first ordering and retain their existing path-suite coverage, while ordinary and MMIO instruction-budget regressions prove that exhausting a lower-ID core's final instruction slot still lets later cores drain already-retired data work. The implementation revokes transient issue authority on fetch rebind, HTM abort, and hart restart; detailed-mode disable preserves only already-owned live retirement timing until ordered drain and then suppresses new O3 admission, each instruction still drains only its own ROB row, and restore reissues from restored state rather than serializing a stale execution record. Dedicated four-row evidence is locked by `rem6_run_o3_detailed_four_row_fu_window_direct`, `rem6_run_o3_detailed_four_row_fu_window_cache_fabric_dram`, `rem6_run_o3_detailed_four_row_fu_window_remains_resident_at_tick_limit`, `rem6_run_o3_detailed_head_dependency_stops_fourth_fu_row_prefetch`, `rem6_run_o3_timing_fu_window_preserves_architecture_without_live_rows`, and `rem6_run_host_switch_transfers_live_o3_fu_authority_until_retirement`: direct and cache-backed fan-in completion drives `sim.cpu0.o3.max_rob_occupancy` and `system.cpu.rob.maxOccupancy` to four, the tick-limited and switch-transfer snapshots expose exactly four live rows and live mappings for `x3` through `x6`, an unshadowed head dependency stops residency at three rows and suppresses the fourth fetch, inherited issue/writeback timing survives a detailed-to-timing switch, and timing mode preserves architecture without admitting the first post-window instruction into O3. Existing evidence also preserves `sim.cpu0.o3.rename_map_entries` and `system.cpu.rename.mapEntries` values above one, v21 checkpoints restore all three staged rows and the rename overlay without physical-ID reuse while public-snapshot payloads preserve committed rollback provenance, malformed staged metadata and legacy public-overlay physical aliases are rejected, v20/v19 compatibility remains decoded, interrupt/manual redirect cleanup restores the prior committed mapping, and stats reset preserves live pressure plus pending dependency-producer identity. Separate detailed scalar-memory evidence is locked by `rem6_run_o3_detailed_scalar_memory_issue_response_retire_direct`, `rem6_run_o3_detailed_scalar_memory_issue_response_retire_cache_fabric_dram`, `rem6_run_o3_detailed_scalar_load_is_resident_before_response`, `rem6_run_o3_timing_scalar_memory_has_no_live_o3_lsq`, `rem6_run_o3_detailed_scalar_memory_instruction_limit_retires_response_direct`, `rem6_run_o3_detailed_scalar_memory_instruction_limit_retires_response_cache_fabric_dram`, and `rem6_run_o3_detailed_mmio_instruction_limit_retires_response`: a transport-owned standalone scalar load owns one same-sequence ROB/LSQ row while waiting for its real response, and the stopped cache-backed run exposes that incomplete LSQ row at `/cores/0/o3_runtime/snapshot/lsq/count` plus its unready dependent younger ROB row. The same scalar-memory owner now separates the four-entry memory cap from a configurable untranslated scalar live-window depth of four, six, or eight: the depth-eight lane keeps one load LSQ row and seven scalar ROB/rename rows resident together, depth six proves the bound is configurable rather than an exact-depth special case, widths 1/2/4 exercise scheduler-owned typed data/control dependency scopes with transitive and fan-in wakeup across direct and cache/fabric/DRAM routes, an unshadowed load-destination consumer remains an admitted but blocked terminal row, shadow-before-read remains legal, all rows retain ordered architectural commit, JSON/text/`m5_dump_stats` expose the issue counters, live checkpoints reject, detailed-to-timing transfer preserves issue/writeback/commit timing and carries eight ROB/one LSQ rows plus seven younger rows, focused retry/failure/redirect cleanup removes the deep suffix, and timing mode preserves architecture without O3 runtime or aliases. This boundary is locked by `rem6_run_o3_deep_scalar_window_matrix`, `rem6_run_o3_deep_scalar_window_text_stats`, `rem6_run_o3_deep_scalar_window_dump_stats`, `rem6_run_o3_scalar_live_depth_eight_combines_four_memory_and_four_scalar_rows`, `rem6_run_o3_scalar_live_depth_eight_rejects_fifth_memory_row`, `rem6_run_o3_deep_scalar_window_rejects_live_checkpoint_and_restores_drained`, `rem6_run_host_switch_preserves_deep_scalar_window_timing`, and `rem6_run_timing_suppresses_deep_scalar_window_surfaces`. Mixed scalar-memory-prefix/ALU response-forwarding evidence is locked by `rem6_run_o3_detailed_store_then_disjoint_load_overlap_direct`, `rem6_run_o3_detailed_store_then_disjoint_load_overlap_cache_fabric_dram`, `rem6_run_o3_detailed_aliasing_store_load_forwards_without_second_memory_request_direct`, `rem6_run_o3_detailed_aliasing_store_load_forwards_without_second_memory_request_cache_fabric_dram`, `rem6_run_o3_detailed_store_load_width_mismatch_merges_store_byte`, `rem6_run_o3_detailed_forwarded_load_dependent_alu_remains_resident_before_store_response`, and `rem6_run_o3_detailed_depth_two_keeps_dependent_alu_out_of_store_load_window`: explicit depth three lets normal, contained-forwarded, and partial-overlay completed loads wake one resident dependent scalar ALU at response-plus-one while older memory rows still block commit. The normal and contained-forwarded matrices prove response/issue/writeback/commit ordering across direct and cache/fabric/DRAM paths, while the contained-forwarded resident snapshot proves exact three-ROB/two-LSQ residency plus pre-store-response architectural-register suppression; the partial-overlay case proves merge semantics, response-plus-one wakeup, LSQ occupancy, and one transport request. Depth-two suppression, retry suffix cancellation, direct isolation, and timing-mode suppression remain separately covered. A store-led scalar-memory window now owns one leading cacheable store plus up to three younger independent loads: detailed fetch-ahead fills the configured depth before data issue, a depth-four direct run proves all four requests issue before the first response with exact LSQ occupancy four, three-row direct and cache-fabric-DRAM runs preserve oldest-first commit, a tick-limited three-row run exposes three unready live rows, timing mode suppresses O3 surfaces, leading-store forwarding still crosses an intervening load, and focused leading-store and middle-load failures cancel the correct younger suffix. Exact-range youngest-store selection is locked by `rem6_run_o3_detailed_store_store_load_forwards_from_youngest_store_direct`, `rem6_run_o3_detailed_store_store_load_forwards_from_youngest_store_cache_fabric_dram`, `rem6_run_o3_detailed_store_store_load_rows_are_resident_before_store_responses`, and `rem6_run_o3_timing_store_store_load_serializes_without_an_o3_window`: both same-address word stores occupy O3 before the leading response while Memory trace proves only the leading store is transport-visible, the younger store drains after predecessor success, the later load completes locally from the second store with exact three-row LSQ residency, direct and cache/fabric/DRAM runs preserve the younger value in both register and memory witnesses, timing mode emits all three transport requests without O3 surfaces, and focused `leading_store_retry_cancels_the_younger_store_before_transport_side_effects` plus `middle_store_retry_cancels_the_forwarded_load_after_buffer_drain` regressions prove external-side-effect suppression, suffix cancellation, and replay, while `cluster_batch_buffers_and_drains_the_younger_store_in_two_phases` proves the same local-admission and predecessor-gated drain lifecycle through the prepared cluster-batch path. Overlap-connected partial-range mixed-width byte composition is locked by `rem6_run_o3_detailed_multi_store_byte_composition_direct`, `rem6_run_o3_detailed_multi_store_byte_composition_cache_fabric_dram`, `rem6_run_o3_multi_store_byte_composition_rows_are_resident_before_responses`, and `rem6_run_o3_timing_multi_store_byte_composition_uses_transport_without_o3_merge`: a word/halfword/byte prefix admits all four ROB/LSQ rows before responses, composes four load bytes with youngest-store precedence, keeps only the leading store transport-visible among the store prefix before its response while the partial load reaches transport for uncovered bytes, preserves exact final memory/register witnesses through direct and cache/fabric/DRAM routes, suppresses O3 merging in timing mode, and uses `failed_middle_partial_store_cancels_the_younger_composed_suffix` to prove that a middle-store failure cancels the composed younger suffix before side effects. The exact same-width store/load lifecycle still owns two same-sequence live scalar-memory rows, completes the younger load locally at its issue tick from the older store bytes without a second transport request, keeps architectural commit behind the older store response, and drives `sim.cpu0.o3.max_lsq_occupancy` plus `system.cpu.lsq0.maxOccupancy` to two in direct and cache-fabric-DRAM runs. Completed transport responses and local forwarding completions each hand one retirement observation from the CPU to O3 stats, instruction probes, committed counters, instruction-limit control, CLI summaries, and GDB accounting; direct and cache-fabric-DRAM instruction-limit runs reach `x12=0x2a` before stopping and clean all speculative ROB/LSQ state after the forwarded load retires, while detailed readfile MMIO reaches `x5=0x123456789abcdef` before stopping with empty ROB/LSQ state. Completed direct and cache-fabric-DRAM runs preserve `2a0000002b000000` at `0x80000060` and end with zero ROB/LSQ rows; the cache-fabric-DRAM run additionally exercises cache/data-cache/transport/fabric/DRAM activity, while timing mode exposes no core detailed runtime or gem5-style O3 aliases and keeps the registered debug O3 counters at zero. Focused CPU/system tests also prove retry aborts without retirement, stats reset preserves resident identity, reset/redirect remove O3 lifecycle state while mode-disable drains already-owned authority and checkpoint capture still rejects unsnapshotted data work, MMIO issue now owns the same resident ROB/LSQ lifecycle until device completion without enabling younger MMIO overlap, and a non-quiescent multicore checkpoint rejects before any bank writes then succeeds after cleanup.
**Next evidence:** For CPU O3, link-sourced coroutine target shapes outside exact same-link authority and producer-forwarded linked-call returns beyond the exact direct/scalar continuations, producer-forwarded controls beyond the exact scalar-plus-return continuation, deeper non-scalar/control/result/device chains beyond the configured eight-row untranslated scalar-only lane, arbitrary broader mixed windows beyond the current bounded no-link and linked direct-call/indirect-call/return scalar-memory-prefix lanes, general IQ/wakeup/select beyond bounded scoped issue authority, broader mixed-data result depth, translated or MMIO result pairs, dependent stores/atomics, FP/vector dependent addresses, multiple unresolved addresses, and younger side-effect chains beyond one independent disjoint unordered AMO in result row two, broad vector shapes, restorable transport ownership, and a general O3 engine remain open; broader per-cycle in-order squash/stall correlation beyond the current live fetch-/data-wait-to-branch-flush matrix and completed execute-wait/later-trap non-correlation boundary, including additional live interrupt-backed backlog cells across stall causes and cores, additional block-kind/stage combinations, and widened trap/interrupt cases, remaining non-contiguous masked fault-suppression edge cases beyond current unit-stride load/store all-inactive no-touch, active-span, masked e32/m1 later-line fault-only-first active reduction/inactive preservation, and strided/indexed e32/m1 translated leading-gap slices, remaining leading translated segment variants, broader strided, indexed,
remaining segment forms beyond current e16/m1 masked, e32/m1 masked/unmasked, unmasked e32/m1 three-field/four-field, and e64/m1 two-line slices, remaining supported full-register-group fault-only-first later-line fault/suppression cases, arbitrary multi-line, and broader register-group vector
load/store forms beyond current unit-stride m1, no-fault fault-only-first loads covering four unmasked e8/e16/e32/e64 m1 cases and three full e32 register-group cases (m2/m4/m8), three e8/m1, two e16/m1, two e32/m1, and one e64/m1 single-line plus one e64/m1 non-contiguous multi-line unmasked/masked strided slices, four e8/m1 including e16-index, e32-index, and e64-index and four e16/m1 including e8-index, e32-index, and e64-index unmasked/masked non-contiguous unordered indexed, four e32/m1 including e8-index, e16-index, and e64-index unmasked/masked contiguous plus six e32/m1 including e8-index, e16-index, e64-index, and three e32-index unmasked/masked non-contiguous variants and four e64/m1 including e8-index, e16-index, e32-index, and e64-index unmasked/masked contiguous plus four e64/m1 including e8-index, e16-index, e32-index, and e64-index unmasked/masked non-contiguous unordered indexed slices, plus remaining segment/full-register-group slices beyond current e16/e32/e64 m1 segment, e16/m2 two-field segment, and unit-stride e16/m2/m4 plus e32/m2/m4/m8 evidence.
For CPU interrupt correlation, next evidence is wider multi-stage squashes and additional block-kind/stage combinations beyond the current CPU0 timer-backed all-stall-cause rows, CPU1 timer-backed data-/execute-wait rows, and CPU1 SBI IPI/SSIP fetch-wait row, plus interrupt causes beyond supervisor timer/software, broader normal in-order FU matrices beyond scalar MUL/DIV including vector/FP latency classes and cancellation interactions, and broader checker integration beyond the current initial timing/detailed mode-lane stats plus switch quiescence/debug target stats, then
broader scalar-memory concurrency beyond the current configurable bounded four-outstanding independent-load window, configurable bounded four-row one-leading-store/younger-load mixed window with direct four-row overlap evidence and exact forwarding across an intervening load, bounded overlap-connected and disjoint partial-range mixed-width store-prefix byte composition with youngest-store precedence, configurable untranslated one-load/scalar-ALU live windows at total depths four/six/eight with one LSQ row and direct/cache-backed evidence, existing two-row contained-forwarding and single-store partial-overlay cases, and single-outstanding MMIO lifecycle, including fifth and deeper memory requests, disjoint prefixes beyond the current bounded four-row untranslated matrix, broader multi-memory-plus-ALU windows beyond the current one-leading-store/one-load/dependent-ALU runtime row and two-load/ALU fetch slice, load-store and broader store-store/mixed outstanding memory requests, translated multi-device overlap, speculative memory/float/vector/branch rows, cross-line and deeper multi-store byte composition, and serializable mixed live windows, plus broader mode-transfer breadth beyond the current scheduled scalar FU lane, single-/multicore same-run untranslated cacheable scalar-load matrix, single-core direct/cache-fabric-DRAM translated memory, two-worker CPU1 cache/fabric/DRAM translated memory with an overlapping translated peer request, translated readfile-MMIO, and single-/multicore untranslated readfile-MMIO rows, including broader translated multicore/memory shapes, broader MMIO device classes and multi-device concurrency, and restorable transport ownership,
broader cycle-visible O3 ownership beyond the current targeted bounded four-row scalar FU/ALU live ROB/rename staging window plus configurable bounded four-entry independent-load and one-leading-store/younger-load mixed/forwarding windows, configurable untranslated one-load/seven-younger-scalar live windows at total depths four/six/eight, and single-MMIO issue-response-retire ownership and core-to-observer retire handoff, including wider decode/rename residency, broader mixed-class younger issue/dependency wakeup beyond the current bounded scalar transitive/fan-in chains and one dependent scalar consumer woken by normal, contained-forwarded, or partial-overlay load completion, LSQ-backed memory ordering, and general squash/rollback, broader FU-latency opcode classes beyond current scalar/vector integer mul/div plus vector multiply-add/widening/saturating-multiply and scalar/vector float add/compare/misc/mul/FMA/div/sqrt event count/cycle/min/max/avg stats, translated/MMIO plus disjoint, cross-line, and deeper multi-store byte composition and forwarding selection, vector LSQ byte accounting, additional direct/cache-backed LSQ data-response/op-class total/min/max/avg latency beyond current ordered-atomic plus float/vector aggregate/op-class runtime latency, direct-conditional taken/not-taken/predicted-target-match/predicted-taken-not-taken-mismatch, direct-unconditional, direct-call, return, indirect-unconditional, indirect-unconditional predicted-wrong-target, and linked indirect-call branch event/taken/target/link/squash/misprediction/wrong-target classification, broader scalar/vector/float/atomic LSQ operation-shape/order matrices beyond current ordered atomic and float/vector runtime JSON/stat counters plus ordered-atomic and float/vector aggregate/op-class latency runtime stats and LSQ matrix/latency dump/reset scoping, failed store-conditional outcome, and data-response/op-class total/min/max/avg latency plus branch-target/squash/outcome matrices, broader `m5_reset_stats` scoping beyond current ROB/LSQ byte, LSQ matrix/latency, IQ/IEW dispatch, and O3 debug reset epoch/tick trace metadata, broader restored-counter checkpoint dump/replay traces beyond current restored O3 debug checkpoint-restore scope metadata, same-checkpoint/distinct-label two-restore trace rows, CPU1 FU restored dumps, and CPU1 LSQ-forwarding `m5_checkpoint` replay, plus broader multicore checkpoint trace matrices beyond the current two-hart O3 restore and sparse three-core restore rows. O3 LSQ data-response provenance note: top-level `rem6 run --execute --debug-flags O3` now carries the LSQ operation class and ordering lane into compact event-window JSON rows for maximum data-response latency, covering `/cores/0/o3_runtime/event_window/max_lsq_data_latency/lsq_operation`, `/cores/0/o3_runtime/event_window/max_lsq_data_latency/lsq_ordering`, `/cores/0/o3_runtime/event_summary/event_window/max_lsq_data_latency/lsq_operation`, `/cores/0/o3_runtime/event_summary/event_window/max_lsq_data_latency/lsq_ordering`, and `/debug/o3_trace/0/event_summary/event_window/max_lsq_data_latency/lsq_ordering` against the detailed debug event stream.
O3 LSQ event-window stat provenance note: table-driven single-AMO top-level runs one-hot encode `acquire`, `release`, and `acquire_release` for the selected maximum data-response row under `sim.cpu0.o3.event_summary.event_window.max_lsq_data_latency.lsq_ordering.*`, `sim.cpu0.o3.event_window.max_lsq_data_latency.lsq_ordering.*`, `sim.debug.o3_trace.event_window.max_lsq_data_latency.lsq_ordering.*`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_lsq_data_latency.lsq_ordering.*`, and resettable `sim.host_actions.stats_dump.cpu0.o3.event_window.max_lsq_data_latency.lsq_ordering.*`, tied back to the raw event's `lsq_ordering` field while non-selected lanes remain zero; representative acquire-release anchors are `sim.cpu0.o3.event_summary.event_window.max_lsq_data_latency.lsq_ordering.acquire_release`, `sim.cpu0.o3.event_window.max_lsq_data_latency.lsq_ordering.acquire_release`, `sim.debug.o3_trace.event_window.max_lsq_data_latency.lsq_ordering.acquire_release`, `sim.debug.o3_trace.cpu.cpu0.event_window.max_lsq_data_latency.lsq_ordering.acquire_release`, and `sim.host_actions.stats_dump.cpu0.o3.event_window.max_lsq_data_latency.lsq_ordering.acquire_release`. Direct and `cache-fabric-dram` command rows preserve the same AMO memory witness, with the hierarchy-backed row proving data transport/fabric/DRAM activity and the current atomic data-cache bypass. The same event windows retain operation-class one-hot stats under `sim.cpu0.o3.event_summary.event_window.max_lsq_data_latency.lsq_operation.*`, `sim.cpu0.o3.event_window.max_lsq_data_latency.lsq_operation.*`, `sim.debug.o3_trace.event_window.max_lsq_data_latency.lsq_operation.*`, and `sim.debug.o3_trace.cpu.cpu0.event_window.max_lsq_data_latency.lsq_operation.*`; top-level O3 checkpoint, checkpoint-restore, and execution-mode switch-transfer artifacts now decode serialized LSQ operation load/store counts and aggregate plus per-load/per-store data-response latency counters from the `o3-runtime-state` chunk and expose selected decoded additive/max/min fields as host-action checkpoint/checkpoint-latest-component/restore/restore-latest-target/switch-transfer chunk stats, CPU-scoped restore-latest-target chunk stats, and HostAction debug-trace checkpoint/checkpoint-latest-component plus restore and switch-transfer aggregate/latest-target chunk stats; the scheduled FU checkpoint/restore CLI run decodes serialized aggregate FU latency plus integer-mul, integer-div, and zero-lane float-misc class count/cycle/min/max/avg counters from the same chunk and exposes selected decoded additive and max/min O3 runtime fields as host-action, CPU-scoped, and debug restore chunk stats while keeping unrecomputed avg fields out of aggregate restore stat paths.
### Memory, Cache, Coherence, Fabric, and DRAM - 73% representative
**Score calculation:** 11 of 15 items have executable evidence, or 73% raw. The bucket cap is representative because request-side FIFO/LIFO/least-recently-granted/no-QoS competing-flow arbitration and same-tick response-side FIFO/LIFO/LRG contention now have typed executable evidence; the top-level three-core matrix traverses configured `cpu_r0`/`r0_mem` hops through `router0`/`router1`, joins request grants to request VN 7 and router VCs 11/12, and joins response grants to response VN 8 and router VCs 13/14 with architectural witnesses; matrix-gapped remains blocked by Ruby-scale protocol networks and gem5-class flit/routing/VC breadth beyond the current composed matrix, remaining DRAM refresh modes beyond per-bank/all-bank/bank-group policy plus fine-granularity rows, and named full JEDEC timing-table breadth beyond custom timing rows.

- [x] Memory stores, page maps, translation queues, and TLB state have tests.
- [x] Cache banks model replacement, MSHRs, write queues, maintenance, sector and compressed tags.
- [x] MSI/MESI/MOESI/CHI line and bank models have protocol tests.
- [x] DRAM/NVM profiles expose bank, timing, QoS, refresh-policy/fine-granularity, low-power, and routed execution slices, including custom CLI DRAM timing rows in executed JSON and stats.
- [x] Fabric and transport expose multi-hop routing, credits, virtual networks, credit-delay attribution, and activity records.
- [x] Named DDR4, DDR5, HBM, and LPDDR4 JEDEC-style refresh presets validate `tREFI`/`tRFC` cycles and DDR4 4x fine-granularity scaling through profile constructors, controller refresh scheduling, and `rem6 run --dram-memory-profile` stats.
- [x] Cache-local prefetch queues expose enqueue, issue, drop, and translation counters from queued prefetch and translation flows.
- [x] Top-level CLI instruction-cache and data-cache prefetch consume identity-mapped page-crossing translation requests and emit queue-level stats from executed RISC-V runs.
- [x] Top-level RISC-V instruction/data traffic uses the L1/L2/L3 cache hierarchy and fabric-routed DRAM by default, with `rem6 run --memory-system cache-fabric-dram` selecting the same MSI hierarchy and `--memory-system direct` preserving direct-memory runs when explicitly requested.
- [x] Top-level CLI data-cache page-crossing prefetch preserves `PrefetchRead` lower-fill intent through an MSI L1/L2/L3 hierarchy and emits per-level prefetch-fill counters.
- [ ] Ruby-scale protocol transactions and topology races are migrated.
- [ ] NoC router, flit, virtual-channel, and routing detail match gem5-class coverage.
- [ ] Remaining DRAM refresh modes beyond per-bank/all-bank/bank-group policy and 2x/4x fine granularity plus named full JEDEC timing tables beyond custom timing rows are complete.
- [x] Broader instruction-cache and non-MSI prefetch translation consumers and queue-level stats are complete across hierarchy paths.
- [ ] Bank/cache/fabric/DRAM resource counters are complete enough for full-system studies.

**Migrated:** Typed memory primitives, cache banks, protocol harnesses, DRAM
profiles, controller-level refresh timing slices, routed topology slices,
CLI-selectable JEDEC refresh presets and 2x/4x fine-granularity scaling,
cache-local queued-prefetch and missing-translation queue counters, top-level
CLI RISC-V tagged next-line data-cache and instruction-cache prefetch issue and
identity-mapped page-crossing translation stats, and trace replay
consumers; optional CLI RISC-V data traffic can drive MSI-bank,
MESI-line, MOESI-line, and CHI-line data-cache runs and emit CPU response and
directory decision counters from those runs; CLI RISC-V MSI-bank data-cache
runs also emit bank accepted, immediate-hit, scheduled-miss, and coalesced-miss
counters from the executed cache-bank cycles; three-core CLI RISC-V runs can share
MSI, MESI, MOESI, and CHI data-cache runtimes, observe cross-core data
coherence, and emit per-core data route stats; three-core CLI RISC-V instruction
fetch traffic can drive MSI, MESI, MOESI, and CHI instruction-cache runtimes
and emit per-core fetch route stats; DRAM-backed MSI data-cache line fills emit
backing DRAM read counters from a CLI run using `--dram-memory` and
`--data-cache-protocol msi`, and read-only cache fetch/load responses do not
emit false DRAM writes; an explicit CLI run using `--data-cache-protocol msi`
and `--data-cache-l2-protocol msi` with optional
`--data-cache-l3-protocol msi` routes data misses through L1/L2 or L1/L2/L3
before DRAM; equivalent instruction-cache flags do the same for fetches, and
both can share explicit single-hop or comma-list multi-hop fabric links with request/response virtual networks; run artifacts expose the ordered configured `/fabric/hops` topology rather than first-hop-only aliases,
shared physical-link and separate request/response FIFO/LIFO/least-recently-granted QoS queue arbitration, direction-typed candidate/grant/memory-order-suppression/LRG-state activity, a composed three-core FIFO/LIFO/LRG/no-QoS competing-flow matrix across two hops and two routers, credit depth, and aggregate, per-virtual-network, per-link, per-hop, and per-link/per-VN lane
fabric JSON/stat counters including flit, credit-delay, and run-path wait-for queue-window counts, optional per-hop router input-VC/output-port stage latency and queue-delay timing with split request/response router virtual-channel overrides plus executed-run router/port/VC transfer, byte, flit, latency, and queue-delay aggregate JSON/stat counters mirrored into memory-resource summaries, and
`--debug-flags Fabric` exposes aggregate lane/hop transfer, byte, flit, occupied-tick, queue-delay,
max-queue-delay, credit-delay, and max-credit-delay stats from emitted fabric trace records while `--debug-flags Dram` exposes aggregate target/port/bank activity, row, refresh, command, and ready-latency stats, target/port turnaround stats, and bank byte stats from emitted DRAM trace records. Volatile CLI external-memory
profiles carry refresh/recovery, per-bank/all-bank/bank-group refresh policy, fine-granularity refresh scaling, and custom timing-table rows; DDR4, DDR5, and
HBM presets validate `tREFI`/`tRFC`; DDR and LPDDR CLI RISC-V paths emit
configurable refresh/recovery and low-power timing stats from routed fetches. DRAM target activity
exposes per-bank access/read/write, byte, row, command, refresh, controller all-bank and bank-group sibling-bank refresh accounting, HBM2 JEDEC terminal all-scheduler-bank refresh counters from a top-level CLI run that crosses `tREFI`, and LPDDR terminal low-power plus multi-bank exit counters.
CLI RISC-V runs aggregate executed instruction-cache, data-cache,
memory-transport, and DRAM activity into unified memory-resource activity and
active-resource JSON/stat counters, including unified cache CPU-response,
directory-decision, backing-DRAM access, cache-bank accepted, immediate-hit,
scheduled-miss, and coalesced-miss counters plus L1/L2/L3 cache hierarchy
resource breakdowns. CLI trace
replay can route packet traces through an explicit workload fabric link and
configure request/response virtual networks, link credit depth, and router-stage timing from CLI or
TOML config while emitting active-lane, active-virtual-network, transfer, byte, flit,
occupancy, queue-delay, credit-delay, contention, per-virtual-network, per-link, per-link/per-VN lane pressure, and per-hop activity stats from
the resulting workload parallel summary, plus aggregate resource-activity and
active-resource stats from the same summary; the same explicit-fabric path emits fabric wait-for edge counts, queue/credit kind windows, blocked packet windows, target lane/credit resource windows, and stable `sim.trace_replay.fabric.wait_for.*` stats. CLI trace replay with an explicit
data-cache protocol emits data-cache run accounting, per-protocol run stats,
CPU-response and directory-decision counters, scheduler epoch, dispatch, batch,
active-partition, worker-count, profiled backing-DRAM access,
target/port/bank, row, command, and ready-latency stats in artifact JSON, plus
QoS stats in `StatsRegistry`; a combined CLI trace-replay run proves explicit MSI data-cache plus explicit fabric routing
compose in one execution with cache-maintenance and fabric activity counters.
CLI `run` with `--data-cache-prefetcher tagged-next-line` consumes real RISC-V
data loads, issues prefetch reads through the selected data-cache protocol,
routes page-crossing next-line candidates through the queued missing-translation
flow with identity completion, counts a later demand hit on an issued prefetch
line as useful, preserves useful-but-miss, useful-span-page, unused, and late-hit subcounts, records demand MSHR misses for coverage denominators, and emits prefetch
identified/enqueued/issued/useful plus page-span, in-cache drop, translation queue, and fixed-point `accuracy_ppm`/`coverage_ppm` stats from the executed run, including configured single-core gem5-style
`system.cpu.dcache.prefetcher.pfIdentified`/`pfIssued`/`pfUseful`/`pfUsefulButMiss`/`pfUnused`/`pfHitInCache`/`pfHitInMSHR`/`pfHitInWB`/`pfLate`/`pfSpanPage`/`pfUsefulSpanPage`/`pfInCache`/`demandMshrMisses`
aliases plus derived `accuracy`/`coverage` aliases. CLI
`run` with `--instruction-cache-prefetcher tagged-next-line` consumes real
RISC-V instruction fetches, issues prefetch reads through the selected
instruction-cache protocol, routes page-crossing next-line candidates through
the same identity-completed queue flow, suppresses repeated next-line issues,
and emits the same prefetch queue and translation queue stats from the executed run, including configured single-core gem5-style
`system.cpu.icache.prefetcher.pfIdentified`/`pfIssued`/`pfUseful`/`pfUsefulButMiss`/`pfUnused`/`pfHitInCache`/`pfHitInMSHR`/`pfHitInWB`/`pfLate`/`pfSpanPage`/`pfUsefulSpanPage`/`pfInCache`/`demandMshrMisses`
aliases plus derived `accuracy`/`coverage` aliases. Bare top-level `rem6 run --isa riscv --execute` now selects the cache/fabric/DRAM hierarchy by default; `--memory-system direct` remains an explicit direct-memory configuration for timing-focused runs.
Top-level `rem6 run` with data-cache tagged next-line prefetch plus MSI L2/L3 preserves the `PrefetchRead` operation for lower hierarchy fills and exposes L1, L2, and L3 `prefetch_fills` artifact JSON and `prefetch.fills` stats from the translated page-crossing prefetch path; instruction-cache tagged next-line prefetch plus MESI/MOESI/CHI L1/L2/L3 preserves translated page-crossing hierarchy fills and exposes L1 translation-queue counters plus L1/L2/L3 `prefetch_fills` JSON/stat evidence.

**Not migrated:** Ruby-scale protocol networks and gem5-class router, flit, routing, and virtual-channel NoC detail beyond the current composed two-hop/two-router direction-typed QoS matrix, named full JEDEC timing-table breadth beyond custom timing rows, and remaining DRAM refresh modes beyond per-bank/all-bank/bank-group policy plus fine-granularity scaling.

**Evidence:** `MsiCacheBank`, `MsiCacheController`, protocol directory harnesses, `DramController`, `DramMemoryController`, `FabricModel`, `MemoryTransport`, and tests `riscv_topology_msi_data`, `riscv_topology_chi_data`, `memory_controller`, `timing`, `fabric_timing`, `workload_replay_fabric_hop_activity`, `system_run_resource_activity`, `prefetch_queue_stats`, `prefetch_queue_translation`, `refresh_presets`, and CLI `run` data-cache smoke coverage with three-core MSI/MESI/MOESI/CHI data-cache coherence routing, three-core MSI/MESI/MOESI/CHI instruction-cache fetch routing, DRAM-backed MSI data-cache fill read accounting, MSI-bank accepted, immediate-hit, scheduled-miss, and coalesced-miss counters from an executed RISC-V data-cache run, explicit MSI L1-to-L2-to-DRAM plus L1-to-L2-to-L3-to-DRAM data/instruction-cache fill smoke coverage, and a combined top-level RISC-V cache/fabric/DRAM smoke exposing aggregate, per-virtual-network, per-link, and per-link/per-VN lane `sim.memory.fabric.*` counters including flits and credit-delay ticks, fabric roll-up inside aggregate memory-resource stats, and `--memory-system cache-fabric-dram` preset coverage. DRAM memory-profile tests cover bank-level resource counters, resource-activity stats, and activity-window counter deltas.
CLI `run` default-memory-system smoke coverage proves bare RISC-V load/store execution drives L2/L3 cache activity, fabric transfers, DRAM accesses, and unified stats; `--memory-system direct` proves explicit direct-memory execution remains available.
CLI `run` also has DDR profile refresh smoke coverage that exposes default and custom refresh timing fields, all-bank refresh-policy JSON/stats, bank-group refresh-policy JSON/stats on HBM bank-group geometry, DDR4 4x fine-granularity refresh JSON/stats with scaled `tREFI`/`tRFC`, and nonzero refresh stats from RISC-V DRAM execution, HBM custom timing-table coverage for activate/read/write/precharge, bus-turnaround, burst-spacing, same-bank-group spacing, and command-window rows in executed JSON and stats, HBM2 JEDEC terminal refresh coverage that emits one `tRFC` window across all 16 scheduler banks after a top-level run crosses `tREFI`, plus controller sibling-bank accounting for all-bank refresh and bank-group same-group refresh, LPDDR default and custom low-power timing, active-powerdown, precharge-powerdown, self-refresh, target/port/bank hierarchy low-power stats from routed RISC-V fetch requests, all profiled LPDDR scheduler banks after terminal idle residency, repeated-access low-power exits across multiple scheduler banks, and NVM-profile active-powerdown/self-refresh low-power residency with zero refresh activity from a top-level RISC-V run; CLI `run` cache/DRAM resource smoke coverage exposes unified memory-resource cache CPU-response, directory-decision, backing-DRAM access, cache-bank, L1/L2/L3, instruction/data cache hierarchy, and hierarchy-scoped cache prefetch queue/translation-queue counters in artifact JSON and `StatsRegistry`.
CLI `run` cache/fabric/DRAM resource smoke coverage also exposes fetch/data memory-transport resource activity, active-resource, arrival/response, round-trip, and max-round-trip counters in artifact JSON and `StatsRegistry`, derived from real fetch/data `MemoryTrace` summaries. The same smoke exposes memory-resource fabric active-lane, active-virtual-network, active-router, transfer, byte, flit, occupied-tick, queue-delay, credit-delay, contention, ordered configured `/fabric/hops`, router latency/queue counters, split request/response router-VC configuration and observed hop activity, and aggregated router/port/VC transfer, byte, flit, latency, and queue-delay counters in artifact JSON and `StatsRegistry`, plus configured QoS queue policy, direction-typed `/fabric/qos_grant_activities` candidate, selected-index, grant, suppression, and LRG-before/after fields, request- and response-scoped aggregate/per-requestor/per-priority QoS stats exactly reconciled with each typed grant set, explicit zero activity/stats without QoS, `/fabric/wait_for_edge_count`, `wait_for_edge_kind_windows`, `sim.memory.fabric.wait_for.edges`, and `sim.memory.fabric.wait_for.kind.queue.edges`, link/lane/hop/router artifact JSON, and virtual-network/per-link/per-VN/per-hop/per-router resource stats derived from the executed run fabric summary and matched back to emitted lane and hop records; a focused three-core CLI matrix covers FIFO, LIFO, least-recently-granted, and no-QoS execution across `cpu_r0`/`r0_mem` and `router0`/`router1`, makes FIFO/LIFO produce different request grant and architectural order, proves live request LRG rotation snapshots, requires two request-hop transfers per request grant and two response-hop transfers per response grant, joins request grants to request VN 7/router VCs 11/12 and response grants to response VN 8/router VCs 13/14, proves the mirrored single-producer response path remains singleton rather than inventing contention, and keeps zero QoS artifacts/stats in the no-policy row. Focused fabric/transport regressions prove request LRG chooses order `[0, 2, 1]` for repeated requestors, records nonempty request memory-order suppression, gives response FIFO/LIFO/LRG same-tick candidate widths `[3, 2, 1]` with policy-specific order under one and three workers, shares separate request/response arbiters plus one monotonic direction-typed grant ledger across transports using the same fabric QoS state, starts response LRG without inherited request history, coalesces response batches across shared transports, recovers stale response batches after worker rollback, emits no response grants without QoS, and transactionally rolls back bounded fabric resource/telemetry state, request/response arbiter state, scheduled source events, activity, and batch IDs after failed request or response batches. A separate focused CLI fabric run routes cache/DRAM traffic across the same two links and routers, asserting per-hop/per-router JSON, memory-resource mirrors, and request/response-VN stats. The Fabric debug smoke also exposes aggregate lane/hop transfer, byte, flit, occupied-tick, queue-delay, max-queue-delay, credit-delay, and max-credit-delay `sim.debug.fabric_trace.*` counters derived from emitted records; the DRAM debug smoke exposes aggregate target/port/bank activity, row, refresh, command, and ready-latency counters, target/port turnaround counters, and bank byte counters derived from emitted records.
Top-level `rem6 run` cache/DRAM resource smoke coverage exposes memory-resource DRAM active target/port/bank, access, read/write, byte, row plus split read/write row-hit, refresh, low-power residency, command, turnaround, ready-latency, and read-ready-latency counters in artifact JSON and `StatsRegistry`, derived from the executed DRAM summary. The same run path exposes gem5-style memory-controller request, burst, and byte aliases under `system.mem_ctrl.*` plus DRAM-interface burst, per-bank burst, row-hit, byte, memory-access-latency, and NVM-profile `nvmBytesRead`/`nvmBytesWritten` aliases under `system.mem_ctrl.dram.*`, backed by executed DRAM/NVM summary request counts, bank request counts, split row-hit counts, bank byte totals, and read ready-latency ticks, plus shared L2/L3 text-output overall hit/miss/access/miss-rate and MSHR aliases such as `system.l2.overallMshrHits`/`system.l3.overallMshrMissRate` backed by executed instruction/data L2/L3 cache-bank summaries. Top-level `rem6 run --dram-memory` smoke coverage also exposes executed DRAM target, port, and bank hierarchy counters as structured `/dram/targets` and `/memory_resources/dram/targets` artifact JSON, including port active-bank counts, target/port byte totals, port row, refresh, and ready-latency counters, bank read/write counts, and target/port/bank low-power residency, plus matching `sim.memory.resources.dram.target*.port*.bank*` stats.
CLI
trace-replay fabric-route smoke coverage exposes nonzero active-lane,
active-virtual-network, transfer, byte, and flit stats plus request/response virtual
network and credit-depth config fields plus per-virtual-network, per-link, and per-link/per-VN lane pressure
`sim.trace_replay.fabric.*` counters, per-lane flit counts, and per-hop activity JSON from the top-level replay command, including aggregate, per-VN, per-lane, and per-hop
credit-delay fields, router config JSON, and per-link/per-VN/per-hop trace-replay stat families for transfer, byte, flit, occupied-tick, queue-delay, max-queue-delay, credit-delay, router-latency, and router-queue counters plus per-link/per-VN lane backpressure counters; CLI trace-replay fabric-route wait-for coverage exposes queue/credit edge-kind windows, blocked fabric packet windows, target lane/credit resource windows, and matching `sim.trace_replay.fabric.wait_for.*` stats from a top-level replay with fabric credit depth; CLI
trace-replay data-cache protocol smoke coverage exposes data-cache run,
protocol, scheduler, and profiled backing-DRAM resource stats from the top-level
replay command. CLI `run` data-cache and instruction-cache prefetch smoke
coverage exposes tagged next-line queue enqueue/issue/useful, useful-but-miss, useful-span-page, unused, late-hit fields, demand MSHR miss, fixed-point accuracy/coverage, and
gem5-style prefetcher alias stats during real RISC-V loads and fetches, plus identity-mapped page-crossing translation queue stats from real RISC-V loads and fetches.
Data-cache prefetch hierarchy smoke coverage proves a translated page-crossing prefetch is serviced through MSI L1/L2/L3 with per-level `prefetch_fills` artifact JSON and `sim.data_cache.l2/l3.prefetch.fills` plus `sim.memory.resources.cache.data.l2/l3.prefetch.fills` stats; instruction-cache prefetch hierarchy smoke coverage proves the same translated page-crossing fill path through MESI/MOESI/CHI L1/L2/L3 with `sim.instruction_cache.l2/l3.prefetch.fills` and `sim.memory.resources.cache.instruction.l2/l3.prefetch.fills` stats.
**Next evidence:** Ruby-scale protocol-network execution and gem5-class flit/routing/VC breadth beyond the current direction-typed three-core two-hop/two-router queue-policy matrix, broader response-side priority/resource/routing matrices beyond the current same-tick transport contention and serialized top-level producer path, remaining DRAM refresh modes beyond per-bank/all-bank/bank-group policy and fine-granularity rows, named full JEDEC timing-table coverage beyond custom timing rows, and broader low-power matrices beyond the current LPDDR and NVM rows.

### RISC-V SE, Workloads, and Linux Boot - 45% single-axis

**Score calculation:** 5 of 11 items have executable evidence, or 45% raw. The
bucket cap is single-axis because static newlib smokes are high-value but
tool-detected, and broad workload coverage is not present.

- [x] User-mode ecalls reach `RiscvSyscallTable`.
- [x] Startup stack, argv/envp/auxv, `brk`, `mmap`, in-place `mremap` slice, `mprotect`, mapped-page `mincore` present-vector reporting, `madvise` known-advice, mapped-range validation, anonymous `MADV_DONTNEED` page-zeroing, and file-backed `MADV_DONTNEED` backing restore, `msync` flags and mapped-range validation, `sync`/`fsync`/`fdatasync`/`sync_file_range`/`syncfs` validation, `readahead` regular-file hint validation, `mlock`/`munlock` `mmap`/`brk` range validation, `mlock2` range/flag validation, `mlockall` flag validation, single-node `mbind` mapped-range and nodemask validation slice, default and set/readback `get_mempolicy`/`set_mempolicy` mode/nodemask slices, stdio, file create/path `truncate`/`ftruncate`/read/write/append, `mknodat` regular-file creation, `unshare` single-process resource-flag slices, `setns` fd and namespace-type validation, positioned I/O, vector I/O, positional vector I/O, flags-zero `preadv2`/`pwritev2` positional vector I/O, same-process `process_vm_readv`/`process_vm_writev` guest-memory copies, path/fd xattr set/get/list/remove lifecycle, pipe `ioctl(FIONREAD)` unread-byte reporting, terminal `ioctl(TIOCGWINSZ/TCGETS)` reporting, pipe `fcntl(F_GETPIPE_SZ/F_SETPIPE_SZ)` capacity reporting/resizing and nonblocking full-pipe `EAGAIN`, guest-only `socket(AF_UNIX, SOCK_STREAM)` fd creation, unconnected read/write error boundaries, and abstract-name bind/listen/connect/accept4 stream handoff plus bound/listener/client/accepted `getsockname`/`getpeername` local/peer-name, `socklen` truncation/writeback, and accept/accept4 peer-address `sockaddr_un`/`addrlen` writeback slices, guest-only `socketpair` stream fd allocation/read/write/null-address sendto/recvfrom/sendmsg/sendmmsg/recvmsg/recvmmsg with common no-network flags/name/shutdown/options/poll/close and pipe-identity exclusion slices, `sendfile`, `splice`, `tee`, `vmsplice`, `statx`, `faccessat2`, `utimensat`, advisory `flock`, `fadvise64`, mode-zero `fallocate`, `fcntl` byte-range advisory lock no-conflict slices, legacy and typed `fcntl` signal-owner state plus signal-number state (`F_SETOWN`/`F_GETOWN`/`F_SETOWN_EX`/`F_GETOWN_EX`/`F_SETSIG`/`F_GETSIG`) with dup aliasing, `fchmodat2` permission updates and flag validation, and `fchownat`/`fchown` validation, `statfs`/`fstatfs`, `sysinfo`, `syslog` and admin syscall deterministic error boundaries including chroot and privileged terminal/module/kexec denials, value-mode `riscv_hwprobe` base key reporting, `prctl` process-name set/get, no-new-privs, dumpable, and parent-death-signal state, `personality` query/set state, `ppoll` timeout/sigmask validation, finite-timeout expiration/writeback, indefinite blocking, and fd readiness slices, `pselect6` fd-set readiness slices, `eventfd2` counter/semaphore/nonblock/close-on-exec/poll slices, `epoll_create1`/`epoll_ctl`/`epoll_pwait` eventfd-readiness slices, `sched_setparam`, `sched_setscheduler`, `sched_getscheduler`, `sched_getparam`, `sched_setattr`, `sched_getattr`, `sched_get_priority_max/min`, `sched_rr_get_interval`, single-word `sched_setaffinity`/`sched_getaffinity`, single CPU/node `getcpu`, single-process `membarrier` slice, current-thread `rseq` register/unregister with guest struct initialization and validation, `set_tid_address` exit clear-child-tid write and futex wake behavior, `set_robust_list` head-size validation, current-thread `get_robust_list`, unknown-thread rejection, and guest-write fault handling, `waitid` no-child and siginfo writeback plus `wait4` option/status filtering slices, zero-duration `nanosleep` and `clock_nanosleep` validation, no-restart-block `restart_syscall` `EINTR`, `gettimeofday`, `settimeofday` validation/denial, `adjtimex` and `clock_adjtime` query/denial, `clock_getres`, `clock_settime` validation/denial, `clock_gettime64`, `CLOCK_TAI` `clock_gettime`, interval timer query/disarm state, POSIX timer create/set/get/delete lifecycle, `kill(..., 0)`, `tkill(..., 0)`, `tgkill(..., 0)`, and current-process `pidfd_open`, `pidfd_send_signal(..., 0)` fd-lifecycle existence checks, and `pidfd_getfd` current-process fd duplication, installed `SIG_IGN` and default-ignored nonzero `kill`/`tkill`/`tgkill` plus `rt_sigqueueinfo`/`rt_tgsigqueueinfo` non-delivery success, blocked ignored/default-ignored pending mask reporting plus disposition and unblock clearing, current-process scoped process-group/session `setpgid`/`getpgid`/`getsid`/`setsid` slices, `getresuid`/`getresgid` identity triples, current-credential `setresuid`/`setresgid` validation and identity updates, current-credential `setreuid`/`setregid` real/effective identity updates, current-credential `setuid`/`setgid` validation and effective-identity updates, file-system `setfsuid`/`setfsgid` return/update semantics, empty supplementary `getgroups` reporting and `setgroups` `EPERM`, `capget` zero-capability reporting/version probe and `capset` zero-capability/`EPERM` validation, stateful `setrlimit`, legacy `getrlimit` stack/data/NPROC/NOFILE limits, `prlimit64` stack/data/NPROC/NOFILE reporting and limit updates, NOFILE fd-allocation enforcement, plus unknown-pid rejection, `sigaltstack` query/set/disable state, basic `rt_sigaction`/`rt_sigprocmask`, `rt_sigpending` mask reporting, no-pending zero-timeout and blocked pending-signal `rt_sigtimedwait` with `siginfo_t` writeback, `rt_sigqueueinfo`/`rt_tgsigqueueinfo` target and guest `siginfo_t` validation with non-delivery `ENOSYS` records, futex mismatch, zero-timeout wait, wait-bitset zero and elapsed absolute timeout validation, wake-bitset count/bitset behavior, requeue wake/move behavior, compare-requeue mismatch handling, and `FUTEX_WAKE_OP` guest-word update plus conditional two-address wake behavior, `close_range` close and `CLOSE_RANGE_CLOEXEC` slices, `openat2` `open_how` parsing, mode validation, and close-on-exec slice, `umask` masking for `mkdirat` directories and `openat(O_CREAT)` regular files, time, cwd, `chdir`/`fchdir`, random, resource, and wait slices have tests.
  `getpriority`/`setpriority` current process, process-group, and user scopes, `ioprio_get`/`ioprio_set`, `sched_setattr`/`sched_getattr`, `pidfd_open`/`pidfd_send_signal(..., 0)`, and `pidfd_getfd` raw slices are covered by syscall table tests and raw CLI smokes; same-process `process_vm_readv`/`process_vm_writev` copies are covered by syscall table tests, with raw CLI smoke generation gated by the RISC-V toolchain.
  `FUTEX_WAKE_OP` guest-word update and conditional two-address wake behavior are covered by syscall table tests and a static raw CLI/qemu smoke.
  `splice` pipe/file movement, `tee` pipe duplication, `vmsplice` guest-iovec pipe writes, `copy_file_range` guest-file copy, `mknodat` regular-file creation, `unshare` resource flags, `setns` validation, and `riscv_flush_icache` global/local no-op plus reserved-flag rejection are covered by syscall table tests and static raw CLI/qemu smokes.
  Signal pending coverage includes blocked ignored, default-ignored, and nonignored deliveries,
  with unsupported nonignored delivery recorded when the pending signal is unblocked.
  `signalfd4` coverage consumes blocked pending signals through a guest fd with
  read, nonblock, write-rejection, poll-readiness, and close-cleanup behavior.
  `inotify_init1`/`inotify_add_watch`/`inotify_rm_watch` coverage tracks guest-backed
  `IN_CREATE` from `openat(O_CREAT)` through read, poll, rm-watch, close, and CLI/qemu smoke.
  `waitid` no-child behavior, option validation, and `siginfo_t` writeback plus `wait4` Linux option-mask and no-child behavior are covered by syscall table tests and no-libc static CLI/qemu smokes.
  `capget`/`capset` zero-capability, version-probe, pid, null-pointer, and
  nonzero-set error paths are covered by syscall table tests and a no-libc
  static CLI/qemu smoke.
  `execve` missing-path/fault and flags-zero `execveat` missing-path slices have
  table coverage; unsupported `execveat` flag/fd-relative boundaries and existing
  guest exec paths stay typed unknown, while missing-path CLI smokes have no unknown diagnostics.
  `memfd_create` anonymous regular-file fd allocation and file-seal state are
  covered by syscall table tests plus a static raw CLI/qemu smoke; `setfsuid` and `setfsgid` return previous file-system identity values, update allowed
  credentials, reject unprivileged new identities without `errno`, and are
  covered by table tests plus a no-libc static CLI/qemu smoke.
  `socket(AF_UNIX, SOCK_STREAM)` creates deterministic guest-only fds with `SO_TYPE`, `getsockname`, close-on-exec/nonblock flags, unconnected `read`/`write` error boundaries, abstract-name bind/listen/connect/accept4 stream handoff, bound listener/client/accepted local/peer name queries, `socklen` truncation plus actual-length writeback, and accept/accept4 peer-address `sockaddr_un`/`addrlen` writeback; `socketpair(AF_UNIX, SOCK_STREAM)` has deterministic guest-only stream queues for bidirectional `read`/`write`, `getsockname`/`getpeername`, `shutdown`, selected `SOL_SOCKET` `getsockopt`/`setsockopt`, null-address `sendto`/`recvfrom`/`sendmsg`/`sendmmsg`/`recvmsg`/`recvmmsg` with `MSG_NOSIGNAL`/`MSG_DONTWAIT`, `poll`, close cleanup, and pipe-only fcntl exclusion, with syscall table coverage plus a static raw CLI/qemu smoke.
- [x] Unknown syscalls and unsupported signal-frame restore (`rt_sigreturn`) return `ENOSYS` with typed diagnostics, while known no-implementation entries (`io_*` AIO including `io_pgetevents`, `lookup_dcookie`/`nfsservctl`, `ptrace`, POSIX mqueue and SysV IPC probes, `clone`/`clone3`, kernel facility and key-management probes, legacy memory-placement and protection-key probes, `io_uring_*`, modern mount API and quota probes, `process_madvise`/`process_mrelease`, `landlock_*`, `memfd_secret`, `futex_waitv`/futex2 probes, `set_mempolicy_home_node`, `cachestat`, `map_shadow_stack`, stat/list mount probes, LSM probes, and `mseal`) return `ENOSYS` without unknown records.
- [x] Static no-libc and newlib smoke binaries can be generated and compared with qemu when tools exist; shared CLI smoke support detects RISC-V tools from `PATH` and the local module toolchain path, tool-detected newlib directory-open, `O_NOCTTY`/`O_NOFOLLOW`, and path-backed `--riscv-se-file` writeback coverage run through the legacy `open` syscall and registered guest files, while `/proc/self/exe`, `/proc/self/cwd` after `chdir`, file-backed and pipe-backed `/proc/self/fd/<fd>` readlink coverage, `/proc/self/maps` open/read after raw `mmap`, `/proc/self/comm` open/read from the `PR_SET_NAME` process name, `/proc/self/status` open/read from modeled identity and process state, pipe roundtrip, unconnected `socket` creation, abstract listener connect/accept4 stream handoff with listener/client/accepted name-query and accept4 peer-address checks, socketpair name/shutdown/option plus null-address sendto/recvfrom/sendmsg/sendmmsg/recvmsg/recvmmsg flag roundtrip coverage run through direct ecalls, and a top-level `--riscv-se` m5 ROI/stat-hook smoke exposes no unknown syscalls plus host-action stats.
- [x] Linux at-family hard-link, `symlinkat`, `renameat`, `renameat2` flags=0, `RENAME_NOREPLACE`, and regular-file `RENAME_EXCHANGE`, unlink, legacy `mkdir`, `mkdirat`, `unlinkat` with `AT_REMOVEDIR`, legacy `time`, raw `clock_gettime64`, and registered-directory `getdents64` syscalls mutate or expose registered guest files/directories or tick-derived time and have raw smoke evidence. Current qemu-riscv64 reports `ENOSYS` for raw `renameat`, raw `clock_gettime64`, raw `get_mempolicy`/`set_mempolicy`, and raw legacy `mkdir`/`open`/`time`, so those smoke tests record the qemu boundary and verify rem6 registered-file, registered-directory, memory-policy, and deterministic-time behavior directly.
- [ ] Process/thread lifecycle, signals, permissions, and blocking wait/futex semantics are broad enough for distro-like programs.
- [ ] Broad Linux syscall table parity exists.
- [ ] Host filesystem policy matches the needed gem5 SE cases.
- [ ] SBI/OpenSBI-class firmware behavior exists.
- [ ] A real Linux kernel boots to userspace or clean shutdown.
- [ ] PARSEC or comparable workload programs run through ROI/stat hooks.

**Migrated:** RISC-V SE ecall path; startup stack and auxv setup; `brk`,
`mmap`, in-place `mremap` shrink and tail-free expansion, `mprotect`,
mapped-page `mincore` present-vector reporting, `madvise` known-advice,
mapped-range validation, anonymous `MADV_DONTNEED` page-zeroing, and
file-backed `MADV_DONTNEED` backing restore,
`msync` flags and mapped-range validation,
`sync` success plus fd-validating `fsync`/`fdatasync`/`sync_file_range`/`syncfs`,
regular-file `readahead` hint success and bad-fd validation with static raw
CLI/qemu smoke coverage,
`mlock`/`munlock` `mmap`/`brk` range validation, `mlock2` range/flag validation, `mlockall`
valid/invalid flag validation with direct ecall coverage, single-node `mbind`
mapped-range, nodemask, privilege, and unsupported-flag validation slice,
default and set/readback `get_mempolicy`/`set_mempolicy` mode/nodemask slices, stdio, guest-backed file create/path `truncate`/`ftruncate`/read/write/append/positioned read-write/readback, xattr lifecycle, and open
fd/link visibility, vector I/O, positional vector I/O, flags-zero `preadv2`/`pwritev2` positional vector I/O, same-process `process_vm_readv`/`process_vm_writev`, guest-file `sendfile`,
`splice`, `tee`, `vmsplice`, mode-zero `fallocate`, and `copy_file_range`, `waitid`, `ppoll` timeout,
sigmask, expiration/writeback, indefinite blocking, and readiness behavior, `pselect6`,
`eventfd2` counter, semaphore, nonblock, close-on-exec, poll-readiness, and
actual ecall read-to-exit smoke slices, `epoll_create1`, `epoll_ctl`,
`epoll_pwait`, and `epoll_pwait2` eventfd-readiness slices,
`signalfd4` blocked pending-signal read, nonblock, write-rejection,
poll-readiness, and close-cleanup slices with
direct ecall smoke coverage, `inotify_init1`/`inotify_add_watch`/`inotify_rm_watch`
guest-backed `IN_CREATE` event production from `openat(O_CREAT)`, read,
nonblock, poll-readiness, ignored-watch cleanup, and direct ecall smoke
coverage, plus `timerfd` create, set/gettime, read,
nonblock, write-rejection, and poll-readiness slices with direct ecall smoke coverage,
`sched_setparam`, `sched_setscheduler`, `sched_getscheduler`, `sched_getparam`, `sched_setattr`, `sched_getattr`,
`sched_get_priority_max/min`, `sched_rr_get_interval`, single-word
`sched_setaffinity`/`sched_getaffinity`, current process/group/user `getpriority`/`setpriority`
raw priority reporting, nice-state updates, `ioprio_get`/`ioprio_set` state, and
current-process `pidfd_open`/`pidfd_send_signal(..., 0)`/`pidfd_getfd` fd-lifecycle checks,
`statx` basic stat buffer writes,
`faccessat2` registered-path, missing-path, directory-fd relative path,
`AT_SYMLINK_NOFOLLOW`, flag-validation, and `AT_EMPTY_PATH` fd access checks,
`utimensat` registered-path, missing-path, `AT_EMPTY_PATH` fd,
`AT_SYMLINK_NOFOLLOW`, flag-validation, and timespec validation checks,
advisory `flock` fd and operation validation with direct ecall smoke coverage,
`fadvise64` fd/advice validation with static raw CLI/qemu smoke coverage,
pipe `fcntl(F_GETPIPE_SZ/F_SETPIPE_SZ)` capacity reporting/resizing with
nonblocking full-pipe `EAGAIN` behavior,
anonymous `memfd_create` backed by guest file descriptions and consumed by
existing read/write/seek/truncate/stat syscalls, including basic file-seal
state and enforcement,
`fcntl` byte-range advisory `F_GETLK` no-conflict reporting and
`F_SETLK`/`F_SETLKW` validation with guest `struct flock` ABI tests plus direct
ecall memory-write coverage, unknown-command `EINVAL` validation without
unknown-syscall record pollution, shared-description legacy and typed
`F_SETOWN`/`F_GETOWN`/`F_SETOWN_EX`/`F_GETOWN_EX` signal-owner state plus
`F_SETSIG`/`F_GETSIG` signal-number state with dup aliasing and
invalid-signal validation, and static raw CLI/qemu smokes,
advisory `fchownat`/`fchown` registered-path, missing-path, bad-flag, fd,
`AT_EMPTY_PATH`, bad-fd, no-op owner forms, non-no-op `EPERM`, and
`AT_SYMLINK_NOFOLLOW` plus normalized symlink-target checks,
`fchmodat2` registered-path permission updates, invalid-flag rejection without
unknown-syscall records, and raw chmod-family CLI/qemu smoke coverage,
`symlinkat` guest-link creation consumed by `readlinkat` and followed by
`faccessat` in a static raw CLI/qemu smoke,
`statfs`/`fstatfs` deterministic guest-namespace filesystem statistics,
`sysinfo` uptime and configured SE-visible memory-capacity writes, `uname`
`new_utsname` writes including the cleared domain-name field,
value-mode `riscv_hwprobe` base key reporting, `riscv_flush_icache`
global/local no-op and reserved-flag validation, `prctl` process-name set/get, no-new-privs, dumpable, and parent-death-signal state slices, `personality` query/set state, single CPU/node
`getcpu`, `membarrier` single-process command query, registration, and
private-expedited barrier slices, current-thread `rseq` register/unregister
with guest struct initialization and validation, `set_tid_address` exit clear-child-tid
write and futex wake behavior, `set_robust_list` head-size validation,
current-thread `get_robust_list`, unknown-thread rejection, and guest-write
fault handling, zero-duration `nanosleep` and
`clock_nanosleep` validation, `gettimeofday`, `settimeofday` validation/denial, `adjtimex` and `clock_adjtime` query/denial, `clock_getres`, `clock_settime` validation/denial, `clock_gettime64`, `CLOCK_TAI` `clock_gettime`,
interval timer query/disarm state, POSIX timer create/set/get/delete lifecycle,
time, cwd, random, resource, wait, `close_range`, unknown syscall diagnostics,
unsupported `rt_sigreturn` `ENOSYS` records, `kill(..., 0)`
process-existence checks, `tkill(..., 0)` and `tgkill(..., 0)` current-thread
existence checks, installed `SIG_IGN` and default-ignored nonzero
`kill`/`tkill`/`tgkill` plus `rt_sigqueueinfo`/`rt_tgsigqueueinfo` non-delivery success without
unknown-syscall records, blocked ignored/default-ignored/nonignored pending
mask reporting plus disposition/unblock clearing and unsupported nonignored
delivery records, process-group/session
`setpgid`/`getpgid`/`getsid`/`setsid`
query, current-leader rejection, and nonleader transition slices,
`getresuid` and `getresgid` real/effective/saved identity triple writes,
current-credential `setresuid` and `setresgid` validation and identity updates,
current-credential `setuid` and `setgid` validation and effective-identity
updates, file-system `setfsuid` and `setfsgid` return/update semantics,
empty supplementary `getgroups` reporting and `setgroups` `EPERM`,
`capget` zero-capability reporting and version-probe behavior, `capset`
zero-capability acceptance plus `EPERM` nonzero/missing-pid rejection,
`sigaltstack` query/set/disable state, basic signal action/mask state for
`rt_sigaction` and `rt_sigprocmask`,
stateful `setrlimit`,
legacy `getrlimit` and `prlimit64` resource-limit reporting and updates
including stack/data/NPROC/NOFILE limits, NOFILE fd-allocation enforcement, and unknown-pid rejection, `rt_sigpending` mask reporting,
no-pending zero-timeout and blocked pending-signal `rt_sigtimedwait` with
`siginfo_t` writeback, `rt_sigsuspend` sigset validation
and blocking wait, `rt_sigqueueinfo`/`rt_tgsigqueueinfo` target and guest `siginfo_t` validation
with non-delivery `ENOSYS` records,
futex wait mismatch, zero-timeout wait, wait-bitset zero and elapsed absolute timeout validation,
wake-bitset count/bitset behavior,
requeue wake/move behavior, compare-requeue mismatch handling, and
`FUTEX_WAKE_OP` guest-word update plus conditional two-address wake behavior,
`umask` state applied to legacy `mkdir`/`mkdirat` directory modes and `openat(O_CREAT)`
regular-file modes, cwd-aware registered-path lookup, at-family
hard-link/`renameat`/`renameat2` flags=0, `RENAME_NOREPLACE`, and regular-file `RENAME_EXCHANGE`/unlink/legacy `mkdir`/`mkdirat`/`AT_REMOVEDIR`, and
registered-directory `getdents64` slices; supervisor SBI base read-only
identity/probe calls, SBI 2.0 spec-version reporting, conservative standard
extension probes plus legacy console putchar/getchar probes, top-level CLI
legacy console putchar, legacy console empty-getchar, and DBCN shared-memory write, top-level CLI TIME
`set_timer` deadline artifact/stat reporting plus boot-hart and HSM-started secondary-hart supervisor-handler interrupt delivery and retentive HSM wake by SBI TIME/STIP, library/workload
replay DBCN shared-memory read/write, direct write-byte debug-console output,
DBCN advertisement when functional guest memory I/O is configured, and workload replay Linux handoff DBCN read
input from a manifest `Input` resource into backing-store and data-cache-resident
guest memory using functional line replacement, without topology-routed firmware
memory transactions, minimal TIME `set_timer` STIP
scheduling and CLI S-mode interrupt-handler delivery on boot and HSM-started secondary harts, IPI
`send_ipi` scheduled completion events, SSIP pending-bit injection for
registered harts, and top-level CLI HSM-started secondary-hart S-mode handler
delivery with IPI artifact/stat records, with scheduler-error rejection before
partial target delivery,
standard SRST shutdown stop requests, invalid-param returns, top-level
shutdown, cold-reboot, warm-reboot, and system-failure reset artifact/stat
records, RFENCE probe reporting plus
top-level CLI RFENCE remote FENCE.I artifact/stat records plus remote
SFENCE.VMA completion artifact/stat records, and
remote SFENCE.VMA finite-range and ASID-scoped data TLB flushes through
translated execution, remote SFENCE.VMA scheduled completion events, explicit
RFENCE remote FENCE.I target fetch-stream reset on scheduled completion,
HFENCE.GVMA whole and physical-range scoped modeled data TLB invalidation
through translated execution, HFENCE.VVMA range-scoped data TLB invalidation, HFENCE.VVMA.ASID
scoped data TLB invalidation that preserves other address spaces, and HFENCE
invalid hart mask, ASID-width, VMID-width, and range validation, plus HSM probe,
`hart_get_status`, `hart_start` secondary-hart `START_PENDING` reporting before
the scheduled entry event, secondary-hart release with supervisor entry state,
`satp=0`, `sstatus.SIE=0`, `a0=hartid`, and `a1=opaque`, top-level CLI HSM
`hart_start` secondary-hart release through `rem6 run --riscv-sbi --cores 2`
with HSM start artifact/stat records,
top-level CLI boot-hart already-started `hart_start` `-6` return, and
library `hart_start` already-started and suspended-target `-6` error boundaries, top-level and library current-hart `hart_stop` as a no-return stop that
does not write `sbiret`, `STOP_PENDING` reporting until the scheduled stop
event completes, with HSM stop artifact/stat and idle-stop records, retentive current-hart `hart_suspend` through
`SUSPEND_PENDING` until the scheduled suspend event reaches the CPU execution
gate, default non-retentive current-hart `hart_suspend` reporting
`RESUME_PENDING` until the scheduled resume event re-enters at `resume_addr`
with the same supervisor entry-state contract plus top-level HSM suspend
artifact/stat records, stale non-retentive resume events
ignored after intervening state changes, checkpoint roundtrip coverage for all
modeled hart run states, and retentive HSM wake by SBI IPI and TIME/STIP with top-level HSM wake artifact/stat records; workload replay Linux boot handoff starts the boot hart in
supervisor mode with `a0=hartid` and `a1=dtb_addr`, keeps secondary harts
stopped before HSM start, and routes replay SBI base ecalls through firmware;
typed unknown-syscall records, including raw `rt_sigreturn` CLI records instead of
silent success; `execve` missing-path/fault and flags-zero `execveat` missing-path
errors avoid unknown pollution; unsupported exec boundaries stay typed unknown;
direct ecall CLI coverage exists for missing-path cases; static smoke coverage; a static newlib
`fopen("w+")` create, write, seek, readback, and exit-code roundtrip; and a
static newlib program that reads `/proc/self/exe` through a direct `readlinkat`
ecall and compares the exit path with qemu; a static raw program that creates and enters `work`, reads `/proc/self/cwd` through `readlinkat`, and checks the `/work` suffix against qemu; static raw programs that read file-backed and pipe-backed `/proc/self/fd/<fd>` links through `readlinkat`, check guest-path, equal `pipe:[...]`, missing-fd `ENOENT`, and pipe-link trailing-slash `ENOTDIR` behavior, and compare the exit path with qemu; a static raw program
that opens and reads `/proc/self/maps` after raw `mmap` and checks the mapped
guest address against qemu; a static raw program that sets `PR_SET_NAME`, opens
and reads `/proc/self/comm`, and checks the formatted task name against qemu;
static raw `/proc/self/status` open/read coverage validates the modeled process
name and single-process identity lines through guest writes;
static raw `prctl`
programs that roundtrip the Linux process name, no-new-privs, and
parent-death-signal state through direct ecalls and compare the exit paths with qemu; a static raw `personality` program that
matches qemu query/set return values; a static raw `sigaltstack` program that
matches qemu query/set/disable state; static raw ignored-signal programs that
drive `rt_sigaction(SIG_IGN)`, default ignored `SIGCHLD`/`SIGURG`/`SIGWINCH`,
blocked default-ignored `rt_sigpending`/unblock behavior, and blocked
nonignored pending/unblock unsupported-delivery behavior through `rem6 run
--riscv-se`, with table-level pending disposition clearing coverage; direct
and vector I/O unit coverage for pipe
endpoints, positional vector I/O unit coverage for guest files, pipe `ioctl(FIONREAD)` unread-byte reporting, terminal `ioctl(TIOCGWINSZ/TCGETS)` reporting, deterministic guest-only unconnected `socket` creation/error-boundary coverage, abstract listener connect/accept4 stream handoff with listener/client/accepted name queries, `socklen` writeback, and accept/accept4 peer-address `sockaddr_un`/`addrlen` writeback, and `socketpair` stream queue coverage for bidirectional direct ecall I/O, name queries, shutdown, selected `SOL_SOCKET` options, null-address `sendto`/`recvfrom`/`sendmsg`/`sendmmsg`/`recvmsg`/`recvmmsg` with no-network flags, and poll readiness without pipe identity, plus a static newlib program that roundtrips bytes through `pipe2`,
`write`, `read`, and `close` direct ecalls; and a static
raw `preadv`/`pwritev` program that matches qemu for split-offset
positional scatter/gather file access without changing the file offset; a static
raw `preadv2`/`pwritev2` program that verifies flags-zero split-offset positional scatter/gather file access through `rem6 run --riscv-se` without unknown-syscall records; same-process `process_vm_readv`/`process_vm_writev` syscall-table coverage plus a toolchain-gated raw CLI smoke; a static
raw `truncate` program that matches qemu for guest-file shrink, missing-path
`ENOENT`, reread contents, guest writes, and an empty unknown-syscall list; a static raw `mknodat` program that matches qemu for regular-file creation, duplicate `EEXIST`, open/read/write consumption, and an empty unknown-syscall list; a static
raw xattr program that matches qemu for user xattr set/get/list membership/remove
and an empty unknown-syscall list when the host temp filesystem supports xattrs; a static
newlib `open(".", O_DIRECTORY | O_CLOEXEC)` directory traversal smoke plus
`open` with `O_NOCTTY`, `O_NOFOLLOW`, and `O_SYNC` through the legacy `open`
syscall with newlib/libgloss flags; `openat2` table coverage for `open_how`
parsing, zero extension, size/fault errors, mode constraints, and unsupported
resolve rejection plus direct `openat2` ecall coverage for close-on-exec fd
state; a static raw `faccessat2` program that
matches qemu for existing and missing registered guest files; and a static raw
`utimensat` program that matches qemu for registered guest files, null times,
missing paths, bad flags, invalid nanoseconds, `AT_EMPTY_PATH`, and bad fds;
a static raw `sendfile` program that matches qemu for explicit input-offset
copying, output reads, and unchanged input fd offsets; a static raw `splice`
program that matches qemu for pipe/file movement, offset writeback, and file-to-file rejection; a static raw `tee` program that matches qemu for non-consuming pipe duplication; a static raw `vmsplice` program that matches qemu for guest-iovec pipe writes; a static raw `copy_file_range` program that matches qemu for explicit input/output offset
writeback, guest-file contents, unchanged fd offsets, bad flags, and bad fds;
a static raw `flock` program that matches qemu for advisory lock, unlock, and
bad-fd returns; a static raw `fcntl` byte-range advisory lock program that
matches qemu for no-conflict reporting, lock/unlock success, bad lock type, and
bad-fd returns; a static raw pipe-size `fcntl` program that matches qemu for
positive pipe-size queries, set/get consistency, non-pipe `EBADF`, and bad-fd
returns; a static raw `fcntl` owner program that matches qemu for
`F_SETOWN`/`F_GETOWN`, `F_SETOWN_EX`/`F_GETOWN_EX`, `F_SETSIG`/`F_GETSIG`, dup aliasing, invalid-signal validation, and close; a static raw
`fchownat`/`fchown` program that matches qemu
for no-op owner changes, missing paths, bad flags, fd-based ownership calls,
`AT_EMPTY_PATH`, and bad fds; a static raw chmod-family program that drives
`fchmod`, `fchmodat`, and `fchmodat2` through `rem6 run --riscv-se`; a static raw
`prlimit64` programs that set and requery `RLIMIT_STACK`, set `RLIMIT_NOFILE`, and observe fd-allocation `EMFILE` through `rem6 run --riscv-se`; static raw
`prctl` no-new-privs, dumpable, and parent-death-signal programs that match qemu; static raw `get_mempolicy`/`set_mempolicy` programs that record the qemu `ENOSYS` boundary and verify rem6 memory-policy writeback; a static raw
interval-timer program that matches qemu for zero-state query/disarm and Linux
argument errors; static raw `getpriority`/`setpriority`, `ioprio_get`/`ioprio_set`, and `sched_setattr`/`sched_getattr`
programs plus an encoded current group/user priority probe cover deterministic priority state, invalid-target returns, and scheduler state/error boundaries; an encoded no-restart-block `restart_syscall` `EINTR` probe through `rem6 run --riscv-se`; and a no-libc static raw capability program that
matches qemu for `capget`/`capset` zero-capability and Linux error paths.

**Not migrated:** Broad Linux SE parity, process/thread lifecycle, signal
handler/frame delivery beyond ignored-action non-delivery, broad SBI
timer/IPI/reset power-state behavior, remaining HSM wake semantics beyond IPI/TIME retentive wake and the
`hart_start`, `hart_stop`, retentive `hart_suspend`, and default
non-retentive `hart_suspend` slices, VMID/G-stage-tagged HFENCE.GVMA
completion coverage beyond physical-range modeled TLB invalidation, full
Linux boot, and real benchmark workloads.

**Evidence:** `RiscvSyscallTable::handle_with_guest_memory_io_at_tick`,
`RiscvSyscallEmulation::handle_pending_core_trap`, CLI static newlib tests,
shared RISC-V CLI tool detection in `cli_run::support`,
`riscv_syscall_emulation::riscv_syscall_getrusage`, `riscv_syscall::tests::wait4_tests`, `riscv_syscall::tests::exit_tests`,
`riscv_se_resource`, `riscv_se_chdir`, `riscv_se_poll`,
`riscv_se_links`, `riscv_se_mkdir`, `riscv_se_rename`, `riscv_se_getdents`,
`riscv_se_fd`, `riscv_se_open_flags`, `riscv_se_permissions`, `riscv_se_proc`,
`riscv_se_stdio`, `riscv_se_pvec`, `riscv_se_sync`, `riscv_se_tee`, `riscv_se_truncate`, `riscv_se_mknod`, `riscv_se_vmsplice`, `riscv_se_socket`, `riscv_se_xattr`, `riscv_syscall_emulation::riscv_syscall_mknod`, `riscv_syscall_emulation::riscv_syscall_pipe`, `riscv_syscall_emulation::riscv_syscall_socket`, `riscv_syscall_emulation::riscv_syscall_eventfd`, `riscv_syscall_emulation::riscv_syscall_ioctl`, `riscv_syscall_emulation::riscv_syscall_readv`,
`riscv_syscall_emulation::riscv_syscall_writev`, `riscv_syscall::tests::cpu_tests`,
`riscv_syscall::tests::fcntl_tests`,
`riscv_syscall::tests::memfd_tests`, `riscv_syscall::tests::namespace_tests`,
`riscv_syscall_emulation::user_ecall_fcntl_getlk_writes_no_conflict_lock_before_exit`,
`riscv_se_memfd`,
`riscv_se_fd::rem6_run_riscv_se_runs_static_raw_fcntl_locks_against_qemu`,
`riscv_se_fd::rem6_run_riscv_se_runs_static_raw_pipe_size_fcntl_against_qemu`,
`riscv_se_fd::rem6_run_riscv_se_runs_static_raw_fcntl_owner_against_qemu`, `riscv_se_setns`,
`riscv_syscall::tests::hwprobe_tests`, `riscv_syscall_emulation::riscv_syscall_riscv_flush_icache`, `riscv_se_riscv`,
`riscv_syscall_emulation::riscv_syscall_close_range`,
`riscv_syscall_emulation::riscv_syscall_openat2`,
`riscv_syscall_emulation::riscv_syscall_eventfd`, `riscv_syscall::tests::timerfd_tests`, `riscv_se_timerfd`,
`riscv_syscall_emulation::riscv_syscall_signalfd`,
`riscv_se_signalfd`,
`riscv_syscall_emulation::riscv_syscall_inotify`, `riscv_se_inotify`,
`riscv_syscall_emulation::riscv_syscall_epoll`, `riscv_se_epoll`,
`riscv_syscall_emulation::riscv_syscall_pselect`,
`riscv_syscall::tests::mlock_tests`, `riscv_se_mmap`,
`riscv_syscall::tests::memory_policy_tests`,
`riscv_syscall::tests::mmap_tests`,
`riscv_syscall::tests::msync_tests`,
`riscv_syscall_emulation::riscv_syscall_prlimit64`,
`riscv_syscall::tests::sync_tests`,
`riscv_syscall::tests::positioned_io_tests`,
`riscv_syscall::tests::mkdir_tests`,
`riscv_syscall::tests::truncate_tests`,
`riscv_syscall::tests::xattr_tests`,
`riscv_syscall::tests::stat_tests`,
`riscv_syscall::tests::statfs_tests`, `riscv_syscall::tests::admin_tests`,
`riscv_syscall::tests::process_tests`, `riscv_syscall::tests::unshare_tests`,
`riscv_syscall::tests::capability_tests`,
`riscv_se_permissions::rem6_run_riscv_se_runs_static_raw_capability_syscalls_against_qemu`, `riscv_se_admin`, `riscv_se_known_ni`, `riscv_se_ioprio`,
`riscv_se_process::rem6_run_riscv_se_runs_static_raw_futex_wake_op_against_qemu`,
`riscv_se_process::rem6_run_riscv_se_runs_static_raw_futex_wait_bitset_elapsed_timeout_against_qemu`,
`riscv_syscall::tests::robust_tests`,
`riscv_syscall::tests::scheduler_tests`, `riscv_syscall::tests::scheduler_getattr_tests`, `riscv_syscall::tests::scheduler_setattr_tests`,
`riscv_syscall::tests::signal_tests`,
`riscv_syscall::tests::futex_tests`,
`riscv_syscall::tests::nanosleep_tests`,
`riscv_syscall::tests::sysinfo_tests`, `riscv_syscall::tests::syslog_tests`,
`riscv_syscall::tests::utsname_tests`,
`riscv_se_statx`,
`riscv_se_sysinfo`, `riscv_se_syslog`,
`riscv_se_time`,
`riscv_syscall_emulation::riscv_sbi_base`, `riscv_syscall_emulation::riscv_sbi_debug_console`, `riscv_syscall_emulation::riscv_sbi_firmware`,
`riscv_system_translation`, `riscv_se_process`, `riscv_se_unshare`,
`riscv_se_signal`,
`riscv_sbi::tests::remote_hfence_gvma_rejects_missing_target_before_scheduling_flush`,
`riscv_sbi::tests::remote_hfence_gvma_rejects_invalid_range_before_scheduling_flush`,
`riscv_sbi::tests::remote_hfence_gvma_flushes_target_tlb_when_completion_event_runs`,
`riscv_sbi::tests::remote_hfence_gvma_range_flushes_overlapping_physical_pages_only`, `riscv_sbi::tests::remote_hfence_gvma_vmid_preserves_non_overlapping_physical_ranges_without_vmid_tag`,
`riscv_sbi::tests::remote_hfence_gvma_vmid_rejects_invalid_vmid_before_scheduling_flush`,
`riscv_sbi::tests::remote_hfence_vvma_asid_rejects_invalid_asid_before_scheduling_flush`,
`riscv_sbi::tests::remote_hfence_vvma_range_flushes_overlapping_pages_only`,
`riscv_sbi::tests::remote_hfence_vvma_asid_preserves_other_address_spaces`,
`riscv_sbi::tests::remote_fence_i_resets_target_fetch_stream_when_completion_event_runs`,
`riscv_sbi::tests::handle_pending_core_trap_schedules_ipi_completion_event`,
`riscv_sbi::tests::parallel_handle_pending_core_trap_schedules_ipi_completion_event`,
`riscv_sbi::tests::send_ipi_scheduler_error_leaves_no_partial_target_events`, `riscv_sbi::rem6_run_riscv_sbi_starts_secondary_hart_through_hsm`, `riscv_sbi::rem6_run_riscv_sbi_secondary_hart_receives_ipi_interrupt_after_hsm_start`, `riscv_sbi::rem6_run_riscv_sbi_ipi_wakes_retentive_hart_suspend`, `riscv_sbi::rem6_run_riscv_sbi_timer_wakes_retentive_hart_suspend`, `riscv_sbi::rem6_run_riscv_sbi_remote_fence_i_records_rfence_request`, `riscv_sbi::rfence_completion::rem6_run_riscv_sbi_remote_sfence_vma_records_completion`, `riscv_sbi::rem6_run_riscv_sbi_shutdown_reset_records_shutdown_stat`, `riscv_sbi::rem6_run_riscv_sbi_reboot_resets_record_reboot_type_stats`, `riscv_sbi::rem6_run_riscv_sbi_system_reset_records_reset_request`, `riscv_sbi::rem6_run_riscv_sbi_hart_start_reports_already_available_for_boot_hart`,
`workload_replay_linux_boot::workload_replay_linux_boot_handoff_enters_supervisor_sbi`,
`riscv_sbi_remote_hfence_gvma_flushes_translated_data_tlb`, `riscv_sbi_remote_hfence_gvma_range_flushes_each_overlapping_physical_page`,
`RiscvLinuxBootHandoffConfig`, and RISC-V DTB handoff tests.

**Next evidence:** Broader static libc program coverage, broader SBI completion
coverage, then a real Linux boot smoke.

### Devices and Platforms - 50% single-axis

**Score calculation:** 5 of 10 items have executable evidence, or 50% raw. The
bucket cap is single-axis because real Linux driver interaction, host
networking, non-RISC-V boards, and coherent DMA timing are not complete.

- [x] MMIO bus, UART, PL011, CLINT, PLIC, RTC, timers, interrupt routes, platform readfile checkpoint attachment, and CLI readfile MMIO have tests.
- [x] PCI, VirtIO MMIO/PCI, block, console, RNG, storage image, SimpleDisk, and IDE slices exist.
- [x] Ethernet switching and SINIC MMIO/PCI/DMA/checkpoint paths exist.
- [x] RISC-V DTB and initrd handoff APIs exist.
- [x] Device and topology validation reject malformed ownership and routes.
- [ ] PS/2, QEMU bridge, and broader board-specific devices exist.
- [ ] Real TUN/TAP or host networking adapters exist.
- [ ] Non-RISC-V board trees and boot paths exist.
- [ ] Devices are validated by real Linux boot and driver interaction.
- [ ] Device timing and DMA paths are complete across cache/coherence/DRAM.

**Migrated:** Broad typed device slices and topology validation.

**Not migrated:** Several board devices, real host networking adapters,
non-RISC-V boards, and Linux-driver validation.

**Evidence:** `PlatformBuilder`, `PlatformRiscvDeviceTreeConfig`, topology
tests, PCI/VirtIO/storage/network checkpoint tests, CLINT/PLIC/UART tests,
`readfile_device` platform-MMIO tests, `riscv_topology_readfile`, and CLI
`run --readfile` guest-load tests covering host-file and resource payloads.

**Next evidence:** Board-level Linux boot with console, timer, storage, and
network evidence.

### Stats, Probes, Debug, Host Actions, and Checkpointing - 74% representative

**Score calculation:** 24 of 26 items have executable evidence, or 92% raw. The bucket cap is
representative because canonical normal-run target selection spans four memory routes
and both power formats, with exact cache and DRAM calibration, while complete hierarchy counters,
broader GDB CSR coverage, broad O3 execution, and physical fabrication/vendor
coefficient calibration remain open.

- [x] Hierarchical stats, reset/dump history, and CLI stats artifacts exist.
- [x] Probe registry plus real RISC-V retired-instruction, retired-PC target,
  and data-access producers exist.
- [x] m5 exit/fail/stats/checkpoint/work markers reach typed host actions,
  with direct `rem6 run` JSON evidence for repeated RISC-V work-marker
  payloads, store-backed and DRAM-backed checkpoint memory component/chunk
  metadata with checkpoint chunk checksum changes, m5 hypercall
  selector/argument/configured-response metadata, m5 return-value `sum` execution, periodic m5
  stats dump/reset and checkpoint tick repeats, and
  `m5_switch_cpu` as top-level execution-mode metadata plus continued execution,
  checkpoint-backed transfer counts, and run-level `sim.host_actions.*` stats including SE ROI/stat hooks.
- [x] Decode-first checkpoint capture/restore exists across scheduler, memory, device, storage, VirtIO, timer, interrupt, RISC-V hart run-state, platform, workload, and manifest owners, with file-image restore capability preflight rejecting read-only backing targets before host checkpoint metadata or execution-mode authority commits.
- [x] GDB remote packet/session parsing and RISC-V integer/PC register paths exist.
- [x] GDB RV64D floating-point, including XML-aligned `fflags`/`frm`/`fcsr` and placeholder numbering, advertised RV64 CSR target descriptions including supervisor `sscratch`, supervisor environment `senvcfg`, machine environment `menvcfg`, translation `satp`, interrupt aliases `sie`/`sip`, machine identity `mhartid`/`mvendorid`/`marchid`/`mimpid`/`mconfigptr`, machine ISA `misa`, counter inhibit `mcountinhibit`, counter `cycle`/`time`/`instret`, machine-counter aliases `mcycle`/`minstret`, packed PMP config `pmpcfg0`/`pmpcfg2`, and PMP address registers `pmpaddr0` through `pmpaddr15`, and RV64 machine status, interrupt, trap, identity, ISA, environment-config, counter-inhibit, counter, and PMP CSR register-cache paths exist, with `cycle`/`mcycle`, `time`, and `instret`/`minstret` remaining GDB-visible across real GDB single-step execution and PMP0 through PMP15 writes updating the same `RiscvCore` PMP state consumed by CPU access checks.
- [x] GDB RV64 vector fixed-point and vector-configuration CSR target descriptions and register-cache read/write paths exist for `vxsat`, `vxrm`, `vcsr`, `vl`, `vtype`, and `vlenb`.
- [x] GDB RV32D floating-point plus RV32 CSR target descriptions and register-cache read/write paths exist for FP registers, `fflags`/`frm`/`fcsr`, supervisor, machine, interrupt, translation, vector fixed-point CSRs, RV32 `mstatush`, RV32 counter-high CSRs, `mconfigptr`, `mcountinhibit`, and XLEN-mapped vector-configuration CSRs; RV32 CSR target descriptions also advertise packed PMP config CSRs `pmpcfg0` through `pmpcfg3`, with `pmpcfg1`/`pmpcfg3` core packet-handler writes updating live PMP entries 4 through 7 and 12 through 15.
- [x] GDB RV32/RV64 vector data register target descriptions and register-cache read/write paths exist for `v0` through `v31`.
- [x] Power and thermal models plus external power-analysis exports exist.
- [x] Host actions and guest events are typed and checkpoint-aware.
- [x] First-class histogram stats have registry snapshots, deltas, resets,
  CLI JSON/text bucket output, and real data-access stack-distance producer
  output.
- [x] RISC-V in-order pipeline timing state and fetch-steering branch predictor
  state are captured and restored by core checkpoints.
- [x] GDB packet byte streams drive typed step/resume and break/watch control state in memory-backed sessions.
- [x] CLI `run --gdb-listen` applies pre-execution RISC-V register writes,
  memory writes, and software breakpoint packets before the normal run consumes
  the mutated core and memory state, including vector data registers, vector-configuration CSRs, machine environment CSR `menvcfg`, machine counter-inhibit CSR `mcountinhibit`, supervisor/machine trap CSRs, and machine exception delegation CSR `medeleg`; it also serves RV32 target descriptions from RV32 ELF metadata and accepts RV32-only packed PMP config CSR read/write packets.
- [x] CLI `run --gdb-listen` single-step packets drive one real RISC-V
  instruction, return a GDB stop reply, and leave the stepped state visible to
  subsequent register reads and detach-time execution.
- [x] CLI `run --gdb-listen` continue and `vCont;c` packets drive the normal
  RISC-V run driver to a guest stop, return a GDB stop reply, and feed the
  completed run into CLI execution summaries, including cache-protocol runtime
  stats when instruction and data caches are selected.
- [x] CLI `run --gdb-listen` write watchpoints accept `Z2`/`z2`, stop after a
  real RISC-V store data access completes with `T05watch:<addr>;`, and leave later guest instructions for detach-time execution.
- [x] CLI `run --gdb-listen` read/access watchpoints accept `Z3`/`z3` and
  `Z4`/`z4`, stop after real RISC-V load or store data accesses complete with `T05rwatch:<addr>;`/`T05awatch:<addr>;`, and leave later guest instructions for detach-time execution.
- [x] CLI `run --gdb-listen` hardware breakpoints accept `Z1`/`z1`, stop before
  a matching RISC-V instruction retires, and continue after removal without
  patching guest memory.
- [x] CLI `run --execute --stats-format json --debug-flags Exec,Fetch,Data,Cache,Dram,Fabric,Memory,Syscall,HostAction` emits
  deterministic instruction, fetch, data-access, cache hierarchy, DRAM hierarchy including low-power target/port/bank state counters, fabric lane/hop, memory-transport with response-latency ticks, RISC-V SE syscall, and m5 host-action trace records; separate CLI `run --execute --riscv-sbi --stats-format json --debug-flags Sbi` emits RISC-V SBI firmware trace records from real RISC-V execution paths.
- [x] Stricter gem5 text-stat compatibility exists.
- [ ] Cache/bank/fabric/DRAM hierarchy counters are complete.
- [ ] Broader GDB CSR register-cache coverage exists.
- [x] Normal-run power target selection follows canonical memory activity, with exact cache and DRAM calibration.
- [x] O3 pending-state checkpoints exist.

**Migrated:** Structured stats, real RISC-V probe producers, checkpoint banks including read-only backing file-image restore preflight before host checkpoint metadata commit,
m5ops, host actions, run-level `sim.host_actions.*` stats, GDB packet/session parsing, RISC-V integer/PC register
paths, RV64D floating-point target descriptions including XML-aligned
`fflags`/`frm`/`fcsr` and placeholder numbering,
advertised RV64 CSR target descriptions including supervisor `sscratch`, supervisor environment `senvcfg`, machine environment `menvcfg`, translation `satp`, interrupt aliases `sie`/`sip`, machine identity
`mhartid`/`mvendorid`/`marchid`/`mimpid`, machine ISA `misa`, counter `cycle`/`time`/`instret`, packed PMP config `pmpcfg0`/`pmpcfg2`, and PMP address registers `pmpaddr0` through `pmpaddr15`,
RV64 machine status, interrupt, trap, identity, ISA, environment-config, counter, exception-delegation, and PMP CSR register-cache paths, including independent
GDB-visible `cycle` and `time` state across CSR writes and single-step execution and PMP0 through PMP15 writes reflected in the live `RiscvCore` PMP table, and
RV64 vector fixed-point and vector-configuration CSR register-cache paths for
`vxsat`, `vxrm`, `vcsr`, `vl`, `vtype`, and `vlenb`, plus RV32D floating-point target descriptions and register-cache
read/write paths for FP registers and `fflags`/`frm`/`fcsr`, plus RV32 CSR
target descriptions and register-cache read/write paths for supervisor/machine
trap, status, interrupt, translation, vector fixed-point CSRs, RV32 counter-high CSRs, and XLEN-mapped vector-configuration CSRs, plus RV32/RV64
vector data register target descriptions and register-cache read/write paths
for `v0` through `v31`,
RISC-V software breakpoint
patch/restore through the
system GDB memory handler, gem5-style final-tick, committed-instruction, and
sim-frequency stat aliases, text output with gem5-compatible no-leading-blank
headers plus `simOps`, deterministic `simSeconds`, single-core
`system.cpu.numInsts`/`system.cpu.numOps`/`system.cpu.commitStats0.numInsts`/`system.cpu.commitStats0.numOps`/`system.cpu.numCycles` aliases plus text-output derived `system.cpu.ipc`/`system.cpu.cpi` and `system.cpu.commitStats0.ipc`/`system.cpu.commitStats0.cpi`, and multicore
`system.cpuN.numInsts`/`system.cpuN.numOps`/`system.cpuN.commitStats0.numInsts`/`system.cpuN.commitStats0.numOps`/`system.cpuN.numCycles` aliases plus text-output derived `system.cpuN.ipc`/`system.cpuN.cpi` and `system.cpuN.commitStats0.ipc`/`system.cpuN.commitStats0.cpi`,
target/port/bank-level DRAM runtime resource counters, RISC-V in-order pipeline
per-stage current/max in-flight and occupied-cycle stats, fetch-steering branch predictor checkpoint
capture/restore including live fetch-ahead pending speculation, and GShare, BiMode, Tournament, and multiperspective perceptron predictor checkpoint capture/restore, GDB byte-stream packet handling, debug
execution-control state for packet-stream step/resume and break/watch requests,
CLI run aggregate memory-resource activity, active-resource, fabric active-link/active-hop resource counters, instruction/data cache-resource hierarchy with per-protocol run counts, prefetch queue/translation-queue, DRAM byte, split row-hit, and read-ready-latency, refresh/low-power stats, custom LPDDR low-power timing, gem5-style `system.mem_ctrl` request/burst/byte aliases plus `system.mem_ctrl.dram` burst/per-bank burst/row-hit/byte and text-output `avgRdBW`/`avgWrBW`/`readRowHitRate`/`writeRowHitRate`/`pageHitRate` aliases, and target/port/bank DRAM low-power hierarchy stats derived from executed cache, transport, and DRAM paths,
CLI `run --gdb-listen` attach-before-execute socket handoff for RISC-V
stop-reason, register, memory, and detach packets, pre-execution register and
memory writes, and pre-execution software breakpoints consumed by the following
run, plus pre-detach single-step execution through the normal RISC-V run driver,
runtime GDB continue and `vCont;c` execution through the normal RISC-V run
driver with completed-run CLI summary handoff, including instruction-cache and
data-cache runtime summaries from cache-backed GDB sessions,
runtime GDB data-watchpoint execution through real RISC-V load and store
data-access completions with reason/address stop replies and detach-time continuation of later guest
instructions, runtime GDB hardware breakpoints that stop before the matching
RISC-V instruction retires without patching guest memory,
top-level `rem6 run --execute --stats-format json --debug-flags Exec,Fetch,Data,Cache,Dram,Fabric,Memory,Syscall,HostAction`
execution-trace JSON emitted from real RISC-V instruction execution events with
CPU/retirement-scoped retired-record and instruction-byte stats, fetch-trace JSON emitted from real
RISC-V fetch issue records with CPU/endpoint-scoped issue-record and byte stats, and data-trace JSON emitted from
completed RISC-V data-access records with CPU/kind-scoped load/store/atomic count and byte stats, cache-trace JSON emitted from executed instruction/data L1/L2/L3 memory-resource cache summaries with activity, CPU-response, directory-decision, backing-DRAM, bank, and hierarchy-scoped prefetch queue/translation/ratio stats,
DRAM-trace JSON
emitted from real top-level DRAM target, port, and bank activity with target
access/read/write, port-command, low-power state/residency/exit-latency fields, hierarchy-scoped target/port/bank activity, row, refresh, command, and ready-latency stats, target/port turnaround stats, and bank byte stats, fabric-trace JSON
emitted from real top-level fabric lane and hop activity with aggregate and hierarchy-scoped lane/hop transfer, byte, flit, and timing stats, memory-transport trace
JSON emitted from real fetch/data `MemoryTrace` events with top-level debug trace
record-count, channel-scoped route/endpoint/request-agent, event-kind, response-status, and response-latency tick stats, plus data-trace load/store/atomic classification stats,
RISC-V SE syscall-trace JSON/outcome plus hierarchy-scoped syscall-number, call-site, CPU, and argument stats emitted from real syscall trap handling, separate `rem6 run --execute --riscv-sbi --stats-format json --debug-flags Sbi` RISC-V SBI trace JSON plus console/timer/HSM/reset counters emitted from executed SBI summaries, host-action trace JSON plus ROI/stat/checkpoint/restore/stop counters, final execution-mode authority, O3 debug target/mode labels and event tick/span and occupancy stats, and restored checkpoint component details emitted from real m5 host-action outcomes, and power-trace JSON plus target-hierarchy/state/residency/temperature/microwatt/microwatt-tick stats emitted from executed-run activity including shared L2/L3 cache and fabric/NoC power records derived from memory-resource summaries,
top-level debug trace record/category/active-flag/payload-byte roll-up stats emitted from the same debug summary,
target-description-aligned register-cache seeding, top-level trace-replay
fabric-route activity and flit counters, top-level run fabric resource roll-up, top-level trace-replay aggregate resource
activity counters, top-level trace-replay data-cache run and protocol
counters, top-level trace-replay data-cache scheduler resource and backing DRAM
target/port/bank, row, command, latency, and QoS counters,
top-level `rem6 run` retired-instruction probe summaries and CLI-configured
retired-PC target counter summaries,
RISC-V core checkpoint capture/restore of O3 runtime payloads covering
ROB entries, LSQ entries, rename maps, pending scopes, deferred writeback completions, live-retire-gate request/absolute-ready-tick state, v21 live-staged ROB/rename identity, restored refetch rebinding, v20 staged-field defaults, and v19 gate zero-fill compatibility, and canonical normal-run power records derived from `memory_resources` across direct, transport-plus-DRAM, explicit MSI instruction/data L1 plus shared L2/L3, and cache-fabric-DRAM routes.
Imported McPAT XML and DSENT CSV target presence for CPU L1, shared caches, transport, and fabric follows matching canonical artifact and stats `active` fields; DRAM follows `active || profiled_targets`, including `sim.memory.resources.dram.profiled_targets`; unselected targets are suppressed, while exact cache coefficients and DRAM primitive events, operations, bytes, and residency are separately guarded. Custom plus library-level and `rem6 run --power-output`, `rem6 trace-replay --power-output`, and `rem6 gpu-run --power-output` exports retain shared-cache, fabric/NoC, replay, and GPU route evidence.

**Not migrated:** Complete gem5 text-stat parity, full debug execution control,
complete cache/bank/fabric/DRAM hierarchy counters, remaining broad CSR GDB register-cache coverage, broader debug-run stat parity, physical fabrication or vendor coefficient calibration,
and broad running O3 execution beyond the current targeted bounded four-row scalar FU/ALU live ROB/rename staging window and checkpointed runtime state.

**Evidence:** `StatsRegistry`, `ProbeRegistry`, `RiscvInstructionStats`,
`RiscvDataAccessStats`, `SystemActionExecutor`, `GdbRemoteSession`,
`cli_run::pc_count_probes::rem6_run_emits_riscv_pc_count_probe_stats`,
checkpoint tests including `storage_image_checkpoint_bank_rejects_read_only_file_target_during_validation`, `file_storage_checkpoint_replays_read_only_snapshot_on_writable_target`, `storage_image_checkpoint_bank_rejects_target_aliases`, `storage_checkpoint_alias_attach_leaves_no_orphan_component`, `storage_checkpoint_bank_attach_rolls_back_partial_registration`, `read_only_storage_restore_fails_before_host_checkpoint_metadata_commit`, RISC-V hart run-state, in-order pipeline restore,
fetch-steering branch predictor restore with live fetch-ahead pending
speculation, and GShare, BiMode, Tournament, and multiperspective perceptron predictor checkpoint restore,
RISC-V core O3 runtime checkpoint restore tests plus O3 pending/writeback payload tests, GDB byte-stream,
RV64D FP, FP CSR, RV64 CSR register-cache including supervisor `sscratch`, exception delegation `medeleg`, `senvcfg`, `menvcfg`, and `mcountinhibit`,
translation `satp`, interrupt aliases `sie`/`sip`, counter
`cycle`/`time`/`instret`, and machine-counter aliases `mcycle`/`minstret`, RV64 machine CSR register-cache including `mscratch`
write/readback plus machine identity `mhartid`/`mvendorid`/`marchid`/`mimpid`/`mconfigptr`
and machine ISA `misa` readback through guest CSR execution and top-level GDB,
RV32 CSR target/register-cache coverage for `mstatush`, counter-high CSRs, `mcountinhibit`, plus packed `pmpcfg0` through `pmpcfg3`
with core packet-handler writes reflected in the live `RiscvCore` PMP table,
PMP CSR target/register-cache coverage for packed `pmpcfg0`/`pmpcfg2` and `pmpaddr0` through `pmpaddr15` in both the core packet handler and top-level `rem6 run --gdb-listen`,
top-level RV32 ELF-selected `rem6 run --gdb-listen` target-description service plus `mstatush` and `pmpcfg1`/`pmpcfg3` register read/write packets,
live `cycle`/`time`/`instret` plus `mcycle`/`minstret` reads after top-level GDB single-step execution,
and independent counter writes consumed by subsequent GDB single-step
execution,
RV64 vector fixed-point CSR register-cache tests
covering `vxsat`, `vxrm`, and `vcsr`, RV32D FP target/register-cache tests,
RV32 CSR target and register-cache tests, RV32/RV64 vector target and
register-cache tests, and control-state tests,
`gdb_remote_packet` execution-control tests,
CLI `run --gdb-listen` smoke tests for RISC-V pre-execution state and
pre-execution writes consumed by the subsequent run, including RV64 vector
data register target-description and register-cache smoke coverage and RV32
target-description plus packed PMP config smoke coverage,
CLI `run --gdb-listen`
single-step, continue, `vCont;c`, hardware-breakpoint, and data-watchpoint
stop-reply smoke tests, trap CSR pre-execution smoke tests, GDB cache-runtime smoke tests,
Power-analysis export and CLI power-output evidence includes `rem6_run_power_activity_matches_canonical_resource_matrix`, which imports McPAT XML and DSENT CSV for direct, transport-plus-DRAM, explicit MSI L1/L2/L3, and cache-fabric-DRAM runs and checks artifact/stat active-field plus DRAM profile-count agreement, canonical target selection, positive records, primitive-event and actual-byte DRAM dynamic watts, and unselected-target suppression.
Separate focused power unit tests cover profile-only, refresh-only, and low-power-only DRAM activity; exact access residency via `run_dram_power_uses_accesses_when_they_dominate_residency`; command/event separation via `run_dram_power_keeps_commands_out_of_event_count`; exact bytes via `run_dram_power_uses_canonical_byte_total`; zero-resource suppression; and exact cache coefficients via `run_cache_power_preserves_legacy_target_calibrations`.
System GDB software
breakpoint patch/restore tests, CLI data-access probe tests, CLI `Branch`, `Pipeline`, `O3`, `Exec`, `Fetch`, `Data`, `Cache`, `Dram`, `Fabric`, `Memory`, `Syscall`, `Sbi`, `HostAction`, and `Power` debug-flag tests, including `Branch` in-order branch-prediction JSON with `sim.debug.branch_trace.records`, `sim.debug.branch_trace.cpu.cpu0.records`, and `sim.debug.branch_trace.mispredictions`, `Pipeline` in-order cycle-plan JSON with `sim.debug.pipeline_trace.records`, `sim.debug.pipeline_trace.branch_prediction_flushed`, and `sim.debug.pipeline_trace.cpu.cpu0.records`, `O3` detailed-mode single/multicore aggregate and CPU-scoped ROB/LSQ/rename stats (`sim.debug.o3_trace.cpu.cpu0.records`, `sim.debug.o3_trace.cpu.cpu1.event.tick_span`) plus event ROB/LSQ/rename occupancy, sequence-ordered ROB allocation/commit, FU-latency-class including scalar/vector integer mul/div plus multiply-add/widening/saturating-multiply and scalar/vector float add/compare/misc/mul/FMA/div/sqrt event count/cycle/min/max/avg stats, LSQ address-matched store-forwarding, address-mismatch suppression, vector LSQ load/store byte event JSON, scalar/vector/float/atomic LSQ operation-shape including aggregate float counters and failed store-conditional outcome JSON/stats, direct/cache-backed LSQ data-response tick/latency JSON plus aggregate total/min/max/avg plus sample-count and operation-class total/min/max/avg Tick stats, aq/rl ordering, direct-conditional taken/not-taken/predicted-target-match/predicted-taken-not-taken-mismatch branch event/squash-target JSON/stats, direct-unconditional branch event/squash-kind JSON/stats, direct-call branch event/misprediction-kind JSON/stats, and return plus indirect-unconditional/call branch event/taken/resolved-target/predicted-not-taken/link-kind, per-event wrong-target, and indirect-unconditional/indirect-call wrong-target JSON/stats with timing-mode suppression and `sim.debug.o3_trace.*` counters, `Exec` CPU/retirement-scoped retired-record and byte stats, `Fetch` CPU/endpoint-scoped issue-record and byte stats, `Data` CPU/kind-scoped load/store/atomic classification and byte stats, `Cache` instruction/data L1/L2/L3 activity, CPU-response, directory-decision, backing-DRAM, bank, and hierarchy-scoped prefetch queue/translation/ratio stats, `Memory` channel-scoped route/endpoint/request-agent, event-kind, response-status, and response-latency tick stats, `Syscall` return/exit/blocked outcome plus hierarchy-scoped syscall-number, call-site, CPU, and argument stats, `Sbi` console/timer/HSM/reset trace records plus `sim.debug.sbi_trace.records` and console-byte counters, `HostAction` m5 ROI/stat/checkpoint/restore/stop trace records plus nested execution-mode state-transfer, quiescence-gate checker JSON, and checkpoint-restore authority JSON/stat counters including `/debug/host_action_trace/0/state_transfer/quiescence_gate/checker/checked_instructions`, `sim.debug.host_action_trace.records`, and kind counters, `Power` target-hierarchy/state/residency/temperature/microwatt/microwatt-tick stats, `Memory` debug trace record-count stats, `Dram` debug trace record-count plus hierarchy-scoped target/port/bank access, row, refresh, command, latency, low-power state/residency/exit-latency, and byte stats, `Fabric` debug trace record-count plus hierarchy-scoped lane/hop transfer, byte, flit, queue-delay, credit-delay, max-credit-delay, and occupancy stats, and histogram registry/output tests. O3 debug-flag coverage now includes targetless branch-mismatch JSON/stat counters for predicted-target cases that resolve as fall-through with no resolved target, distinct from wrong-target counters, plus reset-scoped `stats_epoch`/`stats_reset_tick` metadata and checkpoint-restore count plus label-list and latest label/tick/manifest/payload/component scope metadata with post-reset/post-restore-only event counters for O3 traces after `m5_reset_stats`, single scheduled restore, same-checkpoint two-restore replay, distinct-label two-restore replay, or two-hart one-restore replay with scoped-record stats.
CLI `Exec,Fetch` debug-flag evidence covers top-level debug trace record, category, active-flag, and payload-byte roll-up stats; CLI `Sbi`, `HostAction`, and `O3` cover record/category/active-flag roll-up stats against emitted debug JSON, including inactive timing-mode O3 suppression, and O3 host-action dump/reset evidence covers resettable scalar/vector float-misc FU-latency class samples plus IQ/IEW issued/dispatch/commit and combined IEW/commit branch-mispredict samples.
CLI retired-instruction probe tests expose InstTracker-backed event counts and
tracked-instruction counts from executed RISC-V instructions.
The CLI data-access probe tests include stack-distance histogram stats emitted
from executed RISC-V loads. CLI DRAM-backed execution tests include
target/port/bank-level DRAM resource counters emitted from executed RISC-V
instruction fetches. CLI text stats include gem5-style final-tick,
committed-instruction, sim-ops, sim-seconds, sim-frequency, single-core CPU
`numInsts`/`numOps`/`commitStats0.numInsts`/`commitStats0.numOps`/`numCycles` aliases plus CPU and `commitStats0` `ipc`/`cpi` rates, single-core no-prefetch L1 I-cache/D-cache demand and overall hit/miss/access/miss-rate aliases plus `demandMshrHits`/`overallMshrHits`/`demandMshrMisses`/`overallMshrMisses`/`demandMshrMissRate`/`overallMshrMissRate`, configured single-core L1 I-cache/D-cache prefetcher `pfIdentified`/`pfIssued`/`pfUseful`/`pfUsefulButMiss`/`pfUnused`/`pfHitInCache`/`pfHitInMSHR`/`pfHitInWB`/`pfLate`/`pfSpanPage`/`pfUsefulSpanPage`/`pfInCache`/`demandMshrMisses` aliases plus derived `accuracy`/`coverage` aliases and artifact/stat `accuracy_ppm`/`coverage_ppm` fields, shared L2/L3 overall hit/miss/access/miss-rate plus MSHR aliases backed by executed instruction/data L2/L3 cache-bank summaries, multicore CPU `numInsts`/`numOps`/`commitStats0.numInsts`/`commitStats0.numOps`/`numCycles` aliases plus CPU and `commitStats0` `ipc`/`cpi` rates, detailed-switch O3 per-core runtime JSON including branch-repair outcome/kind matrix fields and scalar integer IEW producer/consumer dependency fanout fields, table-driven scalar/vector FU-latency class JSON/text counters including float-misc/vector-float-misc, structured IQ/IEW JSON and dump/reset counters including branch-issued, writeback count, producer/consumer fanout counts, path-safe `sim.cpu0.o3.iew.writeback_rate_ppm`/`sim.cpu0.o3.iew.producer_consumer_fanout_ppm`, and final JSON `system.cpu.iew.wbRate`/`system.cpu.iew.wbFanout`, JSON/text `system.cpu.iew.branchMispredicts` and `system.cpu.commit.branchMispredicts`, JSON `system.cpu.iew.branchRepair.targetlessMismatch`/`system.cpu.iew.branchRepair.directionOnly`/`system.cpu.iew.branchRepair.wrongTarget`/`system.cpu.iew.branchRepair.total`, JSON/text `system.cpu.iew.branchRepair_0::TargetlessMismatch`/`system.cpu.iew.branchRepair_0::DirectionOnly`/`system.cpu.iew.branchRepair_0::WrongTarget`/`system.cpu.iew.branchRepair_0::total`, and first-class JSON/text aliases `system.cpu.rob.writes`, `system.cpu.rob.reads`, `system.cpu.rob.maxOccupancy`, `system.cpu.rename.renamedInsts`, `system.cpu.iew.dispatchedInsts`, `system.cpu.iew.dispLoadInsts`, `system.cpu.lsq0.addedLoadsAndStores`, `system.cpu.lsq0.maxOccupancy`, `system.cpu.lsq0.loadBytes`/`storeBytes`, `system.cpu.lsq0.forwLoads`, `system.cpu.lsq0.operation.load`/`system.cpu.lsq0.operation.total`/`system.cpu.lsq0.ordering.acquireRelease`/`system.cpu.lsq0.ordering.total`, `system.cpu.lsq0.dataResponse.samples`, `system.cpu.lsq0.dataResponse.totalLatency`, `system.cpu.lsq0.dataResponse.load.totalLatency`, `system.cpu.lsq0.dataResponse.storeConditional.totalLatency`, `system.cpu.iq.instsIssued`, `system.cpu.iq.memInstsIssued`, and `system.cpu.iq.branchInstsIssued`, plus JSON/text IEW compatibility aliases `system.cpu.iew.instsToCommit::total`, `system.cpu.iew.writebackCount::total`, `system.cpu.iew.producerInst::total`, and `system.cpu.iew.consumerInst::total`, JSON/text IQ op-class bucket aliases `system.cpu.iq.issuedInstType_0::MemRead`/`MemWrite`/`IntMult`/`IntDiv`/`FloatMisc`/`SimdFloatMisc`, raw `sim.cpu0.o3.commit.committed_inst_type.mem_read`/`sim.cpu0.o3.commit.committed_inst_type.mem_write`/`sim.cpu0.o3.commit.committed_inst_type.int_mul`/`sim.cpu0.o3.commit.committed_inst_type.int_div`/`sim.cpu0.o3.commit.committed_inst_type.float_misc`/`sim.cpu0.o3.commit.committed_inst_type.vector_float_misc`, and JSON/text commit op-class bucket aliases `system.cpu.commit.committedInstType_0::MemRead`/`MemWrite`/`IntMult`/`IntDiv`/`FloatMisc`/`SimdFloatMisc`, and in-order text aliases including `system.cpu.pipeline.inOrder.stallCycles`, `system.cpu.pipeline.inOrder.fetchWaitCycles`, `system.cpu.pipeline.inOrder.resourceBlocked`, `system.cpu.pipeline.inOrder.orderingBlocked`, `system.cpu.pipeline.inOrder.flushCycles`, `system.cpu.pipeline.inOrder.branchPredictionFlushes`, `system.cpu.pipeline.inOrder.branchPredictionFlushCycles`, `system.cpu.pipeline.inOrder.branchSpeculationPredictions`, `system.cpu.pipeline.inOrder.branchSpeculationRepairs`, `system.cpu.pipeline.inOrder.branchSpeculationRemovedYoungers`, `system.cpu.pipeline.inOrder.branchSpeculationMaxPending`, `system.cpu.pipeline.inOrder.stage.fetch1.resourceBlocked`, `system.cpu.pipeline.inOrder.stage.fetch1.resourceBlockedCycles`, `system.cpu.pipeline.inOrder.stage.decode.branchPredictionFlushed`, multicore `system.cpu0.pipeline.inOrder.stallCycles`, `system.cpu0.pipeline.inOrder.resourceBlocked`, `system.cpu1.pipeline.inOrder.stallCycles`, and `system.cpu1.pipeline.inOrder.resourceBlocked`
plus memory-controller `readReqs`/`writeReqs`/`readBursts`/`writeBursts`/`bytesReadSys`/`bytesWrittenSys`/`avgRdBWSys`/`avgWrBWSys` and DRAM/NVM-interface `readBursts`/`writeBursts`/`perBankRdBursts`/`perBankWrBursts`/`readRowHits`/`writeRowHits`/`dramBytesRead`/`dramBytesWritten`/NVM-profile `nvmBytesRead`/`nvmBytesWritten`/`totMemAccLat` plus text-output `avgRdBW`/`avgWrBW`/`avgMemAccLat`/`readRowHitRate`/`writeRowHitRate`/`pageHitRate`
aliases emitted from an executed RISC-V run. CLI
trace-replay fabric-route tests include
nonzero fabric active-lane, active-virtual-network, transfer, byte, and flit stats
from an executed packet replay, request/response virtual-network and
credit-depth config fields from CLI and TOML entry points, aggregate
resource-activity and active-resource stats, while occupancy, queue-delay,
credit-delay, contention, per-virtual-network and per-lane flit counts, and per-hop activity details are emitted
from the same workload parallel summary. Top-level RISC-V cache/fabric/DRAM runs and trace-replay fabric routes now also emit per-link/per-VN/per-hop transfer, byte, flit, occupied-tick, queue-delay, max-queue-delay, and credit-delay stats matched to the same emitted hop activity records. CLI trace-replay data-cache protocol
tests include data-cache run, protocol, CPU-response, directory-decision, and
scheduler resource counters emitted from the executed workload parallel
summary.
Detailed O3 commit JSON/text-stat and dump/reset coverage includes `system.cpu.commit.committedInstType_0::MemRead`, `system.cpu.commit.committedInstType_0::IntMult`, `system.cpu.commit.committedInstType_0::FloatMisc`, path-safe `sim.cpu0.o3.iew.writeback_rate_ppm`/`sim.cpu0.o3.iew.producer_consumer_fanout_ppm`, final JSON aliases `system.cpu.iew.wbRate`/`system.cpu.iew.wbFanout`, raw `sim.cpu0.o3.iew.branch_mispredicts`/`sim.cpu0.o3.commit.branch_mispredicts`, `sim.host_actions.stats_dump.cpu0.o3.commit.committed_inst_type.mem_read`, `sim.host_actions.stats_dump.cpu0.o3.commit.committed_inst_type.int_mul`, `sim.host_actions.stats_dump.cpu0.o3.commit.committed_inst_type.float_misc`, `sim.host_actions.stats_dump.cpu0.o3.iew.branch_mispredicts`, `sim.host_actions.stats_dump.cpu0.o3.commit.branch_mispredicts`, `sim.host_actions.stats_dump.cpu0.o3.iew.writeback_rate_ppm`, `sim.host_actions.stats_dump.cpu0.o3.iew.producer_consumer_fanout_ppm`, and resettable dump aliases `system.cpu.iew.wbRate`/`system.cpu.iew.wbFanout` derived from path-safe raw committed-inst-type, branch-mispredict, reset-scoped pipeline-cycle, and IEW producer/consumer counters. Final O3 JSON count-alias coverage also includes dotted `system.cpu.iew.instsToCommit.total`, `system.cpu.iew.writebackCount.total`, `system.cpu.iew.producerInst.total`, and `system.cpu.iew.consumerInst.total` plus bucket aliases `system.cpu.iew.instsToCommit::total`, `system.cpu.iew.writebackCount::total`, `system.cpu.iew.producerInst::total`, and `system.cpu.iew.consumerInst::total`, while final JSON/text occupancy aliases include `system.cpu.rob.maxOccupancy` and `system.cpu.lsq0.maxOccupancy`; final JSON op-class aliases include dotted and `_0::` bucket forms for `system.cpu.iq.issuedInstType.MemRead`/`system.cpu.iq.issuedInstType.MemWrite`/`system.cpu.iq.issuedInstType.IntMult`/`system.cpu.iq.issuedInstType.IntDiv`/`system.cpu.iq.issuedInstType.FloatAdd`/`system.cpu.iq.issuedInstType.FloatCmp`/`system.cpu.iq.issuedInstType.FloatMisc`/`system.cpu.iq.issuedInstType.FloatMult`/`system.cpu.iq.issuedInstType.FloatMultAcc`/`system.cpu.iq.issuedInstType.FloatDiv`/`system.cpu.iq.issuedInstType.FloatSqrt`/`system.cpu.iq.issuedInstType.SimdMult`/`system.cpu.iq.issuedInstType.SimdDiv`/`system.cpu.iq.issuedInstType.SimdFloatAdd`/`system.cpu.iq.issuedInstType.SimdFloatCmp`/`system.cpu.iq.issuedInstType.SimdFloatMisc`/`system.cpu.iq.issuedInstType.SimdFloatMult`/`system.cpu.iq.issuedInstType.SimdFloatMultAcc`/`system.cpu.iq.issuedInstType.SimdFloatDiv`/`system.cpu.iq.issuedInstType.SimdFloatSqrt`, `system.cpu.commit.committedInstType.MemRead`/`system.cpu.commit.committedInstType.MemWrite`/`system.cpu.commit.committedInstType.IntMult`/`system.cpu.commit.committedInstType.IntDiv`/`system.cpu.commit.committedInstType.FloatAdd`/`system.cpu.commit.committedInstType.FloatCmp`/`system.cpu.commit.committedInstType.FloatMisc`/`system.cpu.commit.committedInstType.FloatMult`/`system.cpu.commit.committedInstType.FloatMultAcc`/`system.cpu.commit.committedInstType.FloatDiv`/`system.cpu.commit.committedInstType.FloatSqrt`/`system.cpu.commit.committedInstType.SimdMult`/`system.cpu.commit.committedInstType.SimdDiv`/`system.cpu.commit.committedInstType.SimdFloatAdd`/`system.cpu.commit.committedInstType.SimdFloatCmp`/`system.cpu.commit.committedInstType.SimdFloatMisc`/`system.cpu.commit.committedInstType.SimdFloatMult`/`system.cpu.commit.committedInstType.SimdFloatMultAcc`/`system.cpu.commit.committedInstType.SimdFloatDiv`/`system.cpu.commit.committedInstType.SimdFloatSqrt`, and multicore dotted plus `_0::` bucket forms for `system.cpu1.iq.issuedInstType.MemRead`/`system.cpu1.iq.issuedInstType.MemWrite`/`system.cpu1.iq.issuedInstType.IntMult`/`system.cpu1.iq.issuedInstType.IntDiv` plus `system.cpu1.commit.committedInstType.MemRead`/`system.cpu1.commit.committedInstType.MemWrite`/`system.cpu1.commit.committedInstType.IntMult`/`system.cpu1.commit.committedInstType.IntDiv`, with multicore coverage suppressing inactive `system.cpu0.*`, multicore vector-integer/float/vector-float `system.cpu1.*`, and ambiguous `system.cpu.*` paths.
**Next evidence:** Broader gem5 text-stat compatibility, complete cache/bank/fabric/DRAM hierarchy counters and remaining CSR register-cache families, physical fabrication/vendor coefficient calibration and external-tool correlation, and broader cycle-visible ROB/LSQ-backed O3 execution beyond the current targeted bounded four-row scalar FU/ALU live ROB/rename staging window, including broader FU/opcode, LSQ, branch, scheduled restore/replay, and checkpoint-state evidence.

### Configuration, Resources, Suites, GPU, and Accelerators - 74% representative

**Score calculation:** 17 of 21 items have executable evidence, or 81% raw,
capped to 74% by the representative bucket. The bucket cap is representative
because top-level GPU evidence now spans
queued multi-CU scheduling, cross-line coalescing, and direct/cache/DRAM/fabric rows with negative suppression, while manifest and suite
acquisition have representative local/host/archive/HTTP paths but incomplete external breadth, run and trace-replay
resource handoffs remain selected rather than broad, and benchmark orchestration has only bounded top-level
mixed-command multi-run slices including GPU and accelerator runs.

- [x] CLI `run`, `gups`, `trace-replay`, `gpu-run`, and `accelerator-run` plus TOML configuration have tests; repository `gups`, `gpu-run`, `accelerator-run`, and multi-run example configs run through the top-level CLI without recompilation, TOML-driven `run` output layouts create nested artifact directories for run JSON, stats JSON, and power analysis output, `gups` emits traffic profile summaries from the executed controller, TOML-driven `gpu-run` executes recorded GPU global memory traffic through cache/DRAM and writes activity-derived power-analysis and NoMali-compatible adapter output, and TOML-driven `accelerator-run` emits child JSON/stats artifacts.
- [x] Workload manifests, resource identity, disk-image construction records, and suite planning exist.
- [x] CLI workload-resource acquisition consumes a resource executor for manifest required artifacts.
- [x] CLI workload-resource acquisition consumes a resource executor for suite required artifacts.
- [x] CLI `run` consumes a manifest-acquired kernel resource at runtime.
- [x] CLI `run` consumes unique or selected suite-acquired kernel resources at runtime.
- [x] CLI `run` consumes acquired input/initrd resources as guest readfile/load-blob payloads and RISC-V SE stdin/guest-file inputs, including `suite-resource:<workload>/<resource>` selectors for same-name suite resources, RISC-V SE input-source artifact summaries, and runtime rejection of non-initrd load-blob resources.
- [x] CLI `trace-replay` consumes manifest and suite-acquired trace resources at runtime, including TOML/CLI `suite-resource:<workload>/<resource>` selectors for same-name suite traces and runtime rejection of non-input trace resources.
- [x] GPU and accelerator command routing, DMA routes, topology validation, replay evidence, and top-level `accelerator-run` NPU/GPU command execution with stats exist.
- [x] Dispatch plans and execution summaries expose typed parallel evidence.
- [ ] gem5-style ergonomic experiment definitions cover broad full-system sweeps.
- [ ] External workload-resource acquisition executors cover host, network, archive, and broader artifact kinds.
- [x] GPU ISA-level execution exists.
- [x] GPU queued workgroups expose compute-unit assignment and coalesced memory access records from scalar ISA memory intents.
- [x] CLI `gpu-run` routes recorded coalesced scalar GPU global memory requests through direct memory, explicit fabric, or MSI data-cache and DRAM-backed runtime stats after GPU workgroup completion, with bank-level cache counters, tagged next-line data-cache prefetch counters, fabric aggregate/per-VN/per-link/per-hop stats, shared-link queue contention, and link/lane/hop activity exposed in the top-level artifact.
- [x] CLI `gpu-run` reports per-compute-unit workgroup completion, queue-wait, busy-cycle, activity-window, and coalesced global-memory read/write stats from real scheduled completions and recorded memory accesses; GPU library tests cover queued-workgroup restore for the same queue-wait summary boundary.
- [x] CLI `multi-run` sequentially orchestrates multiple real `run --config`, `gups --config`, `gpu-run --config`, `accelerator-run --config`, and `trace-replay --config` entries from TOML and command-qualified `--run` entries through the top-level binary, preserves and reports child-configured output and extra artifacts, rolls up run-child checkpoint/restore action counts, restored component/chunk/payload metadata, and accelerator command/completion counts into suite JSON/stats output, and emits aggregate JSON/stats artifacts plus explicit failure summaries when configured to continue after child errors.
- [x] CLI `trace-replay` accepts planned direct-memory and data-cache line-synchronized, same-tick trace-overlap-guarded host checkpoint and restore events from TOML or flags, executes them through `RiscvWorkloadReplay` with a nonempty memory checkpoint payload captured and restored, exposes trace-replay checkpoint and restore counts plus checkpoint payload metadata in JSON and host-action counts in stats, and lets `multi-run` roll up trace-replay child checkpoint and restore counts plus restored payload metadata.
- [x] GPU CU scheduling, memory coalescing, and cache/DRAM interactions are representative across queued two-CU direct, MSI+DRAM, and explicit-fabric plus MSI/DRAM rows with cross-line load coalescing, per-CU transport attribution, inactive-CU suppression, and bounded max-tick failure evidence.
- [ ] Multi-run simulator orchestration and artifact compatibility are complete.
- [ ] PARSEC or comparable workload suites run end to end.

**Migrated:** Typed configuration, manifests, suite dispatch, resource identity
and suite-level required-resource declarations, top-level manifest resource
acquisition through local artifacts and the in-memory executor, runtime-consumable
resolved resource construction for acquired manifest payloads, suite replay-plan
resource acquisition through the same top-level local-artifact executor path,
top-level `preloaded` acquisition locators for already materialized local artifacts,
top-level host-file acquisition through config-relative host paths and the same
executor validation flow, top-level uncompressed tar archive entry acquisition
including USTAR prefix member paths and gzip-compressed tar archive entry acquisition, standalone gzip artifact
acquisition, plus stored and deflated ZIP archive entry acquisition through the same executor validation flow,
deterministic generated zero-fill artifact acquisition through the same executor
validation flow with tagged locators for multiple generated resources,
top-level HTTP `remote-uri` acquisition requiring an
`artifact_digest` content SHA-256 and validating response bodies for basic and
chunked transfer responses plus absolute-URL, absolute-path, and relative HTTP redirects through the same
executor validation flow for explicit pre-simulation `resource-acquire`,
runtime `run` and `trace-replay` resource handoffs rejecting `remote-uri`
resources before artifact reads to keep simulation entry points network-free,
top-level `rem6 run` handoff of a manifest-acquired
kernel resource into the normal ELF load and execution path, top-level
`rem6 run` handoff of unique and `suite-resource:<workload>/<resource>`
selected suite-acquired kernel resources into the normal ELF load and execution
path, top-level `rem6 run` handoff of unique and
`suite-resource:<workload>/<resource>` suite-acquired input/initrd resources
into the guest readfile and load-blob paths, manifest-acquired and suite-selected
RISC-V SE stdin bytes, and suite-selected RISC-V SE guest-file bytes consumed by guest syscalls,
including generated zero-fill initrd payloads validated by memory dump, top-level
`trace-replay` handoff of an acquired
trace manifest resource into `RiscvWorkloadReplay`, top-level `trace-replay`
handoff of unique and TOML/CLI `suite-resource:<workload>/<resource>`
suite-acquired trace resources into `RiscvWorkloadReplay` with the selected
trace resource exposed in the JSON artifact, GPU/accelerator
shells, DMA routing, top-level `accelerator-run` NPU/GPU command dispatch and completion stats with TOML artifact output, and a
minimal GPU scalar ISA program execution path with completion, queued-workgroup
snapshot evidence, visible compute-unit assignment, coalesced memory access
records, plus a top-level `gpu-run` three-row representative matrix routing the
same queued three-workgroup/two-CU scalar program through direct memory, MSI data-cache plus DRAM, and
explicit fabric plus MSI/DRAM after GPU workgroup completion, with one cross-line load and one coalesced store per workgroup,
deterministic 2:1 CU assignment and queue wait, per-CU response counts, round-trip totals/maxima, and response windows derived from `MemoryTrace`,
exact reconciliation to aggregate transport counters, cache/DRAM activity, request/response virtual networks, shared-link queue delay, and link/lane/hop activity,
inactive-CU response-window stat suppression and bounded hierarchy max-tick rejection, TOML-driven `gpu-run` cache/DRAM/fabric execution and config-value validation,
top-level
`gpu-run` per-CU completion, busy-cycle, activity-window, coalesced global-memory read/write, and memory-transport stats derived from scheduled workgroup completions,
recorded memory accesses, and final source response events without claiming memory-gated wave-slot completion, checked-in GUPS, GPU, accelerator, and multi-run
example TOML files that run through the top-level CLI without recompilation,
top-level `rem6 run --config` creation of nested run, stats, and power-analysis artifact directories from TOML-relative paths, top-level `rem6 trace-replay` planned direct-memory and data-cache line-synchronized, same-tick trace-overlap-guarded host checkpoint and restore execution through `RiscvWorkloadReplay` with nonempty memory checkpoint payload capture/restore, JSON checkpoint counts and payload metadata, and stats checkpoint counts, top-level `rem6 multi-run` sequential orchestration of real `run --config`, `gups --config`, `gpu-run --config`, `accelerator-run --config`, and `trace-replay --config` TOML and command-qualified `--run` entries with child-configured output and power/GPU extra-artifact path summaries, run-child checkpoint/restore payload roll-up, accelerator-child, and trace-replay-child count roll-up, aggregate JSON/stats artifacts, and explicit child-failure summaries, and top-level GUPS traffic profile JSON/stats output.

**Not migrated:** Full gem5 stdlib ergonomics, host/network/archive resource
acquisition beyond the host-file, tar-entry, gzip-tar-entry, stored/deflated
ZIP-entry, standalone gzip payload, preloaded-local, generated zero-fill, basic
HTTP, chunked HTTP, and broader HTTP redirect slices, HTTPS, cache/policy controls, broader archive and artifact kinds, broad
runtime handoff of acquired suite resources beyond the
unique run-kernel, `suite-resource:<workload>/<resource>` readfile/load-blob,
RISC-V SE stdin/guest-file, and selected trace-resource replay slices, broad GPU
ISA semantics, memory-response-gated wave-slot/workgroup completion, cache/DRAM backpressure into CU scheduling, broader GPU topology/protocol matrices, complete multi-run artifact compatibility, full cache-controller checkpoint payload/state preservation beyond trace-replay cache-line synchronization and same-tick trace-overlap guards, broader checkpoint restore orchestration beyond the current trace-replay host-event slices, and broad benchmark orchestration.

**Evidence:** `Rem6RunConfig`, `run_config`, `WorkloadManifest`,
`WorkloadResource`, `WorkloadSuiteReplayPlan`,
`WorkloadInMemoryResourceAcquisitionExecutor`, `WorkloadResolvedResources`,
`rem6 resource-acquire` CLI tests, `rem6 run` manifest and suite
resource-config kernel handoff tests including preloaded-local acquisition locator handoff, `rem6 run` suite resource-config
readfile, load-blob, selected RISC-V SE stdin source-summary, and RISC-V SE guest-file handoff tests
including `suite-resource:<workload>/<resource>` same-name suite resource
selection and generated zero-fill load-blob memory dump coverage,
`rem6 resource-acquire` remote-uri
content-digest, content-address requirement, chunked-transfer, absolute/relative HTTP redirect, USTAR-prefix tar
archive-entry, ZIP archive-entry, standalone gzip artifact, and generated zero-fill artifact tests, `rem6
run` remote-uri runtime rejection tests, `rem6 trace-replay` manifest and suite
resource-config handoff tests including TOML and CLI
`suite-resource:<workload>/<resource>` same-name trace selection, `rem6
trace-replay` remote-uri runtime rejection
tests, suite tests, resource acquisition executor tests, repository GUPS, GPU,
accelerator, and multi-run example-config CLI tests, `rem6 run --config` nested multi-artifact output
layout CLI test, `rem6 trace-replay` planned direct-memory and data-cache line-synchronized, same-tick trace-overlap-guarded host checkpoint and restore TOML plus flag CLI tests with nonempty memory checkpoint payload metadata, `rem6 multi-run` mixed TOML and command-qualified run/GUPS/GPU/accelerator/trace-replay aggregate artifact/stat, run-child checkpoint/restore payload roll-up, accelerator-child command/completion, and trace-replay-child checkpoint action count roll-up, child-output and power/GPU extra-artifact path, and continue-on-failure CLI tests, `rem6 gups` profile-summary CLI tests, `rem6 gpu-run --config`
cache/DRAM execution and config validation tests, GPU and
accelerator topology tests, GPU compute tests covering scalar ISA execution,
coalesced memory records, and snapshot restore of queued ISA programs, `rem6 accelerator-run` CLI/TOML smoke coverage plus multi-run child coverage for scheduled NPU inference and GPU kernel commands with command, completion, trace-event, scheduler, child-output, and child-stats evidence, and
`rem6 gpu-run` CLI smoke coverage with TOML and flag-driven direct-memory store/dump evidence, MSI cache-run, bank-level cache counters,
DRAM and explicit-fabric link/lane/hop activity, plus `gpu::representative_matrix::rem6_gpu_run_correlates_queued_cu_coalescing_across_memory_hierarchy_rows`
for exact direct, MSI+DRAM, and fabric+MSI/DRAM scheduling/coalescing/cache/DRAM/fabric/transport rows, per-CU final-response attribution and aggregate reconciliation,
`gpu::representative_matrix::rem6_gpu_run_omits_activity_window_stats_for_inactive_compute_units` for zero/null JSON plus response-window stat suppression,
`gpu::representative_matrix::rem6_gpu_run_representative_hierarchy_rejects_response_after_max_tick` for bounded hierarchy completion failure, and per-CU completion,
queue-wait, busy-cycle, activity-window, coalesced read/write, and memory-transport JSON/stat evidence from the top-level scheduled GPU run.

**Next evidence:** Broader suite-level workload replay beyond selected run-kernel,
`suite-resource:<workload>/<resource>` readfile/load-blob, RISC-V SE guest-file,
and selected trace-resource handoffs, network-backed workload acquisition,
broader archive and artifact kinds, data-driven full-system workload declarations,
GPU memory-response-gated wave-slot/workgroup completion with cache/DRAM backpressure
into CU scheduling, and broader GPU ISA/topology/protocol matrices.

## Test Migration Ledger

This table is a crosswalk from gem5 test anchors to rem6 owners. Its estimates
are compact row-level status markers, not component scores. `Row score` entries
use the same percentage and bucket vocabulary as component scores, but the
checklist-backed component sections above define the auditable percentages.

| gem5 test anchor | rem6 owner | Row score | Migrated boundary | Next evidence |
| --- | --- | --- | --- | --- |
| `tests/gem5/arm_boot_tests` | future ARM ISA crate, `rem6-platform` | 0% open | ARM device slices exist, but this row requires Arm ISA boot. | Add Arm ISA, board handoff, device tree, and kernel boot tests. |
| `tests/gem5/asmtest` | ISA crates, `rem6` CLI | 50% single-axis | RISC-V no-libc, ISA unit tests, and CPU fetch-stream tests cover selected instruction, ecall, scalar FP directed integer-to-float, `fadd.d`/`fsub.d` finite directed-rounding plus directed overflow flags, `fmul.s` exact-product and `fmul.d` normal finite exact-product plus directed-overflow and directed-underflow subnormal rounding, and `fdiv.s`/`fdiv.d` finite-quotient directed-rounding paths, RVV vector configuration, unmasked integer `vadd.vv` LMUL=1 plus m2, `vadd.vx`, `vadd.vi`, masked integer `vadd.vv`/`vadd.vx`/`vadd.vi`, `vsub.vv` LMUL=1 plus m2, `vsub.vx`, `vrsub.vx`/`vrsub.vi`, bitwise `vand`/`vor`/`vxor` vv/vx/vi, shift `vsll`/`vsrl`/`vsra` vv/vx/vi, min/max `vminu`/`vmin`/`vmaxu`/`vmax` vv/vx, multiply `vmul`/`vmulhu`/`vmulhsu`/`vmulh` vv/vx, divide/remainder `vdivu`/`vdiv`/`vremu`/`vrem` vv/vx, integer carry/borrow `vadc`/`vmadc`/`vsbc`/`vmsbc` vv/vx plus add-immediate forms with reserved-encoding, representative ISA, and CPU fetch-stream/fetch-ahead slices, single-width integer multiply-add `vmadd`/`vnmsub`/`vmacc`/`vnmsac` vv/vx decode plus representative masked/unmasked ISA and unmasked CPU fetch-stream slices, integer reductions `vredsum`/`vredand`/`vredor`/`vredxor`/`vredminu`/`vredmin`/`vredmaxu`/`vredmax` plus widening reductions `vwredsumu`/`vwredsum` and base widening add/subtract `vwaddu`/`vwadd`/`vwsubu`/`vwsub` vv/vx/wv/wx decode plus representative masked/unmasked ISA and unmasked CPU fetch-stream slices, widening multiply `vwmulu`/`vwmulsu`/`vwmul` vv/vx and widening multiply-add `vwmaccu`/`vwmacc`/`vwmaccsu` plus vx-only `vwmaccus` decode plus representative masked/unmasked ISA and unmasked CPU fetch-stream slices, equality mask compare `vmseq`/`vmsne` vv/vx/vi, ordered mask compare `vmsltu`/`vmslt`/`vmsleu`/`vmsgtu`/`vmsgt` supported vv/vx/vi, unmasked slide `vslideup`/`vslidedown`/`vslide1up`/`vslide1down`, gather `vrgather`, mask reductions `vcpop.m`/`vfirst.m`, mask prefixes `vmsbf.m`/`vmsof.m`/`vmsif.m`, mask indexes `viota.m`/`vid.v`, merge/move/compress `vmerge`/`vmv.v`/`vmv.x.s`/`vmv.s.x`/`vmv<nr>r.v`/`vcompress.vm`, zero/sign extension `vzext`/`vsext` masked/unmasked decode plus `vf2` hart execution and unmasked slide/gather, mask reductions, mask prefixes, mask indexes, plus `vzext.vf2` CPU fetch-stream execution, unmasked fixed-point saturating `vsaddu`/`vsadd`/`vssubu`/`vssub` vv/vx plus `vsaddu`/`vsadd` vi forms, unmasked fixed-point averaging `vaaddu`/`vaadd`/`vasubu`/`vasub` vv/vx forms, unmasked fixed-point signed fractional multiply `vsmul` vv/vx forms, unmasked fixed-point scaling shifts `vssrl`/`vssra` vv/vx/vi, fixed-point narrowing shifts `vnsrl.wv`/`vnsra.wv`/`vnsrl.wx`/`vnsra.wx`/`vnsrl.wi`/`vnsra.wi` and narrow clip `vnclipu.wv`/`vnclip.wv`/`vnclipu.wx`/`vnclip.wx`/`vnclipu.wi`/`vnclip.wi`, `vxrm`/`vxsat`/`vcsr`, mask logical `.mm`, and floating-point `vfadd`, `vfsub`, `vfmin`, `vfmax`, `vfmul`, and `vfdiv` vv/vf forms plus `vfrsub.vf`, `vfrdiv.vf`, `vfmacc`, `vfnmacc`, `vfmsac`, and `vfnmsac` vv/vf exact finite SEW=32 lane slices, E64 `vfadd.vv/vf`, `vfsub.vv/vf`, `vfrsub.vf`, `vfmul.vv/vf`, `vfdiv.vv/vf`, and `vfrdiv.vf` exact finite CPU fetch-stream slices, E64 `vfmin.vv/vf` and `vfmax.vv/vf` signed-zero and NaN CPU fetch-stream slices, `vfclass.v` SEW=32 classification slices, `vfmv.v.f` SEW=32 scalar splat slices, `vfmerge.vfm` SEW=32 masked scalar merge slice, `vfmv.s.f` SEW=32 scalar-to-lane-zero slice, `vfmv.f.s` SEW=32 lane-zero-to-scalar slice, integer-to-float `vfcvt.f.xu.v` and `vfcvt.f.x.v` plus float-to-integer `vfcvt.xu.f.v`, `vfcvt.x.f.v`, `vfcvt.rtz.xu.f.v`, and `vfcvt.rtz.x.f.v` dynamic/directed SEW=32 slices with NX accrual, E64 `vfcvt.f.xu.v`, `vfcvt.f.x.v`, `vfcvt.xu.f.v`, `vfcvt.x.f.v`, `vfcvt.rtz.xu.f.v`, and `vfcvt.rtz.x.f.v` CPU fetch-stream slices, FP mask compare `vmfeq`, `vmfne`, `vmflt`, and `vmfle` vv/vf SEW=32 mask slices, `vfmin`, `vfsqrt.v`, and FP compare invalid-flag accrual, `vfsqrt.v` non-exact finite rejection, and `vfsgnj`, `vfsgnjn`, and `vfsgnjx` vv/vf sign-bit slices with reserved-`frm` and NaN-boxing trap coverage. | Split RV32/RV64 and extension families with architectural-state comparison. |
| `tests/gem5/checkpoint_tests` | `rem6-checkpoint`, subsystem checkpoint banks | 65% representative | Scheduler, memory, devices, storage, VirtIO, timer, interrupt, RISC-V started/stopped/suspended hart run-state, RISC-V in-order pipeline state, RISC-V fetch-steering branch predictor state including live fetch-ahead pending speculation, platform, workload, manifest, and O3 pending-state plus ROB/LSQ/rename runtime checkpoints, checkpoint-v23 bounded writeback-port counter persistence with historical v1-v22 deferred-state normalization, live-checkpoint rejection, and drained restore, plus scheduled run restore/replay evidence for detailed-mode O3 runtime state with restored component/chunk/payload stats and O3 debug restore-scope trace metadata including same-checkpoint trace-count, distinct-label restore-label, and two-hart restore-record evidence exist. | Add broader non-quiescent restore matrices and running O3 execution evidence. |
| `tests/gem5/chi_protocol` | `rem6-coherence`, protocol crates, `rem6-cache` | 40% single-axis | CHI-like line, controller, bank, dirty peer sourcing, reservation, and Evict-hazard tests exist. | Add Ruby-scale CHI transactions, topology networks, directory races, and workload checks. |
| `tests/gem5/chi_tlm_tests` | `rem6-proto`, future adapter crates, `rem6-coherence` | 19% scoped | A library-level co-simulation boundary can register TLM endpoints, validate transaction shape, hand off events, and checkpoint clean adapter state in self-tests. | Add runtime TLM bridge tests with coherence traffic. |
| `tests/gem5/config_output_files` | `rem6` CLI, `rem6-workload` | 45% single-axis | CLI output paths, stats-output paths, JSON artifacts including RISC-V SBI input-source provenance, text stats output, TOML-driven nested run/stats/power artifact directory creation, TOML-driven GPU run config tests, and TOML-driven GPU power-output plus NoMali adapter artifact creation exist. | Add config-driven file layouts for full-system manifests and broader multi-artifact workloads. |
| `tests/gem5/cpu_tests` | `rem6`, `rem6-cpu`, `rem6-system` | 40% single-axis | Atomic RISC-V execution, scheduler-owned normal serial/parallel/translated Fetch1/Fetch2/Decode/Execute/Commit ticks with direct one-/two-core and cache/fabric/DRAM pre-commit suppression evidence, frontend slices, retired predictor training, direct completed-fetch overlap in in-order timing, bounded normal-driver straight-line and conditional-branch fetch-ahead including compressed straight-line fetch-ahead through the normal parallel-cluster path, explicit `--riscv-branch-lookahead 2` two-deep conditional-branch fetch-ahead rollback through `rem6 run`, CLI-selected `gshare`, `bimode`, `tage-sc-l`, and `multiperspective-perceptron` branch-predictor fetch steering changing deterministic timing or branch-steering counters, direct RISC-V `JAL` and `JALR` target fetch-ahead preserved through retire in driver tests plus direct `JAL` CLI run coverage, pending GShare, BiMode, Tournament, TAGE-SC-L, and multiperspective-perceptron fetch-ahead history for older conditional-branch and direct-jump speculation, selected GShare/BiMode/Tournament mutable speculative-history rollback counters through top-level nested-branch runs, top-level retired Tournament predictor local/global selection stats and live predictor-family counter stats, pending-fetch retire overlap for older completed straight-line fetches, issued fetch-ahead occupancy in in-order timing before response completion, normal parallel-cluster pending-fetch resource-stall stats including top-level per-stage blocked instruction and cycle counters, retained non-retire in-order cycle history consumed by top-level stats, branch speculation history repair/commit with younger-speculation removal stats, completed younger fetch squash, per-retired-instruction in-order stage timing stats, top-level configured stage width changing executed max in-flight occupancy, top-level per-stage width, in-flight, occupied-cycle, blocked-cycle, and branch-redirect flush stats, top-level fetch/data wait stats, top-level in-order cycle-plan advance/block/flush stats, top-level Pipeline debug aggregate/stage in-flight and movement count-cycle/advance-retire-flush/resource-blocked-cycle/stall-cause stage blocked plus data-wait stage blocked-cycle and aggregate/branch-prediction/trap-redirect-cause stage-cycle stats, branch/trap-specific stage flushed-cycle, flush-cause/stage flushed, trap redirect-cause stats with wrong-path register suppression, normal CPU branch-prediction/trap redirect split stats with text aliases, execute-wait resource-blocked CLI matrices plus parallel-driver ordering-blocked matrices, detailed interrupt redirect flush-with-youngers Pipeline+Fetch evidence and interruptRedirect text aliases, and CPU-local trap-redirect flush counters, a detailed-mode scalar DIV live-retire gate with direct CLI pre-commit memory/final-tick evidence, single-wake stats, valid younger fetch-ahead retention, execution-owned dependent-negative two-row ROB/rename residency with unready `live_staged` rows and no dependent pre-writeback issue claim, plus a bounded four-row `x3`/`x4`/`x5`/`x6` live window with independent `x4` issue, forwarded `x5` wakeup, and transitive fan-in `x6` wakeup before DIV writeback, core checkpoint-v21 absolute-ready-tick plus staged-rename restore/refetch rebinding, mode-change and stats-reset survival, redirect rollback to the committed mapping, v20 staged-field defaults, and v19 no-gate compatibility, configurable `--riscv-o3-scalar-memory-depth 4` evidence for four independent untranslated scalar loads issuing before the first response with exact four-row ROB/LSQ residency, direct and cache-fabric-DRAM activity, oldest-first retirement, timing-mode suppression, dependency/PMA rejection, failure suffix cancellation, and HTM cleanup, one leading cacheable store with up to three independent younger loads, direct four-row overlap, three-row cache-backed/resident evidence, forwarding across an intervening load, and suffix cancellation without widening branch speculation, plus configurable untranslated standalone-load/scalar-ALU live windows at total depths four/six/eight, one-LSQ/eight-ROB deep residency, issue widths 1/2/4, scheduler-owned typed dependency scopes, transitive/fan-in pre-response wakeup, a blocked load-dependent terminal row, direct/cache-backed Data/Memory evidence, stats/checkpoint/mode-transfer/cleanup coverage, and timing suppression, bounded mixed direct-conditional control-window evidence with scalar load plus two younger ALUs plus terminal no-rename branch, exact 4-row ROB / 1-row LSQ residency, deterministic predicted-not-taken then resolved-taken squash, wrong-path-store suppression, direct/cache-fabric-DRAM and timing suppression, schema-v7 non-restorable handoff, and live-reject/drained checkpoint evidence, bounded linked direct-call/indirect-call/return control-window evidence with one delayed scalar load, exact four-ROB/one-LSQ positives, staged link rename/writeback ownership, RAS push/pop/use/correct/squash proof, older-repair, unresolved-target suppression, and exact-adjacent live scalar-target forwarding, timing-preserving non-restorable transfer, live-reject/drained checkpoint evidence, and timing suppression, bounded scoped O3 issue evidence for `--riscv-o3-issue-width`/`riscv_o3_issue_width` widths 1, 2, and 4, load-head reservation, cross-resource ALU/MUL co-issue, same-MUL serialization, producer-ready dependency blocking, direct/cache-fabric-DRAM rows, JSON `/cores/0/o3_runtime/issue`, final/text counters `sim.cpu0.o3.issue_cycles`, `sim.cpu0.o3.issued_rows`, `sim.cpu0.o3.resource_blocked_row_cycles`, `sim.cpu0.o3.dependency_blocked_row_cycles`, `sim.cpu0.o3.max_rows_per_cycle`, `m5_dump_stats` `sim.host_actions.stats_dump.cpu0.o3.issue_cycles`, checkpoint-v22 plus historical v21 stat compatibility, mode-switch issue/writeback/commit continuity and transfer stats, live-checkpoint rejection, drained cleanup/restore, and timing suppression, bounded shared scalar FU/load writeback-port ownership for configured widths 1, 2, and 4 with deterministic older-sequence precedence, admitted-tick dependency wakeup, rollback/retry cleanup, direct and cache-fabric-DRAM collisions, explicit trace ticks, checkpoint-v23 plus historical v1-v22 compatibility, live-checkpoint rejection, drained restore, mode-transfer issue/writeback/commit continuity, timing suppression, and JSON/text/m5_dump_stats counters, top-level scalar integer M-extension, vector integer multiply and multiply-accumulate including fixed-point signed fractional multiply and widening variants, divide, remainder, fixed-point scaling shifts, narrowing shifts, narrow clip, and integer/widening reductions, scalar FP arithmetic, compare, conversion, and misc, decoded vector FP add/sub/min/max, mask compare, conversion/move/merge, sign/class, multiply/FMA/divide/sqrt execute-stage latency stats, top-level unmasked/masked e32/m1 plus unmasked e16/m2 full-register-group RVV segment load/store byte movement and inactive-field preservation plus contiguous active-lane and non-contiguous unit-stride masked load/store active-span fault suppression, all-inactive unit-stride masked load/store no-extra-access preservation through direct/cache-backed run paths, top-level branch redirect/misprediction/branch-prediction-flush stats, top-level `m5_switch_cpu` transfer stats exposing cpu0 in-order/O3 chunks, O3 debug event ROB/LSQ/rename occupancy, system-event provenance, prediction-direction provenance, predicted-target-kind provenance, targetless-squash provenance, wrong-target-squash/link-state/squashed-target provenance, resolved-target provenance, and squash-target provenance, scalar/vector-integer including multiply-add/widening/saturating-multiply plus scalar/vector float add/compare/misc/mul/FMA/div/sqrt FU-latency event count/cycle/min/max/avg stats, LSQ forwarding address/suppression stats, scalar/vector/float/atomic LSQ operation-shape and ordering stats plus failed store-conditional outcome stats, direct/cache-backed LSQ operation/order count aliases plus LSQ data-response aggregate/op-class total/min/max/avg runtime latency stats plus `system.cpu.lsq0.dataResponse.*` aliases, delayed-reset cross-epoch direct LR and cache/fabric/DRAM AMO response-latency samples with pre-reset operation-count and raw-event suppression, and direct-conditional taken/not-taken/predicted-target-match/predicted-taken-not-taken-mismatch/direct-unconditional/direct-call/return/indirect-unconditional/indirect-call branch-event/taken/target/link/squash/misprediction plus indirect-unconditional and linked indirect-call wrong-target O3 debug stats plus CPU1 direct-call `m5_dump_stats` FTQ active-hart aliases plus CPU1 scalar/vector float-misc op-class aliases, `m5_reset_stats`-scoped detailed O3 ROB/LSQ JSON counters plus O3 debug reset epoch/tick trace scope metadata, scheduled checkpoint-restore scoped O3 debug replay metadata with post-restore store/load memory evidence plus same-checkpoint trace-count, distinct-label restore-label, two-hart restore-record evidence, and CPU1 restored branch-event/FTQ `m5_dump_stats` snapshots across call-indirect/direct-unconditional/link/squash lanes plus post-restore direct-call replay, CPU1 reset-scoped branchWrongTarget `m5_dump_stats` aliases with generic/CPU0 suppression, sequence-aware Pipeline-debug live fetch-/data-wait ordering backlog to branch-flush correlation plus completed execute-wait/later-trap non-correlation with lookahead-one/timing zero suppression, plus SBI timer-backed CPU0 fetch-/data-/execute-wait, two-worker CPU1 data-/execute-wait, and SBI IPI/SSIP CPU1 fetch-wait backlog-to-interrupt termination coverage with explicit-younger and cancelled-current ordering/resource leaves, lookahead-one younger-flush suppression, timing-mode suppression where applicable, exact O3 cancellation, and data-transport evidence, and table-driven all-3x3 typed unit coverage, and O3 policies exist. Bounded same-window link-return evidence covers direct `JAL x1` and independent `JALR x5` calls, RAS-only live return targets, exact four-ROB/one-LSQ direct and hierarchy positives, lookahead-one and overwritten-link suppression, older repair squashing the call, return, and both speculative RAS operations, timing-preserving non-restorable transfer, live-reject/drained checkpoint, and timing suppression, without claiming general indirect-target forwarding. Bounded same-window coroutine evidence is locked by `rem6_run_o3_same_window_coroutine_commits_direct`, `rem6_run_o3_same_window_indirect_coroutine_commits_cache_fabric_dram`, `rem6_run_o3_same_window_coroutine_requires_branch_lookahead_two`, `rem6_run_o3_same_window_indirect_coroutine_requires_branch_lookahead_two`, `rem6_run_o3_same_window_overwritten_coroutine_source_stays_terminal`, `rem6_run_o3_same_window_indirect_overwritten_coroutine_source_stays_terminal`, `rem6_run_o3_older_branch_discards_same_window_coroutine_chain`, `rem6_run_o3_older_branch_discards_same_window_indirect_coroutine_chain`, `rem6_run_host_switch_transfers_o3_same_window_coroutine`, `rem6_run_o3_same_window_coroutine_checkpoint_boundary`, `rem6_run_timing_suppresses_o3_same_window_coroutine`, `rem6_run_o3_same_window_coroutine_wrong_target_repairs_descendants`, and `rem6_run_o3_same_window_indirect_coroutine_wrong_target_repairs_descendants`: direct and indirect positives feed distinct-link coroutine PopThenPush controls in both link directions, then one scalar descendant, with exact RAS lineage, staged rename/writeback ownership, and direct and hierarchy activity. Both `x1->x5` and `x5->x1` rows cover positive execution, lookahead/overwrite suppression, older/wrong-target repair, timing-preserving detailed-to-timing switch, live-reject/drained checkpoint evidence, and timing suppression. The exact adjacent call `Push` -> distinct-link coroutine `PopThenPush` -> ordinary return `Pop` round trip is additionally locked by `rem6_run_o3_same_window_coroutine_round_trip_commits_direct`, `rem6_run_o3_same_window_indirect_coroutine_round_trip_commits_cache_fabric_dram`, `rem6_run_o3_same_window_coroutine_round_trip_requires_branch_lookahead_three`, `rem6_run_o3_same_window_coroutine_round_trip_middle_repair_discards_return`, `rem6_run_o3_same_window_coroutine_round_trip_terminal_return_repairs_direction`, `rem6_run_host_switch_transfers_o3_same_window_coroutine_round_trip`, `rem6_run_o3_same_window_coroutine_round_trip_checkpoint_boundary`, and `rem6_run_timing_suppresses_o3_same_window_coroutine_round_trip`: the forward `x1->x5` positive runs on direct memory and the reverse `x5->x1` positive runs on `cache-fabric-dram` hierarchy memory; lookahead-two suppression, mode-switch, live/drained checkpoint, and timing-suppression evidence iterate both cases and directions; middle wrong-target repair and terminal direction-only repair are focused direct-only fixtures. The route and repair rows remain case-specific, with no broader forwarding claim. The adjacent second-linked replacement matrix is locked by `rem6_run_o3_second_linked_coroutine_consumes_replacement_direct`, `rem6_run_o3_second_linked_coroutine_consumes_replacement_cache_fabric_dram`, and `rem6_run_o3_second_linked_coroutine_requires_branch_lookahead_three`: direct `x1->x5->x1` and cache/fabric/DRAM `x5->x1->x5` rows prove two linked `PopThenPush` consumers, exact four-ROB/one-LSQ residency, pre-response RAS target opening, newest-link rename ownership, route/architectural witnesses, and lookahead-two suppression to three rows. The same-link matrix additionally covers committed and exact adjacent live scalar-producer targets across both link registers and direct/cache/fabric/DRAM routes, stale architectural target suppression, typed producer authority, target-fetch issue before delayed data response, one bounded target descendant, exact predicted-target retention, representative diagonal unresolved load-produced target suppression, push-only RAS behavior, older repair, and ISA read-before-write semantics through `rem6_run_o3_committed_same_link_calls_cover_link_and_route_matrix`, `rem6_run_o3_live_same_link_targets_forward_across_link_and_route_matrix`, `rem6_run_o3_unresolved_live_same_link_targets_stay_terminal`, `rem6_run_o3_older_branch_discards_same_link_call_and_descendant`, `live_same_link_control_exposes_exact_producer_forwarded_target`, `detailed_live_same_link_control_uses_runtime_forwarded_target`, and `hart_jalr_reads_same_register_source_before_writing_link`, while the separate `rem6_run_o3_producer_forwarded_jalr_targets_cover_link_route_matrix` matrix covers exact adjacent no-link and split-link scalar-produced targets across direct and cache/fabric/DRAM routes, with unresolved-load, depth-two, and timing-mode suppression boundaries, and `rem6_run_o3_nonadjacent_producer_forwarded_jalr_targets_cover_link_route_matrix` plus `rem6_run_o3_warmed_producer_forwarded_targets_issue_descendants_before_load_response` prove dependency-derived non-adjacent producer selection and a hierarchy-warmed depth-four target descendant that issues strictly before the older DRAM-backed load response; `rem6_run_o3_warmed_producer_forwarded_target_descendant_requires_depth_four`, `rem6_run_o3_warmed_target_does_not_bypass_unresolved_jalr_source`, `rem6_run_host_switch_transfers_nonadjacent_producer_forwarded_jalr_window`, `rem6_run_rejects_live_warmed_producer_forwarded_jalr_checkpoint`, and `rem6_run_timing_suppresses_producer_forwarded_lineage_windows` cover depth, unresolved-source, transfer, checkpoint, and timing boundaries without claiming a fifth row. The producer-forwarded direct-return matrix adds pairwise same-link `x1`/direct and `x5`/cache-fabric-DRAM plus split-link `x11->x5`/direct and `x11->x1`/cache-fabric-DRAM call-to-return execution, target-source/link-destination separation, producer/call rename and direct/hierarchy route evidence, RAS target opening, lookahead-one suppression, response-boundary or hierarchy response-plus-ten return issue, timing-mode suppression, focused successful post-head runtime retention, and fail-closed invalidation through `rem6_run_o3_producer_forwarded_return_descendants_cover_link_shape_route_matrix`, `rem6_run_o3_producer_forwarded_return_requires_branch_lookahead_two`, `rem6_run_timing_suppresses_o3_producer_forwarded_returns`, `producer_forwarded_linked_calls_append_target_returns`, `producer_forwarded_linked_call_rejects_nonordinary_target_controls`, `producer_forwarded_split_link_call_appends_return_after_data_head_retires`, `pending_data_gate_admits_producer_forwarded_call_target_return`, `pending_data_gate_admits_same_and_split_link_direct_returns`, `producer_forwarded_return_apply_fails_closed_after_descendant_invalidation`, `producer_forwarded_return_apply_fails_closed_after_ras_lineage_changes`, and `branch_lookahead_one_does_not_stage_producer_forwarded_return`. The producer-forwarded linked-call scalar-return continuation adds a pairwise same-/split-link, `x1`/`x5`, direct/cache-fabric-DRAM matrix through `rem6_run_o3_producer_forwarded_scalar_returns_cover_link_shape_route_matrix`, separates JALR target-source authority from link-destination/RAS dependencies, proves exact RAS push/pop/use/correct evidence, response-boundary or hierarchy response-plus-ten link-consuming scalar issue, three-row pre-response residency with one target fetch and no early return fetch, rejects a scalar that does not consume the link, preserves lookahead-one suppression, and keeps timing mode free of O3 surfaces without claiming a five-row live window or strict pre-response scalar-row residency. Task 6 positive child `tests/cli_run/m5_host_actions/o3/writeback_port/result_classes.rs` owns `rem6_run_o3_memory_result_writeback_matrix_direct`, `rem6_run_o3_memory_result_writeback_matrix_cache_fabric_dram`, `rem6_run_o3_memory_result_writeback_width_two_exact_fit`, and `rem6_run_o3_memory_result_writeback_readfile_mmio`; bounded suffix child `tests/cli_run/m5_host_actions/o3/writeback_port/result_classes/scalar_suffix.rs` owns `rem6_run_o3_memory_result_scalar_suffix_matrix_direct`, `rem6_run_o3_memory_result_scalar_suffix_matrix_cache_fabric_dram`, `rem6_run_o3_memory_result_scalar_suffix_width_two_exact_fit_direct`, `rem6_run_o3_memory_result_scalar_suffix_readfile_mmio`, and `rem6_run_timing_suppresses_o3_memory_result_scalar_suffix`; paired-result child `tests/cli_run/m5_host_actions/o3/writeback_port/result_classes/pairs.rs` owns `rem6_run_o3_memory_result_pair_matrix_direct`, `rem6_run_o3_memory_result_pair_matrix_cache_fabric_dram`, `rem6_run_o3_memory_result_pair_width_two_exact_fit_direct`, `rem6_run_o3_memory_result_pair_boundaries`, and `rem6_run_timing_suppresses_o3_memory_result_pairs`; negative child `tests/cli_run/m5_host_actions/o3/writeback_port/result_boundaries.rs` owns `rem6_run_o3_memory_result_writeback_rejects_resultless_and_unsupported_shapes`, `rem6_run_o3_memory_result_writeback_all_inactive_vector_issues_no_request`, `rem6_run_o3_memory_result_writeback_denied_amo_traps_before_transport`, `rem6_run_o3_memory_result_writeback_live_checkpoint_rejects`, `rem6_run_o3_memory_result_writeback_live_mode_switch_rejects`, and `rem6_run_timing_suppresses_o3_memory_result_writeback_surface`. Focused SC child `tests/cli_run/m5_host_actions/o3/writeback_port/store_conditional_result.rs` owns the six direct-width, hierarchy, local-failure, live-action, and timing-suppression anchors above. Younger-atomic parent `tests/cli_run/m5_host_actions/o3/writeback_port/younger_atomic_result.rs` owns the direct/hierarchy result-class matrix and timing suppression; its `younger_atomic_result/boundaries.rs` child owns ordering, overlap, dependency, zero-destination, and live-action boundaries. Dependent-result-address parent `tests/cli_run/m5_host_actions/o3/writeback_port/dependent_result_address.rs` owns `rem6_run_o3_dependent_result_address_matrix_direct`, `rem6_run_o3_dependent_result_address_matrix_cache_fabric_dram`, and `rem6_run_timing_suppresses_o3_dependent_result_address`; its `dependent_result_address/boundaries.rs` child owns `rem6_run_o3_dependent_result_address_boundaries_and_live_actions`. Those rows prove one untranslated scalar-load or disjoint unordered-atomic integer result feeding a scalar `LD`, addressless LSQ residency before the producer response, width-one memory priority, width-two memory/ALU co-issue, normal PMP/PMA/route/request binding, direct and cache/fabric/DRAM activity, and atomic-overlap, dynamic-route, retry/failure, checkpoint, mode, and timing boundaries. The CLI children cover observable route/collision/architectural/suppression/PMP/lifecycle assertions; focused `rem6-cpu` tests cover typed completion physical address/size/offset/bytes, AMO two-sequence LSQ span ownership, and failed-SC publication. | Add wider trap/interrupt correlation, additional block-kind/stage combinations and interrupt causes beyond supervisor timer/software, broader in-order stall/squash cause-stage matrices, and cycle-visible ROB/LSQ-backed O3 execution beyond the current targeted bounded four-row scalar FU/ALU, standalone-load/ALU, mixed load/ALU/direct-branch terminal control-window slices, bounded no-link and linked direct-call/indirect-call/return mixed-control windows, and bounded predicted-control windows covering one branch with scalar MUL/dependent ADD plus a two-branch nested chain with distinct outer/inner rollback, load-dependent inner suppression, complete split-fetch identity, schema-v7 timing-preserving transfer, checkpoint boundaries, hierarchy activity, and timing suppression plus a mixed BNE/BLT/BGEU three-branch chain with lookahead-three admission, lookahead-two suppression, position-specific rollback, dependent terminal control, schema-v7 timing-preserving transfer, checkpoint boundaries, hierarchy activity, and timing suppression; link-sourced coroutine target shapes outside exact same-link authority and producer-forwarded linked-call returns beyond the exact direct/scalar continuations, producer-forwarded controls beyond the exact scalar-plus-return continuation, deeper control or mixed scalar/control chains beyond the configured untranslated scalar-only lane, arbitrary broader mixed windows, general IQ/wakeup/select beyond bounded scoped issue authority, broader mixed-data result depth, translated or MMIO result pairs, dependent stores/atomics, FP/vector dependent addresses, multiple unresolved addresses, and younger side-effect chains beyond one independent disjoint unordered AMO in result row two, broad vector shapes, restorable transport ownership, and a general O3 engine remain open. |
| `tests/gem5/dram_lowp` | `rem6-dram`, `rem6-power` | 40% single-axis | DRAM/NVM profile counters, low-power constants, top-level LPDDR routed-request active-powerdown, precharge-powerdown, self-refresh, custom timing, terminal all-bank residency, and repeated-access multi-bank exit-latency stats are surfaced. | Add broader executable low-power state-transition matrices across profiles, banks, and power states. |
| `tests/gem5/example_configs`, `tests/gem5/learning_gem5` | `rem6` CLI, `rem6-platform`, `rem6-workload` | 40% single-axis | CLI and TOML tests cover several execution, trace-replay, GPU, accelerator, and multi-run micro-suite paths, including checked-in GUPS, GPU, accelerator, and multi-run example configs that run without recompilation. | Add broader example suites spanning run, trace replay, resources, and full-system handoff. |
| `tests/gem5/fdp_tests` | `rem6-cache` | 45% single-axis | Fetch-directed prefetcher state, errors, and cache-local queue/translation counters have cache tests. | Add FDP execution through cache-bank and CPU/frontend consumers. |
| `tests/gem5/fs` | `rem6-platform`, `rem6-system`, device crates | 15% scoped | Generic device and handoff slices exist, but the gem5 row is mainly full-system boot. | Add full-system Linux boot with SBI, console, storage, network, timer, and shutdown evidence. |
| `tests/gem5/gem5_resources` | `rem6-workload`, `rem6` CLI | 58% single-axis | Resource declarations, identity, provenance, disk-image construction records, library-level in-memory acquisition executor records, manifest/suite-level `rem6 resource-acquire` execution with local-artifact, host-file, uncompressed/gzip tar-entry including USTAR prefix paths, standalone gzip payload, stored/deflated ZIP-entry, generated zero-fill artifact, and content-checked basic, chunked, and redirected HTTP remote inputs, explicit pre-simulation HTTP `remote-uri` acquisition through `rem6 resource-acquire`, while `run` and `trace-replay` reject `remote-uri` resources before artifact reads so simulation/replay entry points remain network-free, plus manifest run-kernel, unique/selected-suite run-kernel, `suite-resource:<workload>/<resource>` suite readfile/load-blob payloads, manifest and selected-suite RISC-V SE stdin handoff, suite-selected RISC-V SE guest-file handoff, generated zero-fill load-blob memory dump coverage, and manifest plus unique/selected-suite trace-replay resource-config handoff through TOML or CLI selector exist. | Add broader network-backed, broader archive/artifact acquisition, and suite runtime handoff beyond the current selector-based slices. |
| `tests/gem5/gpu` | `rem6-gpu`, `rem6-accelerator`, `rem6-transport`, `rem6` CLI | 60% representative | GPU and accelerator topology, command, DMA route, scalar ISA, CU assignment, and coalesced-memory tests exist; a top-level `gpu-run` matrix executes one queued three-workgroup/two-CU cross-line-load plus store program through direct, MSI+DRAM, and explicit-fabric plus MSI/DRAM routes with exact per-CU queue/busy/read/write/response ownership, trace-derived round-trip totals/maxima/windows reconciled to aggregate transport, cache/DRAM/fabric/VN/queue-delay activity, inactive-CU timing-stat suppression, and max-tick failure. Tagged GPU prefetch counters, top-level scheduled NPU/GPU accelerator commands including TOML/multi-run children, and NoMali GPU/MMU/job-slot/cycle-counter/config/IRQ/power/fault/interrupt PIO slices also exist. | Add memory-response-gated wave-slot/workgroup completion, cache/DRAM backpressure into CU scheduling, broader GPU ISA/topology/protocol matrices, and broader register-level GPU device modeling. |
| `tests/gem5/insttest_se` | future SPARC owner, ISA crates | 10% scoped | Current RISC-V evidence belongs under `asmtest`; this gem5 anchor is SPARC SE focused. | Add SPARC or explicitly retire the row as out of scope. |
| `tests/gem5/kvm_fork_tests`, `tests/gem5/kvm_switch_tests` | future host adapters | 0% open | No KVM or host-assisted fast-forward adapter exists; the previous synthetic admission-only facade was retired because it did not execute, fork, or switch a host-backed CPU. | Add a real fast-forward adapter plus executable KVM-like switch/fork tests before claiming scoped coverage. |
| `tests/gem5/m5_util`, `tests/test-progs/m5-exit` | `rem6-isa-riscv`, `rem6-system`, `rem6-workload` | 50% single-axis | RISC-V m5 exit, fail, stats, checkpoint, hypercall, switch_cpu, and work markers reach typed host actions; direct `rem6 run` JSON records repeated work-marker payloads, stats reset/dump id and tick metadata, periodic stats dump/reset and checkpoint tick repeats, scheduled checkpoint restore count plus restored component/chunk/payload stats and HostAction debug restore trace counters, nonempty RISC-V core checkpoint metadata including branch-predictor state plus store-backed and DRAM-backed memory checkpoint component/chunk metadata, checkpoint chunk checksum changes after guest stores, hypercall selector/argument/configured-response metadata plus argument/payload/response aggregate stats, m5 `sum` return-value execution, m5 `switch_cpu` execution-mode aggregate/per-component/per-chunk state-transfer stats, and `--riscv-se` ROI/stat hook counts in `sim.host_actions.*`. | Add broader payload breadth, other ISA entries, and calibrated clock-domain behavior. |
| `tests/gem5/m5threads_test_atomic` | `rem6-isa-riscv`, `rem6-cpu`, `rem6-coherence` | 40% single-axis | RISC-V LR/SC and AMO plus coherence reservation invalidation tests exist. | Add multi-threaded SE or full-system atomic tests through shared memory. |
| `tests/gem5/se_mode` | `rem6-system`, `rem6` CLI | 50% single-axis | RISC-V SE startup, ecalls, static newlib smokes including `fopen("w+")` create/write/readback and path-backed `--riscv-se-file` host writeback, `/proc/self/exe`, `/proc/self/cwd` after `chdir`, and file-backed plus pipe-backed `/proc/self/fd/<fd>` readlink through direct `readlinkat` ecall, `/proc/self/maps` open/read after raw `mmap`, `/proc/self/comm` open/read from the `PR_SET_NAME` process name, `/proc/self/status` open/read from modeled identity state, pipe roundtrip through direct `pipe2`/`write`/`read`/`close` ecalls, unconnected `socket(AF_UNIX, SOCK_STREAM)` creation/error-boundary behavior, abstract-name bind/listen/connect/accept4 stream handoff plus listener/client/accepted name queries, `socklen` writeback, and accept/accept4 peer-address `sockaddr_un`/`addrlen` writeback, `socketpair(AF_UNIX, SOCK_STREAM)` guest-only bidirectional stream read/write, name/shutdown/selected `SOL_SOCKET` option behavior, null-address sendto/recvfrom/sendmsg/sendmmsg/recvmsg/recvmmsg with no-network flags, poll/close through direct ecalls without pipe identity, raw terminal `ioctl(TIOCGWINSZ/TCGETS)`, pipe-size `fcntl(F_GETPIPE_SZ/F_SETPIPE_SZ)`, `open` directory traversal with `O_DIRECTORY` and `O_CLOEXEC`, and `open` regular-file access with `O_NOCTTY` and `O_NOFOLLOW` through legacy `open` with newlib/libgloss flags, selected syscalls including `sendfile`, `splice`, `tee`, `vmsplice`, `copy_file_range`, `memfd_create`, `mknodat`, `unshare`, `setns`, mode-zero `fallocate`, `readahead`, `statx`, `faccessat2`, `fchmodat2`, `utimensat`, advisory `flock`, `fadvise64`, `fcntl` byte-range advisory lock no-conflict slices, legacy and typed `fcntl` signal-owner state plus signal-number state (`F_SETOWN`/`F_GETOWN`/`F_SETOWN_EX`/`F_GETOWN_EX`/`F_SETSIG`/`F_GETSIG`), `symlinkat` creation consumed by `readlinkat` and followed by `faccessat`, `fchownat`/`fchown`, path/fd xattr set/get/list/remove, `statfs`/`fstatfs`, `sysinfo`, `syslog` and admin syscall deterministic error boundaries including chroot and privileged terminal/module/kexec denials, `uname` `new_utsname`, value-mode `riscv_hwprobe`, `riscv_flush_icache`, `prctl` process-name set/get, no-new-privs, dumpable, and parent-death-signal state, `personality` query/set state, `getresuid`/`getresgid` identity triples, current-credential `setresuid`/`setresgid` validation and identity updates, current-credential `setreuid`/`setregid` real/effective identity updates, current-credential `setuid`/`setgid` validation and effective-identity updates, file-system `setfsuid`/`setfsgid` return/update semantics, empty supplementary `getgroups` reporting and `setgroups` `EPERM`, `capget`/`capset` zero-capability and error-path slices, `sigaltstack` query/set/disable state, `ppoll` timeout/sigmask validation, finite-timeout expiration, timeout writeback, and fd-readiness static smoke, `pselect6` fd-set readiness, `eventfd2` counter/semaphore/nonblock/close-on-exec/poll behavior plus direct ecall readback, `epoll_create1`/`epoll_ctl`/`epoll_pwait` eventfd readiness plus direct ecall smoke, `inotify_init1`/`inotify_add_watch`/`inotify_rm_watch` guest-backed `IN_CREATE` event readiness/readback plus direct ecall smoke, `timerfd_create`/`timerfd_settime`/`timerfd_gettime` tick-derived expiration, read, nonblocking, write-rejection, and poll-readiness slices plus direct ecall smoke, in-place `mremap`, `mprotect`, `madvise` known-advice, mapped-range validation, anonymous `MADV_DONTNEED` page-zeroing, and file-backed `MADV_DONTNEED` backing restore, `msync` flags and mapped-range validation, `sync`/`fsync`/`fdatasync`/`sync_file_range`/`syncfs`, `mlock`/`munlock` `mmap`/`brk` range validation, `mlock2` range/flag validation, `mlockall` flag validation, single-node `mbind` mapped-range/nodemask validation slice, default and set/readback `get_mempolicy`/`set_mempolicy` mode/nodemask slices, `truncate`, `ftruncate`, `pread64`, `pwrite64`, `preadv`, `pwritev`, flags-zero `preadv2`, flags-zero `pwritev2`, `sched_setparam`, `sched_setscheduler`, `sched_getscheduler`, `sched_getparam`, `sched_setattr`, `sched_getattr`, `sched_get_priority_max/min`, `sched_rr_get_interval`, current process/group/user `getpriority`/`setpriority`, `ioprio_get`/`ioprio_set`, single-word `sched_setaffinity`/`sched_getaffinity`, single CPU/node `getcpu`, single-process `membarrier` slice, current-thread `rseq` register/unregister with guest struct initialization and validation, `set_tid_address` exit clear-child-tid write and futex wake behavior, `waitid` no-child behavior and `siginfo_t` writeback, `wait4` Linux option-mask no-child behavior, zero-duration `nanosleep` and `clock_nanosleep` validation, no-restart-block `restart_syscall` `EINTR`, `gettimeofday`, `settimeofday` validation/denial, `adjtimex`/`clock_adjtime` query/denial, legacy `time=1062`, `clock_gettime64`, `clock_getres`, `clock_settime` validation/denial, `CLOCK_TAI` `clock_gettime`, interval timer query/disarm state, POSIX timer create/set/get/delete lifecycle, `kill(..., 0)`, `tkill(..., 0)`, `tgkill(..., 0)`, and current-process `pidfd_open`/`pidfd_send_signal(..., 0)` existence checks, current-process scoped process-group/session `setpgid`/`getpgid`/`getsid`/`setsid` slices, stateful `setrlimit`, legacy `getrlimit` stack/data/NPROC/NOFILE limits, `prlimit64` stack/data/NPROC/NOFILE reporting and limit updates, NOFILE fd-allocation enforcement, plus unknown-pid rejection, basic `rt_sigaction`/`rt_sigprocmask`, `rt_sigpending` mask reporting, no-pending zero-timeout and blocked pending-signal `rt_sigtimedwait` with `siginfo_t` writeback, `rt_sigqueueinfo`/`rt_tgsigqueueinfo` target and guest `siginfo_t` validation with non-delivery `ENOSYS` records, futex mismatch, zero-timeout wait, wait-bitset zero and elapsed absolute timeout validation, wake-bitset count/bitset behavior, `FUTEX_WAKE_OP` guest-word update plus conditional two-address wake behavior, `close_range` close and `CLOSE_RANGE_CLOEXEC` behavior, `openat2` `open_how` parsing, mode validation, and close-on-exec behavior, `umask` masking for legacy `mkdir`/`mkdirat` directories and `openat(O_CREAT)` regular files, cwd-aware registered paths, guest-backed file output/readback and open visibility, at-family file and directory mutation including `renameat2` `RENAME_NOREPLACE` plus regular-file `RENAME_EXCHANGE`, registered-directory enumeration, `ENOSYS` records including unsupported `rt_sigreturn`, `ptrace`, legacy clone, known no-implementation, and modern probe entries, and guest writes exist. | Split hello, multicore SE, RVV intrinsic, and other-ISA subrows; add broader libc and lifecycle behavior. |
| `tests/gem5/memory` | `rem6-memory`, `rem6-cache`, `rem6-dram`, `rem6-fabric` | 73% representative | Stores, page maps, cache banks, topology slices, optional CLI RISC-V MSI-bank, MESI-line, MOESI-line, and CHI-line data-cache routing, top-level CLI RISC-V MSI-bank cache-cycle bank counters, three-core shared MSI/MESI/MOESI/CHI data-cache coherence routing, three-core MSI/MESI/MOESI/CHI instruction-cache fetch routing, top-level `RiscvTopologySystem` MSI instruction-cache fetch plus MSI data-cache load routing, explicit CLI RISC-V MSI data-cache and instruction-cache L1-to-L2-to-DRAM plus L1-to-L2-to-L3-to-DRAM fills, explicit run cache/fabric/DRAM aggregate/per-VN, per-link, per-link/per-VN lane, router/port/VC, wait-for queue-window, ordered configured-hop artifact, and direction-typed three-core FIFO/LIFO/LRG/no-QoS request/response candidate/grant/LRG-state fabric evidence joining request grants to two request VN 7/VC 11/12 hops and response grants to two response VN 8/VC 13/14 hops across two routers, plus same-tick response policy ordering under one/three workers, shared-transport batching, packet joins, architectural witnesses, request/response failure rollback, flit, latency, queue-delay, credit-delay, and request/response-scoped QoS aggregate/per-requestor/per-priority counters plus fabric memory-resource roll-up, default bare `rem6 run --isa riscv --execute` cache/fabric/DRAM smoke coverage, explicit `--memory-system cache-fabric-dram` preset smoke coverage, explicit `--memory-system direct` direct-transport coverage, DRAM-backed MSI fill accounting, DRAM/NVM counters, CLI-selectable JEDEC-style refresh presets, custom refresh timing, per-bank/all-bank/bank-group refresh policy, and 2x/4x fine-granularity refresh scaling, cache-local prefetch queue counters, top-level CLI RISC-V tagged next-line data-cache and instruction-cache prefetch issue/useful/useful-but-miss/useful-span-page/unused/late-hit/demand-miss and fixed-point accuracy/coverage stats, top-level instruction-cache and data-cache identity-mapped prefetch translation queue stats, translated data-cache MSI and instruction-cache MESI/MOESI/CHI L1/L2/L3 prefetch fill stats, top-level CLI run aggregate cache/transport/DRAM resource activity, top-level trace-replay fabric-route aggregate/per-VN, per-link, and per-link/per-VN lane activity with backpressure, aggregate resource-activity, virtual-network config stats, and fabric wait-for queue/credit edge-window JSON/stats, top-level trace-replay data-cache run/protocol/scheduler/profiled backing-DRAM and DRAM QoS stats, and combined trace-replay data-cache plus explicit-fabric route stats exist. | Add Ruby-scale protocol networks, broader response-side priority/resource/routing matrices, broader router/flit/routing/virtual-channel NoC matrices beyond the current direction-typed queue-policy/two-router rows, and remaining DRAM refresh breadth beyond policy and fine-granularity rows. |
| `tests/gem5/multisim`, `tests/gem5/suite_tests` | `rem6-workload`, `rem6-kernel`, `rem6` CLI | 52% single-axis | Suite planning, dispatch, execution summaries, occupancy contracts, and top-level `rem6 multi-run` sequential orchestration of real `run --config`, `gups --config`, `gpu-run --config`, `accelerator-run --config`, and `trace-replay --config` TOML and command-qualified `--run` entries with child-configured output, power/GPU extra-artifact path summaries, run-child checkpoint/restore count and restored payload roll-up, accelerator-child command/completion, direct-memory trace-replay child checkpoint/restore count plus component/chunk/payload roll-up from nonempty memory checkpoint payload metadata, checked-in multi-run example aggregate/child artifact layout, aggregate artifacts, and explicit child-failure summaries exist. | Add broader checkpoint restore orchestration and suite-compatible multi-artifact workload semantics. |
| `tests/gem5/parsec_benchmarks` | `rem6-workload`, `rem6-system`, ISA crates | 0% open | Workload suites exist, but PARSEC-class programs do not run. | Add PARSEC-class static or dynamic user workload support and suite-scale ROI/stat validation. |
| `tests/gem5/processor_switch_tests` | `rem6-system`, `rem6-cpu` | 20% unit-slice | Execution-mode metadata, top-level real RISC-V `m5_switch_cpu` detailed-mode metadata recording including repeated-switch previous-mode metadata, target-scoped mode/previous-mode stats, and distinct generated transfer manifest labels, CLI-selectable `timing` and TOML-backed `functional` switch target modes, continued execution, checkpoint-backed core/memory/scheduler/cache-bank state-transfer artifact counts, component/chunk metadata, top-level transfer stats, repeated-switch `xregs` and O3-runtime transfer checksum accumulator stats, quiescence-gate metadata including checker handoff counters in HostAction debug JSON, workload replay restore-by-label of generated transfer manifests, reserved-label protection, generated-transfer rollback of pre-switch execution-mode authority, top-level scheduled restore of a generated `m5_switch_cpu` transfer manifest exposing cleared final authority plus checkpoint-restore authority JSON/stat lanes, and live FU, direct/cache-hierarchy scalar-load, two-worker CPU1, single-core readfile-MMIO, and mixed two-worker CPU0-memory/CPU1-MMIO non-restorable transfers with typed target JSON, target-only components, preserved completion timing, and width-one resident O3 writeback calendar transfer preserving inherited issue/writeback/commit ticks plus future-reservation metadata exist. | Add broader quiescence-gated model handoff and rollback validation across CPU backends. |
| `tests/gem5/py_port` | `rem6` CLI, `rem6-workload` | 0% open | No Python embedding port exists. | Decide on a typed external control adapter or document a Rust/CLI replacement. |
| `tests/gem5/pyunit` | rem6 test crates, `rem6-workload`, `rem6-stats` | 35% unit-slice | Rust tests cover selected typed stats, workload, config, and helper behavior. | Map each pyunit helper family to a Rust owner. |
| `tests/gem5/readfile_tests` | `rem6-platform`, `rem6-system`, `rem6` CLI | 55% single-axis | DTB/initrd handoff, CLI input-file plumbing, `PlatformBuilder` read-only readfile MMIO-window tests, topology host-checkpoint capture of attached readfile payloads, and `rem6 run --readfile` host-file plus manifest and `suite-resource:<workload>/<resource>` suite resource-config input payload binding into untranslated and page-map-translated guest-visible MMIO loads, including live mode transfer, exist. | Validate Linux consumption and board-level boot handoff. |
| `tests/pyunit` | `rem6-stats`, `rem6-workload`, future utility owners | 35% unit-slice | Selected pystats and stdlib semantics are covered by typed Rust tests. | Split HDF5, pystats, registry/probes, stdlib helpers, and parsing rows. |
| `tests/gem5/regression_tests` | all rem6 crates | 35% unit-slice | Workspace tests act as the current regression suite. | Add migration tags or per-family regression rows. |
| `tests/gem5/replacement_policies` | `rem6-cache` | 60% representative | Multiple replacement, indexing, dueling, compressed, and sector tag tests exist. | Add remaining policies and exact trace/reference parity where useful. |
| `tests/gem5/riscv_boot_tests` | `rem6-platform`, `rem6-system`, `rem6-isa-riscv`, `rem6-cpu`, `rem6-kernel` | 35% unit-slice | DTB/initrd handoff, workload replay Linux boot handoff into supervisor boot-hart state with secondary harts stopped before HSM start, CLINT/PLIC, traps, CSRs, page-fault causes, translated faults, SBI base read-only ecalls, top-level `rem6 run --riscv-sbi` supervisor base ecall smoke with SBI 2.0 base version reporting, conservative standard extension probes plus legacy console putchar probe, top-level CLI legacy console input/output and DBCN shared-memory read/write, top-level CLI TIME `set_timer` deadline artifact/stat reporting and boot-hart plus HSM-started secondary-hart S-mode interrupt-handler delivery, top-level CLI HSM `hart_start` secondary-hart release and HSM start artifact/stat records through `rem6 run --riscv-sbi --cores 2`, top-level CLI boot-hart already-started `hart_start` `-6` return, top-level CLI current-hart `hart_stop` no-return idle completion with HSM stop artifact/stat records, top-level CLI non-retentive `hart_suspend` resume-address completion with HSM suspend artifact/stat records, top-level CLI HSM status-query artifact/stat records, top-level CLI retentive HSM wake by IPI and SBI TIME/STIP with artifact/stat records, library/workload replay DBCN shared-memory read/write, direct write-byte debug-console output, DBCN advertisement when functional guest memory I/O is configured, and workload replay Linux handoff DBCN read input from a manifest `Input` resource into backing-store and data-cache-resident guest memory using functional line replacement, without topology-routed firmware memory transactions, minimal TIME `set_timer` STIP scheduling including `u64::MAX` timer disable semantics, IPI `send_ipi` scheduled completion, SSIP pending-bit injection, top-level CLI HSM-started secondary-hart S-mode handler delivery with IPI artifact/stat records, and no partial event delivery on scheduler errors, standard SRST shutdown stop requests plus top-level shutdown, cold-reboot, warm-reboot, and system-failure reset artifact/stat records, RFENCE remote FENCE.I fetch-stream reset plus top-level artifact/stat records, remote SFENCE.VMA data TLB flushes with finite-range, ASID scope, scheduled completion events, and top-level completion artifact/stat records, HFENCE.GVMA whole and physical-range scoped modeled data TLB flush execution, HFENCE.VVMA range-scoped flush, HFENCE.VVMA.ASID scoped flush preservation, HFENCE validation, and HSM start entry-state, `START_PENDING`, status, no-return stop, retentive-suspend, default-non-retentive `RESUME_PENDING`/resume, and IPI/TIME-wake slices are tested. | Add broader SBI timer/IPI/reset power-state behavior, topology-routed firmware shared-memory reads/stores, remaining HSM wake semantics, VMID/G-stage-tagged HFENCE.GVMA completion coverage beyond physical-range modeled TLB invalidation, and a real Linux boot smoke. |
| `tests/gem5/stats` | `rem6-stats`, `rem6` CLI, `rem6-power` | 74% representative | Hierarchical counters, reset/dump histories, deltas, first-class histogram buckets, real probe producers, top-level retired-instruction probe stats and retired-PC target probe summaries, power bindings, instruction/data cache counters, top-level CLI RISC-V MSI-bank cache-cycle bank and cache-resource per-protocol counters, cache-local and top-level data-cache and instruction-cache prefetch queue counters, top-level instruction-cache and data-cache identity-mapped prefetch translation queue counters, top-level run and trace-replay aggregate/per-VN, per-link, and per-link/per-VN lane fabric-route counters including active virtual networks, flits, credit-delay ticks, and aggregate resource activity, plus trace-replay per-link/per-VN lane backpressure ticks, top-level trace-replay data-cache run/protocol/scheduler, CPU-response, directory-decision, and profiled backing-DRAM target/port/bank, row, command, latency, and QoS counters, top-level gpu-run data-cache bank and prefetch counters plus fabric aggregate/per-VN active-lane and contention counters and per-VN/per-link/per-hop transfer, byte, flit, occupancy, queue-delay, and credit-delay counters in stats and artifact JSON, top-level SBI console byte, HSM start/stop/suspend/wake/status-query, IPI request/target, RFENCE request/target/completion, SRST reset request/type/failure stats, and `Sbi` debug-flag console/timer/HSM/reset trace counters from executed `rem6 run --riscv-sbi`, CLI text-stat output with gem5-style final-tick, committed-instruction, sim-ops, sim-seconds, sim-frequency, single-core m5 ROI work-marker text aliases `numWorkItemsStarted`/`numWorkItemsCompleted` plus work-item duration histogram aliases `work_item_typeN`, single-core CPU `numInsts`/`numOps`/`commitStats0.numInsts`/`commitStats0.numOps`/`numCycles` aliases plus CPU and `commitStats0` `ipc`/`cpi` rates, single-core no-prefetch text-output L1 I-cache/D-cache `demandHits`/`demandMisses`/`demandAccesses`/`demandMissRate` plus `overallHits`/`overallMisses`/`overallAccesses`/`overallMissRate` and MSHR `demandMshrHits`/`overallMshrHits`/`demandMshrMisses`/`overallMshrMisses`/`demandMshrMissRate`/`overallMshrMissRate` aliases, configured single-core L1 I-cache/D-cache prefetcher `pfIdentified`/`pfIssued`/`pfUseful`/`pfUsefulButMiss`/`pfUnused`/`pfHitInCache`/`pfHitInMSHR`/`pfHitInWB`/`pfLate`/`pfSpanPage`/`pfUsefulSpanPage`/`pfInCache`/`demandMshrMisses` aliases plus derived `accuracy`/`coverage` aliases and artifact/stat `accuracy_ppm`/`coverage_ppm` fields, single-core `conditional_branch_predictions`/`conditional_branch_predicted_taken`/`conditional_branch_mispredictions` counters feeding `system.cpu.branchPred.condPredicted`/`system.cpu.branchPred.condPredictedTaken`/`system.cpu.branchPred.condIncorrect` conditional-branch text aliases, multicore per-core `conditional_branch_predictions`/`conditional_branch_predicted_taken`/`conditional_branch_mispredictions` counters feeding `system.cpu0.branchPred.condPredicted`/`system.cpu0.branchPred.condPredictedTaken`/`system.cpu0.branchPred.condIncorrect` and `system.cpu1.branchPred.condPredicted`/`system.cpu1.branchPred.condPredictedTaken`/`system.cpu1.branchPred.condIncorrect` text aliases, top-level RISC-V Tournament predictor local/global selection counters under `sim.cpu*.branch_predictor.tournament.*` and artifact JSON, modeled RISC-V fetch-ahead BTB probe lookup/hit counters, including conditional and direct-jump probes whose target may not steer fetch, feeding `system.cpu.branchPred.BTBLookups`/`system.cpu.branchPred.BTBHits` and multicore `system.cpu0.branchPred.BTBLookups`/`system.cpu0.branchPred.BTBHits` plus `system.cpu1.branchPred.BTBLookups`/`system.cpu1.branchPred.BTBHits` text aliases, and shared L2/L3 text-output `overallHits`/`overallMisses`/`overallAccesses`/`overallMissRate` plus `overallMshrHits`/`overallMshrMisses`/`overallMshrMissRate` aliases backed by executed cache-bank summaries including `system.l2.overallMshrHits` and `system.l3.overallMshrMissRate`, multicore CPU `numInsts`/`numOps`/`commitStats0.numInsts`/`commitStats0.numOps`/`numCycles` aliases plus CPU and `commitStats0` `ipc`/`cpi` rates, gem5-style memory-controller request/burst/byte aliases plus text-output `avgRdBWSys`/`avgWrBWSys` bandwidth aliases and DRAM/NVM-interface burst/per-bank burst/row-hit/byte/memory-access-latency plus text-output `avgRdBW`/`avgWrBW`/`avgMemAccLat`/`readRowHitRate`/`writeRowHitRate`/`pageHitRate` aliases, including NVM-profile `nvmBytesRead`/`nvmBytesWritten`, detailed-switch O3 table-driven FU-latency class JSON/text counters, bounded O3 writeback-port JSON/text/m5_dump_stats counters for cycles, admitted rows, deferred rows, deferred-row cycles, maximum ready rows per cycle, and maximum deferred rows, plus ROB/rename/IEW/LSQ/IQ branch-issued JSON/text aliases, path-safe `sim.cpu0.o3.iew.writeback_rate_ppm`/`sim.cpu0.o3.iew.producer_consumer_fanout_ppm`, final JSON `system.cpu.iew.wbRate`/`system.cpu.iew.wbFanout`, raw `sim.cpu0.o3.iew.branch_mispredicts`/`sim.cpu0.o3.commit.branch_mispredicts`, JSON/text `system.cpu.iew.branchMispredicts`/`system.cpu.commit.branchMispredicts`, JSON `system.cpu.iew.branchRepair.targetlessMismatch`/`system.cpu.iew.branchRepair.directionOnly`/`system.cpu.iew.branchRepair.wrongTarget`/`system.cpu.iew.branchRepair.total`, and JSON/text `system.cpu.iew.branchRepair_0::TargetlessMismatch`/`system.cpu.iew.branchRepair_0::DirectionOnly`/`system.cpu.iew.branchRepair_0::WrongTarget`/`system.cpu.iew.branchRepair_0::total`, including `system.cpu.rob.writes`, `system.cpu.rob.reads`, `system.cpu.lsq0.loadBytes`/`storeBytes`, `system.cpu.lsq0.forwLoads`, `system.cpu.lsq0.operation.load`/`system.cpu.lsq0.operation.total`/`system.cpu.lsq0.ordering.acquireRelease`/`system.cpu.lsq0.ordering.total`, `system.cpu.lsq0.dataResponse.samples`, `system.cpu.lsq0.dataResponse.totalLatency`, `system.cpu.lsq0.dataResponse.load.totalLatency`, `system.cpu.lsq0.dataResponse.storeConditional.totalLatency`, `system.cpu.iq.instsIssued`, `system.cpu.iq.memInstsIssued`, `system.cpu.iq.branchInstsIssued`, JSON/text IQ op-class bucket aliases `system.cpu.iq.issuedInstType_0::MemRead`/`MemWrite`/`IntMult`/`IntDiv`/`FloatMisc`/`SimdFloatMisc`, raw `sim.cpu0.o3.commit.committed_inst_type.mem_read`/`sim.cpu0.o3.commit.committed_inst_type.mem_write`/`sim.cpu0.o3.commit.committed_inst_type.int_mul`/`sim.cpu0.o3.commit.committed_inst_type.int_div`/`sim.cpu0.o3.commit.committed_inst_type.float_misc`/`sim.cpu0.o3.commit.committed_inst_type.vector_float_misc`, and JSON/text commit op-class bucket aliases `system.cpu.commit.committedInstType_0::MemRead`/`MemWrite`/`IntMult`/`IntDiv`/`FloatMisc`/`SimdFloatMisc`, and per-stage in-order pipeline width, resource/ordering blocked instruction and cycle, flush, and branch-prediction flush attribution stats from executed RISC-V runs, CLI GDB attach-before-execute register/memory smoke coverage exists; pre-execution writes cover RV64 vector data registers, RV64 supervisor CSR `sscratch`, supervisor environment CSR `senvcfg`, machine environment CSR `menvcfg`, supervisor interrupt CSR `sie`, translation CSR `satp`, machine CSR `mscratch`, machine counter-inhibit CSR `mcountinhibit`, RV64 vector fixed-point CSR `vxsat`/`vxrm`/`vcsr`, RV64 vector-configuration CSR `vl`/`vtype`, and packed PMP CSR `pmpcfg0`/`pmpcfg2` plus `pmpaddr0` through `pmpaddr15` writes into live `RiscvCore` PMP state; RV64 vector-configuration CSR `vlenb`, machine identity CSR `mhartid`/`mvendorid`/`marchid`/`mimpid`/`mconfigptr`, and machine ISA CSR `misa` readback through top-level GDB and matching guest CSR execution exist; RV64 counter-enable CSR `scounteren`/`mcounteren` GDB writes are read back through the register cache and consumed by matching guest CSR execution, with counter-read privilege gating; RV64 counter-inhibit CSR `mcountinhibit` GDB writes gate subsequent guest `cycle`/`instret` increments while `time` continues advancing; RV32 counter-enable CSR `scounteren`/`mcounteren` GDB writes through `p9e`/`p9f` are read back through the RV32 register cache and consumed by matching guest CSR execution; RV32/RV64 `mcountinhibit` register-cache read/write coverage exists; RV64 GDB-written `mstatus`, `mtvec`, `mie`, and `mip` machine-interrupt CSRs vector before the first guest instruction, and GDB-written `mepc`, `stvec`, `mideleg`, `sie`, and `sip` delegated-interrupt state survives `mret` into supervisor-vector execution; RV32 CSR register-cache read/write coverage, RV32/RV64 vector data register-cache read/write coverage, RV32/RV64 vector-configuration CSR coverage, and counter CSR `cycle`/`time`/`instret` plus machine-counter alias `mcycle`/`minstret` readback with counter writes after top-level GDB single-step execution exist, with selected CSR writes consumed by guest execution and counter CSR reads consumed after real execution, plus software and hardware breakpoints, single-step execution consumed by the following run, continue and `vCont;c` execution with completed-run summary handoff, cache-backed GDB run-control summary stats, data watchpoints stopping after real RISC-V load/store data-access completion with reason/address stop replies, top-level `Exec` debug-flag instruction trace records with CPU/retirement-scoped retired-record and byte stats, `Fetch` debug-flag issue trace records with CPU/endpoint-scoped issue-record and byte stats, `Data` debug-flag data-access trace records with CPU/kind-scoped load/store/atomic classification and byte stats, `Cache` debug-flag instruction/data L1/L2/L3 cache hierarchy trace records with activity, CPU-response, directory-decision, backing-DRAM, bank, and prefetch queue/translation/ratio stats, `Dram` debug-flag target/port/bank DRAM hierarchy trace records with record-count plus hierarchy-scoped access, row, refresh, command, latency, and byte stats, `Fabric` debug-flag lane/hop fabric activity trace records with record-count plus hierarchy-scoped transfer, byte, flit, occupancy, queue-delay, and credit-delay stats, `Memory` debug-flag fetch/data transport trace records with record-count, channel-scoped route/endpoint/request-agent, event-kind, response-status, and response-latency tick stats, `Syscall` debug-flag return/exit/blocked outcome plus hierarchy-scoped syscall-number, call-site, CPU, and argument stats, `Sbi` debug-flag console/timer/HSM/reset trace records plus counters from executed RISC-V SBI firmware, `HostAction` debug-flag m5 ROI/stat/checkpoint/restore/stop trace records plus kind counters from executed RISC-V m5ops, `O3` debug-flag single/multicore aggregate and CPU-scoped ROB/LSQ/rename stats (`sim.debug.o3_trace.cpu.cpu0.records`, `sim.debug.o3_trace.cpu.cpu1.event.tick_span`) plus event ROB/LSQ/rename occupancy, sequence-ordered ROB allocation/commit, scalar/vector-integer including multiply-add/widening/saturating-multiply plus scalar/vector float add/compare/misc/mul/FMA/div/sqrt FU-latency event count/cycle/min/max/avg stats, LSQ address-matched store-forwarding, address-mismatch suppression, and LSQ data-response/op-class total/min/max/avg latency event stats with timing-mode suppression from executed RISC-V m5ops, and `Power` debug-flag activity-derived power trace records with target/state/residency/microwatt stats from executed RISC-V runs, and library-level plus run-CLI and gpu-run-CLI McPAT/DSENT-shaped export tests exist. Canonical normal-run route/format/presence evidence is anchored by `rem6_run_power_activity_matches_canonical_resource_matrix`: direct, transport-plus-DRAM, explicit MSI L1/L2/L3, and cache-fabric-DRAM routes span McPAT XML and DSENT CSV; CPU L1/shared-cache/transport/fabric presence follows matching artifact/stat active fields while DRAM follows canonical active-or-profiled-target fields; and the matrix reconciles both selection inputs before verifying primitive-event/operation plus canonical actual-byte DRAM dynamic watts. Separate focused power unit tests include `run_dram_power_uses_accesses_when_they_dominate_residency`, `run_dram_power_keeps_commands_out_of_event_count`, `run_dram_power_uses_canonical_byte_total`, and `run_cache_power_preserves_legacy_target_calibrations` alongside profile-only, refresh-only, low-power-only, and zero-resource boundaries. | Add complete hierarchy counters, physical fabrication/vendor coefficient calibration and broader external-tool correlation, broader branch-predictor vector/thread and unsupported branch-kind coverage, broader stat naming/debug categories, remaining CSR register-cache families, and broader cycle-visible O3 execution. |
| `tests/gem5/stdlib` | `rem6-workload`, `rem6-platform`, `rem6` CLI | 54% single-axis | Workload manifests, resource payloads, manifest/suite-level CLI resource acquisition including host-file, uncompressed/gzip tar-entry with USTAR prefix paths, and standalone gzip inputs, manifest-acquired and unique-suite run kernel handoff, `suite-resource:<workload>/<resource>` suite readfile/load-blob/trace runtime handoff, resource-backed RISC-V SE stdin and guest-file handoff, suite dispatch plans, Linux handoff intent, and TOML/CLI tests including GPU micro-run config exist. | Add broader stdlib object coverage, remote/cache policy acquisition, and ergonomic topology/workload definitions. |
| `tests/test-progs` | `rem6-system`, `rem6` CLI, ISA crates | 35% unit-slice | Static RISC-V no-libc, newlib, and raw syscall smoke binaries, including `sendfile`, `splice`, `tee`, `vmsplice`, `copy_file_range`, `memfd_create`, raw `mknodat`, raw `unshare`, raw `setns`, mode-zero `fallocate`, raw `readahead`, raw `socket` unconnected fd creation/error boundaries and abstract-name listener connect/accept4 handoff with listener/client/accepted name queries plus accept4 peer-address writeback, raw `socketpair` plus name/shutdown/selected option and null-address `sendto`/`recvfrom`/`sendmsg`/`sendmmsg`/`recvmsg`/`recvmmsg` flags, raw `timerfd`, raw `inotify`, raw terminal `ioctl(TIOCGWINSZ/TCGETS)`, raw `pidfd_open`/`pidfd_send_signal(..., 0)`, raw `riscv_flush_icache`, raw `restart_syscall` no-restart `EINTR`, raw `setfsuid`/`setfsgid`, raw current group/user priority scopes, raw `ioprio_get`/`ioprio_set`, raw `sched_setattr`/`sched_getattr`, raw `rt_tgsigqueueinfo`, raw `mlock2`, `madvise(MADV_DONTNEED)` anonymous page-zeroing and file-backed restore, raw `get_mempolicy` default query and raw `set_mempolicy` set/get/reset query, `truncate`, xattr lifecycle, `statx`, `faccessat2`, `fchmodat2`, `utimensat`, advisory `flock`, `fadvise64`, `fcntl` byte-range advisory lock, pipe-size `fcntl`, legacy and typed `fcntl` owner and signal-number state (`F_SETOWN`/`F_GETOWN`/`F_SETOWN_EX`/`F_GETOWN_EX`/`F_SETSIG`/`F_GETSIG`), legacy `mkdir`, `preadv`/`pwritev`, flags-zero raw `preadv2`/`pwritev2`, raw `process_vm_readv`/`process_vm_writev`, `symlinkat`, raw `renameat2` `RENAME_NOREPLACE` and regular-file `RENAME_EXCHANGE`, and `fchownat`/`fchown`, `waitid`, raw `wait4` option-mask no-child behavior, `ppoll` timeout/sigmask/finite-expiration/readiness, futex wake-op, `sysinfo`, raw `syslog`, raw admin syscall rejection boundaries including chroot and privileged terminal/module/kexec denials, raw known no-implementation AIO including `io_pgetevents`, `ptrace`, POSIX mqueue, SysV IPC, clone plus kernel-facility/key-management, legacy memory-placement and protection-key, modern mount API, quota, memfd-secret, map-shadow-stack, futex2, stat/list-mount, LSM, and seal modern-probe ENOSYS boundaries, raw `prctl` no-new-privs, dumpable, and parent-death-signal state, raw `gettimeofday`, raw `settimeofday` validation/denial, raw `adjtimex`/`clock_adjtime` query/denial, raw `clock_gettime64`, raw `clock_settime` validation/denial, raw legacy `time=1062`, raw interval timer query/disarm, raw POSIX timer lifecycle, raw `capget`/`capset` capability checks, newlib file-create roundtrip, newlib `/proc/self/exe` readlink coverage, raw `/proc/self/cwd` after `chdir` readlink coverage, raw file-backed and pipe-backed `/proc/self/fd/<fd>` readlink coverage, raw `/proc/self/maps` after `mmap` coverage, raw `/proc/self/comm` after `PR_SET_NAME` coverage, raw `/proc/self/status` identity-state coverage, newlib pipe2 roundtrip coverage, newlib directory-open coverage, and newlib open-flag coverage, are generated when tools exist. | Add durable generated fixtures for hello, threads, and m5 utility shapes across ISAs. |
| `tests/gem5/traffic_gen` | `rem6-traffic`, `rem6-system`, `rem6-workload`, `rem6` CLI | 55% single-axis | Text config parsing, GUPS, packet trace replay including manifest and unique or TOML/CLI `suite-resource:<workload>/<resource>` suite resource-config trace handoff with non-input trace resource rejection, flags, maintenance, HTM, responses, workload summaries, typed generator/memory-profile summaries, top-level trace-replay fabric-route aggregate/per-VN, per-link, and per-link/per-VN lane pressure/backpressure stats with aggregate resource-activity, virtual-network, credit-depth config, credit-delay fields, and fabric wait-for queue/credit windows, top-level trace-replay data-cache run/protocol/scheduler, CPU-response, and directory-decision stats, and top-level GUPS profile JSON/stats output exist. | Add cache hierarchy matrix and broader trusted stats. |
| `tests/gem5/x86_boot_tests` | `rem6-isa-x86`, future platform work | 0% open | Narrow x86 prefix and interrupt-flag semantics exist, but no x86 boot path exists. | Add x86 ISA execution, paging, interrupt, platform, and boot-image tests. |
BTB text-alias update: executed single-core and multicore RISC-V branch-prediction text runs now map live child-BTB lookup totals, branch-kind lookup lanes, miss totals, branch-kind miss lanes, update totals, branch-kind update lanes, mispredict totals, and eviction counters into `system.cpu.branchPred.btb.lookups::total`, `system.cpu.branchPred.btb.lookups::DirectCond`, `system.cpu.branchPred.btb.lookups::DirectUncond`, `system.cpu.branchPred.btb.misses::total`, `system.cpu.branchPred.btb.misses::DirectCond`, `system.cpu.branchPred.btb.misses::DirectUncond`, `system.cpu.branchPred.btb.updates::total`, `system.cpu.branchPred.btb.updates::DirectCond`, `system.cpu.branchPred.btb.updates::DirectUncond`, `system.cpu.branchPred.btb.mispredict::total`, `system.cpu.branchPred.btb.evictions`, `system.cpu0.branchPred.btb.lookups::total`, `system.cpu0.branchPred.btb.lookups::DirectCond`, `system.cpu0.branchPred.btb.lookups::DirectUncond`, `system.cpu0.branchPred.btb.misses::total`, `system.cpu0.branchPred.btb.misses::DirectCond`, `system.cpu0.branchPred.btb.misses::DirectUncond`, `system.cpu0.branchPred.btb.updates::total`, `system.cpu0.branchPred.btb.updates::DirectCond`, `system.cpu0.branchPred.btb.updates::DirectUncond`, `system.cpu0.branchPred.btb.mispredict::total`, `system.cpu0.branchPred.btb.evictions`, `system.cpu1.branchPred.btb.lookups::total`, `system.cpu1.branchPred.btb.lookups::DirectCond`, `system.cpu1.branchPred.btb.lookups::DirectUncond`, `system.cpu1.branchPred.btb.misses::total`, `system.cpu1.branchPred.btb.misses::DirectCond`, `system.cpu1.branchPred.btb.misses::DirectUncond`, `system.cpu1.branchPred.btb.updates::total`, `system.cpu1.branchPred.btb.updates::DirectCond`, `system.cpu1.branchPred.btb.updates::DirectUncond`, `system.cpu1.branchPred.btb.mispredict::total`, and `system.cpu1.branchPred.btb.evictions`, with BTB branch-kind counts preserved through checkpoint version 6 and a colliding direct-jump run with positive BTB eviction evidence, without changing stats scores.
Stats ledger note: the BTB, lookup, committed, and mispredict-cause slice now exposes `sim.cpu*.branch_predictor.btb.{lookups,hits,misses,updates,evictions,mispredictions,predicted_taken_misses}`, branch-kind `sim.cpu*.branch_predictor.btb.mispredict_due_to_btb_miss.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}`, branch-kind `sim.cpu*.branch_predictor.lookups.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}`, non-return `sim.cpu*.branch_predictor.{indirect_hits,indirect_mispredicted}`, branch-kind `sim.cpu*.branch_predictor.committed.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}`, branch-kind `sim.cpu*.branch_predictor.mispredicted.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}`, branch-kind `sim.cpu*.branch_predictor.corrected.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}`, and branch-kind `sim.cpu*.branch_predictor.mispredict_due_to_predictor.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}` counters in top-level run JSON and stats. It also maps real BTB update, hit-ratio, no-target/wrong-target, predicted-taken BTB-miss, indirect lookup/hit/miss/mispredicted aggregates, branch-predictor lookup-by-branch-kind, committed-branch-by-branch-kind, committed-misprediction-by-branch-kind, committed taken-misprediction BTB-miss-by-branch-kind, and committed predictor-cause misprediction-by-branch-kind values into gem5-style text aliases: `system.cpu.branchPred.BTBUpdates`, `system.cpu.branchPred.BTBHitRatio`, `system.cpu.branchPred.BTBMispredicted`, `system.cpu.branchPred.predTakenBTBMiss`, `system.cpu.branchPred.indirectLookups`, `system.cpu.branchPred.indirectHits`, `system.cpu.branchPred.indirectMisses`, `system.cpu.branchPred.indirectMispredicted`, `system.cpu.branchPred.lookups_0::DirectCond`, `system.cpu.branchPred.lookups_0::DirectUncond`, `system.cpu.branchPred.lookups_0::CallIndirect`, `system.cpu.branchPred.lookups_0::IndirectUncond`, `system.cpu.branchPred.committed_0::DirectCond`, `system.cpu.branchPred.committed_0::CallIndirect`, `system.cpu.branchPred.committed_0::IndirectUncond`, `system.cpu.branchPred.committed_0::total`, `system.cpu.branchPred.mispredicted_0::DirectCond`, `system.cpu.branchPred.mispredicted_0::CallIndirect`, `system.cpu.branchPred.mispredicted_0::IndirectUncond`, `system.cpu.branchPred.mispredicted_0::total`, `system.cpu.branchPred.corrected_0::CallIndirect`, `system.cpu.branchPred.targetWrong_0::CallIndirect`, `system.cpu.branchPred.mispredictDueToBTBMiss_0::DirectCond`, `system.cpu.branchPred.mispredictDueToBTBMiss_0::CallIndirect`, `system.cpu.branchPred.mispredictDueToBTBMiss_0::IndirectUncond`, `system.cpu.branchPred.mispredictDueToBTBMiss_0::total`, `system.cpu.branchPred.mispredictDueToPredictor_0::DirectCond`, `system.cpu.branchPred.mispredictDueToPredictor_0::IndirectUncond`, `system.cpu.branchPred.mispredictDueToPredictor_0::total`, multicore `system.cpu0.branchPred.BTBUpdates`, `system.cpu0.branchPred.BTBHitRatio`, `system.cpu0.branchPred.BTBMispredicted`, `system.cpu0.branchPred.predTakenBTBMiss`, `system.cpu0.branchPred.indirectLookups`, `system.cpu0.branchPred.indirectHits`, `system.cpu0.branchPred.indirectMisses`, `system.cpu0.branchPred.indirectMispredicted`, `system.cpu0.branchPred.lookups_0::DirectCond`, `system.cpu0.branchPred.lookups_0::DirectUncond`, `system.cpu0.branchPred.lookups_0::IndirectUncond`, `system.cpu0.branchPred.committed_0::DirectCond`, `system.cpu0.branchPred.committed_0::IndirectUncond`, `system.cpu0.branchPred.mispredicted_0::DirectCond`, `system.cpu0.branchPred.mispredicted_0::IndirectUncond`, `system.cpu0.branchPred.mispredictDueToBTBMiss_0::DirectCond`, `system.cpu0.branchPred.mispredictDueToBTBMiss_0::IndirectUncond`, `system.cpu0.branchPred.mispredictDueToPredictor_0::DirectCond`, `system.cpu0.branchPred.mispredictDueToPredictor_0::IndirectUncond`, `system.cpu1.branchPred.BTBUpdates`, `system.cpu1.branchPred.BTBHitRatio`, `system.cpu1.branchPred.BTBMispredicted`, `system.cpu1.branchPred.predTakenBTBMiss`, `system.cpu1.branchPred.indirectLookups`, `system.cpu1.branchPred.indirectHits`, `system.cpu1.branchPred.indirectMisses`, `system.cpu1.branchPred.indirectMispredicted`, `system.cpu1.branchPred.lookups_0::DirectCond`, `system.cpu1.branchPred.lookups_0::DirectUncond`, `system.cpu1.branchPred.lookups_0::IndirectUncond`, `system.cpu1.branchPred.committed_0::DirectCond`, `system.cpu1.branchPred.committed_0::IndirectUncond`, `system.cpu1.branchPred.mispredicted_0::DirectCond`, `system.cpu1.branchPred.mispredicted_0::IndirectUncond`, `system.cpu1.branchPred.mispredictDueToBTBMiss_0::DirectCond`, `system.cpu1.branchPred.mispredictDueToBTBMiss_0::IndirectUncond`, `system.cpu1.branchPred.mispredictDueToPredictor_0::DirectCond`, and `system.cpu1.branchPred.mispredictDueToPredictor_0::IndirectUncond`. The current RISC-V execution path uses gem5-style link-register classification for direct calls, indirect calls, returns, and direct/indirect unconditional jumps, with real lookup and committed coverage for direct-conditional, direct-unconditional, and executable indirect-call lanes, top-level indirect-call text alias coverage for lookup and committed paths plus zero-valued mispredicted, corrected, target-wrong, and BTB-miss-attribution aliases in the target-correct run, positive BTB-miss attribution coverage for direct-conditional, return, and indirect-unconditional lanes, and predictor-cause coverage for direct-conditional wrong-direction and wrong-target repairs; direct `jal`/`jal ra` targets are statically known, and current-register indirect calls can be target-correct from fetch-ahead, so correct cold-BTB jumps remain scalar BTB misses rather than `mispredictDueToBTBMiss`. Remaining gaps are broader branch-predictor vector/thread stat families beyond the current lookup, target-provider, committed, corrected, target-wrong, and mispredict-cause lanes plus executable coverage for branch-kind lanes that are not yet produced by RISC-V fetch-ahead scenarios.
The lookup total text aliases are also covered as `system.cpu.branchPred.lookups_0::total`, `system.cpu0.branchPred.lookups_0::total`, and `system.cpu1.branchPred.lookups_0::total`; the branch squash branch-kind slice exposes `sim.cpu*.branch_predictor.squashes.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}` plus artifact JSON `branch_predictor.squashes.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}` and maps real nested fetch-ahead rollback to text/JSON `system.cpu.branchPred.squashes_0::{DirectCond,total}`; the RAS stat slice exposes `sim.cpu*.branch_predictor.ras.{pushes,pops,squashes,used,correct,incorrect}` plus artifact JSON `branch_predictor.ras` and maps real call/return fetch-ahead runs to text/JSON `system.cpu.branchPred.ras.{pushes,pops,squashes,used,correct,incorrect}`, `system.cpu0.branchPred.ras.{pushes,pops,squashes,used,correct,incorrect}`, and `system.cpu1.branchPred.ras.{pushes,pops,squashes,used,correct,incorrect}` aliases.
The corrected branch-kind slice exposes `sim.cpu*.branch_predictor.corrected.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}` counters and maps them to `system.cpu.branchPred.corrected_0::DirectCond`, `system.cpu.branchPred.corrected_0::IndirectUncond`, `system.cpu.branchPred.corrected_0::total`, multicore `system.cpu0.branchPred.corrected_0::DirectCond`, `system.cpu0.branchPred.corrected_0::IndirectUncond`, `system.cpu0.branchPred.corrected_0::total`, `system.cpu1.branchPred.corrected_0::DirectCond`, `system.cpu1.branchPred.corrected_0::IndirectUncond`, and `system.cpu1.branchPred.corrected_0::total`.
The target-provider slice exposes `sim.cpu*.branch_predictor.target_provider.{no_target,btb,ras,indirect,total}` counters and maps current real BTB-selected, no-target, single/multicore RAS return, and single/multicore indirect JALR providers to `system.cpu.branchPred.targetProvider_0::BTB`, `system.cpu.branchPred.targetProvider_0::NoTarget`, `system.cpu.branchPred.targetProvider_0::RAS`, `system.cpu.branchPred.targetProvider_0::Indirect`, `system.cpu.branchPred.targetProvider_0::total`, multicore `system.cpu0.branchPred.targetProvider_0::BTB`, `system.cpu0.branchPred.targetProvider_0::NoTarget`, `system.cpu0.branchPred.targetProvider_0::RAS`, `system.cpu0.branchPred.targetProvider_0::Indirect`, `system.cpu0.branchPred.targetProvider_0::total`, `system.cpu1.branchPred.targetProvider_0::BTB`, `system.cpu1.branchPred.targetProvider_0::NoTarget`, `system.cpu1.branchPred.targetProvider_0::RAS`, `system.cpu1.branchPred.targetProvider_0::Indirect`, and `system.cpu1.branchPred.targetProvider_0::total`; RAS is produced by top-level call/return fetch-ahead execution, indirect is produced by top-level register-sourced `jalr` fetch-ahead execution, and remaining target-provider gaps are broader provider/thread/vector scenarios beyond these executable RISC-V paths.
The target-wrong branch-kind slice exposes `sim.cpu*.branch_predictor.target_wrong.{no_branch,return,call_direct,call_indirect,direct_conditional,direct_unconditional,indirect_conditional,indirect_unconditional,total}` counters and maps them to `system.cpu.branchPred.targetWrong_0::DirectCond`, `system.cpu.branchPred.targetWrong_0::IndirectUncond`, `system.cpu.branchPred.targetWrong_0::total`, multicore `system.cpu0.branchPred.targetWrong_0::DirectCond`, `system.cpu0.branchPred.targetWrong_0::IndirectUncond`, `system.cpu0.branchPred.targetWrong_0::total`, `system.cpu1.branchPred.targetWrong_0::DirectCond`, `system.cpu1.branchPred.targetWrong_0::IndirectUncond`, and `system.cpu1.branchPred.targetWrong_0::total`. CPU test-anchor note: the O3 debug path now distinguishes direct-conditional predicted-target mismatches with no resolved target from wrong-target repairs through top-level `rem6 run --execute --debug-flags O3` per-event `branch_repair` JSON plus `sim.debug.o3_trace.event.branch_repair_targetless_mismatches`, `sim.debug.o3_trace.event.branch_repair_wrong_targets`, `sim.debug.o3_trace.event.branch_repair_direction_only_mismatches`, `sim.debug.o3_trace.event.branch_repair_targetless_mismatch_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_repair_wrong_target_kind.call_indirect`, `sim.debug.o3_trace.event.branch_repair_wrong_target_kind.indirect_unconditional`, and `sim.debug.o3_trace.event.branch_repair_direction_only_kind.direct_unconditional` stat evidence, and final stats JSON now exposes `/cores/0/o3_runtime/branch_repair` outcome/kind matrix fields plus `sim.cpu0.o3.branch_repair_wrong_target_kind.call_indirect`, `sim.cpu0.o3.branch_repair_direction_only_kind.direct_unconditional`, IEW split counters, branchRepair dotted JSON aliases, branchRepair_0 JSON/text bucket aliases, and IEW/commit branch-mispredict aliases for the same top-level call-indirect wrong-target run, while preserving fall-through memory evidence and partitioning targetless no-link repairs through `sim.debug.o3_trace.event.branch_targetless_mismatch_without_link_writes` plus squashed-target `sim.debug.o3_trace.event.branch_targetless_mismatch_squashed_target_without_link_writes` and direct-conditional per-kind counters, partitions direction-mismatch outcome through `sim.debug.o3_trace.event.branch_direction_mismatches`, squashed-target/no-link aggregates `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_targets`, `sim.debug.o3_trace.event.branch_direction_mismatch_without_link_writes`, `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_target_without_link_writes`, link aggregate `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_target_link_writes`, kind counters `sim.debug.o3_trace.event.branch_direction_mismatch_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_direction_mismatch_kind.direct_unconditional`, zero `sim.debug.o3_trace.event.branch_direction_mismatch_kind.call_indirect`, zero `sim.debug.o3_trace.event.branch_direction_mismatch_kind.indirect_unconditional`, per-kind squashed-target counters `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_target_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_target_kind.direct_unconditional`, zero `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_target_kind.call_indirect`, no-link counters `sim.debug.o3_trace.event.branch_direction_mismatch_without_link_write_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_direction_mismatch_without_link_write_kind.direct_unconditional`, `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_target_without_link_write_kind.direct_conditional`, `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_target_without_link_write_kind.direct_unconditional`, and link-write suppression counters `sim.debug.o3_trace.event.branch_direction_mismatch_link_write_kind.call_indirect`, `sim.debug.o3_trace.event.branch_direction_mismatch_squashed_target_link_write_kind.call_indirect`, partitions call-indirect wrong-target squashed-target link writes through `sim.debug.o3_trace.event.branch_wrong_target_squashed_target_link_writes` plus `sim.debug.o3_trace.event.branch_wrong_target_squashed_target_link_write_kind.call_indirect`, and partitions general branch squashed-target link state through `sim.debug.o3_trace.event.branch_squashed_target_link_writes`, `sim.debug.o3_trace.event.branch_squashed_target_without_link_writes`, `sim.debug.o3_trace.event.branch_squashed_target_link_write_kind.call_indirect`, and `sim.debug.o3_trace.event.branch_squashed_target_without_link_write_kind.indirect_unconditional` with timing-mode suppression.
O3 host-action stats note: top-level `rem6 run --execute` now covers resettable `m5_dump_stats` samples for scalar/vector float-misc FU-latency classes, including `sim.host_actions.stats_dump.cpu0.o3.fu_float_misc_*` and `sim.host_actions.stats_dump.cpu0.o3.fu_vector_float_misc_*`, reset-scoped FU-class and IQ/IEW issued-type dump evidence where pre-reset float/vector-float misc work is cleared by `m5_reset_stats` and `m5_dump_reset_stats` snapshots the old epoch before post-reset integer mul/div stats, including resettable dotted aliases `system.cpu.iq.issuedInstType.IntMult`, `system.cpu.iq.issuedInstType.IntDiv`, `system.cpu.iq.issuedInstType.FloatMisc`, `system.cpu.iq.issuedInstType.SimdFloatMisc`, `system.cpu.commit.committedInstType.IntMult`, `system.cpu.commit.committedInstType.IntDiv`, `system.cpu.commit.committedInstType.FloatMisc`, and `system.cpu.commit.committedInstType.SimdFloatMisc`, plus resettable dump-time IEW rate samples `sim.host_actions.stats_dump.cpu0.o3.iew.writeback_rate_ppm`, `sim.host_actions.stats_dump.cpu0.o3.iew.producer_consumer_fanout_ppm`, `system.cpu.iew.wbRate`, and `system.cpu.iew.wbFanout`, reset-scoped branch-repair dump evidence where warmed direct-conditional and direct-unconditional repair counters are snapshotted before reset through `sim.host_actions.stats_dump.cpu0.o3.branch_repair_targetless_mismatches`, `sim.host_actions.stats_dump.cpu0.o3.branch_repair_direction_only_mismatches`, `sim.host_actions.stats_dump.cpu0.o3.branch_repair_wrong_targets`, `sim.host_actions.stats_dump.cpu0.o3.branch_repair_targetless_mismatch_kind.direct_conditional`, `sim.host_actions.stats_dump.cpu0.o3.branch_repair_direction_only_kind.direct_unconditional`, `sim.host_actions.stats_dump.cpu0.o3.iew.predicted_taken_incorrect`, `sim.host_actions.stats_dump.cpu0.o3.iew.predicted_not_taken_incorrect`, and resettable aliases `system.cpu.iew.branchRepair.targetlessMismatch`/`system.cpu.iew.branchRepair.directionOnly`/`system.cpu.iew.branchRepair.wrongTarget`/`system.cpu.iew.branchRepair.total` plus bucket aliases `system.cpu.iew.branchRepair_0::TargetlessMismatch`/`system.cpu.iew.branchRepair_0::DirectionOnly`/`system.cpu.iew.branchRepair_0::WrongTarget`/`system.cpu.iew.branchRepair_0::total` and `system.cpu.iew.predictedTakenIncorrect`/`system.cpu.iew.predictedNotTakenIncorrect`/`system.cpu.iew.branchMispredicts`/`system.cpu.commit.branchMispredicts`/`system.cpu.iq.branchInstsIssued` then cleared in the post-reset dump while post-reset FU work remains and final `sim.cpu0.o3.branch_repair_targetless_mismatches`, `sim.cpu0.o3.branch_repair_direction_only_mismatches`, `sim.cpu0.o3.branch_repair_wrong_targets`, and IEW split stats stay zero, CPU1 active-hart reset-scoped branch-event/FTQ dumps where pre-reset `sim.host_actions.stats_dump.cpu1.o3.branch_event.kind.call_indirect`, `sim.host_actions.stats_dump.cpu1.o3.branch_event.kind.direct_unconditional`, `system.cpu1.ftq.squashes_0::CallIndirect`, `system.cpu1.ftq.squashes_0::DirectUncond`, and `system.cpu1.ftq.squashedTargets_0::total` are present then cleared by `m5_reset_stats` while CPU0/generic aliases stay absent, CPU1 scheduled checkpoint-restore branch-event/FTQ dumps preserve the same active-hart lanes across first and restored dumps while final `sim.cpu1.o3.branch_event.kind.call_direct` and `system.cpu1.ftq.squashes_0::CallDirect` show post-restore replay only, reset-scoped LSQ matrix dump evidence snapshots ordered atomic load/store/load-reserved/store-conditional/atomic, aq/rl order counts, and aggregate/op-class LSQ latency samples/ticks through `sim.host_actions.stats_dump.cpu0.o3.lsq_operation.store_conditional`, `sim.host_actions.stats_dump.cpu0.o3.lsq_ordering.*`, `sim.host_actions.stats_dump.cpu0.o3.lsq_data_latency_*`, `sim.host_actions.stats_dump.cpu0.o3.lsq_store_conditional_failures`, nested failed-SC dump lanes `sim.host_actions.stats_dump.cpu0.o3.lsq_operation.store_conditional.store_conditional_failures`, gem5-style resettable operation aliases `system.cpu.lsq0.operation.storeConditional.storeConditionalFailures`, and resettable dump-sample aliases `system.cpu.rob.writes`/`system.cpu.rob.maxOccupancy`/`system.cpu.rename.renamedOperands`/`system.cpu.iew.writebackCount.total`/`system.cpu.iq.issuedInstType.MemRead`/`system.cpu.commit.committedInstType.MemWrite` plus `system.cpu.lsq0.operation.storeConditional`/`system.cpu.lsq0.operation.total`/`system.cpu.lsq0.ordering.acquireRelease`/`system.cpu.lsq0.ordering.total`/`system.cpu.lsq0.maxOccupancy`, then clears them before a post-reset failed store-conditional plus result-store dump records only the new failure/latency slice, scheduled checkpoint-restore dumps that restore integer FU-class samples while excluding post-checkpoint float/vector-float misc mutation until replay, and structured per-core O3 runtime JSON exposing extended scalar/vector float FU-class instruction and cycle fields, final snapshot lanes `/cores/0/o3_runtime/snapshot/rob/count` and `/cores/0/o3_runtime/snapshot/rename_map/entries`, nested IQ/IEW/commit matrices including `/cores/0/o3_runtime/iq/issued_inst_type/int_mul`, `/cores/0/o3_runtime/iew/writeback_count`, and `/cores/0/o3_runtime/commit/committed_inst_type/vector_float_misc`, plus debug O3 trace summary JSON that mirrors the runtime ROB, rename, IQ, IEW, commit, event-window, event-summary branch-event/branch-repair/LSQ byte/forwarding/data-latency/ordering/failed-SC, event-summary FU-latency, and FU-latency class matrices including `/debug/o3_trace/0/rob/allocations`, `/debug/o3_trace/0/rename/writes`, `/debug/o3_trace/0/iq/issued_inst_type/int_mul`, `/debug/o3_trace/0/iew/writeback_count`, `/debug/o3_trace/0/commit/committed_inst_type/int_mul`, `/debug/o3_trace/0/commit/committed_inst_type/vector_float_misc`, `/debug/o3_trace/0/event_summary/records`, `/debug/o3_trace/0/event_summary/span_ticks`, `/debug/o3_trace/0/event_summary/branch_event/branches`, `/debug/o3_trace/0/event_summary/branch_event/predicted_taken`, `/debug/o3_trace/0/event_summary/branch_event/predicted_not_taken`, `/debug/o3_trace/0/event_summary/branch_event/predicted_targets`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_matches`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_mismatches`, `/debug/o3_trace/0/event_summary/branch_event/mispredictions`, `/debug/o3_trace/0/event_summary/branch_event/squashes`, `/debug/o3_trace/0/event_summary/branch_event/kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_taken_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_not_taken_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_match_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/predicted_target_mismatch_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/squash_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/squash_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_repair/targetless_mismatches`, `/debug/o3_trace/0/event_summary/branch_repair/wrong_targets`, `/debug/o3_trace/0/event_summary/branch_repair/direction_only_mismatches`, `/debug/o3_trace/0/event_summary/branch_repair/targetless_mismatch_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_repair/wrong_target_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_repair/direction_only_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/lsq_data_latency/samples`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_candidates`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_matches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_suppressed`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_address_mismatches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_byte_mismatches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/load_bytes`, `/debug/o3_trace/0/event_summary/lsq_operation/atomic/store_bytes`, `/debug/o3_trace/0/event_summary/lsq_operation/load/latency/ticks`, `/debug/o3_trace/0/event_summary/lsq_ordering/acquire`, `/debug/o3_trace/0/event_summary/lsq_ordering/release`, `/debug/o3_trace/0/event_summary/lsq_ordering/acquire_release`, `/debug/o3_trace/0/event_summary/fu_latency_instructions`, `/debug/o3_trace/0/event_summary/fu_latency_class/integer_mul/instructions`, `/debug/o3_trace/0/fu_latency_class/float_mul/instructions`, `/debug/o3_trace/0/fu_latency_class/vector_float_div/cycles`, and `/debug/o3_trace/0/fu_latency_class/vector_integer_mul/instructions`, nested ROB/rename/LSQ matrices including `/cores/0/o3_runtime/rob/allocations`, `/cores/0/o3_runtime/rename/writes`, `/cores/0/o3_runtime/lsq/operation/load/latency/ticks`, `/cores/0/o3_runtime/lsq/operation/load/forwarding_matches`, and `/cores/0/o3_runtime/lsq/ordering/acquire_release`, plus ordered atomic and float/vector LSQ operation/order fields from real detailed-mode execution, with matching `sim.cpu0.o3.lsq_operation.*`, `sim.cpu0.o3.lsq_ordering.*`, `sim.cpu0.o3.commit.committed_inst_type.vector_float_misc`, and `sim.cpu0.o3.lsq_store_conditional_failures` stats; the O3 stats bridge also has executable reset and inactive checkpoint-restore sync coverage proving active O3 dump CPU filters are cleared before later work reactivates them.
Resettable O3 LSQ alias evidence now also covers operation-scoped byte samples `system.cpu.lsq0.operation.load.loadBytes` and `system.cpu.lsq0.operation.store.storeBytes` plus count bucket samples `system.cpu.lsq0.operation_0::Load`, `system.cpu.lsq0.operation_0::StoreConditional`, `system.cpu.lsq0.operation_0::total`, `system.cpu.lsq0.ordering_0::AcquireRelease`, and `system.cpu.lsq0.ordering_0::total` in the same top-level `m5_dump_reset_stats` snapshot, with matching final JSON/text bucket aliases from `rem6 run --execute`; that dump snapshot also exposes resettable O3 event-window structural-pressure phase deltas `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.issue_to_writeback_ticks`, `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.writeback_to_commit_ticks`, and `sim.host_actions.stats_dump.cpu0.o3.event_window.max_structural_pressure.issue_to_commit_ticks`, while live-ROB `m5_dump_stats` exposes resettable event-summary phase totals and dump aliases such as `sim.host_actions.stats_dump.cpu0.o3.event_summary.issue_to_writeback_ticks`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.writeback_to_commit_ticks`, `sim.host_actions.stats_dump.cpu0.o3.event_summary.issue_to_commit_ticks`, `system.cpu.iew.issueToWritebackTicks`, `system.cpu.iew.writebackToCommitTicks`, and `system.cpu.iew.issueToCommitTicks`, and normal detailed-mode `rem6 run --execute --debug-flags O3` now exposes the same phase-delta triplet through raw debug O3 events such as `/debug/o3_trace/0/events/*/issue_to_writeback_ticks`, runtime/debug event-summary totals such as `/cores/0/o3_runtime/event_summary/issue_to_writeback_ticks`, `/debug/o3_trace/0/event_summary/issue_to_commit_ticks`, and `sim.cpu0.o3.event_summary.writeback_to_commit_ticks`, runtime/debug event-window rows such as `/cores/0/o3_runtime/event_window/max_rob_occupancy/issue_to_writeback_ticks` and `/debug/o3_trace/0/event_summary/event_window/max_fu_latency/issue_to_writeback_ticks`, final stats such as `sim.cpu0.o3.event_summary.event_window.max_fu_latency.issue_to_commit_ticks`, aggregate per-event debug trace stats such as `sim.debug.o3_trace.event.issue_to_writeback_ticks` and `sim.debug.o3_trace.cpu.cpu0.event.issue_to_commit_ticks`, aggregate plus CPU-scoped debug trace event-window stats such as `sim.debug.o3_trace.event_window.max_rob_occupancy.issue_to_writeback_ticks` and `sim.debug.o3_trace.cpu.cpu0.event_window.max_fu_latency.issue_to_commit_ticks`, and JSON/text gem5-style IEW phase aliases such as `system.cpu.iew.issueToWritebackTicks`, `system.cpu.iew.writebackToCommitTicks`, and `system.cpu.iew.issueToCommitTicks`.
O3 debug branch-event/event-summary branch-mismatch/IQ/commit matrix evidence now derives not-taken, no-link, misprediction, squashed-target, repair, issue, and commit class lanes from the same top-level emitted O3 debug events, including `/debug/o3_trace/0/branch_event/not_taken_kind/direct_conditional`, `/debug/o3_trace/0/branch_event/without_link_write_kind/direct_conditional`, `/debug/o3_trace/0/branch_event/mispredictions`, `/debug/o3_trace/0/branch_event/misprediction_kind/call_indirect`, `/debug/o3_trace/0/branch_event/misprediction_kind/direct_unconditional`, `/debug/o3_trace/0/branch_event/squashed_target_kind/call_indirect`, `/debug/o3_trace/0/branch_event/squashed_target_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_event/without_link_write_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_event/squashed_target_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_event/squashed_target_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_target_mismatch/wrong_targets`, `/debug/o3_trace/0/event_summary/branch_target_mismatch/wrong_target_squashed_target_link_write_kind/call_indirect`, `/debug/o3_trace/0/event_summary/branch_target_mismatch/targetless_mismatch_kind/direct_conditional`, `/debug/o3_trace/0/event_summary/branch_direction_mismatch/mismatches`, `/debug/o3_trace/0/event_summary/branch_direction_mismatch/without_link_write_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/branch_direction_mismatch/squashed_target_without_link_write_kind/direct_unconditional`, `/debug/o3_trace/0/event_summary/iq/issued_inst_type/mem_read`, `/debug/o3_trace/0/event_summary/iq/issued_inst_type/mem_write`, `/debug/o3_trace/0/event_summary/iq/issued_inst_type/int_mul`, `/debug/o3_trace/0/event_summary/iq/issued_inst_type/int_div`, `sim.debug.o3_trace.event.iq_insts_issued`, `sim.debug.o3_trace.event.iq_issued_inst_type.int_mul`, `/debug/o3_trace/0/event_summary/commit/branch_mispredicts`, `/debug/o3_trace/0/event_summary/commit/committed_inst_type/mem_read`, `/debug/o3_trace/0/event_summary/commit/committed_inst_type/mem_write`, `/debug/o3_trace/0/event_summary/commit/committed_inst_type/int_div`, `sim.debug.o3_trace.event.commit_branch_mispredicts`, and `sim.debug.o3_trace.event.commit_committed_inst_type.int_div` from real `rem6 run --execute --debug-flags O3` JSON/stats.
O3 LSQ forwarding evidence now classifies active address suppression and partial-overlap forwarding while retaining byte-mismatch lanes for historical checkpoint/stat compatibility through top-level `rem6 run --execute --debug-flags O3` per-event JSON, event-summary JSON `/debug/o3_trace/0/event_summary/store_load_forwarding_candidates`, `/debug/o3_trace/0/event_summary/store_load_forwarding_matches`, `/debug/o3_trace/0/event_summary/store_load_forwarding_suppressed`, `/debug/o3_trace/0/event_summary/store_load_forwarding_address_mismatches`, `/debug/o3_trace/0/event_summary/store_load_forwarding_byte_mismatches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_candidates`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_matches`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_suppressed`, `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_address_mismatches`, and `/debug/o3_trace/0/event_summary/lsq_operation/load/forwarding_byte_mismatches`, plus `sim.debug.o3_trace.store_load_forwarding_suppressed`, `sim.debug.o3_trace.store_load_forwarding_address_mismatches`, `sim.debug.o3_trace.store_load_forwarding_byte_mismatches`, `sim.debug.o3_trace.event.store_load_forwarding_suppressed`, `sim.debug.o3_trace.event.store_load_forwarding_address_mismatches`, and `sim.debug.o3_trace.event.store_load_forwarding_byte_mismatches`, and dump-reset evidence snapshots/reset-clears `sim.host_actions.stats_dump.cpu0.o3.lsq_operation.load_forwarding_candidates`, `sim.host_actions.stats_dump.cpu0.o3.lsq_operation.load_forwarding_matches`, `sim.host_actions.stats_dump.cpu0.o3.lsq_operation.load_forwarding_address_mismatches`, and `sim.host_actions.stats_dump.cpu0.o3.lsq_operation.load_forwarding_byte_mismatches`; multicore CPU1 active-hart dump-reset coverage now applies the same pre-reset match and post-reset partial-match plus address-suppression lanes with byte-mismatch counters remaining zero to `sim.host_actions.stats_dump.cpu1.o3.lsq_store_to_load_forwarding_*` plus `system.cpu1.lsq0.operation.load.storeLoadForwarding*` while CPU0/generic samples remain absent, and CPU1 restored float/vector LSQ data-response dumps now preserve active-hart resettable `system.cpu1.lsq0.dataResponse.*` and `system.cpu1.lsq0.operation.*.dataResponse.*` samples across host checkpoint restore while CPU0/generic aliases stay suppressed.
O3 normal-runtime LSQ operation byte evidence now exposes nested and flat JSON lanes `/cores/0/o3_runtime/lsq/operation/load/load_bytes`, `/cores/0/o3_runtime/lsq/operation/atomic/store_bytes`, `/cores/0/o3_runtime/lsq_operation_load_load_bytes`, and `/cores/0/o3_runtime/lsq_operation_atomic_store_bytes`, plus raw stats `sim.cpu0.o3.lsq_operation.load_load_bytes` and `sim.cpu0.o3.lsq_operation.atomic_store_bytes` from top-level detailed-mode `rem6 run` output with checkpoint version 15 compatibility zero-filling those lanes.
## External Adapter Migration
### SystemC and TLM Adapters - 59% single-axis
**Score calculation:** 3 of 4 items have executable evidence, or 75% raw. The
bucket cap is single-axis because `trace-replay` drives the typed SystemC/TLM
adapter boundary, but no external SystemC simulator or TLM model executes through it.
- [x] A typed co-simulation adapter boundary exists.
- [x] Adapter event handoff executes from the top-level trace-replay runtime adapter path.
- [x] Adapter checkpoint capture and restore are consumed by the top-level trace-replay runtime adapter path.
- [ ] Runtime SystemC/TLM model integration executes through the adapter.

**Migrated:** `CoSimAdapterBoundary` SystemC/TLM endpoint tests exist, and `rem6 trace-replay --external-adapter-kind systemc|tlm --external-adapter-endpoint <id>` hands packet-trace requests into that boundary, acknowledges them, captures a runtime checkpoint when `--external-adapter-checkpoint-after-events` is set, restores it, completes later packet requests through the restored boundary, and emits `external_adapter` JSON plus `sim.trace_replay.external_adapter.*` stats.

**Not migrated:** External SystemC simulator or TLM model execution, external model-owned state, and `src/systemc`, `util/tlm`, and `ext/systemc` behavior.

**Evidence:** `cosim_adapter`; `rem6_trace_replay_hands_off_packet_requests_to_systemc_and_tlm_adapters`; TOML/CLI external-adapter validation tests.

**Next evidence:** External SystemC/TLM bridge execution and external model-owned checkpoint restore tests.

### SST Adapter - 59% single-axis

**Score calculation:** 3 of 4 items have executable evidence, or 75% raw. The
bucket cap is single-axis because `trace-replay` drives the typed SST adapter
boundary, but no external SST simulator runtime executes through it.

- [x] A typed SST adapter boundary exists.
- [x] SST traffic handoff executes from the top-level trace-replay runtime adapter path.
- [x] SST adapter checkpoint capture and restore are consumed by the top-level runtime adapter path.
- [ ] Runtime SST execution uses an external SST simulator bridge.

**Migrated:** `CoSimAdapterBoundary` SST endpoint tests exist, and `rem6 trace-replay --external-adapter-kind sst --external-adapter-endpoint <id>` hands packet-trace requests into that boundary, acknowledges them, captures a runtime checkpoint when `--external-adapter-checkpoint-after-events` is set, restores it, completes later packet requests through the restored boundary, and emits `external_adapter` JSON plus `sim.trace_replay.external_adapter.*` stats.

**Not migrated:** External SST simulator bridge execution, external SST-owned runtime state, and `ext/sst` plus `configs/example/sst` behavior.

**Evidence:** `cosim_adapter`; `rem6_trace_replay_hands_off_packet_requests_to_sst_adapter`; TOML/CLI external-adapter validation tests in the `rem6` CLI suite.

**Next evidence:** External SST bridge execution and external SST-owned checkpoint restore tests.

### Power and Physical-Design Export Adapters - 74% representative

**Score calculation:** 6 of 8 items have executable evidence, or 75% raw. The bucket cap is
representative because canonical run-memory target selection spans both export formats
with exact cache and DRAM calibration, while full external parity, broader adapters, and physical calibration remain open.

- [x] rem6-power can export typed power-analysis records.
- [x] McPAT-shaped XML export serializes power, thermal, and residency records.
- [x] DSENT-shaped CSV export serializes power, thermal, and residency records.
- [x] `rem6 run --power-output`, `rem6 trace-replay --power-output`, and `rem6 gpu-run --power-output` write executed-run activity-derived power-analysis artifacts.
- [x] Canonical normal-run memory activity selects CPU L1/shared-cache, transport, fabric, and DRAM targets; cache and DRAM records have exact McPAT/DSENT calibration evidence.
- [ ] McPAT-compatible ingestion/export parity is complete.
- [ ] DSENT-compatible ingestion/export parity is complete.
- [x] NoMali-compatible GPU adapter evidence exists.

**Migrated:** Typed power-analysis export records, deterministic custom XML smoke coverage, McPAT-shaped XML, DSENT-shaped CSV, and
typed McPAT/DSENT adapter ingestion round-trips. Normal `rem6 run --power-output` memory records derive from one canonical `Rem6MemoryResourceSummary`.
The direct and transport-plus-DRAM routes plus explicit MSI instruction/data L1 with shared L2/L3 and cache-fabric-DRAM routes span both export formats.
Imported McPAT XML and DSENT CSV CPU L1/shared-cache, transport, and fabric target presence follows matching artifact and stats `active` fields; DRAM uses canonical `active || profiled_targets`, with both fields reconciled across artifacts and stats; unselected targets are absent.
DRAM events are the maximum of accesses, refreshes, low-power entries, and exits; operations include commands plus refresh and low-power transitions; bytes use exact `read_bytes + write_bytes`; and residency covers final tick, refresh, active/precharge powerdown, self-refresh, exit latency, and accesses. Exact cache target coefficients remain guarded, with CPU responses excluded from L1 operations and included for shared L2/L3.
Top-level `rem6 run --debug-flags Power` emits the same executed-run activity-derived CPU, cache, fabric/NoC, memory-transport, and DRAM power records in deterministic run JSON.
Top-level `rem6 trace-replay --power-output` emits activity-derived data-cache, fabric/NoC, and DRAM records from executed packet replay; `rem6 gpu-run --power-output` retains activity-derived GPU compute-unit,
fabric/NoC, data-cache, and DRAM records, with artifact paths reported in run JSON or CLI envelopes.
Top-level `rem6 power-import` ingests McPAT-shaped XML, McPAT text reports, DSENT-shaped CSV, and gem5 DSENT Python-tuple reports into deterministic typed JSON summaries and optional output artifacts; report imports map printed power while deriving omitted residency and temperature.
Top-level `rem6 gpu-run --nomali-output` emits a deterministic NoMali-compatible T760 adapter artifact from executed GPU run summaries,
including register-file checkpoint, reset/no-effect/cycle-counter/perf/cache command-table, MMU AS0/AS1 config and command, dual job-slot, IRQ-clear, shader/tiler/L2/L3 power-state, register-fault, interrupt-block, status-read, callback-transition, and observed workgroup
plus memory-activity evidence; TOML and flag-driven runs both report the artifact path.

**Not migrated:** Complete `ext/nomali`, `ext/mcpat`, and `ext/dsent` parity, NoMali PIO command/register breadth beyond the existing reset,
GPU/MMU/job-slot command-table, cycle-counter, AS0/AS1 config, IRQ, power-state, fault, interrupt-block, callback-transition slices, and real interrupt-controller delivery, plus broader external ingestion,
full external McPAT/DSENT schema/tool parity, broader GPU and trace-replay calibration, and physical fabrication/vendor coefficient calibration.

**Evidence:** rem6-power custom XML, McPAT XML, DSENT CSV, and import tests; `rem6_run_power_activity_matches_canonical_resource_matrix`; normal-run canonical ownership source-policy tests;
focused `run_power_emits_profile_only_dram_resource_with_legacy_baseline`, `run_power_emits_refresh_only_dram_resource`, `run_power_emits_low_power_only_dram_resource`, `run_dram_power_uses_accesses_when_they_dominate_residency`, `run_dram_power_keeps_commands_out_of_event_count`, `run_power_suppresses_zero_memory_resources`, `run_dram_power_uses_canonical_byte_total`, and `run_cache_power_preserves_legacy_target_calibrations` unit evidence;
`rem6 run` power-output, imported artifact, cache/fabric/transport/DRAM, and `Power` debug tests; trace-replay and GPU activity records; power-import report and artifact tests; envelope reporting and load-only rejection;
and `rem6 gpu-run` CLI/TOML McPAT/DSENT output plus NoMali register-file, reset, command, MMU, job-slot, IRQ, power-state, fault, callback-transition, multi-artifact envelope, and output-path conflict evidence.

**Next evidence:** Full external McPAT/DSENT schema and tool parity, broader external ingestion, GPU/trace-replay calibration breadth, broader NoMali behavior, and physical fabrication/vendor coefficient calibration.

### Native Loader and Math Replacement - 50% single-axis

**Score calculation:** 2 of 4 items have executable evidence, or 50% raw. The
bucket cap is single-axis because loader and softfloat matrix breadth is not
complete.

- [x] Native ELF loading reaches executable RISC-V SE smoke paths.
- [x] Native DTB handoff records exist.
- [ ] libelf replacement breadth covers the needed gem5 loader matrix.
- [ ] softfloat replacement breadth covers all FP rounding and exception paths.

**Migrated:** Native Rust loader and DTB handoff slices, top-level ELF32/ELF64
extended program-header-count (`PN_XNUM`) loading, `PT_LOAD` summary/alignment, `PT_PHDR`/`PT_NOTE` metadata,
`PT_INTERP` interpreter reporting/rejection, `.tbss`/`PT_TLS` TLS metadata,
`PT_GNU_STACK` stack-exec, `PT_GNU_RELRO`, `PT_GNU_EH_FRAME`, and `PT_GNU_PROPERTY` metadata,
symbol type, binding, GNU-unique binding, visibility, section-index, TLS-type, and IFUNC-type counts plus dynamic-symbol type, binding, and visibility counts, section-header/name/basic and extended flag/storage/string-table/array/hash/index-table/type-range/version/group/address/alignment plus `SHT_NOTE` and `SHT_REL`/`SHT_RELA`/`SHT_RELR` count/byte metadata, dynamic needed/path/loader-string/table/lifecycle-scalar/array/REL/RELA/RELR relocation/hash/version/linker/flag and ABI-note OS metadata,
plus RV64D `fadd.d` RoundUp, `fsub.d` RoundDown directed overflow, `fmadd.d` product-overflow and max-boundary overflow, and `fsqrt.d` directed rounding/NX ELF smokes and RV64F/RV64D scalar FP slices including `fcvt.s.d` directed rounding/NX and `fcvt.d.s` signaling-NaN invalid accrual.

**Not migrated:** Complete `ext/libelf`, `ext/libfdt`, and `ext/softfloat` parity.

**Evidence:** CLI static RISC-V smoke tests cover ELF64 extended program-header
counts, `PT_LOAD` summary/alignment JSON/stats, `PT_PHDR`/`PT_NOTE` JSON/stats, `PT_INTERP` rejection/reporting, `.tbss`/`PT_TLS`
TLS metadata, `PT_GNU_STACK`, `PT_GNU_RELRO`, `PT_GNU_EH_FRAME`, and `PT_GNU_PROPERTY` metadata,
symbol type, binding, GNU-unique binding, visibility, section-index, TLS-type, and IFUNC-type counts plus dynamic-symbol type, binding, and visibility counts, section-header/name/basic and extended flag/storage/string-table/array/hash/index-table/type-range/version/group/address/alignment plus `SHT_NOTE` and `SHT_REL`/`SHT_RELA`/`SHT_RELR` count/byte JSON/stats, dynamic needed/path/loader-string/table/lifecycle-scalar/array/REL/RELA/RELR relocation/hash/version/linker/flag JSON/stats,
ELF32 extended counts, ABI-note OS, DTB, RV64D `fadd.d` RoundUp, `fsub.d` RoundDown, `fmadd.d` product-overflow and max-boundary overflow, and `fsqrt.d` ELF smokes, and RV64F/RV64D `fcvt.s.d`/`fcvt.d.s` tests.

**Next evidence:** Expand loader breadth beyond current extended-numbering slices and soft-float parity.

## Update Rules

- Update percentages only when executable rem6 evidence changes; keep the checklist beside each component so the score can be audited.
- Do not count unknown-syscall diagnostics as implemented syscall coverage or tool-detected static smokes as broad workload parity.
- Do not cite exact line ranges from gem5 or rem6.
- Keep detailed proof in tests, artifacts, traces, checkpoints, or manifests.
